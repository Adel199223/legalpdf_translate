# LegalPDF Translate Handoff

## Current continuation — offline diagnostics complete; AR4 cause unresolved

Fresh `NEXT_STAGE_2` completed ONLY the offline guard-diagnostic prerequisite.
Read private `structured_activation_guard_diagnostic_stage2_2026_09_11`:
`assessment.md`, `findings.json`, `independent_review.json`, `validation.json`,
`closeout.json` and `next_stage3_scope.json`. Final fixture snapshot: build_03.
The original consumed AR4 packet and all frozen caller bytes remain unchanged.

The recorder now retains the first allowlisted code/phase/event without exception
text, paths, credentials or document content, with exclusive no-overwrite/no-retry
persistence. All20 stdlib tests passed. Five probes passed their intended checks:
module-origin snapshot, local TLS construction, Boundary.deny, direct need and
network refusal. One deliberate socket-constructor probe was blocked BEFORE
creation; no provider/socket connection occurred. No actual key inspection or
resolution, original ledger constructor/write, source/request rebuild or re-review,
Word/Gmail/native operation, product fix or permission broadening occurred.

All112 code and65 config members were already bound. The tested module/TLS
hypotheses did not reproduce the original rejection. Import timing/full-case
preflight were not identical; specific original cause remains UNCONFIRMED.
The new recorder is test-only, not installed into the frozen paid caller.
Build_01 was unexecuted; build_02 had a new test-launcher preflight defect fixed
in build_03. That unrelated defect is not attributed to the original AR4 failure.

Original USD10 lifetime ledger:149 finalized, USD2.36852785 settled, zero held,
USD7.63147215 remaining. Preserve every consumed launcher/operation/case intent,
canonical13 dirty docs, runtime112/candidate60, original Word journal and saved
legal/formatting preferences. Keep existing key, GPT-5.2/high and ordinary
legacy_text_v1. Codex does AI testing review; user reviews ordinary future work;
no human certification or new external English-reference requirement.

Next `NEXT_STAGE_3` is conditional evidence review ONLY: assess a precise,
separately scoped offline preflight design and stop before new execution while
cause/authority remain unresolved. No paid retry, recovered authority, frozen
file edit, guard exemption, activation, Stage4 or publication is authorized.
Real AR/FR/EN fidelity/coverage, exact-DOCX every-page layout, measured all-call
cost/user acceptance and distinct >=20% efficiency remain open.

## Historical continuation — authorized AR4 stopped; offline diagnostic next

Fresh `NEXT_STAGE_3` plus exact `APPROVE_PAID_AR4` digest 473b8f9b...
authorized one AR 1/5/6/9 launch, not a retry. The exact five templates were
materialized and independently checked; unchanged launcher ran once with
-NoProfile. Read the private v3 packet's
`paid_execution_ar4_20260911_01/assessment.md`, `launch_result.json`,
`independent_accounting_review.json`, `closeout.json` and `next_stage2_scope.json`.
The frozen prior proposal/closeout are historical, not current replay authority.

The child stopped in 9.4597205s with exit 2, BootstrapDenied/pilot_exception.
Exception closeout keeps completed_pages=null; no page/response artifact exists.
The initialized dispatch journal has zero events. The original USD10 ledger
is byte-identical: 149 finalized, USD2.36852785 settled, zero held,
USD7.63147215 remaining.
No new reservation, settlement or dispatch event was recorded; external billing
was not queried. Existing key was resolved only by the approved executor inside
durable intent; no replacement or separate auth test. No automatic retry occurred.

Launcher/operation/case-phase intents remain consumed. Never repeat the argv,
reset markers or remint directories to replay this approval. The specific
guard-denial code/phase was not retained, so the cause remains unconfirmed.
Next is ONLY `NEXT_STAGE_2`: a new private offline sanitized-diagnostics fixture,
fake key, disposable budget and hard-disabled transport; no real main, original
key/ledger constructor, source/request rebuild or re-review. Preserve frozen
code; diagnose before proposing a minimal fix. Any paid recovery requires a
separate reviewed recovery design and fresh digest-specific user approval.

Canonical 13 dirty docs, runtime 112/candidate 60, source/preferences, old private
artifacts and Word journal remain unchanged. Only six feature handoff/reference
docs are synchronized. Keep GPT-5.2/high, ordinary legacy_text_v1, formatting and
legal preferences, original lifetime budget and all consumed intents. Codex does
AI test review; user reviews future ordinary work; no human-certification claim
or new external English-reference requirement. All-language legal fidelity,
exact-DOCX every-page layout, measured all-call cost/user acceptance and >=20%
efficiency remain open. No Word/Gmail, activation, Stage4 or publication authority.

## Historical continuation — all13 offline requests frozen; paid pilot then unapproved

Fresh `NEXT_STAGE_3` completed the single authorized NON-DISPATCH preparation
in private `structured_activation_request_preparation_v3_2026_09_11`.
Read its `assessment.md`, `private_attempt_result.json`, `validation.json`,
`closeout.json` and `proposals/paid_ar4_scope.json` first. Older packets are
historical, not authority to repeat completed operations.

All13 current-candidate requests are saved: AR9/FR2/EN2. The real preparation
completed once in 101.364s with zero provider/credential/auth/ledger/OCR/render/
Word/Gmail operations and zero guard denials. Every final request hash and its
GPT-5.2/high/default-tier/store=False/24000-output controls were verified.
Accepted source decisions remain unchanged; effective preferences equal the
previous preserved preferences. The earlier partial AR9 remains untouched and
is not relabelled as this new preparation identity.

Before intent: ten copied execution/helper/test files remained byte-identical;
only two private bookkeeping scripts were rebound. All111 fictional tests,
cold actual-SDK/mock4, fictional launcher13, deadline/reuse and guard checks
passed. Independent review and a3061-file executable/dependency/interpreter/CA
freeze preceded intent. Runtime112/candidate60 were not edited in this stage.
Prior Stage2 full3934 and Full evidence remain valid; do not rerun completed
paid/Word/Gmail operations or alter frozen executables to bypass a failure.

The original USD10 lifetime ledger remains149 finalized rows:
2.36852785 settled / zero held / 7.63147215 remaining. The proposed first paid
operation is ONLY AR pages1/5/6/9, one primary request each, zero retries or
corrections, maximum USD4.144. This is a conservative reservation ceiling,
not a measured bill. All13 capacity USD13.468 is not guaranteed affordable.
Current Stage3 supplied NO paid authority; the exact separate proposal requires
fresh `NEXT_STAGE_3` AND digest-specific `APPROVE_PAID_AR4 <operation SHA256>`.
Do not materialize approval-ready templates or touch the original ledger/key
before both approvals. Keep the existing key; no replacement or test is needed.

Canonical main and its13 dirty handoff docs remain protected. Model/defaults,
ordinary `legacy_text_v1`, formatting/translation preferences and every consumed
intent are unchanged. Codex performs AI test review; the user reviews future
ordinary output, without human-certification claims. English keeps the agreed
source-versus-output review; no external corrected reference is required.
Real all-language legal fidelity, exact-DOCX every-page layout, measured
all-call cost/user acceptance and >=20% efficiency remain open. Ordinary
activation, Stage4, publication and later paid/native phases are not authorized.

## Historical continuation — header-precedence fix implemented offline

Fresh `NEXT_STAGE_2` implemented the approved two-file glossary prerequisite.
Read private `structured_activation_header_precedence_stage2_2026_09_11/assessment.md`,
`implementation_review.md`, `validation.json`, `closeout.json` and
`next_stage3_scope.json` first. Older continuation packets remain historical.

The six-line change makes generated headers yield only to eligible configured
entries for the exact same source phrase. Configured wording/case/tier/mode,
strict configured conflicts and invalid-entry rejection remain intact.
Citation aliases, exact source matching, non-PT behavior, limits and Arabic
literal/fingerprint checks are unchanged. The prior deliberate header-conflict
test expectation was explicitly revised. There are 28 added cases plus that
changed case: parent RED reproduced 11 failures, then all 212 glossary and
488 focused regressions passed. Independent stage-only review found no issues.
Full pytest passed 3,934 tests in 568.80s on a bounded rerun; the initial process
exited without a pytest summary and is retained with its hang-diagnostic record.
Two nearby tests passed in isolation; no tests were skipped or checks weakened.
Serial Full passed: 233 browser/state, 2 review and 5 intake tests, compilation,
docs and hygiene. Known Dart AOT wrapper failures had successful direct-Dart
fallbacks. See validation.json for exact timings, limitations and final checks.

Only two product/test files and five feature handoff docs change this Stage2.
Cumulative candidate60/runtime112 membership is unchanged. Canonical main and
its13 dirty handoff docs, the original ledger/Word journal, accepted sources,
old private caller/scripts/manifests and consumed partial AR9 remain protected.
No real preparation, source adaptation/re-review, provider/auth/token-count,
credential resolution, original-ledger constructor/write, OCR/render/Word/Gmail/
live operation or publication ran. Existing key, GPT-5.2/high, ordinary
`legacy_text_v1`, translation/formatting preferences and original USD10 cap
remain unchanged: 2.36852785 settled / zero held / 7.63147215 remaining.

Stop at fresh **`NEXT_STAGE_3`** only after the completed closeout. Its proposed
scope is offline: NEW private root, copy/rebind and fictionally revalidate the
reviewed engine, independent executable freeze BEFORE one new non-dispatch
AR9/FR2/EN2 preparation attempt. The historical manifest still pins old product
bytes; never modify it, replay its intent or relabel old AR requests. Keep exact
accepted sources and every failure. Any unexpected defect stops without retry.
No paid/native authority follows from the stage token; only successful complete
preparation can lead to a separate exact AR1/5/6/9 paid proposal.

Codex AI testing review and future user review remain assigned without human
certification. English retains the agreed source-versus-output review route;
no external corrected English reference is required or claimed. Actual
all-language legal fidelity,
exact-DOCX every-page layout, complete all-call cost/user acceptance and the
distinct >=20% efficiency target remain open; ordinary activation/Stage4 are closed.

## Historical continuation — one-shot preparation stopped on header precedence

Fresh `NEXT_STAGE_3` consumed the single NON-DISPATCH preparation attempt.
Read private `structured_activation_request_preparation_v2_2026_09_11/assessment.md`,
`private_attempt_result.json`, `private_glossary_diagnosis.json`,
`validation.json`, `closeout.json` and `next_stage2_scope.json` first.
All older packets below are historical; preserve every consumed intent.

The caller/preparer/launcher were fictionally tested and frozen BEFORE private
intent: 111 private tests passed, the final 20-test timing subset passed, actual
SDK cold mock integration passed four exact requests, and the final fictional
launcher prepared all13. The one real-source attempt then saved AR9 exact
requests, but FR1 failed before glossary completion/request capture/journal;
EN was not reached. No complete 13-request bundle exists. Never retry this
intent or change its frozen executable bytes; retain partial AR as historical
evidence, not as requests approved under a later candidate identity.

Pure read-only diagnosis and a fictional example identify a configured-versus-
generated header conflict for `audiência de julgamento`: generated FR/EN target
capitalization differs from configured wording; AR agrees. This is a product
prerequisite, not an API-key/auth/model or already-fixed citation-alias problem.
Stop at fresh **`NEXT_STAGE_2`** for the proposed narrow offline precedence fix:
eligible exact-source configured entries override generated header defaults;
genuine configured conflicts/invalid entries still fail. The existing test that
expects header/config conflict failure must change explicitly under this scope.
No real preparation, paid operation or activation is authorized in Stage2.

Product candidate60/runtime112 and the 3,061-file executable freeze are unchanged.
Canonical main/13 dirty handoff docs, accepted source decisions, original ledger
and consumed Word journal remain protected. No provider/auth/token-count/OCR,
render/Word/Gmail/live, real credential or original-ledger constructor/write ran.
Existing key, GPT-5.2/high, ordinary `legacy_text_v1`, translation and formatting
preferences remain unchanged. USD10 lifetime cap: 2.36852785 settled, zero held,
7.63147215 remaining. No paid approval is requested with incomplete preparation.

Codex AI test review and future user review remain assigned, without human
certification. All-language legal fidelity, exact-DOCX all-page layout, measured
all-call cost/user acceptance and the distinct >=20% efficiency target remain
open. Stage4/ordinary activation are unavailable. Prior 3,906 product tests/Full
results are historical passing evidence for unchanged product bytes; fresh
private and final docs/protection results are separated in `validation.json`.

## Historical continuation — citation-alias prerequisite implemented offline

Fresh `NEXT_STAGE_2` implemented only the derived ordinal-alias separator fix
and fictional regressions. Read private
`structured_activation_citation_alias_stage2_2026_09_11/assessment.md`,
`implementation_review.md`, `validation.json` and `next_stage3_scope.json`
first; `closeout.json` pins the completed validation and final freeze.
All older continuation packets below are historical; preserve their consumed
intents and do not replay preparation/adaptation/source-review operations.

The five-line runtime change retains exact source spelling and numeric labels,
strict entry/literal validation, configured preferences and the shared legacy
recognizer. 124 new fictional cases were added. Focused validation passed 430
tests; full pytest passed **3,906 in 529.90s**. Serial Full validation passed:
233 browser, 2 review and 5 intake tests, compilation, docs and hygiene.
The known Dart AOT launcher failures had successful direct-Dart fallbacks.
Independent code review found no actionable issue. Initial failed fixture
expectations are preserved and corrected without changing product strictness.

The new candidate has 60 code/test/tool files and 112 runtime Python files.
Canonical main/13 dirty handoff docs, original ledger/Word journal, accepted
sources, existing key, model/defaults and formatting preferences remain unchanged.
No private request preparation, provider/auth/OCR/render/Word/Gmail/live operation,
ledger mutation, publication or ordinary activation ran in this Stage2.

Stop at fresh **`NEXT_STAGE_3`** for the bounded offline-only scope: author,
fictionally test and freeze the actual private caller/launcher FIRST, then one
new non-dispatch preparation attempt for 13 exact requests using accepted source
bytes. Recheck public pricing and freeze exact config/request/source identities.
No real credential/provider/original-ledger/native branch may run. A product
defect or post-intent executable change requires a fresh scope; never auto-retry.
Only afterward may an exact AR1/5/6/9 paid proposal be presented separately.
No paid caller is implemented/frozen now; no complete private requests exist.

Original USD10 cap remains: 2.36852785 settled, zero held, 7.63147215 remaining.
Historical USD4.144 AR4 capacity proposal stays conditional on fresh applicable
pricing/bounds; all13 affordability is not guaranteed. Codex AI test review and
future user review remain assigned. All-language legal fidelity, exact-DOCX
all-page layout, all-call cost/user acceptance and the separate >=20% efficiency
target remain open; Stage4 is unavailable.

## Historical continuation — request preparation blocked by derived citation alias

The latest `NEXT_STAGE_3` performed public pricing verification and guarded
offline request preparation only. Read private
`structured_activation_request_preparation_2026_09_11/assessment.md`,
`closeout.json`, `fictional_reproduction.json` and `next_stage2_scope.json` first.
Do not replay earlier adaptations or the new failed private preparation intent.

Fictional full-case preparation captured 13 requests; the private attempt stopped
on AR1 before capture/provider contact. Pure predicate diagnosis and a harmless
`nº 2` reproduction identified a generated-alias boundary defect: the recognizer
includes the following separator in `nº `, then strict glossary validation rejects
it. The rejected term is code-generated, not an invalid user preference.
No product fix, source edit, glossary change or real preparation retry occurred.

Stop at fresh **`NEXT_STAGE_2`** for the narrow offline alias-span repair and
regression tests. Keep strict entry/literal validation and exact source/preferences.
A later fresh Stage3 may prepare requests in a new private attempt after the fix.
The actual one-shot paid caller also needs its own reviewed executable freeze;
the preparation wrapper is not a paid executor. No paid approval is requested yet.

Fresh public pricing supports a conservative 400000 input / unchanged 24000
inclusive output reservation: USD1.036 per call, USD4.144 for AR1/5/6/9.
All13 capacity reservations total USD13.468 and are not guaranteed affordable.
Later phases remain conditional. These are proposed bounds, not actual spend
or frozen private request hashes; byte-count-plus64 is not billing proof.

Original USD7.63147215 remainder, existing key, model/defaults/preferences,
58 candidate/112 package files, canonical13 dirty docs and consumed Word journal
remain unchanged. No provider/auth/OCR/render/Word/Gmail/live/source-rebuild call.
Codex AI testing review and future user review remain assigned. All-language
fidelity, exact-DOCX layout, all-call cost/user acceptance and separate >=20%
efficiency remain open; ordinary activation/Stage4 remain unavailable.

## Historical continuation — offline sources accepted for AI translation testing

Fresh `NEXT_STAGE_3` completed the two frozen adaptations once each: AR 9 pages
(1.829s), shared FR/EN 2 pages (1.812s), 1037 bound checks before/after each.
Read private `structured_activation_reviewed_source_integration_stage3_2026_09_10/closeout.json`,
`inspection.json`, `source_decisions.json`, `assessment.md` and
`next_operation_proposals.json` before older packets.

Exact text/newlines, all action/baseline ownership, source/readiness/output hashes
and starts AR 1/3/4, FR/EN 1 passed saved-output inspection without another
adapter/build call. Codex separately accepted transcription/document boundaries
for these AI-reviewed tests using the unchanged completed reviews (11 pages,
44 resolved observations). Two proposed source-review envelopes are private
evidence, not config/paid authority. Adapter outputs still say nonproduction/
not_evaluated; geometry/layout and translation acceptance remain unclaimed.
One reviewed block per page does not prove paragraph/table/furniture layout.
Cross-page lists/sentences and literal awkward source syntax are review watchpoints.

No adapter defect was observed. Stop at fresh **`NEXT_STAGE_3`** for the exact
request/price-preparation-only proposal (hash in closeout): read-only official
pricing verification and guarded non-dispatching request/config/bounds preparation.
No paid/provider/auth-test, OCR/render, Word/Gmail, source rebuild, ledger mutation,
ordinary activation, model/default change or publication. Then ask precise paid
operation approval; Word requires later exact new DOCX hashes and separate scope.
The old adaptation intents are consumed and must never be replayed.

All 58 candidate/112 package files, canonical 13 dirty docs, existing key/defaults/
preferences, original USD7.63147215 remainder and Word journal are preserved.
Only five feature handoff docs changed. No paid/OCR/Word/Gmail operation occurred.
Parent AI review is not independent or certified human review; existing agents
remain quota-limited. User reviews future ordinary outputs. All-language quality,
exact-DOCX Word layout, all-call cost/user acceptance and separate >=20% efficiency
remain open. Stage 4 is unavailable.

## Historical continuation — acceptance-only reviewed-source integration validated

Fresh `NEXT_STAGE_2` completed the approved offline integration. Read private
`structured_activation_reviewed_source_integration_stage2_2026_09_10/closeout.json`,
`implementation_review.md`, `validation.json` and the active ExecPlan first.
The existing API key is retained unchanged; no API call or credential operation ran.

The explicit acceptance lane now verifies externally approved candidate/evidence
bytes, requires a separately authored source decision, routes reviewed text before
extraction/OCR, and retains request/accounting/resume guards. New reviewed IDs
map to original actions/blocks. Exact text/newlines are preserved; geometry,
paragraph joins, furniture and layout acceptance are not invented. Ordinary
jobs still use unchanged GPT-5.2/high and `legacy_text_v1`.

Validation: 57 new product tests; 253 focused tests, final 66-test subset and
full pytest **3,782 passed in 527.70s**. Serial Full validation passed, including
233 browser, 2 review and 5 intake tests, compilation and successful direct-Dart
fallbacks after the known wrapper AOT failure. Private continuation tests passed
24 assertions in 17 cold runner processes plus one guard process (16 actual
access denials). These counts overlap; fictional tests are not legal/layout proof.

Stop at fresh **`NEXT_STAGE_3`** for the new frozen `continuation_scope.json`
(exact hash in closeout): one cache-only adaptation per saved AR/shared FR/EN
candidate, full-case saved-output inspection and separate source-decision
preparation only if justified by unchanged completed reviews. No real adaptation
or acceptance record was produced in Stage 2. No candidate rebuild, re-review,
OCR/render, provider, Word/Gmail, live app, publication or ordinary activation.
No automatic retry; preserve every consumed historical intent. The next runner
retains `production_eligible=false` and never authors an acceptance decision.

Cumulative freeze: 58 candidate files / 112 runtime Python files. Only seven
runtime files, two tests and these five feature handoff files changed this turn;
all canonical 13 dirty docs, prior evidence, original USD7.63147215 remainder,
consumed Word journal and translation/formatting preferences are preserved.
Cached A4-assumed page frames are not measured/certified geometry. Codex is the
AI test reviewer; the user reviews future ordinary outputs. Parent-local review
is not independent/human certification; existing agents remain quota-limited.
All-language legal quality, exact-DOCX Word layout, all-call cost acceptance and
the separate >=20% efficiency target remain open. Stage 4 is unavailable.

## Historical unpublished continuation — reviewed-source builds passed

Fresh `NEXT_STAGE_3` is consumed. Both exact approved builds succeeded once:
AR 9 pages and shared FR/EN 2 pages, with 17 variants/128 baseline blocks and all
11 complete review records/44 resolved observations retained. Direct saved-output
inspection checked exact hashes, manifest/review equality, reading order,
before-text/IDs and output text without a rebuild or new source review.
Each build checked 895 files before/after; all 810 protections passed afterward.

Read private `structured_activation_source_review_build_v2_2026_09_10/closeout.json`,
`inspection.json`, `assessment.md` and `next_stage2_scope.json`. The latter's
SHA256 is `e67bf3de711b2d2fff59befff32ec7ba8e2bff36d36cfdcbc290983e084a8cd4`.
Stop at fresh **`NEXT_STAGE_2`** for its acceptance-only reviewed-source adapter/
routing and fictional tests. Current native/OCR routing cannot yet safely consume
the corrected candidate. Preserve distinct provenance; do not attach old OCR
geometry to corrected words or infer source acceptance from record completeness.
Whole-page review regions do not establish fine layout.

No implementation of that proposal occurred. Both new intents are consumed;
preserve the older failed intent and every review/candidate/result byte.
No new prepare/review/OCR/render, provider/Word/Gmail/live or publication ran.
All 56 candidate/111 package files, canonical 13 dirty docs, defaults/preferences,
original USD7.63147215 remainder and Word journal are intact. Only five feature
handoff files are updated. Parent-local review is not independent/human approval.
Candidates remain nonproduction with source/layout not evaluated. Full legal/
layout/cost acceptance and ordinary activation are pending; Stage 4 unavailable.
Next Stage 3 needs its own passing freeze and cache-only scope; paid and Word
authority must be separately renewed for the precise operations.

## Historical continuation — private runner correction validated

Fresh `NEXT_STAGE_2` completed the private cold-import runner correction and
synthetic end-to-end validation only. Preloading stdlib `__future__` fixes the
observed startup failure without opening broad filesystem access. Preflight now
loads the helper under the same guard. The runner rechecks its own bytes and the
externally approved scope along with all protected evidence before result commit,
requires complete reviews, retains exclusive flushed/fsynced intents, validates
output readback/self-hash and commits the result marker last. No prepare/retry.

Final validation: 25 assertions in 21 fresh Python processes, including complete
fictional 9-page and 2-page builds with 17 variants/UTF-8/CRLF input, 13 invalid
data/scope cases, external digest checks, success/failure replay and a real
exclusive-open/read probe with 16 forbidden API operations denied. The two earlier
development runs are retained, overlapping results, not extra unique tests.
No real reviewed source was built. No product/helper/test-source changes.

Read private `structured_activation_source_review_runner_fix_2026_09_10/closeout.json`
and its frozen `continuation_scope.json` before continuing. Stop at fresh
**`NEXT_STAGE_3`**: one replacement AR build and one first FR/EN build from the
unchanged saved review manifests/11 records into the separate
`structured_activation_source_review_build_v2_2026_09_10` root. Original failure
and consumed intent remain intact. Do not use the older unstarted FR/EN dispatch.
No new preparation, review, OCR, rendering, paid/native work or retry.

The 56-file candidate, 111-file package, canonical 13 dirty docs, all preferences/
defaults, original USD7.63147215 remainder and Word journal remain unchanged.
No new provider/Word/Gmail/live/publication operation. Parent-local review is not
independent/human/legal certification. Real candidate/source/translation/layout/
cost acceptance and ordinary activation remain pending; Stage 4 unavailable.

## Historical continuation — full source review saved; runner prerequisite

Fresh `NEXT_STAGE_3` completed one preparation and parent-local AI review of all
11 saved pages/17 raw variants. The 11 explicitly authored records contain
44 resolved observations, unchanged before-text/IDs and proposed source boundaries.
No accepted source/layout review or translation was produced.

The first Arabic build stopped after its exclusive intent but before candidate
validation: the frozen private runner had not preloaded stdlib `__future__`,
so its read allowlist blocked that cold helper import. No candidate output;
FR/EN not attempted; no retry. Read
`structured_activation_source_review_stage3_2026_09_10/closeout.json`,
`failure.json`, `review_freeze.json` and `ai_review_summary.md`.
The reviewed manifests are saved and must not be reauthored automatically.

Two read-only probes reproduced the defect and verified the proposed preload
with helper import/canonical JSON and 11 retained denials. They did not build a
candidate or implement a correction. Prior synthetic guard/preflight checks had
not tested this cold import path; do not equate their success with real execution.

Next gate: **fresh `NEXT_STAGE_2`**, only the bounded private runner correction
and guarded cold-start synthetic end-to-end validation in `next_stage2_scope.json`
(SHA256 `b49234dd722f3256c23d99464a9c9abce65c6366e7059c263df1a690d47ad685`).
No product/helper/test-source edits or real build in that step. Freeze a separate
replacement-build executor/scope after passing tests, then require fresh
`NEXT_STAGE_3` for one replacement AR/one first FR/EN build, without new
preparation, review, OCR or automatic retry. All failed evidence stays intact.

Canonical 13 dirty docs, frozen 56-file/111-package runtime, defaults/preferences,
original USD7.63147215 remainder and Word journal are preserved. Zero paid,
OCR/render/browser/Word/Gmail/network/publication operations. Overall real
quality/layout/cost Stage 3 remains incomplete and ordinary activation unavailable.
The older continuation sections below are historical, not renewed authority.

## Historical continuation — offline source-review helper implemented

The fresh `NEXT_STAGE_2` authorized the offline candidate helper/synthetic-test
slice only. Two new files implement and test it; no production `src` file or
actual source text changed. The 70 new tests, 151 focused tests and 3,725 full
tests (474.76s) passed. Serial Full passed (233 browser, compilation, 2 review,
5 intake tests and known successful direct-Dart fallbacks). Protection/freeze
results are recorded in private `structured_activation_source_review_stage2_2026_09_10/closeout.json`.
Do not move to the next gate without that passing closeout.

The helper verifies supplied bytes, review-plan/image hashes, full page/block
coverage, unique ownership, finite regions containing cited evidence, explicit
ordering and unresolved findings. Raw OCR is immutable evidence; revised text is
a distinct nonproduction candidate. No fake confidence/word boxes, accepted
source review, translation or layout eligibility is emitted. The frozen
candidate now contains 56 changed/new files; the 111-file runtime package is
unchanged. Parent-local review is disclosed; other agents remain usage-limited.

Stage 2 is complete. Stop at fresh **`NEXT_STAGE_3`** for the exact frozen cache-only
11-page exercise in `stage3_scope.json`, with its scope hash in the closeout.
Prepare once and build at most one candidate for each of two complete cases,
using 17 retained OCR variants; Codex explicitly reviews images/text and authors
review records. No new OCR/render/provider/Word/Gmail/default change, production
source replacement or activation. All original protections and USD7.63147215
remainder remain intact. The sections below are historical consumed scopes.

## Historical continuation — corrected pilot complete; source repair proposed

Fresh `NEXT_STAGE_3` was consumed for the corrected six-pass pilot. All six
local recognitions completed once in 9.328 seconds with zero retries, using only
the three existing saved inputs and pinned tool/languages. Codex inspected all
three full-page images and all nine baseline/candidate texts. Both diagnostic
profiles recover useful fields but lose or damage other substantive content;
neither is acceptable as a whole-page replacement. No source winner changed.

Read the active plan's corrected-pilot packet and private
`structured_activation_source_repair_pilot_v2_2026_09_10/closeout.json`,
`ai_comparison.md` and `next_stage2_scope.json`. The no-launch preflight tested
all six actual Windows Popen commands; 780 file checks passed before and after.
Runtime/defaults, canonical 13 dirty docs, USD7.63147215 remainder and Word
journal are unchanged. No provider, browser/render, Word/Gmail, download,
publication or production mutation occurred. Prior failed executor is preserved.

Stop at fresh **`NEXT_STAGE_2`** for an offline reviewed-source candidate
helper plus synthetic tests only. Raw evidence is immutable; image-bound text
repairs have separate review provenance, never fabricated OCR confidence/boxes.
The helper cannot issue accepted source/layout reviews or invoke translation.
No private source repair or production integration is authorized in that slice.
Freeze the later 11-page cache-only exercise at its next Stage 3 boundary.
Overall Stage 3/real legal-layout-cost acceptance is incomplete; no activation.
This current packet supersedes historical continuation requests below.

## Historical continuation — pilot prelaunch stop, no OCR executed

Fresh `NEXT_STAGE_3` authorized the six-pass local pilot. Its first command was
stopped by a private guard TypeError before Windows CreateProcess: the guard
expected a non-null audit executable, but the caller supplied argv without the
explicit executable keyword. Zero OCR passes/candidate outputs/comparative
reviews occurred. No automatic retry ran. Preserve the original executor,
manifest, first intent and failure in private
`structured_activation_source_repair_pilot_2026_09_10`.

The explicit-executable correction passed an actual-Popen dry run with native
process creation replaced by a rejecting stub (one positive, two negative checks,
zero launches). This is a tested private launch correction, not a product/source
repair. All canonical docs, runtime/defaults, original USD7.63147215 remainder
and Word journal are intact; no paid/Word/Gmail/browser/render operation ran.

Read this packet's `closeout.json`, `incident_review.json` and frozen
`continuation_scope.json` plus the active plan. Stop at fresh `NEXT_STAGE_3` for
the same six unexecuted local PSM3/11 comparisons in the separate v2 root, after
freezing corrected launch guards/current feature-doc evidence. Original limits
and exclusions persist: three saved inputs, 90 seconds/pass, 600 seconds overall,
no retry, no source promotion or production/default change. No activation or
Stage 4 authority; full quality/layout/cost acceptance remains open.

### Historical cache-only Stage 3 closeout

Fresh `NEXT_STAGE_3` was consumed for the frozen 11-page cached-source pass.
It completed once in 1.734 seconds: 642 file checks before and after, identical
2,682 words/128 blocks/geometry/ownership and decoded pixels, nine conservative
boundary-review flags, all 11 source/layout verdicts still review-required.
The previously missed AR starts at pages 3 and 4 now request review; no source
text or boundary decision was invented. Four missing/historical-review controls
were rejected before any acceptance execution. No new OCR, browser/PDF render,
provider, Word/Gmail, settings/default/budget change or publication occurred.

Use the active plan and private
`structured_activation_source_readiness_stage3_2026_09_10` packet. The 54-file
candidate, 111-file Python package, original 13 canonical dirty documents,
USD7.63147215 remainder and consumed Word journal remain unchanged. Parent-local
AI comparison is not independent/human certification; full Stage 3 remains open.

Stop at a **fresh `NEXT_STAGE_3`** for `next_local_ocr_pilot_scope.json` only:
six local diagnostic recognitions, PSM3 and PSM11 once each on unchanged AR1/AR4/
FR1 saved inputs, pinned tool and languages, 90 seconds/pass, 600 seconds overall,
one child at a time, no retries. Reuse existing PSM6 baseline; no new pixels,
downloads, production edits, winner promotion, paid/native/Gmail or activation.
This is a new, narrowly bounded scope, not reuse of consumed capture authority.
Stage 4 is unavailable until full source, legal quality, layout and cost acceptance.

### Historical source-readiness corrective Stage 2

Fresh `NEXT_STAGE_2` authorizes the implemented offline source-readiness and
document-boundary safeguards. Implementation and final validation are complete:
428 focused tests, 3,655 full tests and serial Full validation passed with the
known successful Dart fallbacks. The frozen candidate contains 54 changed files
and a 111-file Python package in the existing isolated activation worktree.
The active plan and private
`structured_activation_source_readiness_stage2_2026_09_10` packet supersede
earlier candidate counts. A full-case, externally hash-bound review now gates
paid acceptance; an OCR winner score never certifies fidelity. Missing-title
scan boundaries remain unresolved unless explicitly reviewed against exact
source bytes/geometry, without clearing OCR/layout uncertainty. Stop at fresh
`NEXT_STAGE_3` for the frozen cache-only 11-page revalidation, not new recognition
or paid/native operations. Real source defects still block activation. Canonical
handoff, original USD7.63147215 remainder and Word journal are intact. No activation,
model/default change, new OCR, provider/Word/Gmail operation or publication ran.
Parent-local review is recorded; independent reviewers were usage-limited.

### Historical acquisition and earlier stages

Latest user-approved source capture completed: one owned browser session captured
11 source pages and 11 actual local OCR passes retained TXT/TSV/geometry. Read the
active plan and private `structured_activation_source_capture_2026_09_10/closeout.json`,
`source_review.json` and `source_readiness.json`. All 11 source pages remain
layout-review-required: actual AI inspection found column mixing, corrupted
critical small text/identifiers and missed internal document boundaries. No new
translation or source repair is claimed. The narrow offline source-readiness/
boundary corrective proposal was authorized by the latest `NEXT_STAGE_2` above.
Reuse retained rasters, not the consumed browser/OCR helpers. The initial
prelaunch Windows-command guard failure is preserved; the additive continuation
recognized each page once. Tesseract stays private; global PATH, defaults and the
original budget are unchanged. No paid, Word, Gmail or publication action ran.

Follow the [active ordinary-use ExecPlan](exec_plans/active/2026-09-10_structured_translation_ordinary_use.md)
for the completed user-authorized offline prerequisite implementation and checks:
3,508 full-suite tests and serial full validation passed on the frozen candidate.
It runs only in `legalpdf_translate_structured_activation` on
`feat/structured-translation-activation`; canonical main and the original pending
handoff docs remain preserved. Defaults stay `legacy_text_v1` and GPT-5.2/high.
The user supplied `NEXT_STAGE_3`; preparation found pricing/bounds, service-tier,
saved-price and multi-phase acceptance-continuation prerequisites. The subsequent
new `NEXT_STAGE_2` authorized those offline fixes. Implementation is complete;
the new 46-file candidate passed 385 targeted tests, 3,624 full tests, independent
review and serial full validation with the documented Dart fallbacks. Follow the
plan's corrective closeout and current Stage 3 packet for final status. The fresh
`NEXT_STAGE_3` was consumed for preparation, not paid acceptance. Historical
evidence lacked source/browser/OCR proof; the fresh approved acquisition above
now supplies that proof but fails source readiness. Affordable paid request
bounds remain unapproved. The user appointed Codex to perform full AR/FR/EN
test review, with the user reviewing future ordinary use. Record AI review, not
human sign-off; assignment is not completed acceptance. English uses source-
versus-output review without claiming an external corrected reference. The prior
human-review appointment requirement is superseded only for this testing campaign.
See private `acceptance_draft_v2.json`; Stage 3 has not passed and `NEXT_STAGE_4`
is unavailable. Runtime candidate bytes remain unchanged.
Original lifetime remaining budget is USD7.631472150000, untouched. Paid/native
operations still require separate exact authority. The PR #295 history and
its old Stage 1 recommendation below are retained history, not work to repeat.

## Current State
- Canonical repo: `C:\Users\FA507\.codex\legalpdf_translate`.
- Repository: `Adel199223/legalpdf_translate`.
- Canonical branch: `main`.
- Current product-modernization baseline: `c3ec60e57852af4ff69ba96cd0410c489a25ba69` after PR #226, `[codex] Extract Gmail prepare action presentation`.
- This historical modernization baseline is not the current head; use `git rev-parse HEAD` in the canonical repo and inspect the latest merged PR checks for current repository state.
- Primary UI: local browser app on `127.0.0.1`, normally live on port `8877`.
- Secondary UI: Qt/PySide6 shell.
- Development UI review mode: browser `shadow` mode with isolated app data.
- Formatting integration starts from `main@022b5afb54b9be0b2eba1f2c19d617ed46a10891`. Keep unfinished research in the clean local-only quality worktree at checkpoint `66a7374a2b874f59994215177af4ff48d1d5f100`; do not delete, merge or publish that combined research branch wholesale. Unrelated dependency-update PRs are outside this rollout.

## Current Source-Associated Integration — Merged PR #295

The latest user `yes` authorized exactly one corrected Arabic Word layout check after clock acceptance preparation. It passed in 6.183 seconds with cleanup confirmed. Two complete-page visual reviews and physical glyph coordinates confirm correct `09:30`, coherent names, clear header spacing and bottom footer on one synthetic A4 page. English/French retain package-identical native-reviewed evidence. All 20 source blocks, one join, fonts and saved evidence survive the local rebuilds. The user approved the displayed corrected layout with `looks right` on 2026-09-09, completing the bounded Stage 3 acceptance. Native authority is now consumed; no further Word launch, retry or recovery is permitted. No paid operation, production activation or host repair occurred.

Stage 4 is complete. [PR #295](https://github.com/Adel199223/legalpdf_translate/pull/295) merged at `4780a2e16c32adf7af7479656f0b5fafdff83224` on canonical `main`. Final local full pytest passed 3,391 tests in 428.49s; 318 focused hardening tests and serial full validation passed with the documented Dart fallback. Final exact-head CI passed both jobs, including 3,391 Windows tests. Canonical verification passed 20 offline checks and a guarded UI/API-wiring-only smoke; all 46 runtime/test files matched the publication digest. Both source branches were deleted; `_new_runs` is retained clean/detached at the merge SHA, with its private logs and canonical-venv junction preserved. The [completed implementation plan](exec_plans/completed/2026-09-09_structured_new_translations.md) now has the actual post-merge addendum. Do not repeat publication.

Private `clock_native_review.json` and `clock_human_approval.json` retain the bounded layout evidence and user reply; the old `synthetic_layout_human_approved_stage_4_pending` status records the pre-authorization checkpoint. The original journal legitimately advanced to `94cdbb415aa8439951958680528c461296e3a0f12174ce96a493ad9e7a97dd8f`; do not restore old bytes or replay consumed helpers. Ordinary launches remain `legacy_text_v1`; the verification process's session-only structured flag is not durable activation. Stage 4 adds no paid/native/model/host-repair scope.

Fresh chats should read [FRESH_CHAT_BRIEF.md](FRESH_CHAT_BRIEF.md) before the historical sections. It preserves user requirements, original budget/key policy, private evidence pointers, current model/default limits and the recommended Stage 1 assessment. This handoff's docs are local/uncommitted; preserve them before isolating new implementation work. No new publication or app change is implied.

## Accepted Footer/Native-Safeguard Release — Historical Evidence

The footer/native release below was integrated as PR #294 at the current task's `8470645` baseline; its older pending-publication notes are history, not instructions to repeat that lifecycle.

- The user accepted the v18 one-page Word-rendered Arabic footer pilot, supplied `NEXT_STAGE_4`, and subsequently explicitly approved publication, merge after successful checks and app verification. The integration was prepared in `C:/Users/FA507/.codex/legalpdf_translate_footer_integration`, branch `codex/footer-spacing-integration`, from canonical `main@ec406dac02ec7898dd339c37320f52f7881a043b`.
- The [completed integration ExecPlan](exec_plans/completed/2026-09-09_footer_spacing_integration.md) records the accepted implementation/docs. Only six formatter modules, four native-export modules, relevant synthetic tests and scoped docs are included. Local validation passed: 615 focused tests, 2,492 full-suite tests and serial `validate_dev.ps1 -Full`, with successful direct-Dart fallback and no COM diagnostic in these runs. Verify actual GitHub checks, merge and canonical post-integration results; this branch closeout does not claim those outcomes.
- The footer/spacing changes require compact reflow (`page_breaks=False`) and complete matching source/target sidecars. A proven isolated contact footer may move to a real footer; isolated headers, partial previews and uncertain/substantive material do not gain that exception. Joined sections retain their own headers/footers and source gaps.
- Fresh production TXT translation, OCR, models/prompts and the structured-translation pipeline remain unchanged and unpromoted. Translation deliverables stay DOCX; internal Word PDF/PNG proof is layout evidence only. No new native call, recovery replay, host-setting change or paid operation belongs to this integration validation.
- Direct hidden Word launch retains its process handle and all seven identity guards, followed by document ownership, bounded cleanup and quarantine. Preserve the research tree and private evidence; never transplant their history or tools wholesale.

## Approved Formatting Integration — Historical Evidence

The formatting-only release preserves compact editable Word output, coherent Arabic/Latin runs, real page fields, and section-specific headers/footers and source gaps when complete matching block evidence exists. Private Arabic/French rebuilds match every uncompressed DOCX package entry in the user-approved five-page/one-page Word proofs; evidence and private documents remain outside Git.

At that formatting-only release, production requests returned TXT, so new runs gained typography/RTL/page-number defaults, not fabricated source-aware associations. Exact regions and section furniture require retained validated source/target sidecars; missing evidence remains a manual-layout-review item. Saved text, historical usage and semantic findings survive local rebuilds. No provider call is required for formatting-only rebuilds. PR #295 subsequently connected the opt-in structured new-run path; ordinary launches remain legacy by default.

Translation prompts/models/efforts, OCR routing, browser/Gmail contracts and honorarios export behavior were unchanged by the formatting release. That layout rollout did not accept Terra/Sol policy promotion or structured production extraction/translation. The latter implementation is now merged opt-in; durable activation and real model/legal acceptance remain separate. Honorarios PDF reliability was subsequently repaired and accepted separately, as recorded below. Translations remain DOCX.

See `docs/assistant/exec_plans/completed/2026-09-07_approved_formatting_integration.md` for validation and rollout conditions; verify the merged PR and actual canonical head rather than assuming the old modernization baseline is current.

The recent browser modernization line has been merged through PR #226. The static browser frontend is now split across focused UI/presentation modules while `app.js`, `gmail.js`, `translation.js`, and `power-tools.js` remain coordinator modules for state, API calls, routing, and side effects. This preserved the existing FastAPI/static app, route IDs, payload shapes, selectors, Gmail/native-host contracts, CLI, and PySide6 entry points.

Recent validation baseline after PR #226:
- focused Gmail prepare-action presentation/static coverage
- browser safe-rendering probe
- broad shadow-web, route-state, and translation-browser regression group
- `scripts\validate_dev.ps1 -Full`, with the known Dart wrapper AOT issue accepted only because the direct Dart fallback passed
- light shadow-mode browser smoke on `main@c3ec60e` for Dashboard, New Job, Extension Lab, and Gmail intake, without opening live Gmail/OAuth/native-host flows

## Accepted Honorarios PDF Repair — Historical Evidence

- The original repair was accepted in `C:/Users/FA507/.codex/legalpdf_translate_honorarios_pdf`, branch `feat/honorarios-pdf-export`, from `main@9afe1d05577f271a8618969e8f0be31eb8641a3e`. This section records that repair's evidence, not current rollout status; verify actual canonical HEAD and GitHub checks. The accepted footer release above is the current lifecycle reference.
- Eight real same-host Word exports completed in approximately 6-8 seconds each: canaries, both honorarios kinds, the real isolated interpretation browser route, asynchronous Qt, open/unsaved Word coexistence, and post-timeout recovery. All six retained honorarios PDFs were one page with complete substantive text and every page visually reviewed; original DOCX hashes and tested open Word content/windows were preserved. Browser evidence is a real TestClient route/service/export with a transparent observer, not a UI click.
- Initial export is one bounded 45-second call. Shared ownership checks, serialization, staged output and fresh-PDF verification protect existing files and Word sessions; uncertain cleanup remains quarantined. No automatic timeout retry; explicit Qt manual retry, select-existing-PDF/local-only recovery and Gmail fail-closed contracts remain intact.
- Full pytest passed 2,176 tests and `validate_dev.ps1 -Full` passed with the documented direct-Dart fallback. The pre-existing `0x8001010d` native Qt small-screen layout-test diagnostic remains separately recorded; the real asynchronous export passed.
- No live Gmail, paid API call, production model promotion, honorarios legal-text/rate change or ordinary translation-PDF output was included. Private synthetic artifacts stay outside Git. Preserve the unrelated quality checkpoint and the prior formatting worktree.
- Evidence and implementation history: [completed honorarios repair ExecPlan](exec_plans/completed/2026-09-07_honorarios_pdf_export_reliability.md).

## What The App Does
LegalPDF Translate is a Windows-first legal PDF translation and Gmail intake app. It translates PDFs page by page into DOCX, preserves run artifacts, supports browser and Qt workflows, records translation and interpretation work in the Job Log, and can continue from a real Gmail message through a browser extension/native-host bridge.

Important invariant: do not convert the translation workflow into one whole-document model request. Page-by-page processing, safe rendering, and Gmail/native-host contracts are core product guarantees.

## Fresh-Thread Starting Points
- Compact current continuation and next scope: `docs/assistant/FRESH_CHAT_BRIEF.md`.
- App architecture and status: `APP_KNOWLEDGE.md`.
- Agent runbook: `agent.md`.
- Quick guardrails: `AGENTS.md`.
- Validation commands: `docs/assistant/VALIDATION.md`.
- Google Photos Interpretation runbook: `docs/assistant/features/GOOGLE_PHOTOS_INTERPRETATION_RUNBOOK.md`.
- Live Gmail retest guide: `docs/assistant/GMAIL_LIVE_TESTING.md`.
- PR #46 historical summary: `docs/assistant/PR46_POST_MERGE_SUMMARY.md`.
- Routing map: `docs/assistant/manifest.json`.
- Roadmap resume anchor: `docs/assistant/SESSION_RESUME.md`.

## Current Next Step
Publication/integration is complete. The recommended next task is Stage 1 assessment and an ExecPlan for controlled durable activation and real-document acceptance of the already-merged opt-in workflow. Recheck current Git/caller policy, preserve pending handoff docs, and identify safety/accounting/evidence prerequisites before implementation. Stop at `NEXT_STAGE_2`; do not turn a process-only verification setting into an unreviewed permanent default.

For details use [the compact brief](FRESH_CHAT_BRIEF.md), then inspect only relevant retained quality research. Do not merge the mixed research branch. Real legal/model acceptance and measured cost savings remain separate from opt-in implementation/publication; no Terra/Sol promotion occurred. Preserve the original lifetime benchmark allowance. Shadow/GET bootstrap can trigger Word/account work despite a disabled bridge: follow the [isolation workflow](workflows/HARNESS_ISOLATION_AND_DIAGNOSTICS_WORKFLOW.md), and obtain fresh scope before any paid/native/Gmail operation.

For live Gmail checks, use canonical `main` only:
1. Keep the primary worktree on `main`.
2. Confirm the canonical live browser runtime is running.
3. Confirm `127.0.0.1:8877` and `127.0.0.1:8765` are listening.
4. Have the user open a real Gmail email with an attachment.
5. Have the user click the LegalPDF extension once.
6. Verify attachment review opens and the live path shows the current Gmail Review/Preview polish.

Codex must not click the Gmail extension or operate the user's live Gmail mailbox unless the user explicitly asks and the task allows it.

For feature-branch Review/Preview UI checks, use `mode=shadow` and the shadow-only `Load demo attachments` affordance in the Gmail intake workspace. The demo seeds one safe PDF attachment, opens Review Attachments, and lets reviewers verify that Review/Preview drawers persist on outside click and restore without resetting the selected attachment, start page, or preview page.

## Known Caveats
- Do not switch branches in `C:\Users\FA507\.codex\legalpdf_translate` while a LegalPDF server launched from that worktree is running. Use a separate worktree for edits.
- Live Gmail extension intake requires canonical `main` at the primary repo path. Feature branches should use `mode=shadow` for browser UI review; the shadow Gmail demo fixture is safe for Review/Preview drawer testing.
- The Dart launcher can fail locally with `Unable to find AOT snapshot for dartdev`; when `scripts/validate_dev.ps1` detects this, the direct Dart fallback at `C:\dev\tools\flutter\bin\cache\dart-sdk\bin\dart.exe` is the expected path.
- Generated DOCX/PDF files must be manually reviewed before any Gmail draft is sent.
- The Google Photos validation intentionally did not generate the final honorários DOCX/PDF. Treat Google Photos `createTime` and downloaded EXIF dates as photo-date provenance only: OCR/legal text wins, and photo date is just an editable fallback when OCR has no service date. Do not claim Google Photos place/location or EXIF GPS support from the current validation.
- Numeric mismatch warnings are serious in legal workflows; do not suppress or soften them without a focused safety review.
- Do not print secrets, tokens, `.env` values, private app data, or live Gmail content in reports.

## High-Value Roadmap
- Keep canonical-main live Gmail retests focused on handoff/readiness, with screenshots or notes only from safe user-approved surfaces.
- Keep hardening Gmail failure recovery with narrow tests around bridge ownership, stale runtime metadata, and exact-message handoff.
- Continue browser-first beginner workflow polish only when it preserves route IDs, DOM IDs, payload shapes, and safe rendering.
- Expand deterministic screenshot/probe coverage for high-risk browser states that are hard to reproduce manually.
- Keep Qt Job Log and browser Recent Work behavior aligned so saved legal work remains easy to audit.
