# Validation Guide

Public-copy note: `Álvaro Exemplo` is an explicitly fictional replacement for a personal-name example. Exact original text is retained in private beforeimage evidence; historical execution outcomes and legal artifacts are unchanged.


## Current continuation — offline diagnostics complete; AR4 cause unresolved

Fresh `NEXT_STAGE_2` completed ONLY the offline guard-diagnostic prerequisite.
Read private `structured_activation_guard_diagnostic_stage2_2026_09_11`:
`assessment.md`, `findings.json`, `independent_review.json`, `validation.json`,
`closeout.json` and `next_stage3_scope.json`. Final fixture snapshot: build_03.
The original consumed AR4 packet and all frozen caller bytes remain unchanged.

The recorder now retains the first allowlisted code/phase/event without exception
text, paths, credentials or document content, with exclusive no-overwrite/no-retry
persistence. All20 stdlib tests passed. Five probes passed their intended checks:
module-origin snapshot, local TLS construction, Boundary.deny, direct need and
network refusal. One deliberate socket-constructor probe was blocked BEFORE
creation; no provider/socket connection occurred. No actual key inspection or
resolution, original ledger constructor/write, source/request rebuild or re-review,
Word/Gmail/native operation, product fix or permission broadening occurred.

All112 code and65 config members were already bound. The tested module/TLS
hypotheses did not reproduce the original rejection. Import timing/full-case
preflight were not identical; specific original cause remains UNCONFIRMED.
The new recorder is test-only, not installed into the frozen paid caller.
Build_01 was unexecuted; build_02 had a new test-launcher preflight defect fixed
in build_03. That unrelated defect is not attributed to the original AR4 failure.

Original USD10 lifetime ledger:149 finalized, USD2.36852785 settled, zero held,
USD7.63147215 remaining. Preserve every consumed launcher/operation/case intent,
canonical13 dirty docs, runtime112/candidate60, original Word journal and saved
legal/formatting preferences. Keep existing key, GPT-5.2/high and ordinary
legacy_text_v1. Codex does AI testing review; user reviews ordinary future work;
no human certification or new external English-reference requirement.

Next `NEXT_STAGE_3` is conditional evidence review ONLY: assess a precise,
separately scoped offline preflight design and stop before new execution while
cause/authority remain unresolved. No paid retry, recovered authority, frozen
file edit, guard exemption, activation, Stage4 or publication is authorized.
Real AR/FR/EN fidelity/coverage, exact-DOCX every-page layout, measured all-call
cost/user acceptance and distinct >=20% efficiency remain open.

## Historical continuation — authorized AR4 stopped; offline diagnostic next

Fresh `NEXT_STAGE_3` plus exact `APPROVE_PAID_AR4` digest 473b8f9b...
authorized one AR 1/5/6/9 launch, not a retry. The exact five templates were
materialized and independently checked; unchanged launcher ran once with
-NoProfile. Read the private v3 packet's
`paid_execution_ar4_20260911_01/assessment.md`, `launch_result.json`,
`independent_accounting_review.json`, `closeout.json` and `next_stage2_scope.json`.
The frozen prior proposal/closeout are historical, not current replay authority.

The child stopped in 9.4597205s with exit 2, BootstrapDenied/pilot_exception.
Exception closeout keeps completed_pages=null; no page/response artifact exists.
The initialized dispatch journal has zero events. The original USD10 ledger
is byte-identical: 149 finalized, USD2.36852785 settled, zero held,
USD7.63147215 remaining.
No new reservation, settlement or dispatch event was recorded; external billing
was not queried. Existing key was resolved only by the approved executor inside
durable intent; no replacement or separate auth test. No automatic retry occurred.

Launcher/operation/case-phase intents remain consumed. Never repeat the argv,
reset markers or remint directories to replay this approval. The specific
guard-denial code/phase was not retained, so the cause remains unconfirmed.
Next is ONLY `NEXT_STAGE_2`: a new private offline sanitized-diagnostics fixture,
fake key, disposable budget and hard-disabled transport; no real main, original
key/ledger constructor, source/request rebuild or re-review. Preserve frozen
code; diagnose before proposing a minimal fix. Any paid recovery requires a
separate reviewed recovery design and fresh digest-specific user approval.

Canonical 13 dirty docs, runtime 112/candidate 60, source/preferences, old private
artifacts and Word journal remain unchanged. Only six feature handoff/reference
docs are synchronized. Keep GPT-5.2/high, ordinary legacy_text_v1, formatting and
legal preferences, original lifetime budget and all consumed intents. Codex does
AI test review; user reviews future ordinary work; no human-certification claim
or new external English-reference requirement. All-language legal fidelity,
exact-DOCX every-page layout, measured all-call cost/user acceptance and >=20%
efficiency remain open. No Word/Gmail, activation, Stage4 or publication authority.

## Historical continuation — all13 offline requests frozen; paid pilot then unapproved

Fresh `NEXT_STAGE_3` completed the single authorized NON-DISPATCH preparation
in private `structured_activation_request_preparation_v3_2026_09_11`.
Read its `assessment.md`, `private_attempt_result.json`, `validation.json`,
`closeout.json` and `proposals/paid_ar4_scope.json` first. Older packets are
historical, not authority to repeat completed operations.

All13 current-candidate requests are saved: AR9/FR2/EN2. The real preparation
completed once in 101.364s with zero provider/credential/auth/ledger/OCR/render/
Word/Gmail operations and zero guard denials. Every final request hash and its
GPT-5.2/high/default-tier/store=False/24000-output controls were verified.
Accepted source decisions remain unchanged; effective preferences equal the
previous preserved preferences. The earlier partial AR9 remains untouched and
is not relabelled as this new preparation identity.

Before intent: ten copied execution/helper/test files remained byte-identical;
only two private bookkeeping scripts were rebound. All111 fictional tests,
cold actual-SDK/mock4, fictional launcher13, deadline/reuse and guard checks
passed. Independent review and a3061-file executable/dependency/interpreter/CA
freeze preceded intent. Runtime112/candidate60 were not edited in this stage.
Prior Stage2 full3934 and Full evidence remain valid; do not rerun completed
paid/Word/Gmail operations or alter frozen executables to bypass a failure.

The original USD10 lifetime ledger remains149 finalized rows:
2.36852785 settled / zero held / 7.63147215 remaining. The proposed first paid
operation is ONLY AR pages1/5/6/9, one primary request each, zero retries or
corrections, maximum USD4.144. This is a conservative reservation ceiling,
not a measured bill. All13 capacity USD13.468 is not guaranteed affordable.
Current Stage3 supplied NO paid authority; the exact separate proposal requires
fresh `NEXT_STAGE_3` AND digest-specific `APPROVE_PAID_AR4 <operation SHA256>`.
Do not materialize approval-ready templates or touch the original ledger/key
before both approvals. Keep the existing key; no replacement or test is needed.

Canonical main and its13 dirty handoff docs remain protected. Model/defaults,
ordinary `legacy_text_v1`, formatting/translation preferences and every consumed
intent are unchanged. Codex performs AI test review; the user reviews future
ordinary output, without human-certification claims. English keeps the agreed
source-versus-output review; no external corrected reference is required.
Real all-language legal fidelity, exact-DOCX every-page layout, measured
all-call cost/user acceptance and >=20% efficiency remain open. Ordinary
activation, Stage4, publication and later paid/native phases are not authorized.

## Historical continuation — header-precedence fix implemented offline

Fresh `NEXT_STAGE_2` implemented the approved two-file glossary prerequisite.
Read private `structured_activation_header_precedence_stage2_2026_09_11/assessment.md`,
`implementation_review.md`, `validation.json`, `closeout.json` and
`next_stage3_scope.json` first. Older continuation packets remain historical.

The six-line change makes generated headers yield only to eligible configured
entries for the exact same source phrase. Configured wording/case/tier/mode,
strict configured conflicts and invalid-entry rejection remain intact.
Citation aliases, exact source matching, non-PT behavior, limits and Arabic
literal/fingerprint checks are unchanged. The prior deliberate header-conflict
test expectation was explicitly revised. There are 28 added cases plus that
changed case: parent RED reproduced 11 failures, then all 212 glossary and
488 focused regressions passed. Independent stage-only review found no issues.
Full pytest passed 3,934 tests in 568.80s on a bounded rerun; the initial process
exited without a pytest summary and is retained with its hang-diagnostic record.
Two nearby tests passed in isolation; no tests were skipped or checks weakened.
Serial Full passed: 233 browser/state, 2 review and 5 intake tests, compilation,
docs and hygiene. Known Dart AOT wrapper failures had successful direct-Dart
fallbacks. See validation.json for exact timings, limitations and final checks.

Only two product/test files and five feature handoff docs change this Stage2.
Cumulative candidate60/runtime112 membership is unchanged. Canonical main and
its13 dirty handoff docs, the original ledger/Word journal, accepted sources,
old private caller/scripts/manifests and consumed partial AR9 remain protected.
No real preparation, source adaptation/re-review, provider/auth/token-count,
credential resolution, original-ledger constructor/write, OCR/render/Word/Gmail/
live operation or publication ran. Existing key, GPT-5.2/high, ordinary
`legacy_text_v1`, translation/formatting preferences and original USD10 cap
remain unchanged: 2.36852785 settled / zero held / 7.63147215 remaining.

Stop at fresh **`NEXT_STAGE_3`** only after the completed closeout. Its proposed
scope is offline: NEW private root, copy/rebind and fictionally revalidate the
reviewed engine, independent executable freeze BEFORE one new non-dispatch
AR9/FR2/EN2 preparation attempt. The historical manifest still pins old product
bytes; never modify it, replay its intent or relabel old AR requests. Keep exact
accepted sources and every failure. Any unexpected defect stops without retry.
No paid/native authority follows from the stage token; only successful complete
preparation can lead to a separate exact AR1/5/6/9 paid proposal.

Codex AI testing review and future user review remain assigned without human
certification. English retains the agreed source-versus-output review route;
no external corrected English reference is required or claimed. Actual
all-language legal fidelity,
exact-DOCX every-page layout, complete all-call cost/user acceptance and the
distinct >=20% efficiency target remain open; ordinary activation/Stage4 are closed.

## Historical continuation — one-shot preparation stopped on header precedence

Fresh `NEXT_STAGE_3` consumed the single NON-DISPATCH preparation attempt.
Read private `structured_activation_request_preparation_v2_2026_09_11/assessment.md`,
`private_attempt_result.json`, `private_glossary_diagnosis.json`,
`validation.json`, `closeout.json` and `next_stage2_scope.json` first.
All older packets below are historical; preserve every consumed intent.

The caller/preparer/launcher were fictionally tested and frozen BEFORE private
intent: 111 private tests passed, the final 20-test timing subset passed, actual
SDK cold mock integration passed four exact requests, and the final fictional
launcher prepared all13. The one real-source attempt then saved AR9 exact
requests, but FR1 failed before glossary completion/request capture/journal;
EN was not reached. No complete 13-request bundle exists. Never retry this
intent or change its frozen executable bytes; retain partial AR as historical
evidence, not as requests approved under a later candidate identity.

Pure read-only diagnosis and a fictional example identify a configured-versus-
generated header conflict for `audiência de julgamento`: generated FR/EN target
capitalization differs from configured wording; AR agrees. This is a product
prerequisite, not an API-key/auth/model or already-fixed citation-alias problem.
Stop at fresh **`NEXT_STAGE_2`** for the proposed narrow offline precedence fix:
eligible exact-source configured entries override generated header defaults;
genuine configured conflicts/invalid entries still fail. The existing test that
expects header/config conflict failure must change explicitly under this scope.
No real preparation, paid operation or activation is authorized in Stage2.

Product candidate60/runtime112 and the 3,061-file executable freeze are unchanged.
Canonical main/13 dirty handoff docs, accepted source decisions, original ledger
and consumed Word journal remain protected. No provider/auth/token-count/OCR,
render/Word/Gmail/live, real credential or original-ledger constructor/write ran.
Existing key, GPT-5.2/high, ordinary `legacy_text_v1`, translation and formatting
preferences remain unchanged. USD10 lifetime cap: 2.36852785 settled, zero held,
7.63147215 remaining. No paid approval is requested with incomplete preparation.

Codex AI test review and future user review remain assigned, without human
certification. All-language legal fidelity, exact-DOCX all-page layout, measured
all-call cost/user acceptance and the distinct >=20% efficiency target remain
open. Stage4/ordinary activation are unavailable. Prior 3,906 product tests/Full
results are historical passing evidence for unchanged product bytes; fresh
private and final docs/protection results are separated in `validation.json`.

## Historical continuation — citation-alias prerequisite implemented offline

Fresh `NEXT_STAGE_2` implemented only the derived ordinal-alias separator fix
and fictional regressions. Read private
`structured_activation_citation_alias_stage2_2026_09_11/assessment.md`,
`implementation_review.md`, `validation.json` and `next_stage3_scope.json`
first; `closeout.json` pins the completed validation and final freeze.
All older continuation packets below are historical; preserve their consumed
intents and do not replay preparation/adaptation/source-review operations.

The five-line runtime change retains exact source spelling and numeric labels,
strict entry/literal validation, configured preferences and the shared legacy
recognizer. 124 new fictional cases were added. Focused validation passed 430
tests; full pytest passed **3,906 in 529.90s**. Serial Full validation passed:
233 browser, 2 review and 5 intake tests, compilation, docs and hygiene.
The known Dart AOT launcher failures had successful direct-Dart fallbacks.
Independent code review found no actionable issue. Initial failed fixture
expectations are preserved and corrected without changing product strictness.

The new candidate has 60 code/test/tool files and 112 runtime Python files.
Canonical main/13 dirty handoff docs, original ledger/Word journal, accepted
sources, existing key, model/defaults and formatting preferences remain unchanged.
No private request preparation, provider/auth/OCR/render/Word/Gmail/live operation,
ledger mutation, publication or ordinary activation ran in this Stage2.

Stop at fresh **`NEXT_STAGE_3`** for the bounded offline-only scope: author,
fictionally test and freeze the actual private caller/launcher FIRST, then one
new non-dispatch preparation attempt for 13 exact requests using accepted source
bytes. Recheck public pricing and freeze exact config/request/source identities.
No real credential/provider/original-ledger/native branch may run. A product
defect or post-intent executable change requires a fresh scope; never auto-retry.
Only afterward may an exact AR1/5/6/9 paid proposal be presented separately.
No paid caller is implemented/frozen now; no complete private requests exist.

Original USD10 cap remains: 2.36852785 settled, zero held, 7.63147215 remaining.
Historical USD4.144 AR4 capacity proposal stays conditional on fresh applicable
pricing/bounds; all13 affordability is not guaranteed. Codex AI test review and
future user review remain assigned. All-language legal fidelity, exact-DOCX
all-page layout, all-call cost/user acceptance and the separate >=20% efficiency
target remain open; Stage4 is unavailable.

## Historical continuation — request preparation blocked by derived citation alias

The latest `NEXT_STAGE_3` performed public pricing verification and guarded
offline request preparation only. Read private
`structured_activation_request_preparation_2026_09_11/assessment.md`,
`closeout.json`, `fictional_reproduction.json` and `next_stage2_scope.json` first.
Do not replay earlier adaptations or the new failed private preparation intent.

Fictional full-case preparation captured 13 requests; the private attempt stopped
on AR1 before capture/provider contact. Pure predicate diagnosis and a harmless
`nº 2` reproduction identified a generated-alias boundary defect: the recognizer
includes the following separator in `nº `, then strict glossary validation rejects
it. The rejected term is code-generated, not an invalid user preference.
No product fix, source edit, glossary change or real preparation retry occurred.

Stop at fresh **`NEXT_STAGE_2`** for the narrow offline alias-span repair and
regression tests. Keep strict entry/literal validation and exact source/preferences.
A later fresh Stage3 may prepare requests in a new private attempt after the fix.
The actual one-shot paid caller also needs its own reviewed executable freeze;
the preparation wrapper is not a paid executor. No paid approval is requested yet.

Fresh public pricing supports a conservative 400000 input / unchanged 24000
inclusive output reservation: USD1.036 per call, USD4.144 for AR1/5/6/9.
All13 capacity reservations total USD13.468 and are not guaranteed affordable.
Later phases remain conditional. These are proposed bounds, not actual spend
or frozen private request hashes; byte-count-plus64 is not billing proof.

Original USD7.63147215 remainder, existing key, model/defaults/preferences,
58 candidate/112 package files, canonical13 dirty docs and consumed Word journal
remain unchanged. No provider/auth/OCR/render/Word/Gmail/live/source-rebuild call.
Codex AI testing review and future user review remain assigned. All-language
fidelity, exact-DOCX layout, all-call cost/user acceptance and separate >=20%
efficiency remain open; ordinary activation/Stage4 remain unavailable.

### Request-preparation validation closeout

- Guarded fictional preparation: AR9 + FR2 + EN2; 13 approval-withheld captures.
- Final private-harness boundary tests: 11 passed. Private actual preparation
  stopped on AR1 before capture; one failed intent is preserved, not retried.
- Pure saved-source predicate diagnosis and an independent fictional `nº 2`
  reproduction identified the generated-alias whitespace defect; no product fix.
- Docs/harness tests: 7 passed. Standard validation: 233 passed in 183.26s,
  compilation passed; docs and hygiene passed with the established direct-Dart
  fallback after `Unable to find AOT snapshot for dartdev`.
- Previous full 3,782-test result remains historical; no runtime/test change in
  this turn justified repeating it. All paid/native acceptance remains open.

## Historical continuation — offline sources accepted for AI translation testing

Fresh `NEXT_STAGE_3` completed the two frozen adaptations once each: AR 9 pages
(1.829s), shared FR/EN 2 pages (1.812s), 1037 bound checks before/after each.
Read private `structured_activation_reviewed_source_integration_stage3_2026_09_10/closeout.json`,
`inspection.json`, `source_decisions.json`, `assessment.md` and
`next_operation_proposals.json` before older packets.

Exact text/newlines, all action/baseline ownership, source/readiness/output hashes
and starts AR 1/3/4, FR/EN 1 passed saved-output inspection without another
adapter/build call. Codex separately accepted transcription/document boundaries
for these AI-reviewed tests using the unchanged completed reviews (11 pages,
44 resolved observations). Two proposed source-review envelopes are private
evidence, not config/paid authority. Adapter outputs still say nonproduction/
not_evaluated; geometry/layout and translation acceptance remain unclaimed.
One reviewed block per page does not prove paragraph/table/furniture layout.
Cross-page lists/sentences and literal awkward source syntax are review watchpoints.

No adapter defect was observed. Stop at fresh **`NEXT_STAGE_3`** for the exact
request/price-preparation-only proposal (hash in closeout): read-only official
pricing verification and guarded non-dispatching request/config/bounds preparation.
No paid/provider/auth-test, OCR/render, Word/Gmail, source rebuild, ledger mutation,
ordinary activation, model/default change or publication. Then ask precise paid
operation approval; Word requires later exact new DOCX hashes and separate scope.
The old adaptation intents are consumed and must never be replayed.

All 58 candidate/112 package files, canonical 13 dirty docs, existing key/defaults/
preferences, original USD7.63147215 remainder and Word journal are preserved.
Only five feature handoff docs changed. No paid/OCR/Word/Gmail operation occurred.
Parent AI review is not independent or certified human review; existing agents
remain quota-limited. User reviews future ordinary outputs. All-language quality,
exact-DOCX Word layout, all-call cost/user acceptance and separate >=20% efficiency
remain open. Stage 4 is unavailable.

## Historical continuation — acceptance-only reviewed-source integration validated

Fresh `NEXT_STAGE_2` completed the approved offline integration. Read private
`structured_activation_reviewed_source_integration_stage2_2026_09_10/closeout.json`,
`implementation_review.md`, `validation.json` and the active ExecPlan first.
The existing API key is retained unchanged; no API call or credential operation ran.

The explicit acceptance lane now verifies externally approved candidate/evidence
bytes, requires a separately authored source decision, routes reviewed text before
extraction/OCR, and retains request/accounting/resume guards. New reviewed IDs
map to original actions/blocks. Exact text/newlines are preserved; geometry,
paragraph joins, furniture and layout acceptance are not invented. Ordinary
jobs still use unchanged GPT-5.2/high and `legacy_text_v1`.

Validation: 57 new product tests; 253 focused tests, final 66-test subset and
full pytest **3,782 passed in 527.70s**. Serial Full validation passed, including
233 browser, 2 review and 5 intake tests, compilation and successful direct-Dart
fallbacks after the known wrapper AOT failure. Private continuation tests passed
24 assertions in 17 cold runner processes plus one guard process (16 actual
access denials). These counts overlap; fictional tests are not legal/layout proof.

Stop at fresh **`NEXT_STAGE_3`** for the new frozen `continuation_scope.json`
(exact hash in closeout): one cache-only adaptation per saved AR/shared FR/EN
candidate, full-case saved-output inspection and separate source-decision
preparation only if justified by unchanged completed reviews. No real adaptation
or acceptance record was produced in Stage 2. No candidate rebuild, re-review,
OCR/render, provider, Word/Gmail, live app, publication or ordinary activation.
No automatic retry; preserve every consumed historical intent. The next runner
retains `production_eligible=false` and never authors an acceptance decision.

Cumulative freeze: 58 candidate files / 112 runtime Python files. Only seven
runtime files, two tests and these five feature handoff files changed this turn;
all canonical 13 dirty docs, prior evidence, original USD7.63147215 remainder,
consumed Word journal and translation/formatting preferences are preserved.
Cached A4-assumed page frames are not measured/certified geometry. Codex is the
AI test reviewer; the user reviews future ordinary outputs. Parent-local review
is not independent/human certification; existing agents remain quota-limited.
All-language legal quality, exact-DOCX Word layout, all-call cost acceptance and
the separate >=20% efficiency target remain open. Stage 4 is unavailable.

## Default Rules
- Use the repo Python executable: `.\.venv311\Scripts\python.exe`.
- Do not run pytest through bare or global `python`.
- Run targeted tests first, then broader validation when the touched scope warrants it.
- Stop before merge if local validation, GitHub checks, branch identity, or worktree cleanliness is not clean.

## Approved formatting integration

### Successful saved-review candidate builds (2026-09-10)

The frozen corrected runner completed exactly two permitted builds: AR 9 pages
in 1.343s and shared FR/EN 2 pages in 1.281s. Each made one helper call, verified
895 bound files before/after, committed its result last and retained its consumed
intent. Zero retries/preparation/new review/OCR/render/provider/Word/Gmail.

Read `structured_activation_source_review_build_v2_2026_09_10/inspection.json`
and `closeout.json`. Direct read-only saved-JSON comparison verified candidate
file/self/text hashes, manifests/reviews, exact before-text/baseline IDs, action
coverage/order and output text. No helper import/rebuild. Coverage: 11 pages,
17 variants, 128 baseline blocks, 44 recorded resolved observations, 0 unresolved
assertions; boundaries AR 1/3/4 and shared FR/EN 1. Both candidates retain
nonproduction status and source/layout acceptance not evaluated.

All 810 protected files passed immediately after builds, including frozen feature
docs before their authorized handoff update. The final closeout records five
authorized doc updates separately from all other protections. Original
56-file candidate, 111-file package, canonical 13 dirty docs, ledger and consumed
Word journal remain unchanged. Direct-Dart docs/hygiene and diff checks are
recorded there. No product-test rerun: the unchanged candidate retains the prior
3,725-test pass; this is not a new full-suite result.

The next gate is fresh `NEXT_STAGE_2` for the precise acceptance-only adapter/
routing scope and fictional tests; no real-source adaptation or acceptance
generation in that Stage 2. Real bilingual quality, all-page Word layout and
measured all-call cost remain open. Parent-local inspection is not independent
review or human/legal certification. Stage 4 remains unavailable.

### Private continuation runner correction (2026-09-10)

Final `run_003` passed 25 assertions in 21 cold Python processes using the actual
guarded executor entrypoint and unchanged pure helper. Fictional 9-page/2-page
cases cover 17 variants, UTF-8 accented text and CRLF OCR TXT. Candidate file and
self-hashes, immutable input bytes, unresolved/partial/bad binding cases, external
scope/manifest hashes and successful/failed intent replay were checked.
A separate guarded process passed actual exclusive open/read and 16 denied API
operations, including attempted process/network/registry/native launch/load.
Only the scoped Python harness processes ran; no real OCR/provider/Word/Gmail.

The preload fixes the missing `__future__` import; guard permissions are not
broadened. Preflight loads the helper without candidate calls/writes. Parent-local
review added before/after runner/scope-file rechecks and retained commit-last,
exclusive durable intent semantics. Earlier successful development runs are
preserved, not additional unique assertions. No full-suite rerun: frozen
56-file candidate/111-file package and historical 3,725-pass result are unchanged.

Read `structured_activation_source_review_runner_fix_2026_09_10/closeout.json`
for final direct-Dart docs/hygiene, protection and real-input hash-only preflight
results. Real builds remain gated by fresh `NEXT_STAGE_3` and its exact frozen
scope. No actual source/candidate/schema/legal/layout acceptance follows from
synthetic validation or the 11 previously authored AI review records.

### Cached-source review execution and cold-import stop (2026-09-10)

One preparation and all 11 AI image reviews/17 baseline-pilot text comparisons
completed; 11 explicit records contain 44 resolved observations. External file
hashes bind the final manifests/reviews. This does not prove legal correctness,
candidate schema validity or production source/layout acceptance.

The first AR intent is consumed; the executor stopped before `build_candidate`
because its guard denied the helper's cold stdlib `__future__` import.
No candidate output, FR/EN attempt or retry. The earlier guard/preflight checks
did not load the helper under the audit hook and therefore missed this path.
Read-only cold/preload probes reproduced the denial and passed helper import plus
canonical JSON with 11 retained forbidden-operation/read denials; zero candidate
builds/writes. Do not claim these probes validated the full reviewed candidates.

Preserve `structured_activation_source_review_stage3_2026_09_10`.
Fresh `NEXT_STAGE_2` is limited to a distinct private executor correction and
real cold-entrypoint synthetic 9-page/2-page end-to-end tests, including failure,
replay, binding and guard controls. No real source exercise or production changes.
Only after passing tests/freeze may fresh `NEXT_STAGE_3` authorize replacement
AR/first FR/EN builds in a separate root. Reuse all reviewed input bytes.
Fresh docs/hygiene/protection results belong in the closeout; the unchanged
56-file candidate retains the prior 3,725 full-test evidence, not a new rerun.

### Unpublished ordinary-use prerequisite gate (2026-09-10)

Current offline reviewed-source helper adds two files only, without changing
production `src`. The 70 new tests and combined 151-test focused gate passed
(4.60s); full pytest passed **3,725 tests in 474.76s**, zero failures/errors/skips.
Synthetic checks cover immutable byte/TSV/geometry binding, complete later-page
review, wrong/overlapping image regions, ownership/order, explicit unresolved
findings, candidate tampering, denied ambient I/O and production-schema rejection.
Private future-executor guard checks passed actual exclusive open/read plus
11 denials with zero source reads/candidate builds. Its Stage 2 preflight is
read-only; real cached-source exercise requires fresh `NEXT_STAGE_3`.
Serial Full passed: 233 browser tests in 182.94s, compilation, 2 review tests
in 0.50s, 5 intake tests/9 deselected in 0.92s, plus docs/hygiene via the known
successful direct-Dart fallbacks after the two AOT launcher errors. Exit was 0.
Final candidate/protection and read-only preflight evidence is in private
`structured_activation_source_review_stage2_2026_09_10/closeout.json`.
Do not mistake these synthetic checks for real source/legal/layout acceptance.

Historical corrected v2 pilot: all six local OCR passes completed once in 9.328s,
zero retries/guard violations. Nine negative guard checks, two command-shape
checks and six actual-Popen no-launch command tests preceded real dispatch.
780 file checks passed before/after. Codex visually compared three full saved
pages and read all six new/three baseline texts: no whole-page candidate passes
source fidelity. Keep all six review-required/layout-needs-review statuses and
original winners. PSM3's identity-policy rejection is not a missing TXT/TSV
binding: all six token bindings passed; existing layout policy is unchanged.
Read the active plan and private v2 pilot comparison/closeout. Next is fresh
`NEXT_STAGE_2` for a purely offline reviewed-source candidate helper, not
production integration, further recognition or paid/native acceptance.
The unchanged runtime retains its existing full test evidence below.

Historical first local OCR pilot stopped before its first CreateProcess: the private audit
guard mishandled `executable=None`. Zero OCR passes/candidate outputs; no quality
comparison or automatic retry. The frozen pilot executor had passed eight
synthetic negative/two command-shape checks and 767 file checks, but those tests
did not exercise the actual Popen executable field. A subsequent no-launch
regression exercised the actual Windows Popen path with CreateProcess replaced,
confirming explicit pinned executable success plus missing/altered command
rejection (one positive, two negative, zero launches). Retain this distinction;
synthetic serialization checks alone are insufficient launch-shape evidence.
The new six-pass continuation requires fresh `NEXT_STAGE_3`, corrected frozen
executor and current feature-doc snapshot. Runtime/full-test candidate is unchanged.

Current source-readiness corrective candidate passed 428 focused tests in 56.35s,
3,655 full tests in 476.91s (zero failures/errors/skips), and serial Full validation:
233 browser-state tests in 182.65s, compilation, 2 review-state tests, 5 intake
tests with 9 deselected, docs/hygiene through successful known direct-Dart fallbacks.
Its 54-file candidate and 111-file package are frozen in the private source-readiness
Stage 2 packet. Use the active plan's current section, not earlier totals below.
The subsequent cache-only Stage 3 pass completed once on all 11 real cached pages
in 1.734 seconds, with 642 file checks before/after and unchanged 2,682 words,
128 blocks, geometry/ownership and decoded pixels. Nine non-first-page boundary
flags now conservatively protect joins, including missed AR starts 3/4. All 11
remain source/layout-review-required: nine stop at document_boundary_uncertain,
two at complex_or_uncertain_block; later pixel-layout gates were not reached.
Four missing/historical-review inputs were rejected for the two full cases.
Eight synthetic guard checks and guarded feature imports passed before execution;
the initial harness import incompatibility is preserved separately. No new OCR,
PDF/browser rendering, provider/Word/Gmail operation or production edit occurred.
Do not repeat the completed full suite for this docs/private-evidence-only pass.
Read the active plan and private `structured_activation_source_readiness_stage3_2026_09_10`
packet; the next six-pass local OCR pilot needs its fresh exact `NEXT_STAGE_3`.
Real source fidelity is still blocked. Parent-local AI review is not independent review.
Add `test_source_readiness.py` and the extended real-budget campaign fixtures.
Require saturated OCR-score diagnostics, same-pass confidence/geometry reasons,
missing-title boundary review, explicit hash-bound boundary decisions without
clearing OCR uncertainty, full-case/later-page zero-dispatch checks, physical
review-file membership/drift guards, and byte-preserving layout rebuilds.
The positive simple-scan fixture supplies an explicit synthetic boundary review;
the unreviewed control must not consolidate furniture or join paragraphs.
These tests do not constitute real OCR repair, bilingual review or Word acceptance.

Use the isolated activation worktree and [active ExecPlan](exec_plans/active/2026-09-10_structured_translation_ordinary_use.md),
not the completed PR #295 counts below, for new validation. Keep provider/native
calls fake, import from that worktree, and preserve canonical docs plus protected
budget/Word-journal hashes. Cover both protocols' real manager/workflow resume,
damaged accounting evidence, workspace settings and environment/OCR injection,
app construction and bootstrap/capability tripwires, provider cloning, every
dispatch outcome, inclusive/cache usage, strict price identities, hard-budget
concurrency/crash holds and original-ledger adapter guards. Follow with full
pytest and serial `validate_dev.ps1 -Full`; record the Dart fallback explicitly.
Freeze exact runtime/test digests and independent review before stopping for
`NEXT_STAGE_3`. Passing these checks does not establish real legal quality,
Word-rendered pagination, production price readiness or savings.

Historical first Stage 2 closeout: the frozen 37-file candidate passed 3,508 full tests in 474.87s
(zero failures/errors/skips), then serial full validation passed: 233 browser
tests, compilation, two review tests, five intake tests with nine deselected,
docs and hygiene. Both known Dart AOT launcher failures had successful direct-
Dart fallbacks. The first full run's fixture/offscreen-only failures and the
interrupted first wrapper session remain documented in the active plan; only
the wrapper was repeated after interruption. Canonical 13-file handoff and
original ledger/Word-journal hashes remain unchanged. Stop at `NEXT_STAGE_3`.

Corrective Stage 2 adds genuine ordinary-caller pricing with no accounting-factory
override, tier/scope/currency and endpoint checks, saved exact catalogs, hard-auto
worst-case bounds, legacy unknown-price compatibility, and immutable controls.
The acceptance matrix must use the real v2 original-ledger adapter with fake SDK
responses: pilot AR 1/5/6/9, zero-call pilot replay, remaining five pages, separately
approved pending correction, crash/tamper/duplicate rejection and unchanged prior
rows. Require full executing-package membership plus actual effective config and
preferences, rehashed at reservation and immediately before send. Standalone
continuation mocks are not substitutes for real budget/workflow integration.
The new 46-file freeze passed 385 combined targeted tests in 63.24s and 3,624 full
tests in 519.44s (zero failures/errors/skips). Serial full validation passed:
233 browser tests in 192.35s, compilation, 2 review tests and 5 intake tests with
9 deselected, plus docs/hygiene through both documented successful direct-Dart
fallbacks. Independent review and final frozen/protected-byte checks passed.
Exact artifact hashes and evidence paths are in the active plan's corrective
packet. Stop at a fresh `NEXT_STAGE_3` for preparation, not paid/native authority.
Do not infer real OCR/model/legal/layout acceptance from these fake campaigns.

### Current source-associated integration (PR #295 merged)

PR #295 is merged at canonical `main@4780a2e16c32adf7af7479656f0b5fafdff83224`, still opt-in only. The former task tree is retained clean/detached at that SHA; both source branches are deleted. Assert the exact tested tree's `src` import origins; `.venv311` may reuse the canonical environment through the documented local junction. No provider, auth, OCR or native Word call is needed for offline tests. Keep default protocol/model/effort/OCR routes and all browser/CLI/Qt/Gmail submitted contracts unchanged unless a later task explicitly covers them.

Stage 4 publication/canonical verification and the bounded native/human layout acceptance below are complete. Final source-grapheme validator v5 passed 318 focused tests in 7.39s and 3,391 local full tests in 428.49s; independent review and serial full validation passed, using the documented successful direct-Dart fallback. Final exact-head CI run `34411719376` passed both jobs: Windows 3,391 tests in 635.44s with one upstream Starlette/AnyIO deprecation warning. The initial CI run failed two docs-fixture cases because `branch:` had incorrect capitalization; commit `2ef286c36a2e29191d54d582dcc00bc07f7d8ca6` restored exact `- Branch: \`main\`` and all 72 agent-docs tests passed. Canonical post-merge verification passed 20 tests in 4.11s, import/policy isolation and an exact 46-file runtime/test digest comparison. See [the actual post-merge addendum](exec_plans/completed/2026-09-09_structured_new_translations.md); do not reuse these totals as validation of future changes.

Ordinary launches continue to default to `legacy_text_v1`. The canonical UI smoke used a process-only `legal_blocks_v2` flag, not durable activation. It verified New Job/Recent Jobs, language controls, build identity and UI/API wiring with private state and explicit `not_evaluated` provider/native/auth boundaries. Shadow mode, GET-only requests and disabling the bridge alone are insufficient: bootstrap/capabilities can invoke Word readiness and account/OAuth paths. The unguarded helper was not executed. The corrected private helper used fail-closed boundaries before app construction; its owned server/processes/tab were cleaned up. See the [isolation workflow](workflows/HARNESS_ISOLATION_AND_DIAGNOSTICS_WORKFLOW.md). This is not real provider, translation, Gmail or Word readiness acceptance, and its consumed helper must not be replayed.

Cover `test_new_translation_blocks.py`, `test_new_run_callers.py`, `test_new_run_extraction.py`, `test_new_run_source_binding.py`, `test_new_run_adapter_safety.py`, `test_new_run_preflight.py`, `test_new_run_continuations.py`, `test_translation_structure.py`, `test_openai_structured_transport.py` and the `test_structured_*` suites. Real source extraction, coordinator/publication and DOCX assembly must execute with fake provider responses; mocked coordinator success alone is insufficient. Exercise EN/FR/AR, both layout modes, digital tables/list/empty-cell coverage, actual OCR-winner binding, page selection, event/date swaps, accented Arabic/Latin names, terminal refusal/incomplete output, bounded correction, cancellation and retained failed-call usage.

Require protocol/source/context/glossary version invalidation, no-write rejected resume (including output-probe sentinels), strict commit-last recovery without redispatch, mixed/orphan checkpoint rejection, completed-page whitelists, TXT-only manual rebuild without overwriting prior DOCX, explicit retention opt-out and the existing budget block. Derive confirmed sentence continuations only in memory from verified completed source/target pairs; hash-bound artifacts must survive assembly/resume/rebuild unchanged. Partial or missing pages and manually edited/uncertain evidence must not manufacture continuation proof.

Same-ID clause omissions must remain fidelity `not_evaluated`, not quality acceptance. All-call structured cost remains `not_evaluated_all_calls`; reasoning tokens are a breakdown of output, not added again. Fake-provider passage does not prove live model compatibility, 20% savings, a reservation-safe paid path or Word-rendered page counts. Run full pytest and then serial `validate_dev.ps1 -Full`; record final exact counts and any native diagnostic in [the implementation/publication closeout](exec_plans/completed/2026-09-09_structured_new_translations.md). Past Stage 3/native authorities are consumed; the original lifetime budget and fresh specific authorization remain mandatory for any separately proposed paid/native work.

### Historical candidate and corrective validation evidence

The dated stages below retain their original outcomes, counts and failure evidence. Their then-current continuation tokens and "active ExecPlan" references describe past checkpoints, not present authorization or current-build validation. Completed PR #295 status is above; the plan's retained history is now linked from its completed closeout path. The next assessment is summarized in [FRESH_CHAT_BRIEF.md](FRESH_CHAT_BRIEF.md).

Stage 2 finished-code evidence on 2026-09-09: **2,924 full-suite tests passed in 378.69s**, then serial full validation passed (233 browser tests, compilation, 2 review tests, 5 intake tests with 9 deselected, docs/hygiene and successful direct-Dart fallbacks). Final composition coverage verifies split sentences together with consolidated section headers/contact footers, complete source mapping and unchanged bundle hashes. This is isolated offline implementation acceptance only; the canonical live app remains unchanged.

Stage 3 outcome on the same frozen code: **rollout acceptance failed**. The normal browser-bundle route was exercised with explicitly synthetic OCR/provider responses, not pre-certified digital source evidence. A two-page EN control completed all 26 blocks and flagged both pages for review. One fresh native Word export passed in 6,536 ms with confirmed cleanup; all of its one rendered page was inspected and showed repeated headers/contact groups within body flow. AR source-exact `Álvaro Exemplo` after `Destinatário` and in table-like text was wrongly rejected; pure per-block replay and independent review confirmed missing source-name association. Do not count the corrected private glossary/list-fragment fixture failures as product failures. No actual OCR/model/legal quality, French/full-reference visual result, human approval or paid cost comparison is claimed. See the active ExecPlan for preserved evidence, consumed native authority and corrective regression requirements.

Corrective coverage adds `test_structured_arabic_source_names.py`, `test_source_layout_eligibility.py` and `test_new_run_layout_eligibility.py`. Exercise the real browser bundle, image rendering, OCR helper binding, coordinator and writer with only the recognition command/provider replaced; a pre-certified structure injected at the workflow boundary is insufficient. Require exact per-word confidence/ownership/geometry, raw-raster versus re-encoded OCR-input hash binding, simple-flow-only admission in both layout/furniture planners, same-paragraph list wrapping, split sentences, stale/missing proof, changed raster, partial selection, manual edits and no-call byte-preserving rebuild. Reject columns, ambiguous boundaries, low confidence, word/line overlap and unresolved table/ruling evidence; never clear the original page uncertainty. Freeze a new runtime/test digest, then run targeted/full pytest and serial full validation before the fresh Stage 3 gate. No native/paid operation belongs in corrective Stage 2.

Corrective finished-build evidence: **646 targeted tests passed in 32.07s; 3,085 full-suite tests passed in 437.29s**, with zero failures/errors/skips. Serial full validation passed (233 browser tests in 191.65s, compilation, 2 review tests, 5 intake tests with 9 deselected, docs/hygiene and the two documented successful direct-Dart fallbacks). No COM diagnostic appeared; no native readiness or visual acceptance is inferred. The new 42-file runtime/test digest and log paths are recorded in the active ExecPlan. Its subsequent fresh `NEXT_STAGE_3` is now consumed for the corrective acceptance below; production remains unchanged.

Corrective Stage 3 browser/local checks passed EN/FR/AR with 20 mapped blocks, one join and real header/footer parts using synthetic recognition/provider responses. The old EN checker stopped verdict remains preserved; separate EN visual review passed. Fresh FR/AR native exports passed in 6,157/6,060 ms with confirmed ownership/cleanup. Every-page Poppler review found one A4 page each: French passes; Arabic fails because `09:30` displays `30:09`. PDF glyph x coordinates and DOCX LTR09/RTLcolon/LTR30 runs independently confirm it; logical PDF text can mask the reversal. This historical visual failure remains valid for those unchanged outputs.

The subsequent clock corrective Stage 2 adds `test_docx_clock_runs.py`, `test_docx_clock_contexts.py` and `test_docx_clock_rebuild.py`, plus the v11 profile regression. Require coherent LTR standalone ASCII `HH:MM[:SS]` values across split/whole/plain/mixed tokens, stripped/retained LRI/PDI, six paragraph contexts, typography/alignment/page fields and zero-call legacy/structured rebuilds. Preserve invalid/identifier/prose/line boundaries and unsupported explicit direction controls; do not silently reinterpret ambiguous numeric text. Check formatting invalidation independently from unchanged translation fingerprints and exact source/TXT/target/commits/history plus retained usage/fidelity data. Finished evidence: 298 core tests (9.76s), 910 broader targeted tests (49.90s) and 3,322 full tests (429.73s) pass on the frozen 46-file candidate, with zero failures/errors/skips. Serial `validate_dev.ps1 -Full` also passed: 233 browser tests (181.37s), compilation, 2 review tests, 5 intake tests with 9 deselected, docs and hygiene. Both known Dart AOT wrapper failures used successful direct-Dart fallbacks; no COM diagnostic appeared. Logs, exact digest and final read-only checks are in the active ExecPlan. Offline Stage 2 is complete; await fresh `NEXT_STAGE_3`. No native/paid operation belongs here. Fresh Word rendering and every-page/time-glyph inspection remain required after a new Stage 3 and scoped native authority; XML tests alone cannot accept the corrected Arabic document.

The [completed 2026-09-09 footer-spacing integration](exec_plans/completed/2026-09-09_footer_spacing_integration.md) has user approval of the retained v18 native/visual pilot and passed local new-tree validation: 615 focused tests in 95.66 seconds, 2,492 full-suite tests in 380.77 seconds and serial `validate_dev.ps1 -Full` (233 browser compatibility tests, compilation, 2 review tests, 5 intake tests with 9 deselected, docs and hygiene). Both known Dart AOT wrapper failures used successful direct-Dart fallbacks. No COM diagnostic appeared; this does not claim the historical host diagnostic is fixed. The user explicitly approved publication, merge after checks and app verification; verify actual exact-head CI and canonical post-merge results separately. Do not reuse research-tree totals as integration results or replay the consumed native helper. This lifecycle requires no new Word operation, OCR/API call or recovery publication.

Cover source-folio detection and provenance, header-to-body gaps, footer separation, and the contact-only exception for a complete isolated single-source-page output. Require compact reflow (`page_breaks=False`) and complete matching source/target sidecars. Negative fixtures must retain substantive/uncertain footer-like text in the body, reject isolated-header adoption and partial-preview adoption, and preserve TXT-only production, explicit page matching, separate sections and existing review findings. Include subset dependency/import-origin and unchanged production-policy checks.

Validate compact writer, structure/regions, source-spacing and section-furniture suites, including different/headerless/footerless sections, source/target hash binding, ambiguous evidence, all source-map aliases, readable Arabic/Latin runs, PAGE continuity, tables, legacy TXT and explicit page matching. Run local rebuild integration and production-policy isolation regressions: saved translation bytes, prior usage/costs and existing review findings must remain intact, and no new API/OCR client may be constructed by rebuilding. Keep prompt/effort/image/source-extraction regressions in the compatibility set.

Rich source-aware formatting requires complete matching structured pairs. Current production TXT output cannot be made source-associated merely by matching paragraph counts or numeric anchors. Tests must exercise this fallback rather than use the experimental translation workflow to manufacture a passing release.

Rebuild private accepted examples into new outputs; compare every DOCX package entry against the accepted Word-rendered version. Exact package equality permits reuse of its bound render evidence. Any layout/content difference requires fresh rendering and every-page inspection. Keep private PDFs/DOCXs/PNGs outside Git; use synthetic accented names in published fixtures. Translation deliverables remain DOCX.

The existing native Windows `0x8001010d` diagnostic can occur at the honorarios small-screen dialog's `app.processEvents()` even when assertions pass. Record native and isolated rerun outcomes; do not suppress the diagnostic. This layout-only test does not invoke Word export, and offscreen success is not native export acceptance. The honorarios export repair below does not claim to fix this separate Qt event-processing diagnostic.

Qt render-review subprocess tests must read the renderer's JSON artifact rather than assume stdout contains only JSON: dependency diagnostics can also appear there. Keep strict artifact parsing, successful process exit and geometry assertions; exercise noisy stdout without suppressing warnings or weakening layout checks.

### Clock corrective Stage 3 native verification

The fresh `NEXT_STAGE_3` was supplied after the 3,322-test offline candidate passed. Three public rebuilds on exact private saved-run copies passed all content/mapping/font checks and retained source/TXT/targets/commits/history, usage and fidelity. English/French each match all 23 uncompressed package entries of the original native-reviewed document; independently verified manifest/DOCX/PDF/PNG bindings justify reuse of their one-page synthetic visuals. Preserve the old English runner's stopped verdict and separate visual review. Arabic differs only in body paragraph index 10: LTR `09`, RTL colon and LTR `30` become one LTR `09:30` with the same visible text and numeric run formatting. The subsequent explicitly authorized native check below supplies its fresh visual evidence.

The new private AR-only helper passed 125 offline tests, and file-only preparation bound the corrected DOCX/map, source fixture, 46-file change lock, all 276 Python files, executable identities and original journal. Its manifest is `clock_native_preparation/manifest.json`, SHA256 `7ebdff47c3a162dd991ed42419e29422980ae6171c6a7fa9a3fa630f479ff4c9`. The user's explicit `yes` authorized exactly one 45-second/no-retry Word check. It succeeded in 6.183 seconds: one native call, zero retries/recoveries/paid calls, original-slot journal verification, confirmed owned-process exit and unchanged pre-existing process identities. User document contents were not inspected. Preserve the legitimately advanced original journal, SHA256 `94cdbb415aa8439951958680528c461296e3a0f12174ce96a493ad9e7a97dd8f`; authority is consumed.

Fresh Word PDF SHA256 `e21809d9301eaa45f5a03539d60fe8a462b79368bf949438776e8e3a7c7807c5` contains one A4 page. Both complete-page visual reviewers passed the clock, accented names, header gap, bottom contact footer and page number with no observed clipping or overlap. Physical left-to-right glyph positions for `0`, `9`, `:`, `3`, `0` are respectively 328.134, 334.272, 340.907, 343.490 and 349.629 points; unlike logical extracted text alone, this verifies the corrected display order. Supplemental private `clock_native_review.json` records this review without rewriting helper or historical verdicts. The user approved this exact corrected Arabic preview with `looks right` on 2026-09-09; private `clock_human_approval.json` records the PNG/DOCX hashes and that stage's unchanged runtime digest. This completed bounded Stage 3 layout acceptance; Stage 4 publication was subsequently explicitly authorized as recorded above. No real legal/model/full-reference/cost acceptance or durable production activation follows from the layout result. App/test bytes were unchanged during that private-QA/docs-only pass; the later two-file v5 validator delta requires its own final validation.

## Honorarios Word-to-PDF export

Translation deliverables remain editable DOCX. Validate PDF generation only for the separate honorarios document; retain its DOCX and do not change legal wording, fees or layout to obtain a passing export.

Focused offline regressions:

```powershell
.\.venv311\Scripts\python.exe -m pytest -q tests/test_word_automation.py tests/test_word_pdf_control.py tests/test_word_pdf_artifacts.py tests/test_word_pdf_runtime.py tests/test_word_pdf_script.py tests/test_honorarios_docx.py
```

These tests must not launch Word implicitly. Keep the native-launch guards in `tests/conftest.py`; parser, compiler and fake-COM checks do not establish native export readiness. Cover process identity and reuse, existing documents, content-free durable phases, early setup/pipe failures, bounded helper cleanup, cross-process exclusion, uncertain-cleanup quarantine, stale output, atomic publication, origin/security rejection, original-file hashes and accurate cleanup metadata. Browser/Qt callers perform one export with a 45-second allowance and bounded cleanup, not an automatic timeout re-probe/re-export.

For direct hidden startup, include `tests/test_word_process_start.py`, `tests/test_word_direct_launch_safety.py` and `tests/test_word_startup_diagnostics.py`. Compile the exact wrapper without invoking it and exercise native API behavior only with fully substituted fakes. Require all seven startup predicates: returned process, running process, new PID, matching session, `WINWORD` name, start time not before launch, and matching executable. Preserve retained-handle ownership, legacy journal compatibility and bounded content-free diagnostics; failed checks must stop before document opening.

Run opt-in native acceptance on the same Windows host/interpreter as the app, only after the ownership and journal safeguards are active. Keep one original state directory across failure/recovery; changing APPDATA or deleting the journal must not bypass quarantine. Use synthetic data, an isolated shadow workspace and ignored/private artifacts. Never terminate unknown Word sessions or change Office security settings to make a test pass.

Require a real expected-text canary, translation-kind and interpretation-kind honorarios through shared caller paths, the browser export route and asynchronous Qt worker, coexistence with an unsaved synthetic Word document, and an already-open source DOCX. Check unchanged source hashes and pre-existing window/content state, fresh readable PDF pages, confirmed owned-process exit and a healthy export after a bounded startup timeout. A startup-only probe, a PDF header, or an older PDF is insufficient. Render and inspect every page of retained Word-produced PDFs; neither XML checks nor another renderer substitute for Word-native acceptance. Observe cold conditions only when safely available, without closing unknown user work.

Accepted 2026-09-07 evidence: eight successful native exports, including the two canaries, shared/browser/Qt paths and both coexistence cases; all six retained honorarios PDFs were one page and passed complete-page Poppler PNG inspection. Full pytest passed **2,176 tests** and `validate_dev.ps1 -Full` passed with the documented Dart fallback. The separate native small-screen Qt test passed its assertions but repeated `0x8001010d` on serial rerun; preserve that limitation. Browser acceptance exercised the real route with isolated data, not a claimed browser-button click. No live Gmail operation, Office repair or host-security change was part of this acceptance. See the [completed repair ExecPlan](exec_plans/completed/2026-09-07_honorarios_pdf_export_reliability.md) for exact evidence and scope.

## Common Targeted Browser And Gmail Tests
```powershell
.\.venv311\Scripts\python.exe -m pytest -q tests/test_shadow_web_api.py tests/test_shadow_web_route_state.py tests/test_browser_safe_rendering.py tests/test_translation_browser_state.py tests/test_gmail_review_state.py tests/test_profile_browser_state.py
```

Add the Qt Delete-key regression when the task touches Qt Job Log behavior or merge readiness:

```powershell
.\.venv311\Scripts\python.exe -m pytest -q tests/test_qt_app_state.py::test_joblog_window_delete_key_removes_selected_rows_when_table_has_focus
```

## Full Local Regression
```powershell
.\.venv311\Scripts\python.exe -m pytest -q
```

Use full pytest before merge, after test/code/workflow changes, or when a focused failure might be suite-order dependent.

## Validation Wrapper
```powershell
powershell -ExecutionPolicy Bypass -File scripts/validate_dev.ps1
powershell -ExecutionPolicy Bypass -File scripts/validate_dev.ps1 -Full
```

`validate_dev.ps1` runs focused browser tests, compileall, docs validation when docs changed, and workspace hygiene. `-Full` adds focused Gmail review/intake coverage.

## Dart AOT Fallback
On this machine, `dart run ...` can fail with:

```text
Unable to find AOT snapshot for dartdev
```

That is a known launcher issue, not automatically a product failure. The validation wrapper should fall back to:

```powershell
C:\dev\tools\flutter\bin\cache\dart-sdk\bin\dart.exe tooling\validate_agent_docs.dart
C:\dev\tools\flutter\bin\cache\dart-sdk\bin\dart.exe tooling\validate_workspace_hygiene.dart
```

Record both the wrapper failure and direct-Dart fallback success in validation summaries.

## Google Photos Interpretation Validation
Use this section for the Interpretation-only Google Photos Picker import feature.

Focused commands:
```powershell
.\.venv311\Scripts\python.exe -m pytest -q tests/test_google_photos_picker.py tests/test_interpretation_google_photos.py tests/test_metadata_autofill_photo.py
.\.venv311\Scripts\python.exe -m pytest -q tests/test_shadow_web_api.py
.\.venv311\Scripts\python.exe -m pytest -q tests/test_interpretation_review_state.py tests/test_honorarios_docx.py tests/test_qt_app_state.py
.\.venv311\Scripts\python.exe -m pytest -q tests/test_shadow_runtime_service.py
powershell -ExecutionPolicy Bypass -File scripts/validate_dev.ps1
```

Run the safe config gate before live OAuth/Picker work:
- `configured=true`
- `client_id_source=process_env` or `windows_user_env`
- `client_secret_source=process_env` or `windows_user_env`
- scope exactly `https://www.googleapis.com/auth/photospicker.mediaitems.readonly`
- shadow redirect exactly `http://127.0.0.1:8890/api/interpretation/google-photos/oauth/callback`
- live redirect exactly `http://127.0.0.1:8877/api/interpretation/google-photos/oauth/callback`

Live validation acceptance checklist:
- OAuth reaches `connected=true`.
- Token store is present.
- `Choose from Google Photos` is enabled.
- Picker session is created and polled.
- User selects exactly one non-private test photo.
- Google Photos completion screen or auto-close indicates selection finished.
- `mediaItemsSet=true` is observed.
- selected media items are listed.
- import route is called.
- selected image imports into the existing Interpretation photo/OCR autofill flow.
- `Review Case Details` opens.
- Translation controls are avoided.
- `createTime` and downloaded EXIF date are photo-date provenance only; OCR/legal dates win, and photo date may prefill service date only as an editable fallback.
- `service_city` and `case_city` remain OCR/document- or user-confirmed; Google Photos place/location is not available from the Picker API.
- Review Details does not silently default blank service city or KM to the case city.
- Recovered distinct case/service evidence stays distinct, for example case city `Beja` and service location `Serviço de Turno | Moura`.
- KM is keyed to the effective service city and refreshes from profile distances when the service city changes.
- City-aware court email options use the case city, not the service city.
- Picker session cleanup succeeds.
- No final honorários DOCX/PDF is generated unless explicitly approved and then manually reviewed.

Sanitized route logs for this flow may contain method/path only. Drop query strings immediately and normalize Picker session IDs.

## CI Expectations
GitHub CI currently runs on Windows Python 3.11 and includes:
- agent docs validation,
- localization contracts,
- workspace hygiene,
- compileall,
- targeted core regressions,
- full pytest.

Do not merge if required checks such as `test (3.11)` or `docs_tooling_contracts` are failing, pending unexpectedly, blocked, or attached to an unexpected head SHA.

## Coverage Map For PR #46 Risks
- Numeric mismatch and decimal-comma preservation: `tests/test_translation_diagnostics.py`, `tests/test_translation_report.py`, `tests/test_quality_risk_scoring.py`, and PR #46 browser state tests.
- Safe rendering: `tests/test_browser_safe_rendering.py`.
- Shadow/test-mode banner and friendly live copy: `tests/test_shadow_web_api.py`.
- Gmail prepared state: `tests/test_translation_browser_state.py` and Gmail review/intake tests.
- Profile summary/list distinction: `tests/test_profile_browser_state.py` and `tests/test_shadow_web_api.py`.
- Recent Work empty-state copy: `tests/test_translation_browser_state.py` and `tests/test_shadow_web_api.py`.
- Qt Job Log Delete-key multi-select: `tests/test_qt_app_state.py::test_joblog_window_delete_key_removes_selected_rows_when_table_has_focus`.
- Windows browser ESM UTF-8 probe behavior: `tests/browser_esm_probe.py` with Gmail/interpretation browser ESM tests.
- Validation wrapper fallback: `scripts/validate_dev.ps1` and completed validation artifacts.
