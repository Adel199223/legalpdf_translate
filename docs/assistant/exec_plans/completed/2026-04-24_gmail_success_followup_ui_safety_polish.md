# Gmail Success Follow-Up UI Safety Polish

## Goal and non-goals
- Goal: improve feature-branch browser UI presentation after the canonical-main Gmail success, without changing Gmail extension/native-host/backend contracts.
- Goal: surface numeric mismatch diagnostics before save/finalization, keep raw translation state in details, clarify Gmail-prepared source state, separate Gmail job target from New Job defaults, soften live/test copy, and keep canonical-main guidance in operator details.
- Non-goal: fix or retest live Gmail extension intake from this feature branch.
- Non-goal: commit, push, merge, switch to main, or run review-bundle tooling.

## Scope
- In: Qt browser frontend copy/presentation helpers, safe-rendered warning cards, focused frontend/source tests, validation summaries, sanitized source snapshot.
- Out: extension contracts, native-host contracts, backend route paths, API payload shape changes, submitted select values, storage key changes, live Gmail runtime tests.

## Worktree provenance
- Worktree path: `C:\Users\FA507\.codex\legalpdf_translate`
- Branch name: `feat/browser-new-job-qt-polish`
- Base branch: `main`
- Base SHA: `36cecfd2381cf102ce30f63aa91050b2785757d2`
- Target integration branch: `main`
- Canonical build status: noncanonical feature branch; live Gmail extension intake is intentionally out of scope for this pass.

## Interfaces/types/contracts affected
- No public API routes, payload shapes, select values, DOM IDs, route IDs, storage keys, Gmail/browser-helper/native-host contracts, or `ui=legacy`/`mode=live|shadow` semantics should change.
- New nonbreaking DOM elements/classes may be added for user-facing numeric warnings.

## File-by-file implementation steps
- `src/legalpdf_translate/shadow_web/static/translation.js`: add numeric warning extraction/rendering, soften raw job status text, improve Gmail-prepared card copy, and separate current Gmail target from default New Job target.
- `src/legalpdf_translate/shadow_web/static/gmail.js`: surface the existing translation numeric warning in Gmail save/finalization surfaces without blocking finalization.
- `src/legalpdf_translate/shadow_web/static/app.js`: soften live/test topbar/banner/dashboard status wording and keep technical canonical-main guidance in operator details.
- `src/legalpdf_translate/shadow_web/static/gmail_runtime_guard.js`: keep normal guard text calmer while preserving detailed canonical-main guidance for diagnostics/operator details.
- `src/legalpdf_translate/shadow_web/templates/index.html`: add nonbreaking warning containers/details where needed.
- `src/legalpdf_translate/shadow_web/static/style.css`: style warnings and prevent banner/header overlap if needed.
- Tests: update focused ESM/static tests for numeric warnings, prepared Gmail copy, raw JSON visibility, target-language separation, banner/dashboard wording, and operator-only canonical guidance.

## Tests and acceptance criteria
- Targeted pytest: `.\.venv311\Scripts\python.exe -m pytest -q tests/test_shadow_web_api.py tests/test_shadow_web_route_state.py tests/test_browser_safe_rendering.py tests/test_translation_browser_state.py tests/test_gmail_review_state.py`
- Validation: `powershell -ExecutionPolicy Bypass -File scripts/validate_dev.ps1`
- Full validation if reasonable: `powershell -ExecutionPolicy Bypass -File scripts/validate_dev.ps1 -Full`
- Numeric mismatch samples appear before save/finalization, raw translation JSON stays in details, Gmail-prepared source no longer looks empty, target-language state is clear, and safe rendering remains intact.

## Rollout and fallback
- Roll out as a feature-branch UI/test patch only.
- If validation shows a contract-risking gap, stop and report instead of changing extension/native-host/backend contracts.

## Risks and mitigations
- Risk: numeric samples only exist after run-report generation. Mitigation: use the existing route silently and keep behavior nonblocking.
- Risk: warning text may accidentally expose raw payloads. Mitigation: render only calm message plus page/sample values through safe text helpers.
- Risk: feature-branch live Gmail failure could distract from this UI pass. Mitigation: do not run live Gmail extension tests here.

## Assumptions/defaults
- Silent run-report generation for completed jobs is acceptable and does not change API shape.
- Current canonical-main Gmail success artifacts are trusted evidence.
- Source snapshot ZIP is a sanitized review artifact, not a repo bundle generated by `scripts/create_review_bundle.ps1`.
