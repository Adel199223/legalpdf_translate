# FRlong01 continued defect and regression assurance

## Goal and non-goals

Continue the user's active goal to resolve the issue and demonstrated related regressions. The earlier bounded reconciliation made progress but did not establish page6's historical charge or inner connection cause. Those unresolved requirements remain part of the goal; do not substitute the passing diagnostic fix for full resolution.

Preserve original USD10 lifetime/USD4 campaign/30-attempt limits, all ledger rows, the USD0.119 hold/block, saved defaults, five-page partial output and historical evidence. No paid retry, old helper replay, ledger unblock, publication, native acceptance, Gmail, sending, hosting or default promotion follows from this offline assurance plan.

## Scope

Verify actual current files against the previous receipt, independently audit affected diagnostics/recovery behavior, check complete-suite isolation, cover the complete repository regression collection when safe, and fix only demonstrated defects. Retain failed results and exact source snapshots. Update touched-scope docs and completion/blocking audit evidence.

## Worktree provenance

- Worktree: `C:/Users/FA507/.codex/legalpdf_translate_frlong_reconciliation`
- Branch: `feat/frlong-reconciliation-20260924`
- Base branch/target integration: `main`
- Base SHA: `f5002be85e33601a8c5f4e13c5fbd2d7dfd209d7`, containing approved floor `4e9d20e`.
- Same isolated task stream; no second editing worktree is necessary. Review agents are read-only.
- Existing local source/tests/docs remain uncommitted and unpublished. Canonical main remains unchanged.
- Start-of-continuation audit: all72 authored/protected pins in prior private `frlong_reconciliation_20260924_01/final_local_receipt.json` match current files.

## Interfaces and contracts

No contract change planned. Preserve route IDs, payload shapes, select values, retries, usage normalization, budget gating and native/Gmail contracts. Any newly demonstrated defect must be reproduced offline before a scoped fix and recorded here.

## File-by-file steps

1. `openai_client.py`, `new_translation_blocks.py` and affected tests: audit the current diff for cause persistence, privacy, missing usage, cancellation and no-retry semantics.
2. `browser_source_review.py`, `translation_service.py`, source-review API and partial-artifact logic: determine whether a real product defect exists rather than mistaking private acceptance restrictions for ordinary app behavior.
3. `tests/conftest.py`, pytest configuration and test entry points: review full-suite external/native/credential isolation before execution.
4. New private assurance directory: retain source/test snapshot hashes, exact command, process identity, logs, JUnit and terminal exit status. Never restart solely because output observation times out.
5. This plan and relevant handoff/validation docs: preserve beforeimages and record actual results, remaining evidence gaps and goal status.
6. `source_review.js`, `source_review_ui.js`, `test_source_review_browser_state.py` and the reviewed-source user guide: repair missing actionable guidance for independent work after a prior reviewed translation. Keep the old tab and exact-operation metadata; explain the existing separate-workspace route. Do not clear nonces, change storage, create a replacement start, or imply that a different workspace recovers the old run. A delegated editor owns only these four files; other agents remain read-only outside fresh private probes.
7. Demonstrated local dependency damage: preflight found that the canonical Python environment's `backports.tarfile`1.2.0 is missing its RECORD-listed108491-byte `__init__.py`, so `keyring` cannot import. Inspect all files for this distribution and preserve the before-state. Restore only a verified missing file from the exact-version wheel with matching installed RECORD hash; do not upgrade dependencies, overwrite existing files, read saved keys, recreate the environment or change Python itself. Recheck `keyring` import with the null backend and rerun safety preflight before the broader tests.
8. As a bounded read-only follow-up under the user's computer/API investigation authority, inspect only Windows System, NetworkProfile, WLAN-AutoConfig and DNS-Client event metadata for UTC08:55–09:00 on September24, capped at32 records per named log. Keep only time/provider/event ID/level/process ID and query status; do not read messages, XML, SSIDs, addresses, usernames or raw properties, enable logs, change networking, or infer causation from timing alone. Historical diagnosis remains unresolved unless reliable evidence supports it.

## Tests and acceptance criteria

- Existing98 affected tests and639 Full-selected checks are prior evidence, not complete collection coverage.
- Cover the repository collection from the isolated worktree using the required Full wrapper and a supplemental pytest invocation that excludes whole files already covered by Full. Run all Gmail intake tests in the supplement because Full selects only five; report this overlap explicitly. This avoids repeating the expensive selected suites and is combined collection coverage, not one complete pytest invocation. Deselect the one actual in-memory COM collection test; no native acceptance is authorized. Retain exact collection, JUnit, stream logs and owned process identities.
- Use child-only sanitized credentials/proxy environment, disabled dotenv, null keyring and fresh app-data/temp roots. A private Python audit hook denies external socket operations and protected live ports in its own worker; subprocess mocks/static review and sanitized environment are separate evidence, not an OS network sandbox.
- Any code change requires a reproducing regression, targeted affected checks, and required Full validation. Do not repeat unchanged broad suites unless a new change/failure/concern justifies it.
- Recheck protected hashes and canonical main after execution.
- Completion requires requirement-by-requirement evidence; unresolved historical charge/cause or other required work prevents claiming the whole goal achieved.

## Rollout and fallback

Local work only. Keep prior results and failure evidence. If safe execution cannot be established, fix the demonstrated isolation issue first without touching user data. Do not use tests to dispatch real requests or unblock the original campaign. Publication remains a separate final boundary.

## Risks and mitigations

The full suite contains many subprocess and browser-service tests; review default mocks and guards before launch and do not assume shadow mode means no provider/native activity. Preserve process identity through long waits. No new dashboard poll is needed merely to repeat unchanged aggregate evidence.

## Assumptions/defaults

The previous goal turn was progress: it independently reconciled preserved evidence, obtained new provider aggregate evidence, repaired a demonstrated diagnostic gap and verified targeted/Full-selected checks. This continuation does not narrow the user's goal. It does not resolve missing authoritative page6 billing or create paid-dispatch authority.

## Progress

- Required state check passed; fresh plan prepared before further code work. Three independent read-only audits cover transport diagnostics, complete-suite safety and recovery/partial-output behavior.
- Six fresh private offline transport probes passed using installed OpenAI2.36.0/httpx0.28.1 and fake workflow boundaries. They cover actual SDK ConnectError/ReadTimeout chains, single dispatch, missing usage with retained fictional hold, diagnostics disabled, event-file write failure, and authentication classification. No source/test changes or real provider/native/OCR/server calls resulted.
- The apparent review-reset bug was narrowed after inspection: rejecting nonce disposal is intentional replay protection. The demonstrated UI defect is missing concrete guidance for preparing an independent document while preserving that recovery state. A fake scope-switch probe preserved old metadata exactly and dispatched no translation. The regression will assert guidance and those protections before/after the fix.
- Exact beforeimages of five potentially touched docs are preserved under the new private assurance directory's `docs_before/`; prior evidence is unchanged.
- Current direct API connectivity was checked with one fixed unauthenticated GET to `/v1/models`, without a key, request body, redirects or retries. It returned HTTP401 in0.75s. This proves a current direct HTTP response only; it neither authenticates the app nor diagnoses the historical failure or charge.
- The UI regression failed first on missing guidance; the final13-test source-review browser module passed, including preserved nonce/storage and safe DOM behavior. Source/test edits are frozen for broad validation.
- Private safety preflights01/02 stopped before pytest because `keyring` import failed on the demonstrated missing installed file. Preserve both failed receipts; this is an environment defect, not a product-test failure. Safeguards remain intact.
- Offline dependency repair restored only the absent108491-byte file, SHA256 `3f07f6a9421f0744a89493c229cc77bf3dd412efda89db38838b007f1cbde2a8`, from the cached1.2.0 wheel. All54 existing hashed files across the three relevant packages remained unchanged;55 current hashes match their RECORD entries. Independent review confirmed no credential/default/ledger operations. Null-backend import and preflight03 passed.
- The supplement collected6051 unique tests across232 modules; its17 ignored modules exactly match Full's whole-file coverage. One real COM test is explicitly excluded. Remainder01 stopped with630 passes and one harness-induced failure: Windows emits `subprocess.Popen` audit events with `executable=None`, which the private classifier incorrectly passed to `Path`. The fresh runner now records a fixed label for that case, and preflight04 includes a harmless local Python child to verify it. Remainder02 is the current owned run; first-run logs/JUnit remain preserved. No product change was needed for this failure.
- Remainder02 reached2987 passes before an existing Qt label-width test failed. Fresh reproduction identified a test-environment defect: Windows' offscreen Qt font database had zero families, so it measured missing-glyph boxes. With child-only `QT_QPA_FONTDIR=C:/Windows/Fonts`,192 families load and both the unchanged test and its production-appearance probe pass. Production labels measure67/72px inside112px buttons. No Qt source/test changes were made. Private preflight05 now checks font availability as well as existing safety controls; it passed. Remainder03 is the current owned run; earlier results remain preserved. This launch allows up to five failures to collect independent findings before stopping, without relaxing any provider/native boundary.
- Source/tests are frozen. Remainder03 worker39284 and the required Full wrapper PowerShell30872 use separate owned temporary roots. Full started at UTC15:27:40 alongside the independently isolated supplement to reduce elapsed validation time. The Full startup guard received its own passing registry-denial/null-backend/JUnit-path check before launch; per-process UUID JUnit paths will be joined to the guard receipts at closeout. Current tests are not final evidence until terminal status and preservation checks complete.
- Remainder03 completed with6051 passed and the one COM test deselected in586.02s; terminal exit0 and all519 source snapshot files unchanged. The Windows process chain joins launcher30964 to launched child39284 to interpreter worker12312. Independent audit matched all6051 JUnit identities to collection and all62 original evidence pin entries (60 distinct paths; two entries repeat). Zero test-phase network denials occurred; ten ambient Environment registry blocks are recorded separately from seven intentional preflight network denials. Full has passed479 selected tests so far; formatting and final validators remain pending.
- The bounded Windows event query completed once: all four named logs returned zero events for UTC08:55–09:00, capped at32 records per log. Only allowed metadata/query statuses were retained in `event_diagnosis/metadata.json`; no messages, XML, payloads or settings were read. This supplies no historical cause or billing disposition and does not establish that no connectivity fault occurred.

## Local assurance closeout

Completed2026-09-24 as bounded local work; the whole user goal remains unresolved because authoritative historical page6 billing and connection-cause evidence are unavailable. Earlier progress entries retain intermediate running states and failures; this closeout records the terminal outcome.

- Required Full reached exit0:641 selected tests, including162 formatting tests in970.57s, plus compilation/docs/hygiene. Both known Dart AOT255 failures are retained; both direct-Dart fallbacks succeeded.
- The supplement reached exit0 with6051 passes and one actual COM test deselected. Five Gmail intake tests overlap Full, yielding6687 unique passes across249 modules. This was combined collection coverage, not one full pytest invocation. The two runs overlapped in time with separate temporary roots; both preserved the same519-file source snapshot.
- Independent final review of the four source files and three changed test files found no actionable regression. Dispatch, retry, accounting, nonce/recovery storage and safe rendering contracts remain unchanged. Six private transport probes and13 UI tests provide focused evidence; they are not real-provider/native acceptance.
- The missing dependency file was restored from the exact installed-version cached wheel; the zero-font offscreen test environment was corrected only in child processes. No Qt source or assertion change was needed. Original failed preflights and test runs remain preserved.
- Final docs validation, runtime/protected-file checks and exact evidence pins are indexed by private `frlong_regression_assurance_20260924_01/final_assurance_receipt.json`. Source changes remain uncommitted and unpublished; canonical tracked main remains at the original base.
- The first read-only final verifier passed every original hash but stopped on its incorrect assumption that62 prior receipt entries meant62 distinct paths. The actual prior receipt repeats two exact entries:62 pins cover60 files. Its helper/failure receipt are preserved; a fresh verifier keeps and checks every entry and records both counts. No test rerun or product change follows from this bookkeeping correction.
- The USD0.119 hold/block, all226 ledger rows, saved defaults and exact five-page partial remain protected. No paid retry, consumed helper replay, native/Gmail acceptance, sending, hosting or default promotion occurred. The prior conditional nine-page/13-call/USD1.547 proposal is unchanged and unarmed. The next dependency is authoritative request disposition followed by supported reconciliation and a fresh bounded paid decision; more unchanged local tests cannot manufacture missing historical evidence.
