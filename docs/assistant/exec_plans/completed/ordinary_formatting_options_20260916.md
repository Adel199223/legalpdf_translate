# Explicit ordinary formatting options

## Completed scope, 2026-09-16

Status: completed locally and moved under PLANS rules 8 and 9; this does not imply publication or canonical-main promotion.

All 35 explicit formatting-option tests passed in F3. Derivative page breaks and source-supported gaps remain explicit and identity-bound; saved options are preserved.

Current full validation: repository F3 passed 6,477 tests with zero failures/errors/skips (result `0cceeb4c`, JUnit `ed5c1d79`); separate Standard Full SF2 passed (`adfcafe8`). Both retained the same 481-file source/test/script snapshot `bec79f29`. These results validate this bounded integrated scope; real French native/visual acceptance and final parent/docs closeout remain separate and pending.

The original implementation/authoring plan follows as historical scope and is superseded by this executed outcome where its status says pending. Exact pre-closeout bytes are retained privately in `application_docs_modernization_20260916_01/final_scoped_closeout_beforeimages_01`.


Root-authored bounded follow-up in the existing reviewed_regions worktree, based on the frozen service6b608d2d and tests44cbe9d0. Exact beforeimages and original harvest manifest are preserved under ordinary_formatting_options_beforeimages_20260916_01. No integration-worktree edits or runtime imports during authoring; parent owns tests after the Arabic code freeze.

Add an explicit page_matched_derivative boolean to the backend service. Its default keeps existing behavior. When deliberately enabled, preserve the original config/checkpoint/settings identity and bind a separate render config with page_breaks=True. Record the choice and both formatting fingerprints in the owner and acceptance evidence; expose it in review views. All adapter calls use that render config, while physical run ownership and source verification use the original saved config. Strip-bidi=False and unsupported selections remain declined. Never mutate settings, checkpoint, accounting or source commits.

Add optional explicit column_gaps_px to typed formatting tables. Missing gaps retain the old v1 decision/schema/output. If any table requests gutters, every table on that page must provide its exact gap list, enabling the existing reviewed_region_layout_v2 source-supported validator. Do not infer widths, gaps, source geometry or ownership. Preserve old serialized decisions by omitting the new field when absent.

Validate the genuine synthetic source→Workflow→formatting path with original page_breaks=False, explicit acceptance and a built DOCX; exercise restart with the same choice and rejection under a different choice, unchanged original bytes/accounting, unsupported settings, malformed booleans/gutters, source support and exact cell margins. Current integration validator/writer dependencies must be used; old W dependencies are not harvested. Independent review and parent-owned runtime tests remain required.

Independent static review caught Python boolean/integer equality in the new persisted derivative identity. Parent/intent/receipt owner joins and derivative evidence now compare canonical JSON bytes, and six cases replace persisted true/false with1/0 at these boundaries. The pre-correction service/tests/manifest/diff remain preserved separately. Default owner data and legitimate old v1 serialized table decisions stay unchanged.

Status: authored; final narrow independent review and parent-owned runtime validation pending.
