# Gmail-to-output and advanced-formatting readiness

## Completed scoped implementation and acceptance — 2026-09-26

Actual Gmail05 acceptance used clean canonical docs-only PR305 checkpoint `a8f795098f15685b106d4c354be6f7d531635b2d` with PR304 product source. Later publication/application must be verified from `gmail_closeout_publication_01/publication_receipt.json` beneath the private root; no new merge is claimed yet. EN03 and AR02 formatting are qualified complete. AR02's one-page job used two fully costed calls/USD0.02136925; all15 fragments passed saved-decision/package audits. One native export completed in6.593seconds with owned cleanup and root/peer review of its sole A4 page. Preserve room-label localization, borderless-table fidelity and Arabic PDF-extraction qualifications. Shadow07 is stopped and consumed; its unsuccessful download remains preserved.

The isolated [Gmail finalization and Arabic closeout plan](2026-09-26_gmail_finalization_and_arabic_closeout.md) governs the current continuation. Gmail04's private attachment-token guard refused before a job/charge; its normal shutdown is complete. A separately reviewed successor corrected only that guard, passed16 no-provider tests and15 binding checks, and reused the existing distinct USD3/max4 phase. Gmail05 then completed genuine extension intake, one one-page EN image-only translation (USD0.02977975), an exact normal browser download and qualified source/Word review. Ordinary Save added row91; normal Gmail Confirm reused it without a duplicate. Finalization generated fee DOCX/PDF; its single draft attempt returned Google403 `insufficientPermissions` (`draft_failed`). No draft or send succeeded. Root/peer reviewed the sole fee A4 page without clipping, overlap or missing body; its template prints the verified150-word count, not a monetary total. Finalization and terminal cleanup each passed28 independent checks: exactly65 rows including unchanged row91/all64 prior rows, schema/settings,232 source pins and21 historical pins matched. Owned processes were absent and ports8877/8765/8893 were free; the unrelated AutoRecovered Word document remained untouched. Temporary draft staging was normally removed after exact verification, while durable artifacts remained intact.

The demonstrated EN save-panel defect is implemented in the isolated closeout branch: bootstrap restoration skips Arabic-review-state requests for known ineligible current jobs. Completed AR, no-current-job restoration and actual error handling are preserved. All 20 targeted no-provider tests passed in 0.91 seconds and independent semantic review is clear. Source commit `24f7ecbd80984223f0a55c012ab369b40ccfb720` passed all659 selected Full tests/nine deselections and compile. The wrapper exited1 at docs-only AD046 marker failures after the known AOT event. Exact marker repair, including a separate branch line, was followed by passing direct docs/hygiene; original failures remain preserved and no passing tests were repeated. Scoped implementation and qualified acceptance are complete; the publication receipt records any verified merge/application revision. An absent or unsuccessful receipt does not establish publication completion. The earlier 655-pass Full is pre-fix evidence. No Gmail/provider/native operation is replayed. `bounded_acceptance_closeout_01.json` indexes 11 completed actual receipts and qualifications.

The original phase is exhausted at16 finalized reservations/USD0.23975525; the fresh phase has one finalized reservation/USD0.02977975, no new hold/block and immutable identity Gmail04 with actual consumer05. Preserve all226 original ledger rows, USD0.119 hold/block, FRlong partial and saved defaults under the original USD10 ceiling. Do not repeat formatting, Confirm row 90/91, reset ledgers or replay consumed helpers. Sending, OAuth expansion, public hosting and default promotion remain excluded. [HANDOFF](../../HANDOFF.md) indexes bounded evidence and beforeimages.

## Historical continuation checkpoint — 2026-09-25 (superseded)

PR #303 is merged on canonical main at `ca395d601efe4e00aad3458526b7d961d3cf5f0c`. Its four readiness fixes passed Full_04 and exact-head CI; earlier pending-publication statements below are chronological evidence only. EN03 reviewed formatting is complete with recorded qualifications. Genuine Gmail03 image-only/OCR-off output passed qualified document/native review and normal Save/Confirm, adding row 90 while preserving all 63 prior job rows. Its later automatic eight-second Word preflight failed; separately reviewed exact-process cleanup and a fresh supported 45-second preflight succeeded. The original Windows delay remains unknown.

The related [completed Word startup deadline plan](../completed/2026-09-25_word_startup_readiness_deadline.md) passed 91 targeted tests, independent review and fresh Full with 655 selected passes/nine expected deselections on source `58a2df5`. All four exact-head CI jobs passed on `11beb6c`, including 6,713 tests in each Windows run. PR #304 merged as canonical `1d6fdb3` with tree equality. One actual default native-readiness aggregate then completed both stages with confirmed cleanup and unchanged protected files (27.673 seconds aggregate; 12.185 launch and 15.392 canary). `word_startup_publication_01/final_application_receipt.json` and `word_startup_acceptance_01/independent_actual_review_01.json` close those Word-specific gates; no replay is needed. This does not establish Gmail draft/finalization or the remaining Arabic output criteria.

AR01 remains a genuine 1/3-page partial failure, not advanced-formatting acceptance. AR02 is a fresh fictional one-page case for reviewed pipe-cell separators, full measured spacing and RTL/mixed literals. Its upload is currently blocked by the browser automation extension's file-URL setting; the user has been asked to enable it. No AR02 provider request occurred. Shadow06 used the unchanged `dad5ea1` formatting worktree, then stopped with zero jobs and confirmed process/port/Word cleanup. It is consumed; use a fresh owned runtime after the upload restriction is resolved. Live03 is stopped with zero jobs and an expected exit 1 solely because authorized metadata confirmation added `court_emails_by_city`. All saved translation defaults and other settings fields remain equal; do not restore the earlier settings file.

The same phase has 14 finalized reservations costing USD0.21838600, no new hold/block and two slots/USD2.78161400 remaining. Preserve the original USD10 ceiling, all 226 old rows, USD0.119 hold/block, saved defaults and consumed evidence. No more Gmail paid checks are planned. Sending, OAuth expansion, public hosting and default promotion remain excluded. The parent goal remains active until the Arabic output criteria are actually verified; current status and bounded evidence routes are in [HANDOFF](../../HANDOFF.md).

## Goal and non-goals

Finish the two current user-requested gaps: demonstrate the complete actual Gmail attachment-to-finished-output workflow and verify the remaining advanced formatting features in actual rendered output. Fix demonstrated related problems. Current explicit autonomous computer/API authorization takes precedence over historical paid/native/Gmail and stage-token stops for this new scope. No outgoing email, public hosting, unrelated changes or global default/model promotion is included.

## Scope

Gmail track: fresh actual extension click on a bounded existing attachment message, exact message and attachment loading, preview/selection, prepared session, real translation, complete editable DOCX/download, job logging and an unsent reply draft with the verified output when supported by the normal flow. Inspect every final output page and retained language/content findings. Do not substitute a posted bridge payload or manual recovery for seamless extension acceptance.

Formatting track: fresh supported manual reviewed-source jobs, complete source/target mappings, editable table cells/rows/column widths and measured gaps, header/footer/folio ownership, contiguous joined-document groups and numbering, physical alignments, bold/italic, both cell-separator and vertical-spacing policies, page-matched derivatives, Arabic mixed-direction output, review state/reload and same-operation recovery. Use real provider results and current public UI; deterministic tests complement actual output review. Retain strict text/package checks separately from rendered visual acceptance.

Current source intentionally excludes Gmail-prepared jobs from manual source-review formatting. These are two genuine workflow tracks, not a claim that Gmail supports reviewed-source formatting. Historical AR03/FRlong operations remain preserved; no job/checkpoint hydration or consumed-helper replay.

## Worktree provenance

- Authoring: C:/Users/FA507/.codex/worktrees/gmail-formatting-readiness/legalpdf_translate, branch codex/gmail-formatting-readiness-20260924.
- Base and integration target: canonical main at 5f49b7363963cb78691e95ef5d0e15b2b1977b82; approved floor 4e9d20e.
- Live Gmail execution stays on canonical C:/Users/FA507/.codex/legalpdf_translate/main. Feature UI/code checks use shadow mode and isolated data. Do not switch canonical branch while its server runs.
- Private evidence: C:/Users/FA507/.codex/legalpdf_translate_private_benchmarks/gmail_formatting_readiness_20260924_01. Fresh preflight/baseline.json pins eight protected files and preserves exact mutable-file beforeimages.

## Interfaces and contracts

Keep route, payload, submitted-value, native-host/extension and exact-recovery contracts. Do not remove source/ownership/budget guards merely to make a test pass. Preserve safe DOM text insertion and original targets/defaults. Any required contract change must be supported by the observed failing workflow and explicitly documented before implementation.

## File-by-file steps

1. Read current Gmail setup/launcher/extension paths and inspect actual browser/runtime/account state. Existing old browser tabs are stale evidence until current identity is verified. Authentication dialogs remain user-operated if required.
2. Bind a fresh additive campaign budget to supported ordinary accounting before paid starts. Keep the original USD10 overall limit, USD0.119 hold and all old rows/block intact. Account for combined exposure; revised new paid ceiling USD3, single worker and bounded requests, supported explicit pricing/tier, no unobserved automatic restart. Record exact chosen documents/settings and pre-dispatch reservation evidence. The original USD1 proposal and reason for revision are retained in Progress below.
3. Capture fresh canonical runtime/build/asset identity and native-journal beforeimages. Normal bootstrap may perform Word readiness probes/canaries; record these operations and verify owned cleanup, with no overlapping or unknown native operation. Preserve each consumed result.
4. Execute the actual Gmail track through normal UI and verify complete output and exact unsent-draft attachments. Use native browser controls for the extension; a fallback/manual message load remains an observed failure until the seamless path is fixed and a fresh click passes.
5. Execute the formatting matrix through current manual source review, provider translation and formatting UI. Existing retained jobs are reusable only with confirmed current in-memory ownership. Otherwise use small fresh representative sources; controlled fictional layout cases must be labeled and complemented by actual Gmail-document acceptance.
6. Inspect actual DOCX XML/text/source-map/assembly evidence and every physical rendered page. Cover all named feature rows with concrete evidence; unresolved or unsupported rows remain incomplete. Retain any original failed checks and qualified linguistic findings.
7. Make only demonstrated fixes in this worktree, with meaningful no-provider regressions and required validation. Likely source routes are gmail_browser_service.py, shadow_web/app.py, translation/source_review/formatting_review browser modules and ordinary formatting services/writers; do not edit speculatively. Fix the verified stale isolated-build guidance during scoped Docs Sync.
8. Integrate necessary tested fixes through the repository publication lifecycle as applicable to the authorized autonomous completion, then verify canonical behavior before counting the user-facing issue resolved. Keep current handoff, validation and relevant user guides coherent with exact beforeimages.

## Tests and acceptance criteria

- Actual fresh extension action succeeds on canonical main; exact message/PDF selection, complete real translation, finished DOCX download, required job/finalization and unsent reply-draft behavior are demonstrated with hashes and current UI evidence.
- Every supported formatting row above has actual current UI/output proof or a documented negative-path test where the product intentionally rejects an unsupported selection. Passing source-only HTTP checks does not satisfy this goal.
- Final Word documents preserve full accepted targets and editable intended structure; all physical pages are visually reviewed for clipping, wrapping, table order, headers/footers, spacing, RTL/LTR and page/group boundaries.
- Changed code receives targeted regressions, required Full and exact-head CI before integration. Do not repeat broad suites for unchanged metadata or replay consumed paid/native helpers.
- Final requirement-by-requirement audit proves both user goals and any discovered related fixes, with current canonical state and no required work left. Otherwise leave the goal active.

## Rollout and fallback

Use fresh operation IDs/output paths and preserve failure evidence. For uncertain provider/build/native outcomes, inspect authoritative same-operation status before any new action. Stop dependent actions while the outcome is unknown. A corrected fresh run requires a concrete diagnosed decision, never blind replay. Keep original outputs, ledger, settings and historical worktrees.

## Risks and mitigations

Saved credentials are read only through existing app/helper paths; do not print config, account identities, tokens or live mail bodies. The app helper and browser sign-in are separate. No sending to third parties is authorized. Native readiness can have effects on bootstrap; current user authorization covers necessary bounded native checks, but cleanup and operation journals must prove what actually happened. Advanced manual review does not restore old in-memory handles across restart or certify legal accuracy.

## Assumptions and defaults

User wants autonomous completion of the two gaps, not repeated approval questions or another status-only handoff. Use staged internal evidence/decision packets without asking for historical NEXT_STAGE tokens. Keep assistant-model preference separate from per-run app settings. The new API ceiling was revised to USD3 before dispatch from verified request-capacity evidence. Further reassessment must remain within the original unchanged USD10 overall limit and use actual reservations/charges and remaining scope; no unsupported cost inference.

## Progress

- Fresh goal confirmed active; current main is clean at the base above and no LegalPDF/Word listener/process was found at initial inspection.
- New isolated worktree and branch created before implementation.
- Current Edge profile/tooling is reachable; an old LegalPDF Gmail tab exists but no active server was found. Native Computer Use is available.
- Static formatting audit establishes the exact matrix and intentional Gmail/manual separation. Retained files alone cannot restore the previous runtime owner. At that initial audit, no new product defect was established; later findings are recorded below.
- Accounting audit confirms the old ledger is unchanged and blocked for old-campaign reservations. Fresh supported ordinary accounting must remain additive and preserve that state.
- Read-only helper status confirms one configured Gmail account with read-only Gmail scope; the existing API credential is present without testing authentication or exposing its value. No compose/send permission is inferred. Finished-file acceptance is required; a reply draft remains conditional on supported existing permission.
- The initial self-imposed USD1 envelope was reassessed before dispatch: stock verified capacity reserves USD2.492 for an ordinary legacy request, and USD1.036 for the structured 24,000-output request. Use a shared USD3 phase with explicit standard tier and the existing output capacities, avoiding artificial truncation. Old known spend plus old hold plus this full new phase is USD7.04037740, within the unchanged USD10 limit. Current official pricing was opened on September24 and recorded in the external registry. No paid dispatch had occurred at that preparation checkpoint.
- Browser jobs initially lacked a caller-owned accounting injection seam even though Workflow and BrowserAppServices supported it. A narrow optional dependency pass-through and no-provider shared-budget test were prepared; default jobs and route contracts remain unchanged. The browser form currently has no budget-cap control, so the test cap is explicitly external and must not be described as a selected UI setting.
- A three-page fictional Portuguese formatting packet, source truth and layout manifest were prepared and visually inspected. It covers all four table column counts, document groups, folios, alignment, emphasis and spacing. This is source-fixture evidence only, not a translated output or completed app job.
- The optional manager accounting dependencies and four no-provider regressions are implemented. The focused 67-test set passed; independent patch review found no concrete regression. Required Full was pending at that checkpoint; its completed result is recorded below.
- Previous goal turn made concrete progress through implementation, tests, fixture and evidence preparation. Native Computer Use then stopped because it could not establish the browser URL with sufficient confidence, before any extension click, runtime launch or paid request. This is the first control-boundary observation, not three consecutive blocked turns. That turn continued independent work until the user brought Gmail to the foreground, preserving the tool's browser safeguard.
- The user brought Gmail to the foreground; a fresh native window-state read now verified its Gmail address and Extensions control. The transient Computer Use blocker is resolved without bypass. Offline review also confirmed no automatic uncapped provider probe in the selected translation/source-review path; interpretation and explicit Settings API-test controls remain outside these acceptance actions.
- The private production-service launcher passed independent static review and 12 offline accounting checks. It requires clean exact Git provenance, shares one USD3 ordinary ledger across sequential shadow/live servers, permits at most 16 reservations, rejects overlapping calls, preserves unknown/restarted holds, and rechecks the old ledger before dispatch. Only the manager accounting dependencies are supplied; actual UI, jobs, provider, Gmail and native services remain real. It does not claim to cap unrelated app controls or provide an OS network sandbox.
- Code and four regression cases were committed locally as `00d58f7`. Scoped docs preserve exact beforeimages and align the current task with completed PR #302. Direct docs validation initially found a missing authoritative branch marker; that marker was corrected and docs/hygiene checks passed. No actual acceptance or publication of this new change is claimed yet.
- Fresh shadow operation `shadow_01` ran the real app at committed `307ea940` on port 8893. Normal UI upload and browser rendering of the fictional three-page packet succeeded. The first source-prepare request returned `browser_source_review_prepare_failed` before any translation. The UI retained its uncertain-outcome notice; neither prepare nor the failed input/run claim was replayed.
- Bounded diagnosis established a deterministic local defect: the fresh profile has no settings file, while source-review registration requires strict existing-file resolution even though ordinary settings readers support absent-file defaults. The later trusted formatting-job lookup repeats that assumption. The subsequently implemented small fix supports an absent backend-resolved settings path without creating or promoting defaults, while retaining non-file and normalized-owner checks. No-provider preparation and completed-job formatting regressions were added.
- The failed run contains only its original one-byte workspace lock. Its UI/network observation and source diagnosis are retained privately. After confirming zero translation reservations and zero active jobs, the owned server stopped through its stop file. Independent PID/port/Word checks confirmed it exited. The old ledger, partial output and protected settings remain unchanged.
- The observed browser-bundle response corroborates a successful Word preflight and canary with owned cleanup. The initial bootstrap response was not captured, so cumulative native-operation counts remain unknown; no real translated-output acceptance is inferred. The existing portable Tesseract runtime passed all 96 manifest hashes, but is absent from the ambient PATH. A fresh server can receive that verified directory and language-data path only in its child environment; no installation or global PATH/default change is needed.
- Required `full_01` completed successfully before the missing-settings fix: 641 tests passed, nine expected Gmail deselections, compile checks passed and both documented direct-Dart fallbacks passed after the known AOT launcher failure. All 486 pinned source/test/config files matched before and after. This result covers the accounting seam at its recorded source snapshot; the newly demonstrated settings fix needs its own targeted checks and fresh Full validation.

- The missing-settings fix passed 98 targeted tests with no skips and Full_02 passed 648 selected tests, with nine expected Gmail deselections and all 486 source/test/config pins unchanged. Both documented direct-Dart fallbacks passed after the known AOT launcher failures. Private `runtime_harness/diagnosis/post_fix_01/result.json` and `validation/full_02/independent_terminal_review.json` retain terminal evidence. Full_01 remains valid for its earlier snapshot.
- Draft PR #303 is at `47f594cde16ac89f84792f7828da552946811456`. The owned shadow runtime stays pinned there; docs edits do not restart it or change its served assets. Canonical main and fresh Gmail remain separate integration/runtime boundaries.
- The fresh fictional English job `tx-712eb9d638b8` completed 3/3 pages and 17 target blocks in three calls at USD0.02882250, with no retry or unknown usage. The ordinary DOCX and exact downloaded copy match; this flow-layout output has no editable tables and physical pagination is unverified. The page 3 title retains a terminology qualification. Accepted source revision `8543f88339084189b0bb77a926a1e53f`, run `20260924_192903`. Independent `english_output_review_01.json` confirms the 38,515-byte output/download identity, SHA256 `3b36cf7f5fc97add2f725c3aae57e810b9c05885e1b30f429b24da74a1f09882`; root observed the actual download click. Detailed content and download qualifications are in [VALIDATION.md](../../VALIDATION.md).
- The rejected page 1 save supplied table gap 36.0136 above the reviewed minimum 27. Page 1 formatting save returned 422 for an invalid measured table gap. An authoritative HTTP200 current-review read confirms generation 1, no page decision, revision, rebuild or derivative, but the editor remains locked behind Retry exact save. The failed decision and unchanged server state are preserved. Supported browser reload and exact-job recovery restored the same completed job; the corrected 27px page decision subsequently saved successfully. The narrow pending-decision recovery fix passed 25 no-provider browser-state tests after independent review identified and resolved two acknowledgement-ordering gaps. Fresh Full_03 is running for those changed files; Full_02 covers the earlier snapshot. Advanced formatting, native output review and fresh Gmail acceptance remain pending. The narrow controller/UI change must allow safe discard of the pending decision after authoritative status while preserving local edits for correction and exact-operation guards. Do not replay the save blindly or claim an accepted formatting derivative.
- The goal remains active: fresh seamless Gmail-to-output, the remaining advanced-formatting matrix and native rendered-page acceptance are incomplete. No email send, public hosting or default promotion occurred.

- The formatting-save recovery fix now has 25 passing no-provider browser-state tests and final independent review with no remaining findings. The first review findings and both targeted runs remain preserved. Fresh Full_03 validates the changed controller/UI/tests with frozen source pins. All three English pages were subsequently saved through the original runtime after supported browser recovery, with complete explicit mappings; the document review selects two source-document groups, strict separators and capped spacing. Build, native output, Arabic and Gmail acceptance remain pending.

- Subsequent bounded evidence supersedes that progress checkpoint: Full_03 passed 653 tests and all four GitHub jobs passed on clean `5285b1c`. EN02 formatting submission was rejected for `invalid_line_partition`; its immutable targets, failed mappings and consumed shadow_02 runtime remain preserved. Fresh EN03 reviewed meaningful source lines, completed 3/3 pages, and passed all 54 actual source/target partitions. The UI accepted formatting revision `5c0f887769b84b4cacec773a1af0d4c0` and built artifact `f47176cd2cb24ec58856e63ffec34647`. Its independently audited DOCX retains 54 fragments, 17 parents and four editable tables. One supported Word export completed with confirmed cleanup and unchanged DOCX; all three actual A4 pages were visually inspected by root and peer with no clipping/overflow. Profile and wording qualifications are preserved privately. The phase has six finalized reservations at USD0.05659675, no new hold/block; the original ledger/hold/defaults are unchanged. Edge blocked the ordinary download and the reviewed download event was unconfirmed; no downloaded-copy identity or browser cause is claimed.
- The actual EN03 correction exposed one further local interface defect: a client-side table-spacing warning remains visible after successful corrected save, acceptance and build because the UI closure retains `localMessage`. Before code work, root preserved the exact plan beforeimage and scoped the next change to clearing that local message at the start of a wrapped UI attempt, preserving current validation/server errors and all recovery guards. Add a meaningful no-provider DOM regression for corrected save/build plus server-error recovery; run focused tests, independent review and fresh required Full/CI for the changed source snapshot. No route/payload/default/provider behavior changes are authorized by this fix. Shadow_03 stopped through its owned stop file at 19:58:59 UTC after the completed EN03 case; active jobs were zero and independent PID/port/Word checks confirmed cleanup. Retained handles are historical after shutdown. Arabic and fresh canonical Gmail acceptance remain pending under this active plan.

- Scoped Docs Sync preserves the latest two root progress bullets above and all earlier checkpoints. Exact seven-file beforeimages are under private `docs_sync_applied_01/beforeimages/`. The current front doors distinguish Full_03/25 targeted/four CI passes at `5285b1c` from the later stale-warning fix awaiting Full_04 and new-head CI. Terminal CI evidence is `validation/pr303_ci_5285b1c_terminal_01.json`; local Full evidence is `validation/full_03/independent_terminal_review.json`.
- The fresh AR proposal and 17 source-parent suggestions are retained under `ar01_preparation/`. Its three literal-pipe rows propose real inline-cell cuts while preserving every character. Source-only slice checks do not validate actual Arabic targets. The proposal selects reviewed table-cell separators and full measured source gaps; no AR invocation has been dispatched or armed by this preparation. Reassess the same phase's USD2.94340325 and ten remaining reservation slots after the UI fix/runtime checks, without inventing a per-case call ceiling or automatic retry. Fresh canonical Gmail-to-output remains pending.

- EN03 evidence is retained under private `gmail_formatting_readiness_20260924_01/`: `en03_reviewed_output_audit_01/result.json` (SHA256 `fd31988a2f4d447fa6b7837f91908debdd07f20f5c0189dcc93006bd4b697855`), `native_review/operations/en_reviewed_native_01/result.json` (`9afdf8b554d48de09d92407581ab16cca0d8764ae00e66471db8ee4fd74ac322`), its `root_visual_review.md`, and `en03_native_independent_review_01.json` (`f75661bea69af21a10d3a33244829e707a7a6520a0e1c40b0b2137cdf8f61cc6`). The reviewed DOCX SHA256 is `71bb0c151efd07ef15d540fc4a9280cb086ce22559767d415522cded4d2a8133`; the PDF SHA256 is `68a583b18c011986e35170a126701ed2bbf75241678410b6ac2cc118383b0c93`. Native/package receipts retain their original pending-review flags; later root/peer receipts supply visual review without altering those consumed results. Qualifications include borderless tables, retained literal pipes and admissible 27px page-1 gutter, intentionally capped spacing, awkward page-3 title `Test term` and varying institutional-header wording. Targets remain unchanged.

- The stale local-warning fix is committed locally as `957cdb6`: two meaningful DOM regressions failed before the fix, then all 27 browser-state tests passed, with final independent review clear. Required Full_04 and new-head CI remain pending integration gates. This does not extend earlier `5285b1c` validation or count the prepared AR proposal as dispatched.


- Integration checkpoint, September 24: the four demonstrated readiness fixes are implementation-complete at source head `404b3d1d64fd3e55bcb768b849f0f3991b0ddcfd`. Full_04 passed 655 selected tests, nine expected Gmail deselections, compilation and hygiene; all 486 pinned files independently match the committed bytes. The clean-worktree wrapper skipped docs, which passed separately through the direct validator. The known Dart AOT failure/direct fallback success remains retained. Evidence: private `gmail_formatting_readiness_20260924_01/validation/full_04/independent_terminal_review.json`. Required exact publication-head CI and PR review remain merge gates; no merge is preclaimed. The active readiness plan continues through canonical Gmail and the remaining Arabic formatting acceptance after integration.
- AR01 completed one of three pages, then page 2 failed structured language/token validation after its correction attempt; page 3 was unattempted. Four fully costed calls totaled USD 0.05276950. Its genuine one-page partial DOCX and source map remain unchanged; no complete output or advanced-formatting acceptance is claimed. The retained diagnostics identify the parent block but omit the actual failed responses and detailed validator code, so invalid model output versus a local false positive cannot be established. No speculative validator change or same-job replay is justified. Private `ar01_preparation/terminal_reconciliation_01.json` retains the evidence. Shadow_04 used clean 404b3d1, asset 54fa93db4c30, and stopped through its owned stop file at 20:39:33 UTC with zero active jobs; its handles are now historical.
- The shared USD 3/16-reservation phase now has ten finalized calls costing USD 0.10936625, zero new holds/block, USD 2.89063375 unspent and six slots remaining. Prioritize fresh canonical Gmail because its stock ordinary request reserves up to USD 2.492; then reassess actual remaining capacity for a new clearly fictional one-page Arabic case covering inline-cell separators, full measured spacing and RTL. AR02 is preparation only; two logical structured calls do not guarantee two actual transport attempts. Preserve the original USD 10 limit, USD 0.119 hold/block, all 226 old ledger rows, saved defaults and every consumed operation. No email send or permission expansion is included.
- The implementation stage is ready for integration after exact-head CI/review. Keep this ExecPlan active for the remaining real-workflow acceptance; merge does not mean the overall goal is complete. The next live runtime must use clean canonical main containing PR #303, a fresh owned operation and the unchanged shared accountant. Source-only checks and prepared fixtures are not Gmail or Arabic acceptance.
