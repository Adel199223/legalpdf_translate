# Approved document formatting integration

## Goal and non-goals

Integrate the user-approved Arabic/French Word presentation into canonical main through scoped commits, a GitHub PR, green checks, merge and post-merge app verification. Preserve unfinished quality/model work locally and leave private reference documents outside Git. This is the rollout continuation of the already-authorized Stage 3, not a new paid benchmark or model-promotion stage. User has approved layout and publication/cleanup; historical translation findings and English-reference approval remain separate.

## Scope

In: deterministic compact typography, Arabic/Latin runs, PAGE fields, source-aware sections/spacing/regions for complete validated structured pairs, local rebuild/layout cache and sticky review status, synthetic regression fixtures, relevant docs, compatibility tests, isolated browser smoke, GitHub lifecycle and recoverable cleanup.

Out: new structured translation protocol, prompts, model IDs/efforts, OCR selection, API accounting/review/correction/glossary/calibration changes, honorarios export behavior, database migrations, live Gmail/mailbox actions and any paid API call. New production TXT runs gain safe typography/spacing/page-number defaults; they cannot acquire exact source-block associations merely from matching line counts. Full source-aware formatting remains evidence-gated. Do not claim this release completes future model/translation-quality promotion.

## Worktree provenance

- Canonical path: `C:\Users\FA507\.codex\legalpdf_translate`, branch/target `main`.
- Integration path: `C:\Users\FA507\.codex\legalpdf_translate_formatting`.
- Integration branch: `codex/approved-formatting-integration`.
- Base branch: `origin/main`; fetched base SHA `022b5afb54b9be0b2eba1f2c19d617ed46a10891`.
- Approved floor: `4e9d20e`, verified ancestor of the base.
- Research path: `C:\Users\FA507\.codex\legalpdf_translate_quality`, branch `feat/translation-quality-balance`; retain local-only safety checkpoint, never merge or push wholesale.
- Canonical is clean; integration is noncanonical and uses isolated shadow state for UI checks. No canonical LegalPDF listener was found on 8877/8765 at initial inspection. Recheck before rollout; never change a running checkout silently.

## Interfaces and contracts

Keep `assemble_docx`, route paths/IDs, payloads, submitted select values, Gmail/native-host contracts, manual-review schemas and saved translation files compatible. Add local source-map/layout evidence only. No new model client construction or API calls during rebuild. Keep honorarios shared save/path helpers unchanged.

## Implementation steps and ownership

1. Root triages the entire research tree and creates a local preservation checkpoint. Create the integration worktree from the fresh approved base.
2. Layout agent transplants the approved writer, pure structure/layout/spacing/furniture code and synthetic tests; extracts formatting-only support from the mixed translation-protocol module. No model/prompt/preparation/evaluation code.
3. Foundation agent adds only assembly/rebuild-adjacent workflow preparation/review integration with tests. Keep production request, OCR, effort, retry, resume and calibration behavior unchanged. Use passive existing sidecars, never invented bilingual alignment.
4. Cost/audit agent independently reviews parity and publication hygiene, including private-name replacement in test fixtures and absence of provider calls from rebuild.
5. Root synchronizes touched docs, runs targeted/full/wrapper validation serially, compares private output package bytes to accepted proofs and inspects the browser workflow. Private test artifacts stay outside Git. If output differs, obtain fresh render QA instead of reusing old visual evidence.
6. Root stages explicit scopes, commits, pushes only the integration branch, creates a PR, waits for required checks and reviews the exact head diff, then merges. No force push, no unrelated remote branch deletion.
7. Root fast-forwards clean canonical main, verifies the merged app/build and formatting outputs, deletes only merged disposable branch/worktree state, and keeps unmerged research recoverable. Verify local/remote main equality and clean tracked worktrees.

## Tests and acceptance

- Targeted writer/structure/layout/furniture/spacing, local rebuild, prompt/image/effort/source extraction and model-call parity regressions.
- Full pytest via `.venv311\Scripts\python.exe`, then `scripts/validate_dev.ps1 -Full` serially. Native Word/Qt diagnostics remain documented; do not suppress them or imply export repair. Record known Dart AOT failure and successful direct-Dart fallback.
- Compare AR/EN/FR mocked call payloads and counts to canonical; verify prompts, provider models/efforts, retry controls and accounting bytes unchanged.
- Saved structured reference rebuilds preserve source/target files and accepted package content; new legacy runs retain all TXT content and report missing source-aware layout honestly.
- Browser shadow smoke checks New Job, language/effort/page-break controls, Recent Jobs and local rebuild without translation dispatch, live Gmail or credentials.
- Required GitHub checks must pass for the exact PR head; post-merge main and output checks must pass before reporting integration complete.

## Rollout and fallback

Use PR merge only. Preserve research checkpoint and original private outputs. If selective transplant fails compatibility, fix within scope or stop before merge; do not enable the combined research workflow to make tests pass. If GitHub CLI authentication is unavailable, use the connected GitHub tools and ordinary Git transport; never print or replace tokens. A blocked publish is reported honestly rather than called complete.

## Risks and mitigations

Mixed research changes are not a formatting-only patch: isolate dependency closure and inspect protected file/hash parity. Structured data is absent in current production: keep safe text fallback and documentation explicit. Existing private fixture names must become synthetic before publication. Do not clean unmerged branches/worktrees by force or remove virtualenv junction targets. Native Word automation is deferred; use existing accepted render evidence only when exact document package identity proves equivalence.

## Progress and outcomes

- Initial canonical/main and fresh origin/main both `022b5af`; public GitHub repository access verified through connector. `gh` GraphQL returned 401, so publication will use the connected tool where needed. No token replacement or paid call.
- Research tree triaged: 48 modified / 66 new intentional files; only caches and the virtualenv are ignored. One existing trailing-whitespace test line was normalized for a clean safety checkpoint; runtime bytes are unaffected.
- Research checkpoint `66a7374a2b874f59994215177af4ff48d1d5f100` preserves all 114 intentional files locally; the quality worktree is clean. The integration branch has no research commit ancestry.
- Frozen agent validation: 506 writer/structure tests; 70 formatting integration/policy/cache/isolation tests and 64 existing workflow/resume tests. Independent audit confirms 38 untouched workflow methods are AST-identical and 19 protected production modules remain text-identical to base. Malformed historical review queues/reasons preserve the entire original summary instead of normalizing away evidence.
- Actual offline `TranslationWorkflow.rebuild_docx` recreated the accepted private AR and French packages with all 35/23 ZIP members byte-identical and exact source-map page equality. Accepted Word visual evidence remains five/one pages. Saved source/target evidence is unchanged, network/provider/extraction hooks were blocked. The bundled artifact interpreter lacks the app's dotenv dependency, so this application integration proof uses the required project virtualenv.
- Isolated browser smoke on port 8888 verified New Job, EN/FR/AR, high/xhigh, existing effort-policy values, unchecked page-break reflow and Recent Work. No job was translated and no live Gmail surface was operated. End-to-end saved-run rebuilding was tested independently through the real workflow, not claimed as a browser rebuild click.
- Full pytest passed: 2038 tests in 316.50 seconds, exit 0. The full validation wrapper also passed, including agent docs and workspace hygiene through the documented direct-Dart fallback after the known dartdev AOT failure. Isolated serial native small-screen dialog plus layout-integration rerun passed 14 tests in 3.37 seconds. No historical native COM diagnostic appeared in these runs; this is not unattended Word export acceptance. A staged diff check found and removed only trailing blank lines in one new test file; its rerun passed. GitHub checks/merge and canonical-main verification remain rollout gates. No paid benchmark call or credential replacement.
- Implementation and branch-scoped review are complete; this plan is moved to completed before publication as required by the commit/publish closeout workflow. Rollout is not called complete until the exact PR head is green, merged canonical main is verified, and merged disposable worktree state is safely retired. The unmerged quality checkpoint is explicitly retained.
- PR #292 publishes three scoped commits at `c939ee9e788f366ea383bbc5407d9a5dba835ca1`; its tree `5572556a628948e81ac586ed11a022f93022edae` exactly matches the locally tested index. GitHub's docs/tooling check passed, but the first hosted Windows suite failed one existing screen-geometry test (2037 passed): a dependency's `fitz` deprecation message precedes the subprocess JSON on stdout. Merge is blocked pending a narrow test-harness repair, not a document-layout change.
- CI repair scope: read the renderer's existing `wide.json` artifact strictly, retain child-process exit checking and all geometry assertions, and exercise both ordinary and deliberately noisy dependency stdout without suppressing warnings. No runtime imports, dependencies, model settings or output layout change. Under the user's integration/test authorization, publish this test-only repair after targeted coverage; repeat full local validation and hosted CI concurrently, with both required before merge. GitHub CLI authentication is unavailable and the user explicitly deferred browser 2FA, so use the connected GitHub service. Its available tools cannot delete the merged remote branch; retain that harmless branch if cleanup still requires browser verification.

## Assumptions and defaults

Existing user-approved AR/French layouts are presentation acceptance only. Model-policy and honorarios PDF work remain deferred. No source/target paragraph association is inferred from equal counts or matching numeric anchors. Cleanup means safe retirement of merged work plus preserved, clean unfinished research—not erasing unfinished work.
