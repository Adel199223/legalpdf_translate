# Publish and apply the tested everyday-app update

## Goal and non-goals

The user explicitly approved publishing the tested update through GitHub review/checks, merging it, and updating the normal local app. Publish the two narrow diagnostics/workspace-guidance fixes and their scoped docs, then verify the canonical update. The historical French page6 uncertainty remains shelved unless recurrence, with its full hold and evidence preserved.

No paid/provider/OCR translation, new native Word or Gmail acceptance, email, public hosting or saved-default promotion is included. Do not diagnose the old incident again or replay consumed helpers.

## Scope

In: scoped local commits, authenticated Git fetch/push, PR, exact-revision CI/review inspection, safe merge, canonical checkout update, fresh source-only canonical HTTP/static/build/launcher proof and safe merged-branch/worktree cleanup. Out: feature expansion, speculative fixes, history rewrite, test weakening and unrelated retained-worktree cleanup.

## Worktree provenance

- Authoring: `C:/Users/FA507/.codex/legalpdf_translate_frlong_reconciliation`, branch `feat/frlong-reconciliation-20260924`.
- Base/target: canonical `main`, `f5002be85e33601a8c5f4e13c5fbd2d7dfd209d7`, with approved floor `4e9d20e` present.
- Fresh authorized fetch found remote main unchanged at that SHA; canonical main is clean and no LegalPDF browser server was running at publication start.
- Source/tests match the previous6687-test assurance snapshot. Four current docs have exact new beforeimages in private `app_update_publication_20260924_01/docs_before/`.
- GitHub repository: `Adel199223/legalpdf_translate`. The installed connector and existing Git transport work; the CLI metadata lookup's401 is retained without changing credentials.

## Interfaces and contracts

No new product/API contract changes. Publish only the existing fixed allowlisted transport-class diagnostics and safely rendered separate-workspace guidance. Preserve all retry, accounting, nonce/recovery, route, native/Gmail and default contracts.

## File-by-file steps

1. Commit `openai_client.py`, `new_translation_blocks.py` and their two regression files as the diagnostics fix.
2. Commit `source_review.js`, `source_review_ui.js`, their regression file and reviewed-source guide as the independent-workspace guidance fix.
3. Commit scoped current docs, external-source references and completed plans after final docs/hygiene/whitespace checks. Keep the user-approved publication state current and old historical evidence unchanged.
4. Create the PR from this exact reviewed branch; inspect actual required contexts and review state, then wait for all required exact-head checks. The local workflow defines Windows `test (3.11)` and Linux `docs_tooling_contracts`; remote protection may require additional contexts.
5. Merge only the green reviewed head, fetch the actual merge, and fast-forward canonical main. Attach the PR to this task.
6. A fresh private source-only HTTP verification may use canonical code with offline browser services, isolated app data and Gmail bridge disabled. Check actual canonical identity and exact served static assets without executing browser JavaScript or normal capability/readiness routes. Normal launcher/server bootstrap is not a safe substitute because it may invoke live/native readiness. Do not run the old consumed verifier.
7. Verify protected hashes, canonical build/launcher routing and owned process cleanup. Preserve private evidence and inspect branch/worktree ownership before safe cleanup; remove only the merged temporary authoring worktree when its records are secured.

## Tests and acceptance criteria

Reuse the frozen source's641 Full-selected plus6051 supplement passes, five overlaps,6687 unique passes and one explicit COM exclusion. Exact-head hosted CI runs unrestricted pytest and covers its own environment. Do not repeat unchanged local broad suites solely for publication. Run docs/hygiene/whitespace checks after current docs change; meaningful code changes or failures would require appropriate new validation.

Completion requires actual merged revision, green exact-head checks, current canonical main/launcher provenance, preserved protected files and qualified verification. A queued run or PR creation alone is not completion.

## Rollout and fallback

Use PR merge and normal fast-forward only. If checks fail, investigate the specific evidence before any rerun or fix. Do not infer a transient failure or bypass a check. Retain original failed results. If the main branch moves, inspect/reconcile its actual diff before continuing. Existing product source/defaults remain unchanged until the accepted patch merges.

## Risks and mitigations

Avoid treating normal browser startup as read-only: default capability evaluation can invoke native readiness and bridge synchronization. Use the bounded offline-service HTTP proof, with no browser execution, and label its limits. Keep proof of safe mock/native boundaries distinct from genuine document acceptance.

## Assumptions and defaults

The user's publication approval persists through the normal commit/PR/check/merge/update lifecycle. No further permission question is needed for those approved steps. The recurrence-only decision applies to the old French incident and does not fabricate provider settlement or change the USD10 limit.

## Progress

- Publication authorized; fresh Git/connector access verified and remote baseline unchanged.
- Independent review confirms the local workflow's two expected checks and no local readiness blocker. A separate bounded review is preparing safe canonical verification guidance.
- Local product commits: `f70ffaf` retains safe structured transport diagnostics; `559f1db` adds independent-workspace guidance with its user guide and regressions. Product source/tests are unchanged from the reviewed snapshot.

## Branch closeout and execution authority

The branch's implementation and documentation preparation are decision-complete for the authorized publication. This plan is archived with that branch closeout before merge, as required by the publication workflow. Its completed location does not assert that remote checks, merge or canonical verification have already succeeded.

Actual terminal outcomes must be read from private `C:/Users/FA507/.codex/legalpdf_translate_private_benchmarks/app_update_publication_20260924_01/final_publication_receipt.json`. That receipt must bind reviewed head, actual PR/check results, merge, canonical main, exact static/runtime proof, protected hashes and cleanup disposition. If it is absent, continue the authorized steps above; do not ask again or invent success. Once terminal success is recorded, no publication/helper replay follows from this plan.

The original page6 uncertainty remains accepted and shelved unless recurrence. The existing hold/history and USD10 cap stay protected. New paid/native/Gmail acceptance and default promotion remain separate.
