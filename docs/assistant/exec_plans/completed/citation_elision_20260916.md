# Bounded citation-head elision

## Completed scope, 2026-09-16

Status: completed locally and moved under PLANS rules 8 and 9; this does not imply publication or canonical-main promotion.

The old-code citation reproducer failed as expected; 203 focused tests and all 74 new F3 cases passed. The exact retained FR02 candidate passed current validation offline (result 1b5c481d), without a new paid commit or relabelling its failed historical run.

Current full validation: repository F3 passed 6,477 tests with zero failures/errors/skips (result `0cceeb4c`, JUnit `ed5c1d79`); separate Standard Full SF2 passed (`adfcafe8`). Both retained the same 481-file source/test/script snapshot `bec79f29`. These results validate this bounded integrated scope; real French native/visual acceptance and final parent/docs closeout remain separate and pending.

The original implementation/authoring plan follows as historical scope and is superseded by this executed outcome where its status says pending. Exact pre-closeout bytes are retained privately in `application_docs_modernization_20260916_01/final_scoped_closeout_beforeimages_01`.


## Current status, 2026-09-16

Implementation and validation are complete on the recorded integration snapshot. The 203-test targeted suite passed, the retained actual response passed candidate-only offline validation, complete repository run03 passed 6,477 tests with zero failures/errors/skips, and separate standard Full run02 passed. Historical failed French operations and paid identities remain unchanged; this parser result does not establish full ordinary French acceptance.

Keep this file in `active/` until the final documentation closeout; the parent explicitly deferred the move. The ordinary browser acceptance plan remains open for the actual French run, independent target review, native export and every-page QA. The current checkpoint edits still require their final docs check.

## Goal and non-goals

Preserve legal article associations when a translation repeats an article noun omitted in a coordinated source citation. A genuine ordinary French response exposed an explicit-head counter false positive after marked subdivisions. Repository regressions use fictional identifiers only. This change does not translate, modify retained responses, replay paid work, change numeric checks, or certify legal equivalence.

## Scope and provenance

- Authoring worktree: `C:/Users/FA507/.codex/legalpdf_translate_reviewed_regions`, branch `codex/reviewed-regions`, HEAD `4780a2e16c32adf7af7479656f0b5fafdff83224`.
- Approved base branch: `main`; declared floor: `4e9d20e`. This is the parent's existing isolated authoring worktree, not a newly launched canonical app.
- Integration target: parent's `legalpdf_translate_structured_activation` review worktree, followed by its controlled canonical integration.
- Actual module baseline comes from that exact current integration worktree and is preserved under `citation_elision_beforeimages_20260916_01` (SHA256 `fc6eaf4960a14b12cab4111a521a07370cbeb1fd52ca31c7e7e15ccd7e0d9ae8`). W matched it before editing. Other concurrent changes are outside this packet.

## Contract and implementation

`new_translation_blocks.py` keeps the explicit article expression and numeric multiset guard unchanged. A bounded helper counts contiguous, coordinated elided articles. It consumes explicitly marked numeric/letter subdivisions first; a following elided article then requires an ordinal or alphabetic suffix. Horizontal whitespace, closed separators and legal-code tails prevent crossing prose, dates, deadlines, identifiers, sentences or source lines. Both comparison sides use the same grammar and retain exact multiplicities and suffixes.

The grammar covers existing Portuguese/French/English article heads, selected subdivision labels and simple Arabic article coordination. Arabic subdivision prose and other unsupported citation grammar remain conservative limitations. This is not a general citation parser or an all-number equivalence fallback.

The policy becomes `article_heads_v3_bounded_coordinated_elision`, changing the existing evaluation-bound translation identity. Historical request and paid protocol identities retain their original values.

## File steps

1. Preserve the exact original module; edit only the parser/constant/comparison call site.
2. Add `tests/test_citation_elision.py` with fictional public-validator and bounded parser regressions. Keep existing test files unchanged.
3. Publish exact source/test/plan pins and a unified diff for parent and independent review.

## Tests and acceptance

Root execution, 2026-09-16: the public reproducer failed on the original module with the expected `block_citation_association_defect` (`a18438`), then the independently reviewed module `5eac4bda` and final test file `8b83a991` were harvested (`12a863`). All **203 targeted tests passed in 9.66 seconds** (`60ad2c` / terminal `873891`): the 74 new citation cases plus existing blocks, literal-contract, structure and ordinary-source suites. Beforeimages, red and green evidence are retained under private `application_ordinary_real_fr_20260916_02/citation_fix_harvest_01`.

Complete repository run03 passed **6,477 tests in 1,795.40 seconds**, with zero failures/errors/skips (launcher 1,797.907787 seconds; result `0cceeb4c`). Separate standard Full run02 passed in **1,262.541817 seconds** (result `adfcafe8`). Both before/after inventories contain the same 481 files and exact snapshot hash `bec79f2960c371adaec995e99403f2117de32b7244415cfa9991dc89e91769bb`. Standard Full included successful docs/hygiene checks through the recorded direct-Dart fallbacks; the later checkpoint/closeout docs check is still pending. Private retained-response validation passed once (`88ca56`, 6.233 seconds), with one candidate block, zero SDK calls, zero audit denials and no review codes. Result `1b5c481d` and independent actual review `67df39aa` preserve the failed operation and historical identity. The existing normalizer removed exactly three blank lines in memory; all other characters matched. No page was committed or full case accepted. Independent review `8da84b4e` also closed the fixture-only no-space comma expectation conflict without changing the numeric guard.

Coverage includes expanded/elided forms in both directions, the realistic subdivision grammar with fictional IDs, repeated heads, suffixes, simple Arabic lists, numeric preservation, same-number article/reference swaps, paragraph lists, date/deadline/prose/identifier and line boundaries, bounded traversal and policy fingerprint changes. The completed private zero-SDK retained-response validation preserves historical ancestry explicitly; it is not a paid replay, page commit or full-case acceptance claim.

## Risks, defaults and rollout

Ambiguous unmarked numbers are deliberately not promoted after subdivisions. Unsupported grammar can still reject a faithful translation and must be diagnosed rather than bypassed. Numeric checks, language checks, source identity, accounting, retries, model defaults and public route schemas are unchanged. The author performed static work only; root owns the recorded harvest and runtime checks. Historical paid responses, protocol identities, failed states and accounting remain unchanged.
