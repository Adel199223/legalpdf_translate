# Prepare the tested update for everyday app use

## Goal and non-goals

Follow the user's September24 decision to set aside the small unresolved FRlong01 page6 charge and historical connection cause, revisit only if the problem recurs, and move the app forward. Prepare the tested transport-diagnostic and independent-workspace guidance changes for publication into the everyday app. Do not keep historical billing reconciliation as a prerequisite for local product work.

The historical record stays truthful: charge and cause unknown, five of nine pages preserved, USD0.119 still held, all226 ledger rows and original USD10 limit unchanged. This decision accepts uncertainty; it is not a provider settlement, refund, zero-cost assertion or proof of an environmental cause. Existing campaign guard data is not rewritten. A later paid-test preparation can account conservatively for the entire held amount and review any required supported acknowledgement separately; this task starts no paid retry.

## Scope

In: verify the current tested patch, update current continuation docs with the user's decision, distinguish private acceptance restrictions from normal app behavior, and prepare a concrete local release description and validation record. Out: new product features or speculative fixes, repeating passing broad tests without source changes, historical billing investigation, consumed helper replay, external publication before approval, paid/provider/OCR/native/Gmail acceptance, sending, hosting and default promotion.

## Worktree provenance

- Existing isolated authoring worktree: `C:/Users/FA507/.codex/legalpdf_translate_frlong_reconciliation`.
- Branch: `feat/frlong-reconciliation-20260924`.
- Base branch and target integration: `main`.
- Base SHA: `f5002be85e33601a8c5f4e13c5fbd2d7dfd209d7`, containing approved floor `4e9d20e`.
- Product/source tests remain frozen at the independently verified assurance snapshot. Canonical main remains the daily-use build; the prepared fixes are not yet published.
- Fresh private evidence root: `C:/Users/FA507/.codex/legalpdf_translate_private_benchmarks/app_readiness_forward_20260924_01`. Exact beforeimages of four current docs are saved before editing; historical completed plans and receipts stay unchanged.

## Interfaces and contracts

No new contract or product-code change. The already-tested patch adds allowlisted transport cause diagnostics only when diagnostics are enabled, plus safely rendered independent-workspace guidance. Routes, request payloads, retries, nonce/recovery storage, accounting and saved defaults remain unchanged.

## File-by-file steps

1. `APP_KNOWLEDGE.md`, `docs/assistant/HANDOFF.md`, `docs/assistant/SESSION_RESUME.md`: make the user's new direction current; keep historical unknowns and qualifications, and route the next step to everyday-app update preparation.
2. `docs/assistant/VALIDATION.md`: retain actual tests and add the recurrence-only investigation policy without converting unknown billing/cause to a passing result.
3. Existing four changed source files and three tests: read-only release scope verification against `final_assurance_receipt.json`; make no speculative code edits.
4. Private `RELEASE_READY.md`: prepare the exact product change, validation, publication operation and subsequent everyday-use check for review.
5. This plan and fresh private receipt: record direct docs/hygiene/whitespace results, current pins, independent findings, and the external-publication boundary.

## Tests and acceptance criteria

- Reuse the completed frozen-source evidence:641 Full-selected plus6051 supplement passes, five overlaps,6687 unique passes; one actual COM test excluded. Do not relabel this as real-provider/native acceptance.
- Verify current source/test hashes against that tested snapshot and review the actual pending tree.
- Run direct agent-docs/workspace-hygiene validators and `git diff --check` after current docs change.
- Preserve all original protected files, source/test hashes and history; final receipt captures only the new docs delta and review preparation.
- Preparation is complete when the scope is concrete and reviewable. Publishing requires the current project's separate approval; only then use its full PR/CI/merge workflow.

## Rollout and fallback

First make the tested diagnostics and workspace guidance available through a reviewed update to canonical main. Verify the resulting canonical build and ordinary local workflow within the subsequently approved scope. Keep paid full-document validation, native/Gmail acceptance and saved-default promotion explicit. If a connection failure recurs, use the newly retained safe diagnostics to investigate that new occurrence while preserving unknown usage conservatively; do not assert that this earlier failure was environmental.

## Risks and mitigations

The user's acceptance of a small uncertain cost must not be converted into a fabricated charge or a silent guard bypass. Conversely, an immutable historical private campaign block must not be presented as a reason that unrelated app development cannot continue. Product tests, publication, normal user operation and private paid acceptance have distinct evidence and scope.

## Assumptions and defaults

The latest user direction supersedes prior continuation text demanding original page6 resolution before all app work. It does not change the original USD10 cap or saved settings. No active goal is registered by the app at this turn; do not create a new background goal unless requested.

## Progress

- Verified all prior protected and authored pins before documentation changes; saved exact beforeimages.
- Two bounded independent read-only reviews cover ordinary-app versus campaign restrictions and local release readiness. No provider/native/Gmail call or broad test rerun is planned.

## Preparation closeout

Completed as local preparation on2026-09-24; publication has not occurred. Source inspection confirms that normal browser jobs use ordinary per-run accounting and do not inject the private acceptance campaign. That historical block is preserved without blocking this update.

Independent release review confirmed clean canonical main at the base, approved-floor ancestry, all seven changed source/test pins and all519 tested source pins. Independent documentation review found no remaining policy contradiction after the old retry condition was converted to historical tense. The current docs now route work to the tested app update and reopen original diagnosis only on recurrence.

Private `RELEASE_READY.md` gives the exact two product changes, route/readiness matrix, validation, proposed PR text and full publication operation for review. `preparation_receipt.json` records final direct docs/hygiene/whitespace exits, current source/protected pins, exact beforeimages and the16-file pending scope. No new product code or broad test run was needed. Old evidence remains immutable.

Next boundary: obtain the publication decision required by current `AGENTS.md`, then commit/push/PR/check/merge and verify the canonical update under that scope. The prepared update contains no paid retry or automatic model/protocol/effort promotion. Do not revive the old historical-billing prerequisite.
