# Current assistant guidance and reviewed-workflow documentation

## Local documentation scope completed, 2026-09-16

The 28-file modernization, standing scoped Docs Sync policy and 102 policy cases are complete. This final R docs pass consolidates current FR04 qualified acceptance, the 178-row accounting checkpoint, complete 6,477-test regression and separate Standard Full; preserves exact historical material; and closes 23 children plus the three bounded acceptance/browser/docs parents. The original global-activation plan remains active but explicitly deferred.

Actual French run, normal formatting, native export and full-page visual review are complete with disclosed qualifications. The strict FQ02 extraction report remains failed solely on its trailing-space cell-bound predicate; no automated unconditional pass is substituted. Final synthesis `b1b82a8e` binds the separate strict report, independent supplement and full-page visual receipts. Current front doors route to the completed evidence and deferred decisions.

The authorized documentation implementation/lifecycle scope is complete. Root final direct-Dart docs (`167bec`), hygiene (`ae4f50`) and CRLF-aware whitespace (`7356af`) checks passed against freeze `abc955bc`, retained in private `final_validation_01.json`. Root local scope review and working-tree triage are complete; the candidate is ready to present unpublished. No relevant code changed. Exact final 12-file beforeimages are private under `application_docs_modernization_20260916_01/final_candidate_closeout_beforeimages_01`; prior 32+3 beforeimages, historical front doors and original chronological bodies remain intact. No source/test changes, publication or global bootstrap maintenance are included.

Everything below records the original isolated-authoring phase and its executed outcomes; earlier pending instructions are historical.

## Goal and authority

The user granted standing authority on 2026-09-16 to run useful Docs Sync without asking again, keep current guidance accurate and reorganize assistant documentation for the present local-browser/Codex workflow. This is scoped project documentation maintenance, not UCBS or vendored-template application. Preserve historical evidence and remaining publication, destructive-operation, live Gmail and provider/native execution boundaries.

## Provenance and isolation

Author only in `C:/Users/FA507/.codex/legalpdf_translate_reviewed_regions`, branch `codex/reviewed-regions`, base `4780a2e16c32adf7af7479656f0b5fafdff83224`. Integration target is the existing `feat/structured-translation-activation` worktree at `C:/Users/FA507/.codex/legalpdf_translate_structured_activation`. Canonical main remains a separate daily-use build. Root owns harvest after current validation snapshots end. No R write, application import, test, validator, provider or native operation is authorized in this authoring scope.

## Scope and implementation

1. Read current R docs-maintenance rules and exact affected front doors. Preserve R and W beforeimages privately, plus the original 29 protected Markdown/JSON bytes under their recorded paths and hashes. Retain old long front doors as exact dated historical copies with an index explicitly denying current execution authority.
2. Update AGENTS/agent/docs-maintenance and GOLDEN_PRINCIPLES standing Docs Sync rules. The final active-policy audit also requires narrow UPDATE_POLICY, PROJECT_INSTRUCTIONS and TRANSLATION_WORKFLOW replacements; preserve each exact R/W baseline. Preserve explicit execution/publication gates and clarify that historical continuation tokens are not current authority. Do not broaden the staged-execution workflow.
3. Replace duplicated historical logs in canonical/bridge APP_KNOWLEDGE, HANDOFF, SESSION_RESUME and VALIDATION with concise current architecture, navigation and validation guidance. Keep dormant roadmap semantics. Point changing acceptance/results to the two living 2026-09-16 plans owned by root.
4. Add the approved reviewed-source/DOCX user guide. Add narrow links to APP/PDF user guides, INDEX and manifest. Explain supported inputs, independent Unicode selections, explicit derivative choice and exact recovery/session limits. Do not add speculative model/tool promises or claim candidate code is already main.
5. Update only the docs-sync policy validation and its meaningful Dart tests: an explicit autonomous project mode must carry dated standing-user authorization and enforce no repeated approval question. Preserve the existing legacy/template approval mode. Do not hide obsolete prompt text in current docs to pass validation.
6. Record the scoped decisions/remaining contradictions in DOCS_REFRESH_NOTES and the private harvest manifest. Add the parent-verified dated official Terra/pricing/reasoning reference above retained historical registry entries. Historical runbooks and original beforeimages are not rewritten. Source, app tests, validation script, routes, preferences and templates remain untouched.

## Interfaces and assumptions

The manifest gains a validated project Docs Sync mode/authorization record. Existing legacy policy fields remain compatible with the old mode. Codex/ChatGPT task model choice (the user's Astra Ultra preference) is separate from the candidate application's Terra evaluation and current saved/global translation defaults. Tool availability is discovered in the active session; no static model capability inventory is asserted.

## Validation and acceptance

Authoring only: compare exact diffs, verify beforeimage hashes and links by reading files, and inspect policy tests. Root will run the direct-Dart validator tests, docs validation and workspace hygiene after integration, then any required final validation. Current integration checkpoints own terminal full-regression and standard Full results for their recorded snapshot; these do not validate this new documentation/Dart scope. Do not repeat paid/native work based on old handoff text.

## Status, risks and rollback

Status: integrated by root after exact 28-file snapshot/baseline checks (receipt `a8a79595`), with independent post-harvest verification `02d2811a`. Direct-Dart docs and hygiene checks passed. Policy tests passed all 102 emitted cases after a narrow dormant-marker fixture correction; the old hard-coded summary count was removed without changing assertions. Earlier Windows temporary-path and stale-fixture failures are retained. This completes the scoped implementation and validation; publication/integration-branch closeout remains part of the parent task. Exact private beforeimages and dated copies preserve evidence. No deletion, history rewrite, commit, push, default promotion or publication occurred.

The two root-owned living plans remain unchanged. If their chronology grows further, preserve a dated exact copy and keep a short current checkpoint plus evidence index when the owner authorizes that separate extraction. Do not flatten operation history or replace consumed-artifact references with summaries.
