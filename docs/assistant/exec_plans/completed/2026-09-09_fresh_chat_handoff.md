# Prepare a compact fresh-chat handoff

## Goal and non-goals

Save the completed integration, user requirements, remaining work and safety boundaries in discoverable local docs so a new chat can continue without the old conversation. This is documentation maintenance, not template/bootstrap replacement, app implementation, production activation or publication.

## Scope

In: compact brief; current resume/handoff/architecture/validation/user-guide anchors; routing; durable browser-isolation lesson; completed PR #295 closeout addendum.
Out: runtime/tests/config changes, paid calls, native/Word/Gmail operations, host repair, credentials, remote writes and research-tree integration.

## Worktree provenance

- Worktree: `C:/Users/FA507/.codex/legalpdf_translate`.
- Branch: `main`; base branch and target integration branch: `main`.
- Base SHA: `4780a2e16c32adf7af7479656f0b5fafdff83224` (merged PR #295).
- Canonical status: routine app authority; clean at entry, no LegalPDF browser server observed. Docs are written here so the fresh chat can read them immediately. There is no concurrent writing initiative; the delegated continuity audit is read-only.
- This request does not authorize commit/push. Leave the local handoff changes visible and preserve them when planning the next isolated implementation worktree.

## Interfaces, types and contracts

Documentation links/routing only. Preserve required resume headings and exact `- Branch: \`main\`` capitalization. No application contract, model or protocol default changes.

## File-by-file implementation

1. Add `FRESH_CHAT_BRIEF.md` with verified merge truth, user decisions, evidence limits, private pointers and next Stage 1 scope.
2. Update current sections of `SESSION_RESUME.md`, `HANDOFF.md`, canonical/bridge `APP_KNOWLEDGE.md`, `VALIDATION.md` and the translation user guide; retain dated failure/history sections.
3. Route the brief through `INDEX.md` and the existing docs-maintenance manifest entry.
4. Add the observed GET/bootstrap side-effect lesson to the isolation workflow and an actual post-merge addendum to the completed implementation plan.
5. Validate docs, hygiene and their direct-Dart regression suites; review the final diff and move this plan to completed.

## Tests and acceptance criteria

- Brief and current anchors agree with local Git and the retained `stage4_publication.json`; no pending publication instructions for PR #295.
- Agent-docs and workspace-hygiene validators and regression suites pass. Run standard `validate_dev.ps1` for docs/product validation; full pytest is not needed for this docs-only pass.
- No secrets/private documents copied into Git; no paid/native/live calls or unrelated edits.
- User receives the brief link and a paste-ready prompt, not a newly created chat.

## Rollout and fallback

Local docs become available immediately in this saved project. Preserve uncommitted changes; no reset/cleanup or automatic publishing. If validation fails, correct only this touched docs scope before handoff.

## Risks and mitigations

- Old plans contain consumed approvals: label history and route through the current brief first.
- UI checks can trigger native/account work: retain fail-closed isolation requirements and honest `not_evaluated` boundaries.
- Opt-in implementation may be mistaken for activation/model acceptance: state the distinction explicitly.

## Assumptions and progress

- Interpret “prepare the harness” as fresh-session continuity, not vendored template synchronization.
- Read-only independent audit confirms PR #295 merge, scoped cleanup, original budget and remaining acceptance limits.
- Implementation complete; independent read-only review found two inherited current-tense/authority ambiguities, both corrected before handoff.

## Executed validation and closeout — 2026-09-10

- Agent-docs and workspace-hygiene validators passed, including reruns after the wording fixes.
- Direct-Dart regression suites passed: 72 agent-docs cases and 7 workspace-hygiene cases.
- Standard `scripts/validate_dev.ps1` passed: 233 browser/API/state tests in 183.78 seconds, compilation, agent-docs and hygiene. Both known `dart run` AOT wrapper failures used successful direct-Dart fallbacks; no failure was hidden. Full pytest and `-Full` were not repeated for this documentation-only change; publication-build totals are explicitly historical in the brief.
- `git diff --check` passed; all 8 checked private evidence/reference pointers exist. Original budget and Word journal hashes remain unchanged.
- Source of publication truth was local Git plus the retained machine-readable publication record; no new network, paid/native/Word, live Gmail, host change, runtime edit, commit or push was performed.
- Local documentation handoff is complete. The next chat should assess the smallest safe controlled-activation/real-document acceptance scope at Stage 1, preserving pending docs and stopping at `NEXT_STAGE_2`. No implementation authority is inferred from this docs pass.
