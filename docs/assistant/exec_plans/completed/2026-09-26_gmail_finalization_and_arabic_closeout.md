# Gmail finalization and Arabic acceptance closeout

## Goal and non-goals

Complete the remaining work in the active Gmail/formatting readiness goal: inspect the actual AR02 reviewed output, then demonstrate the missing normal Gmail finalization and conditional unsent-draft behavior. Preserve earlier genuine acceptance and failures. No sending, OAuth expansion, browser-security bypass, public hosting, default promotion, historical helper replay or old-job hydration.

## Scope

Starting scope: complete AR02 native page review, then at most one fresh genuine Gmail job for the previously verified one-page attachment, retaining exact source identity and explicit per-run choices. Keep the runtime alive through normal download, review, Save/Confirm and finalization; preserve any permission-limited draft outcome without expanding OAuth or sending. The exhausted 16-request phase is never reopened.

Current outcome: AR02 and Gmail05 actual workflows are qualified complete, including normal download, confirmed row91, fee-page visual review, exact joins/preservation and owned cleanup. Google refused the single unsent-draft attempt for insufficient permissions; no draft/send succeeded. Historical AR02 download failures stay separate from the successful Gmail05 transfer. A demonstrated post-save EN review-state defect is now fixed locally with 20 passing no-provider tests and clear independent review. All659 selected Full tests/nine deselections and compile passed; the wrapper exited1 at docs-only AD046 markers. Exact marker repair and direct docs/hygiene then passed, without relabeling the wrapper failure or repeating tests. Scoped implementation/qualified acceptance are complete; publication/application status belongs to the receipt below; an absent or unsuccessful receipt does not establish completion.

## Worktree provenance

- Authoring: `C:/Users/FA507/.codex/worktrees/word-startup-readiness/legalpdf_translate`.
- Branch: `codex/gmail-finalization-20260926`.
- Base: canonical `main`, `a8f795098f15685b106d4c354be6f7d531635b2d`; target integration branch `main`.
- Actual canonical live test: `C:/Users/FA507/.codex/legalpdf_translate`, clean main at `a8f7950`, ports8877/8765, asset `54fa93db4c30`; runtime is now stopped. This identifies the genuine acceptance source, not a future applied revision. Later publication/application belongs in `gmail_closeout_publication_01/publication_receipt.json` after checks/merge.
- Product source remains PR304-tested; PR305 was docs-only. No new product change is demonstrated at plan creation.
- Private evidence root: `C:/Users/FA507/.codex/legalpdf_translate_private_benchmarks/gmail_formatting_readiness_20260924_01`.

## Interfaces, types and contracts

No route, payload, submitted-value, extension/native, accounting recovery or safe-rendering contract changes are planned. Private operation preparation must use existing production services and preserve normal UI actions. Any demonstrated product defect requires an explicit bounded plan amendment before code changes.

## File-by-file steps

1. Complete the already prepared fresh AR02 native-export operation against the exact reviewed DOCX; verify every physical page and append visual qualifications without editing its historical receipts or target text.
2. Preserve shadow07 terminal results, original and phase ledgers, settings/defaults, native journals and old outputs. Record independent cleanup and source/artifact pins.
3. Prepare a separate private Gmail-completion phase and successor launcher, not modifications to consumed helpers. Proposal: USD3 and at most four sequential reservations for one one-page job. Every actual request must fit its full conservative reserve, currently USD2.492 for the verified ordinary image bound. Review policy/pricing/source/budget/native ownership before creating or arming anything.
4. Original known3.92137740 + preserved old hold0.119 + exhausted phase actual0.23975525 + proposed new cap3 = USD7.28013265, below the unchanged USD10 ceiling. Verify these values again at execution; preserve all226 old rows and the exhausted phase's16 finalized rows. Unused old-phase capacity is not new request authority. Unknown usage blocks dependent dispatch.
5. Use a fresh actual LegalPDF Gmail extension intake on canonical main. Require exact attachment SHA256 `533fab2a80fefae4c0d93368ee0da4afeda80c050e6267f09c9ca007d9e0ea8d`; use EN/high/adaptive, Image Always, OCR Off, page1 only, one worker, resume off, existing protocol/model/tier constraints. Do not promote defaults.
6. Review the fresh complete DOCX/native page and obtain normal download evidence if available without bypassing the observed browser block. Save/Confirm the fresh job exactly once, proving a new row while preserving row90 and every prior row. Finish the normal fee-document/finalization flow and retain its actual conditional draft outcome. Keep external helper ownership/timeouts observable; do not replay uncertain finalization.
7. Make useful scoped Docs Sync in this isolated checkout with exact beforeimages: parent active plan, HANDOFF, SESSION_RESUME, VALIDATION and the formatting matrix or relevant guide as warranted. Document incomplete criteria honestly. Publish necessary tested changes through the authorized normal PR lifecycle; no broad historical merge or blanket docs rewrite.
8. Under the demonstrated EN-save amendment below, change only `src/legalpdf_translate/shadow_web/static/translation.js` and `tests/test_translation_browser_state.py`: exclude known ineligible current jobs from Arabic review restoration, preserve eligible/no-current-job behavior and meaningful errors, then run targeted no-provider tests and fresh Full without replaying actual workflows.

## Tests and acceptance

Private preparation gets static/inert no-provider checks and independent review before execution. Unchanged product code does not need another broad suite or paid/native replay. Product changes, if demonstrated, require meaningful targeted regressions, Full and exact-head CI. Docs get targeted hygiene/validation.

Acceptance is actual evidence: AR02 exact target/structure and every rendered page; fresh Gmail extension/source/output/row/finalization joins; generated DOCX/PDF manual review; exact downloaded copy where available; actual unsent-draft or permission-limited outcome. Unknown, unsupported and blocked criteria remain separate and incomplete. Root/peer review cannot certify legal accuracy.

## Rollout and fallback

Use fresh exclusively created operation paths and additive ledgers. Stop on identity mismatch, unknown provider charge, ambiguous native ownership or an unaccounted request. Preserve the genuine result and inspect same-operation status instead of retrying. Only proven owned processes may receive normal cleanup. No resets, overwrites, journal restoration or silent envelope enlargement.

## Risks and mitigations

Bootstrap/session reads may invoke native readiness. Bind and observe normal production work; do not replay PR304 acceptance. Browser “Blocked” is a real failed transfer but does not establish its underlying cause or a local product defect. Existing OAuth has no draft-write permission; actual failure is an honest supported outcome, not permission to expand scopes. Do not expose credentials, live mail content or private configuration in reports.

## Assumptions and current decision

The user's explicit autonomous computer/API goal remains active and permits necessary bounded verification within the original USD10 limit. Historical stage-token and no-new-paid instructions do not replace that later scope. This plan and proposal did not themselves create a budget or arm a helper. Concrete operation bindings were reviewed before their one permitted execution and are now consumed. The later demonstrated EN-save amendment changed only the isolated browser restoration guard and its regression tests; actual Gmail/native acceptance remains on the prior product source.

## Progress

AR02 native operation `ar_reviewed_native_01` completed one export/render with confirmed ownership/cleanup and unchanged protected/original files. Root and peer reviewed its sole A4 page; no clipping/overlap or leaked markup, with readable RTL and mixed literals. The package proves complete mappings, editable3x2table and109.4pt full spacing. Source borders are not replicated; Latin room identifier A was localized; raw Arabic PDF extraction is not codepoint-exact. The three remaining scoped formatting rows are complete with these qualifications. Root binding01 was declined before operation creation because its quiet-window object was null; it remains preserved. The corrected fresh binding02 enabled the only actual native call.

Edge's actual multiple-download permission prompt was discovered after shadow07 shutdown. The user approved “Allow LegalPDF downloads”; root clicked Allow. The queued transfer then failed with a network issue while its owner was already stopped. Preserve this failed transfer; future download checks must inspect native browser prompts before runtime cleanup. No old job is restored or repeated.

A fresh additive Gmail successor was reviewed privately under `gmail_completion_preparation_01`. Independent static and concrete-binding reviews passed; the original false template remains unchanged. Root created the distinct USD3/max4 phase once and launched `gmail_live_04` on canonical clean main at10:38UTC, PID9824, ports8877/8765, asset `54fa93db4c30`. Official rates/capacities were reverified on2026-09-26. The canonical native journal has an exact prelaunch beforeimage.

The genuine extension opened the exact retained Gmail03 message URL, loaded both attachments and prepared only the selected one-page attachment in a fresh output directory. Root set the explicit per-run choices and clicked Start once. Gmail04's private guard refused `genuine_fresh_same_attachment_context_required` before a job or provider reservation; earlier source-hash, choices and output checks passed. Its zero-reservation state and refusal remain preserved. The bounded diagnosis below established a private-helper defect, not a product defect; Gmail04 subsequently stopped normally with zero jobs/charges/new rows and20 independent cleanup checks passing.

### Demonstrated private-guard correction (executed)

The one bounded current-session read and privately preserved source prove that only the historical-versus-fresh `attachment_id` equality fails. Source bytes, message/thread, filename, one selected attachment, page/language and fresh session match. The local guard incorrectly treated a changed Gmail attachment token as persistent identity. Native journal and the zero-reservation phase were unchanged by diagnosis. This is a private verification-helper defect; no product or Gmail contract change is justified.

The reviewed correction required a separate immutable successor helper for `gmail_live_05`, retaining all04 files and the refusal. It must reuse only the exact untouched existing USD3/max4 phase, preserve its original identity and create no new budget or cap. Require terminal04 cleanup, zero reservations/no hold, fresh05 operation/output paths, same message/thread/filename and exact source bytes, a nonempty fresh attachment token, and a new genuine intake/session. Keep all other request, source, ledger, settings, ownership and no-send gates. Prove the narrow predicate with no-provider tests, including changed-token same-source acceptance and wrong source/message/thread/filename/count or stale-session refusal; independently review the concrete successor before starting it. Stop04 normally after evidence capture. No old job hydration, consumed helper replay, phase reset, additional credit or paid retry is authorized by this correction.


### Gmail05 actual outcome — qualified complete

The fresh helper passed16 no-provider predicate tests and15 independent static/binding checks, then launched once on canonical `a8f7950`/asset `54fa93db4c30`, ports8877/8765. It reused the untouched distinct USD3/max4 phase; that ledger keeps its original Gmail04 identity. No budget was reset or enlarged. All consumed helper versions and source/ledger pins remain preserved.

Genuine extension intake produced one page with EN/high/adaptive, Image Always, OCR Off/local, one worker and resume off. Run `20260926_120202` completed one successful finalized call:3,753 input tokens,1,658 output including1,343 reasoning,5,411 total, USD0.02977975. There is no unknown/inflight usage or new hold/block. The terminal accounting peer passed18 checks. The phase now has one finalized reservation, USD2.97022025 remaining and three slots; no further translation is planned for this scope.

One normal browser download succeeded while the live owner remained running, without a permission prompt. The downloaded DOCX exactly matches the38,490-byte app output (SHA256 `3d5b474474cab4f775a0c9151604febee9be91e760ce5e1e019d1d675f4842bf`). Root and peer inspected its complete one-page Word rendering against the actual source image; all29 paragraphs match saved translated text. The sole `source_block_layout_unavailable` warning remains: the image-only source yielded no extracted layout blocks, so numeric/citation diagnostics against empty extracted text do not independently prove omissions. Retain linear reflow, source-emblem omission and professional legal-language review qualifications. Word's separate AutoRecovered document was left untouched; only the reviewed download window was closed normally.

Ordinary Save created row91 once. Normal **Save this Gmail attachment** / Confirm reused row91; it did not add a duplicate. Finalization then generated fee DOCX/PDF and attempted one unsent reply draft. Google returned403 `insufficientPermissions`, recorded as `draft_failed`; no draft/send succeeded and no OAuth expansion or retry followed. Root/peer reviewed the sole fee A4 page without clipping, overlap or missing body; all nonempty DOCX paragraphs appear in its PDF and the case fields/150-word count match. The template does not print a monetary total; profile/payment/legal correctness is not certified. Actual fee export took8.056seconds, separate from successful launch/canary readiness. The valid root visual receipt is `root_fee_visual_review_02.json`; receipt01 remains a null-only PowerShell failure, not review evidence. Do not repeat Save, Confirm or finalization to manufacture a successful draft.

Finalization and terminal cleanup each passed28 independent checks. Exactly65 rows remain, with row91 and all64 prior rows/schema/settings unchanged;232 source pins and21 historical pins match. Runtime returned exit0 with zero active jobs through its owned stop file. Owned runtime/native/Gmail-helper/focus processes were absent and ports8877/8765/8893 were free. The unrelated recovery Word process remained untouched. Temporary draft staging was normally removed at shutdown after exact verification; durable translation/download/fee/source artifacts remain intact. `bounded_acceptance_closeout_01.json` (SHA256 `8a262a05c90642ccd8650079f900a96b40d30fd994594eedca85794bbe88d85c`) indexes 11 actual receipts and qualifications.

The pre-fix Full finished at2026-09-26T11:13:55Z with exit0:655 selected passes/nine expected deselections, compile/docs/hygiene and successful direct-Dart fallback after the known AOT failure. That result remains historical evidence for the unchanged acceptance source. The fresh Full for the implemented guard below completed its tests/compile successfully but stopped at docs AD046; the repair and separate passing docs/hygiene checks are recorded below. The publication/application receipt below owns the later lifecycle status.

### Demonstrated English-save review warning correction — 2026-09-26

The genuine Gmail05 log records successful ordinary Save and Gmail Confirm (HTTP200), each followed by an automatic Arabic-review-state request for the completed EN job that returned422. The visible save panel consequently displayed an Arabic-only error even though row91 and its confirmed session join were correct. This is a demonstrated local product defect, distinct from the actual Google403 draft-permission limit.

Amend the scope only to prevent bootstrap restoration from requesting Arabic review for a known ineligible current job. Preserve real completed-Arabic review, pending-review restoration when no current job is selected, all routes/payloads and the original save/confirm behavior. Implement in this isolated branch with meaningful no-provider browser-state coverage for completed EN/FR, active or ineligible jobs, completed AR and no-current-job restoration. Do not replay Gmail05, provider calls, Word export or finalization. Update touched-scope current docs, run the targeted tests and a fresh Full after the source change, independently review, then publish through the already authorized ordinary PR lifecycle. Earlier Full655 remains pre-fix evidence; actual Gmail05 source remains PR304 until the new revision is merged and applied.

### Local correction outcome and publication gate

The amended implementation changes six lines in `translation.js` and adds 149 lines of browser-state tests. Known ineligible jobs now skip the Arabic-only state route; eligible completed AR, restoration without a current job and real error propagation remain intact. All 20 targeted tests passed in 0.91 seconds; independent semantic review is clear. Frozen SHA256: source `44c4f19603a0104a2e84904314fe37df75f0184e16af56e34a78c757f496ccb4`, test `2645da876660ec828329f296070aa50f6bc68b472b454787041134f6863f0a07`.

Fresh Full finished at2026-09-26T11:50:24Z under `validation/english_save_review_full_01/`: all659 selected tests passed (240+2+5+239+173), nine expected deselections and compile passed. The wrapper exited1 at docs-only AD046 SESSION_RESUME marker failures after the preserved AOT255 event; its fallback failed then. Restoring the required exact markers first exposed an authoritative-branch line parse issue; separating the branch/path resolved it. Both failed docs attempts remain preserved, followed by passing direct docs/hygiene in `docs_sync_ar02_gmail_closeout_01/session_markers_repair_01/result_02.json`. No passing tests were repeated and the wrapper is not relabeled as exit0. Source commit `24f7ecbd80984223f0a55c012ab369b40ccfb720` and both pinned source/test hashes are unchanged. Both plans close for completed scoped implementation/qualified acceptance; authorized ordinary PR/CI/merge/application status belongs to `gmail_closeout_publication_01/publication_receipt.json`, which records any verified merge and applied canonical revision. An absent or unsuccessful receipt does not establish publication completion. The genuine Gmail05 evidence remains pinned to `a8f7950` and no new paid/native run is required. Exact replaced guidance is preserved under `docs_sync_ar02_gmail_closeout_01/beforeimages/`, `docs_sync_ar02_gmail_closeout_01/beforeimages_gmail05_sync_01/` and `docs_sync_ar02_gmail_closeout_01/beforeimages_guard_closeout_01/`.
