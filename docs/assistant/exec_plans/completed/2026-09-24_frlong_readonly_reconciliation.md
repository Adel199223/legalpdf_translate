# FRlong01 charge reconciliation and connection diagnostics

## Goal and non-goals

Reconcile the uncertain page6 attempt against retained evidence, diagnose only what the evidence supports, and prepare a fresh bounded retry proposal. Preserve the original USD10 cap, USD0.119 hold/block, all ledger rows, saved defaults, partial output and historical evidence. This task grants no paid dispatch, OCR, native Word, Gmail, email, public hosting, default promotion or publication authority.

## Scope

Read the private fresh handoff first, current repository instructions and dormant resume section; follow only exact French receipt pins initially. Verify actual Git/protected/runtime state. Inspect local source and installed SDK code where those records identify a diagnostic gap. Fix only a demonstrated local defect using no-provider tests. Perform scoped documentation synchronization with beforeimages.

## Worktree provenance

- Worktree: `C:/Users/FA507/.codex/legalpdf_translate_frlong_reconciliation`
- Branch: `feat/frlong-reconciliation-20260924`
- Base branch and target integration branch: `main`
- Base SHA: `f5002be85e33601a8c5f4e13c5fbd2d7dfd209d7`
- Contains approved floor `4e9d20e`; canonical main and local origin/main match base. No remote fetch or publication performed.
- Noncanonical isolated authoring only; no LegalPDF server/browser launch. Canonical worktree remains unchanged. Later user-authorized provider-dashboard reading is recorded separately below.
- Initial process check found no relevant LegalPDF server or Word process and no listeners on8877/8765/18892. All six final-audit protected file hashes match their published receipt.

## Interfaces, types and contracts

Preserve routes, API payload shapes, submitted values and native/Gmail/browser contracts. Do not alter retries, billing disposition or default settings. Any local diagnostic change must be evidence-driven, privacy-preserving and separate from a claim about the historical transport cause.

## File-by-file steps

1. New private reconciliation receipt: verify exact pinned accounting/request/log artifacts without importing or executing consumed helpers; independently join the uncertain reservation and ordinary dispatch.
2. `src/legalpdf_translate/openai_client.py` and related accounting code: inspect failure propagation and retained diagnostic fields; edit only if an actual local defect is demonstrated.
3. Relevant `tests/`: deterministic fictional SDK/transport regression only if code changes are justified; no provider credentials/network.
4. This plan: record evidence, decisions, validation and closure.
5. `docs/assistant/HANDOFF.md`, `SESSION_RESUME.md`, `VALIDATION.md` and architecture/diagnostics docs only as touched: preserve exact beforeimages, distinguish unpublished work from canonical main, record unresolved billing/cause and retry prerequisites.

## Tests and acceptance criteria

- Current ledger hash/226 rows and prior rows preserved,225 finalized and1 uncertain; exact USD10 cap/known USD3.92137740/hold USD0.119/available USD5.95962260 confirmed or discrepancies reported.
- Partial DOCX exact bytes and absence of committed pages6–9 verified from bounded evidence.
- No usage inferred from zero/missing placeholders; no outage or connection root cause inferred from exception class alone.
- Use canonical `.venv311/Scripts/python.exe` with isolated source path for targeted tests. Standard `scripts/validate_dev.ps1`; required `-Full` if product/test/workflow files change. Record wrapper/direct-Dart fallback accurately.
- Recheck protected hashes, canonical clean state and final diff. Independent evidence and implementation review as applicable.

## Rollout and fallback

Leave changes isolated and reviewable; no commit/push/merge assumed. If evidence cannot establish usage or cause, retain the hold and block and report the precise limit. A retry proposal is conditional only: requires authoritative charge disposition and fresh explicit bounded paid authority; no bypass campaign.

## Risks and mitigations

Avoid browser bootstrap routes, which can invoke Word readiness. Do not read private document bodies/config dumps or search the entire historical corpus. No helper replay, ledger writes/locks, destructive actions or provider calls. User explicitly authorized this bounded read-only/local-fix task; historical stage tokens are not renewed. New paid/native/publication operations remain separately gated.

## Assumptions and defaults

Roadmap remains dormant. Saved app models/effort/protocol are separate from assistant preferences. Historical price snapshots are evidence for old calls only. Available Codex usage tools do not expose API billing for this request; inspect only genuine available billing records, never claim account-wide access from absence of a connector.

## Progress

- 2026-09-24: Required entry points read; base, protected hashes and inactive runtime verified; isolated worktree and fresh plan created before code work. Independent read-only accounting review underway.
- Root verified56 receipt-pinned files; independent accounting review verified57 across its bounded union. All hashes/sizes matched;226 ledger rows and209/213/215/217/220 prefixes and nine historical campaigns are unchanged. Independent same-call joins price pages1–5 at USD0.07093800; page6 remains unknown with its hold/block intact.
- Fresh receipts live under private `C:/Users/FA507/.codex/legalpdf_translate_private_benchmarks/frlong_reconciliation_20260924_01/`. Root evidence SHA256 `d4b481f4f37079dd8a2fd7b8db138f3571c71baaaaae2e1797ffcad9d2cc5182`; independent accounting SHA256 `1ad2b333b9be04c584a815edda88cda8f3f3e7838c7e6d2f028148fa50fe7270`; recovery review SHA256 `b20f360f6d261c9e3f88a92eb0314cd52747fec531fc386f89dc7fa2b5832d27`.
- User subsequently authorized computer/API access for investigation and personally signed in. The bounded read-only provider usage page showed5 requests/4648 input/4486 output, exactly matching the five received French responses. Stored Responses logs had0 results. `platform_usage_observation.json` records the scope. Independent review agrees this does not settle the specific failed call or prove final reporting completeness; no charge/hold/row was altered.
- Demonstrated defect: structured failure handling retained only the outer class; the in-memory explicit transport cause was lost from persisted run events. Changed `openai_client.py` to format only fixed allowlisted HTTPX classes and `new_translation_blocks.py` to record that string through the existing diagnostic event when enabled. No arbitrary messages, URLs, context-only causes, private subclasses, accounting schema, route/API shape or retry policy changed.
- Added meaningful fictional regressions to `test_openai_structured_transport.py` and `test_new_translation_blocks.py`: cause-chain privacy/bounds, durable event, exactly one fake dispatch, missing usage and no committed target. The end-to-end regression failed before the repair; after repair all98 tests in those two files plus `test_transport_dispatch_accounting.py` passed. Independent source/test review found no actionable issue.
- Scoped docs beforeimages are saved byte-for-byte in private `docs_before/` with a manifest. Architecture, handoff, resume, validation and public-source registry updated; no templates or historical operation evidence changed.
- Supported recovery review distinguishes exact backend original-owner restore from browser restart/relocation. Current ordinary-browser route requires a fresh full nine-page review/run. Prepared conditional13-call/USD1.547 proposal in private `RECONCILIATION_AND_RETRY_PROPOSAL.md`, preserving20k-input/6k-output, model/effort/protocol/tier, original campaign30-attempt/USD4 and lifetimeUSD10 caps. It remains blocked, unarmed and unexecuted. No historical helper replay, provider/OCR/native/Gmail operation, support message, default change or publication occurred.
- Required Full wrapper passed639 selected tests (236 browser/route/state,2 Gmail review,5 filtered Gmail intake,234 source review,162 formatting), plus compilation and docs/hygiene. Formatting took907.75 seconds; total Full runtime was about20 minutes. Both `dart run` validators reported `Unable to find AOT snapshot for dartdev`; both direct-Dart fallbacks succeeded. This is not the entire pytest collection and establishes no real provider/native acceptance.
- Independent final review found no blocking source/privacy/accounting/proposal issue. Its two wording clarifications were applied: historical failure is described as the retained exception category, and provider-dashboard browsing is distinguished from LegalPDF app launching.

## Outcome and remaining boundary

Authorized local reconciliation, demonstrated diagnostic repair, scoped docs sync and a fresh bounded retry proposal are complete and unpublished. Move this plan to `completed/`; final direct docs/hygiene/whitespace checks and exact preservation are bound by private `final_local_receipt.json`. Canonical main remains at the base SHA with no source changes. The five translated pages, all original evidence, every ledger row and saved defaults are preserved.

Page6 charge and inner cause remain unresolved. The dashboard corroborates the five known responses only. USD0.119 remains held and further paid dispatch stays blocked; no refund, zero charge or network/provider root cause is asserted. The conditional13-call/USD1.547 proposal is review material, not dispatch authority. An authoritative failed-request billing disposition and supported accounting recovery remain external prerequisites. No consumed helper, paid translation, OCR, native export, Gmail operation, message sending, publication or default promotion is part of this closeout.
