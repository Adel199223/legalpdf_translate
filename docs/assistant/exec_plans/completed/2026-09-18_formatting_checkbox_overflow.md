# Formatting checkbox label overflow

## Local completion — 2026-09-24

The scoped implementation is independently reviewed and integrated in the unpublished acceptance worktree. All four independently reviewed UI fixes are integrated only in the unpublished acceptance worktree. The final affected suites passed 39 tests; the combined Full wrapper passed 639 selected tests plus compilation and docs/hygiene checks, using the documented direct-Dart fallback. Full is not the complete repository pytest collection. Post-edit closeout docs validation is recorded separately. Desktop and narrow normal-click partial downloads passed using fictional loopback bytes; the actual French download used its observed artifact URL before the UI fix. No paid translation was replayed. The implementation scope is complete; publication is separate. Parent testing is concluded with its documented French failure, deferred optional Arabic formatting and qualified Gmail recovery. Earlier pending-status entries below are retained execution history.

## Goal and non-goals

Keep formatting-review checkbox controls and their labels inside their available column. Root observed clipped Bold/Italic labels and horizontal page scrolling in actual ARshort03 at1440x1000. Actual geometry records a550.2px checkbox input inside a550.2px label. This is a concrete UI defect; the older synthetic-only finding did not establish this actual occurrence.

## Scope

Change only formatting-review checkbox sizing/wrapping in style.css. Preserve global inputs, other features, source/target text, review controls, API/contracts and the frozen campaign worktree. No provider, backend, Gmail or Word/native operation.

## Worktree provenance

- Worktree: C:/Users/FA507/.codex/legalpdf_translate_formatting_overflow
- Branch: codex/formatting-control-overflow-20260918
- Base: canonical main7a86cd07a28496eecfbe3cc7e69398a611242184
- Target: parent-owned multilingual worktree integration after the actual operation; publication separately authorized.
- Noncanonical, no application server launched. Render only fictional isolated browser fixtures.

## Interfaces and implementation

No interface/type/route/select change. Preserve style.css exact beforeimage privately. Global input width100% leaks into flex-none formatting checkbox controls; override checkbox dimensions/margins/padding in their existing local selector, and allow adjacent label text to wrap without pushing the column. Do not hide overflow.

## Tests and acceptance

Use the existing actual formatting UI/controller modules and full CSS with fictional mock data in a disposable headless browser context. Route all fixture resources locally and block external requests; no application server or real data. Retain before/after geometry at1440x1000 and narrow width, prove checkbox/label bounds and no horizontal page overflow, and exercise normal checkbox click/uncheck. Capture screenshots and exact source/browser provenance. Keep rendered synthetic evidence separate from root's actual screenshot. Parent runs required combined Full after integration; do not rerun the long wrapper for this isolated CSS preparation.

## Rollout and fallback

Deliver uncommitted CSS diff, exact pins and private rendered evidence for root/peer review. Canonical main and running campaign assets remain unchanged. If rejected, retain the worktree/evidence without operational rollback.

## Risks and assumptions

A checkbox needs intrinsic/fixed control width rather than form-field width. Scope prevents changing text inputs or source-review styles. Wrapped text must stay readable and the control remain keyboard/pointer usable. Root owns exact actual screenshot/geometry evidence and later actual post-integration verification.

## Progress

- Plan and exact beforeimage created before implementation; actual reported geometry independently read.
- 2026-09-24: resumed from the unchanged base and preparation-only worktree. The original plan and CSS beforeimages remain private. One local checkbox selector now restores intrinsic width/margins/padding; adjacent label spans can shrink and wrap. No other input or application file changes.
- The disposable browser fixture used the worktree's real formatting controller/UI modules and full CSS with fictional data and locally fulfilled resources. The original CSS made checkbox elements 549px wide in the desktop column and312px at narrow width, pushing every label outside its owner. Narrow document width was434px for a390px viewport. With the fix, every control is13px wide and all seven label/control pairs fit their containers; document width equals viewport at1440px and390px. Six sampled text/number fields retain their original full-column widths.
- Normal label/pointer and keyboard toggles passed complete on/off cycles for all seven controls. Image full-resolution/fit mode preserves style selections without page overflow, while selecting a new source block correctly resets the fragment style. Desktop and narrow screenshots were individually inspected, including checked styles, table-gap control, page attestations and document acceptance. These are fictional rendered checks, not actual AR03 acceptance or native output evidence.
- One initial after-capture still served baseline CSS because the REPL route closure retained the earlier phase; its original geometry/screenshot remain preserved. A new explicit CSS route verified the loaded CSS rules before the passing checks. One screenshot captured unfinished scroll/compositor state; retained as excluded evidence and replaced by stable captures. No product behavior was inferred from those captures.
- Browser fixture closed after review. Independent patch review, parent integration after the source freeze, required combined Full and actual UI verification remain pending. No server, provider, Gmail, native export, settings or ledger operations occurred.
