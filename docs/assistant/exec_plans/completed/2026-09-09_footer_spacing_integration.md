# Integrate approved footer spacing and Word export safeguards

## Goal and non-goals

Integrate the human-approved source-page-5 footer presentation and the native
Word-export safeguards verified by the v11/v18 pilots. Keep translations editable
DOCX and honorarios export compatible. Do not promote the unaccepted structured
translation pipeline, OCR/source recovery, model policy, benchmark tools or
private documents. A new TXT-only production translation does not gain guessed
source/block associations; these formatting improvements require valid retained
source/target sidecars and benefit local rebuilds.

## Authorization and decision locks

The user supplied exact `NEXT_STAGE_4` after being told this approves the footer
and begins integration. The native v18 run passed once with all seven identity
checks, proven ownership/cleanup and one A4 page visually inspected; the user
has now approved that footer. This is not full-language/model/cost acceptance.
No additional Word exports, paid/OCR/key calls, settings changes or recovery
replay are needed for transplant validation. Preserve all old evidence and the
original lifetime USD10 ledger. The user subsequently explicitly approved
publication, merge after successful checks, and verification of the integrated
app. This authorizes the standard scoped PR/merge/cleanup lifecycle, not force
push, private-evidence publication or a new native/paid operation.

## Scope

In: six existing formatter modules, four native-export modules, directly relevant
synthetic regression tests, scoped product/support/validation/continuity docs.
Out: structured protocol/checkpoints, provider/OCR/extraction changes, acceptance
client/ledger/recovery tools, model/pricing/glossary policy, unrelated harness
changes, browser/Gmail route/payload/select contracts, database changes.

## Worktree provenance

- Integration worktree: `C:/Users/FA507/.codex/legalpdf_translate_footer_integration`.
- Branch: `codex/footer-spacing-integration`.
- Base and target integration branch: canonical `main`.
- Fetched base SHA: `ec406dac02ec7898dd339c37320f52f7881a043b`.
- Approved floor `4e9d20e` is contained by this base.
- Noncanonical review tree only; no live Gmail or production launcher changes.
- Source: preserved dirty `legalpdf_translate_structure` branch at the same base;
  harvest accepted files only, never merge or publish the combined research tree.
- Other formatting/honorarios/quality worktrees were inspected and are clean;
  no overlapping dirty worktree transplant or destructive cleanup is needed.
- Isolated local `.venv311` uses the existing canonical dependency directory
  read-only through an ignored `.pth`; source imports point at this worktree.

## Interfaces and contracts

No public route, payload, submitted select value, native-host/Gmail or database
change. Formatting-only versions invalidate layout derivation/rebuild identity,
not saved translations. Original process ownership, bounded single-export,
cleanup quarantine and legacy journal compatibility remain mandatory.

## File-by-file implementation

1. Transplant exact accepted `document_layout.py`, `document_spacing.py`,
   `section_furniture.py`, `docx_writer.py`, `formatting_support.py` and
   `layout_integration.py`; retain canonical `document_structure.py` and all
   extraction/model/workflow modules. Include the corresponding synthetic folio,
   isolated footer, spacing, boundaries, rebuild and legacy regressions.
2. Transplant exact accepted `word_automation.py`, `word_pdf_control.py`,
   `word_pdf_script.py` and new `word_process_start.py`, with their seven native
   safety/control/script/runtime/diagnostics test files. No private QA helper.
3. Independently review dependencies and the integrated diff, then run focused
   tests, full pytest, serial `scripts/validate_dev.ps1 -Full`, docs/hygiene and
   diff checks. Prove module origins and unchanged production defaults.
4. Verify deterministic local rebuild through synthetic fixtures and compare
   the formatter/native bytes against the accepted source; no consumed native
   helper replay. Keep native visual proof scoped to its exact accepted pilot.
5. Synchronize only relevant docs under existing approval, create scoped commits
   after full-tree triage, and follow applicable publication/merge authorization.
   Keep canonical main stable until validation and required integration gates.

## Acceptance and rollout

No critical regression, preserved complete source IDs/table/list content, no
footer/header adoption from uncertain or TXT-only evidence, no extra document
boundaries, no reduced fonts. Full test suite and full validation pass on this
new subset; historical full-suite counts do not replace integration validation.
Inspect any Windows COM diagnostic serially; record successful direct-Dart
fallback after known AOT errors without claiming a host repair. Publish only
this approved clean integration branch; verify exact-head required checks
before merge and canonical post-integration validation afterward.

## Risks and mitigation

- Subset dependency regression: independent formatter/native reviews plus full
  tests from the clean base, not the research tree.
- Misleading production claims: document that fresh TXT-only runs lack trusted
  block associations and that model/structured activation remains deferred.
- Frozen private evidence: no edits/replay of source artifacts, journal or ledger.
- Scope drift: exclude new OCR, legal protocol and model modules even if useful.
- Publication gates: approval is recorded; stop before merge if exact-head
  checks, branch lineage or review requirements are not satisfied.

## Assumptions and fallback

- The accepted one-page pilot authorizes this narrow formatter/native subset,
  not new production structure, language/model policy or another native export.
- Canonical `main` remains unchanged while this isolated branch is validated.
  If integration validation fails, repair only the demonstrated subset defect;
  do not use the dirty research tree as a substitute or reset user evidence.
- If a publication or merge gate fails, retain the validated local commits and
  report the specific blocker. No unrelated work should begin while this
  accepted integration remains only on its side branch.

## Progress

- Base fetched, floor verified, other worktrees and pending source scope triaged.
- User footer acceptance recorded; independent transplant reviews completed.
- Independent native and formatter scope reviews found no coupling blocker.
  All ten transplanted production modules are byte-identical to the reviewed
  source versions; the new branch retains canonical extraction/workflow/models.
- Carried 15 focused test files, excluding only the final structured-workflow
  test from the boundary-label fixture because that protocol is not promoted.
- Human approval retained in a separate private Stage4 event; immutable native
  and visual closeouts still truthfully preserve their original pending state.
- Publication was requested separately from pilot acceptance. The user then
  explicitly approved publishing, merging after checks and verifying the app.
- Focused integration regressions: 615 passed in 95.66 seconds across 16 suites.
  Full pytest passed 2,492 tests in 380.77 seconds, without a COM diagnostic.
  Serial `validate_dev.ps1 -Full` passed: 233 browser compatibility tests in
  167.99 seconds, compilation, two Gmail review tests, five selected intake
  tests (nine deselected), docs and workspace hygiene. Both known Dart AOT
  wrapper failures used successful direct-Dart fallbacks. This is not a host
  repair or a claim that the historical COM diagnostic has been eliminated.
- Direct docs validation initially caught the missing machine-readable Branch
  marker and its punctuation; restored the expected marker without changing
  the validator. Direct docs validation now passes.
- Final independent scope review found no blockers: ten runtime modules,
  fifteen relevant test files and seven scoped docs/plan files only. All local
  production imports resolve; no private artifacts, provider/default changes,
  OCR/extraction changes, database changes or browser/Gmail contracts are staged.
- Rechecked native/visual closeout and lifetime budget-ledger hashes: unchanged.
  The accepted DOCX was open elsewhere; a read-only shared stream verified its
  unchanged hash without closing or modifying the user's Word session.
- Local runtime/test commits: formatter
  `1d0cd2e37cea73b7fca08cf12ab9258b1c51a1f7`; native safeguards
  `777c6b7a6418e435bd2529080c0ce06e3c82c09e`. Scoped documentation records the
  actual new-tree results and limitations; direct docs/hygiene checks pass.
  No code changed after the successful focused/full/wrapper validation.
- Implementation and scoped documentation are complete. Archived this plan and
  restored canonical session authority for branch closeout before merge; all
  six touched-doc links now target this completed record. Publication approval
  does not itself prove a PR, CI result, merge or post-merge app check.

## Publication handoff and prepared continuation packs

This completed plan records the accepted implementation, scoped docs and
successful local validation. The user approved the remaining publication
lifecycle. At branch closeout, no successful GitHub checks, merge or canonical
post-merge verification is claimed. Verify actual GitHub/main state before
deciding which steps remain; preserve the research worktree and private evidence.

1. Execute the approved publication lifecycle: fetch and check current main/floor,
   confirm this branch still contains the latest approved base, inspect the
   exact local commits and publish only `codex/footer-spacing-integration`.
   Open or update the PR to main and require successful checks on the exact
   final head. Branch-scoped implementation/docs closeout is prepared here;
   check it remains consistent with the reviewed head before merge. No force push,
   private evidence, native retry or experimental production activation.
2. After a successful merge: verify the actual merge, safely synchronize clean
   canonical main without switching a running server's branch, then run focused
   post-integration checks on that build. Verify canonical launcher identity if
   an app is opened. Only then clean up the fully merged temporary integration
   branch/worktree when authorized and safe; retain the unaccepted research
   tree and immutable private evidence. Report actual GitHub/main state and the
   remaining source-associated production/model acceptance work separately.
