# Measured diagnostic cost consistency

## Local completion — 2026-09-24

The scoped implementation is independently reviewed and integrated in the unpublished acceptance worktree. All four independently reviewed UI fixes are integrated only in the unpublished acceptance worktree. The final affected suites passed 39 tests; the combined Full wrapper passed 639 selected tests plus compilation and docs/hygiene checks, using the documented direct-Dart fallback. Full is not the complete repository pytest collection. Post-edit closeout docs validation is recorded separately. Desktop and narrow normal-click partial downloads passed using fictional loopback bytes; the actual French download used its observed artifact URL before the UI fix. No paid translation was replayed. The implementation scope is complete; publication is separate. Parent testing is concluded with its documented French failure, deferred optional Arabic formatting and qualified Gmail recovery. Earlier pending-status entries below are retained execution history.

## Goal and non-goals

Make the final diagnostic cost event agree with durable provider-dispatch accounting. A genuine two-call English acceptance case reports USD0.028399 in ordinary/lifetime accounting and run summary, but a separate stale diagnostic table emits USD0.017896. This correction changes reporting, not provider dispatch, saved defaults, lifetime reservations or the historical evidence.

## Scope

In: final diagnostic event cost/totals/provenance, incomplete coverage behavior, focused offline regression tests, and review of legacy estimate-table relation. Out: active acceptance worktree edits, provider/native/Gmail calls, ledger mutations, publishing, broad pricing redesign, or automatic remote price discovery.

## Worktree provenance

- Worktree: C:/Users/FA507/.codex/legalpdf_translate_cost_reporting.
- Branch: feat/measured-diagnostic-cost-20260917.
- Base branch: main.
- Base SHA: 7a86cd07a28496eecfbe3cc7e69398a611242184.
- Target integration: root-owned multilingual acceptance work after its pinned campaign completes; no commit, push or merge in this subtask.
- Status: isolated noncanonical implementation work; no server launched here. The active acceptance worktree remains unchanged.
- Private finding: application_multilingual_gmail_acceptance_20260917_01/cases/en_short_01/cost_diagnostic_finding_01.json, SHA256 bb41f98e248c99deeaa5d350d79a52001b13cf390bba2412c907e7c3d5b068af. It contains hashes and exact accounting correspondence, without promoting this diagnostic audit to full document acceptance.

## Interfaces, types and contracts

Preserve existing event name and established counters/details consumed by diagnostics and reports. Durable accounting remains the authority for observed calls. Complete known usage can produce a measured total; partial/unknown coverage must keep the total unavailable. Reasoning is included in output tokens; cached input is discounted once by the existing verified snapshot.

## File-by-file implementation steps

1. Inspect workflow final event and summary ordering, diagnostics event helpers and existing accounting tests.
2. Update the diagnostic path to consume the same durable accounting summary, including cached counts and explicit coverage/pricing metadata where compatible.
3. Correct only the two stale GPT-5.2 legacy estimate entries to the already verified public standard rates, keeping pre-run and unmetered fallback estimates distinct from measured evidence. Record the existing official reference in EXTERNAL_SOURCE_REGISTRY.md.
4. Add focused offline tests for the observed discrepancy, cached input, multiple calls/corrections, incomplete or unknown usage, historical incompleteness and estimate override precedence.
5. Record tests and final changes here; hand the isolated diff to root for later integration and required Full validation.

## Tests and acceptance criteria

Use canonical C:/Users/FA507/.codex/legalpdf_translate/.venv311/Scripts/python.exe with this worktree's src/tests. No external/provider/native operations or original ledger opens. Relevant tests include test_translation_diagnostics.py, test_workflow_dispatch_accounting.py and test_cost_guardrails.py. Prove event and run summary agree for complete durable evidence, and never substitute a numeric legacy estimate for incomplete measured coverage. Root will run the long Full wrapper after integration; do not duplicate it here.

## Rollout and fallback

Leave changes uncommitted in this isolated worktree. Root reviews/integrates only after the active pinned campaign. Preserve all actual run evidence; never rewrite the historical incorrect event. Revert only this proposed reporting change if tests show contract regression.

## Risks and mitigations

- A second cost authority can undercount failed/correction calls: consume the durable summary directly.
- Missing price/usage can appear falsely complete: retain null total and coverage state.
- Cached input or reasoning can be double-counted: use existing durable Decimal pricing results.
- Active run pin drift: no changes to the campaign worktree or canonical application files.

## Assumptions and defaults

The existing verified public-price accounting is correct for the witnessed run. No fresh pricing, model or effort default changes are needed for this reporting defect. Root authorization covers this focused fix and targeted offline tests only; no publication is authorized here.

## Execution record

2026-09-17: Created the fresh isolated worktree from current canonical main after preserving the immutable real-run finding. Canonical has an existing untracked browser-tool evidence directory; it remains untouched. Implementation has not started.

Seven new offline workflow regressions failed against the original source: the exact numeric discrepancy, configured estimate-rate override, malformed estimate-rate parsing, three partial-coverage cases (missing usage, unknown model, unknown tier), and a historical-incomplete resume. All use fictional documents and SDK responses; the numeric usage fixture reproduces the witnessed arithmetic without private legal text.

The final event now emits after run_summary.json is persisted, using that exact budget_post_run payload and durable dispatch metadata. This removes the separate diagnostic repricing path and preserves the established event name and existing fields; additional metadata exposes measured/incomplete status, cached counts, known subtotal and pricing snapshot. Unknown totals remain null. Correction calls, inclusive reasoning and cached discounts come from durable accounting. Only the stale GPT-5.2 estimate-table entries changed to the already verified USD1.75/14 standard rates; their legacy_unverified/estimate-only classification remains intact.

Canonical .venv311 targeted validation in this worktree passed 54 diagnostics/workflow/guardrail tests, then 47 report/usage/transport accounting tests and one additional DOCX-write-failure lifecycle regression (102 total). The last test proves cost evidence survives a failed final artifact write after moving emission to summary persistence. Direct-Dart agent-docs and workspace-hygiene validation passed, as did git diff --check. No provider, native Word, Gmail or real lifetime-ledger operation occurred. The active acceptance worktree and exact private finding remain unchanged. Full validation is deliberately deferred to root after later integration; no commit, push or merge was performed.
