# Full Arabic normal-browser acceptance

Completed 2026-09-17 with qualified actual nine-page ordinary-workflow acceptance. Product changes remain unpublished in the isolated worktree. The append-only execution record below preserves superseded checkpoints; its early pending instructions and consumed helpers are not continuation authority. The closeout records the current disposition and bounded private evidence entry points.

## Goal and non-goals

Close the remaining full Arabic ordinary-workflow evidence gap using the real local browser, real source/OCR and provider transport, explicit reviews, editable DOCX download, and actual rendered-output review. Preserve qualifications and distinguish browser, translation, package, extraction, native-render and visual results. This does not establish cross-language readiness, global-default promotion or the separate efficiency objective.

## Scope

In scope: fresh isolated acceptance, bounded real operations charged to the original USD10 lifetime ledger, concrete product fixes, relevant validation and scoped assistant-docs sync. The current user explicitly authorized this acceptance continuation and useful docs sync; historical stage tokens and consumed-operation gates do not renew themselves.

Out of scope: public hosting, live Gmail, native-host registration, global setting promotion, publication, history rewriting, historical-branch merges, destructive cleanup and replay of consumed helpers.

## Worktree provenance

- Worktree: `C:/Users/FA507/.codex/legalpdf_translate_arabic_acceptance`.
- Branch: `feat/arabic-browser-acceptance-20260917`.
- Base and intended integration branch: `main`.
- Base SHA: `2e31edccad2252c20d55332fe5167c7ff439460f`, matching canonical HEAD, origin/main and live remote on 2026-09-17.
- Approved floor `4e9d20e` is an ancestor.
- Noncanonical acceptance build only; isolated shadow workspace and private evidence root. Canonical main stays clean and stable.
- Initial process audit: no relevant LegalPDF/Word processes or listeners on 8877, 8765, 18880, 18887. Record actual owned PID, module path, workspace, served asset identity and browser provenance before UI execution.

## Interfaces and contracts

Use existing public upload, source review, reviewed translation, formatting review, revision/build and download routes. No route, payload, select-value or safe-rendering contract change is planned. The stock server is not the isolation boundary: startup must inject isolated services, disable live bridge and deny unrelated external/native actions while preserving real in-scope provider and OCR behavior. No fabricated accepted source/target decisions or synthetic SDK responses count as real evidence.

## File-by-file implementation steps

1. This plan: maintain current decisions, operation identities, costs, outcomes and residual gaps.
2. Private `application_ar_browser_acceptance_20260917_01/`: new immutable preflight, source/settings hashes, owned runner, browser receipts, accounting and output-review evidence. Never execute prior helpers. Preserve exact beforeimages before scoped doc changes.
3. Production source/tests: change only concrete defects demonstrated by this acceptance; record each affected file and meaningful regression before implementation.
4. `docs/assistant/HANDOFF.md` and applicable architecture/validation/user guide: scoped current-state synchronization, preserving historical outcomes. No vendored template changes.

## Tests and acceptance criteria

- Real normal manual upload and browser page rendering, complete source review with explicit decisions, accepted source, genuine ordinary translation for every selected page, explicit formatting review and derivative choice, exact revision build and downloaded editable DOCX.
- Prefer the retained nine-page Arabic source to close the exact gap; audit source identity and prior review aids before fixing the final case. No partial-page success is a full-document pass.
- Saved global model/effort/protocol/OCR/retention/page-break/bidi choices remain unchanged. Required supported per-run choices must be explicit and recorded.
- Original ledger cap, prior rows and holds remain intact. Before dispatch, lock fresh operation identities, exact model/effort, request/output ceilings, current verified prices and run ceiling. No unlimited retries; unknown outcomes retain reservations and require exact recovery.
- Inspect all source and resulting pages, complete text, editable OOXML ownership and actual native render. Never shorten legal text, clip it or shrink fonts to force a page target. Record source uncertainty and translation/layout qualifications.
- Run smallest relevant targeted tests for fixes, standard validation and Full when code/test/workflow changes require it. Do not repeat completed regression or paid/native evidence without a new reason.

## Rollout and fallback

Keep original ordinary output and reviewed derivative separate. A declined or failed operation remains evidence, not permission for silent retries or reconstructed provenance. Diagnose offline first. Publication of any new changes is a separate approval scope through the current commit/publish workflow.

## Risks and mitigations

- Shadow startup can invoke Gmail/native host: use inspected isolated service composition and owned loopback listener, not stock launcher.
- Review handles are process-owned: preserve the server during review and use exact public recovery controls for lost responses.
- Saved legacy protocol is not reviewed-formatting capable: use an explicit supported per-run reviewed-source choice without changing global defaults.
- Real OCR can differ from old evidence: compare actual full-page images and current baseline, using old decisions only as review aids.
- Costs and native exports are irreversible: fresh journaled identities, bounded calls and timeouts, real usage settlement, no replay.

## Assumptions and defaults

The fresh handoff is supplemental current continuity; source/Git/process evidence wins over dated prose. Original lifetime cap is USD10; the dated checkpoint is 178 finalized rows, USD2.82529675 spent, zero lifetime holds, USD7.17470325 remaining, subject to fresh read-only verification. Historical FR01 local hold remains preserved. App defaults remain separate from the assistant model preference.

## Execution record

2026-09-17: Read the requested private handoff first, then current project rules and handoff. Verified clean canonical/main and live remote at the base above. Created this fresh isolated branch/worktree. Bounded independent audits are reviewing public acceptance routing and budget/source evidence. No new source acquisition, reservation, provider call, native export or acceptance server has started.

Fresh accounting audit matches the original ledger SHA256 `f187c2cf3c5094496776d33bd33c6091f69e9ba23865d45763e9b8c04dba07fb`, all 178 finalized rows and the dated balance. Current normalized settings are high/fixed_high, image auto, OCR auto/local_then_api, retention off and page breaks on. The explicit run will enable retention in the visible form; saved defaults remain untouched. Reviewed-source dispatch selects legal_blocks_v2 internally for this run.

Current official model pricing was searched and opened on 2026-09-17 at https://developers.openai.com/api/docs/models/gpt-5.2: standard input/cached/output USD1.75/0.175/14 per million. Fresh instrumentation is being prepared for at most nine primary requests, one per page, no retries/corrections, high effort, default service tier, 50,000 input-byte upper bound including framing, 12,000 inclusive output tokens, no provider image/tool input. Worst-case request ceiling USD0.2555; nine requests USD2.2995 within a USD2.50 scope ceiling and the unchanged lifetime cap. No reservation or arming has occurred.

Browser capability discovery found callable Playwright tools using Chromium 153.0.8010.48 on Windows. Existing tab 0 was blank; newly allocated tab 1 is acceptance-owned and remains blank. The direct-Dart generic preflight stalled because its Edge `--version` probe launched a browser process. The owned Dart chain and exact preflight-owned Edge process tree were stopped; no successful generic-preflight claim is made. Actual browser tool provenance is available independently. No app UI execution has begun.

Fresh complete source-image review found historical transcription additions: repeated institutional/contact text absent on pages 5, 6, 8 and 9, plus an extra separator on page 5. Fresh private transcription aids correct these and separate metadata cells; old artifacts remain unchanged. Source pages have no extractable PDF text. The new browser review must compare genuine OCR against the complete visible source and apply its own explicit actions; historical acceptance flags are not reused.

The existing private Tesseract 5.5.3.20260724 closure was found and all 96 files rehashed successfully. Only the acceptance child PATH/TESSDATA_PREFIX will select it. Source acquisition is bounded to one complete prepare attempt and at most 27 genuine local OCR passes; no paid OCR. Per-run UI choices are Arabic, one worker, retained intermediates and provider images off, with saved defaults preserved.

The new private lifetime guard passed 27 disposable tests, with SHA256 `95e73c5fccf5adf124e6949862dea0a4db2c0c3e8f5160817f6bfd045cd2a145`. It remains unarmed. It uses the original ledger's lock and atomic fingerprint semantics, preserves historical rows/campaigns, and records each actual ordinary call once in the lifetime ledger. These two accounting views are nonadditive. Unknown costs retain their reservation and block further calls.

Native output review will use at most one fresh owned Word PDF export for the selected reviewed DOCX, through the current supported exporter with a bounded 75-second inner deadline and an outer watchdog. No readiness probe/canary or historical helper replay. The original shared Word slot remains the continuity authority; its current terminal FR04 journal SHA256 is `19e65192eb6d85f0992ab9305ee86ffb7a4da4edac18783441a0976a745314c1`. Preserve exact beforeimages before the new operation, and never restore an old journal after advancing it.

The source-review visual aid v1 is superseded before provider dispatch. A fresh comparison found repeated institutional/contact content and separators present in actual app page images but absent in the multi-page external render set. Per-page fresh-document renders corroborate the app; sequential rendering shows differing results, so the root cause is not yet asserted. Preserve both render sets and v1, use fresh v2 based on all nine actual app images. Only pages 1-2 were saved before this correction; those texts are separately rechecked. No source acceptance, arming or provider call has occurred.

Correction to the preceding provisional diagnosis: the rendered file hashes are identical, and individual-image inspection confirms the supposedly missing content. No PDF renderer or product defect is demonstrated. The error was in the multi-image visual review. The v1 deletions are invalid; v2 will preserve the visible repeated headers, contact footers and separators. Inspect one image per tool result for the remaining source and output review, and corroborate any claimed omissions with the actual file before changing text.

First real normal-browser attempt: source review `22ad9774d219434ea8d361fc39917097`, accepted revision `4b54be1cd3854af7964cde0a6378f841`, fresh start nonce `2e8f94979b89df53aa318098ae3eeda0`, job `tx-6496ce5213c6`. All nine saved source texts exactly match superseding v2 after UI newline normalization, with complete baseline ownership and boundaries [1,2]/[3]/[4,9]. The ordinary run committed pages 1-2, then the page 3 primary response failed strict `block_language_or_token_defect`. The normal workflow requested its correction; the acceptance-only primary-call guard declined before a fourth dispatch. Three real responses were retained and settled at USD0.0381010, zero holds, with 178 original rows unchanged. Preserve terminal job, raw responses, partial DOCX and run state; do not count this as full acceptance.

Recovery preparation, not dispatch: inspect the exact page 3 defect offline before deciding whether a product fix is warranted. Prepare a distinct `_02` case and never replay the consumed `_01` launcher. The ordinary workflow supports one correction after a primary validation failure; a fresh bounded guard may permit that normal path, at most 18 dispatches (9 primary + one correction per page), no transport retry, 20,000 UTF-8 input bound including framing and 6,000 inclusive output tokens. Worst case USD0.119/call and USD2.142 total. The original USD2.50 acceptance scope ceiling remains inclusive of the settled USD0.038101, leaving USD2.461899 for new dispatches; original lifetime USD10 remains unchanged. No recovery dispatch is authorized by preparation alone: root must inspect the concrete defect, current ledger and fresh exact manifest before arming. Saved defaults and original evidence remain untouched.

Concrete product fix selected after read-only reproduction: the complete supported judicial signature title and its exact proper name may be separated by one explicit blank line in the reviewed page transcription. `structured_arabic_entities.py` currently recognizes only immediately adjacent title/name lines, so the repeated name in the real page 3 response fails source-literal occurrence accounting despite matching visible source occurrences. Make a bounded one-blank allowance for this exact title/name and complete place/date-abbreviation chain, preserving hard/table boundaries, source offsets, exact names and count validation. Update literal policy identity if semantics require it. Add synthetic regression/adversarial boundary tests; revalidate retained actual page 3 bytes offline without modifying them. This is a targeted fix within the approved acceptance scope, not relaxed validation. Product/test edits require the standard Full validation before claiming completion.

The narrow signature fix now passes 807 focused tests, including 52 new synthetic spacing/boundary cases; 23 of those cases reproduced the old failure. Exact-contract offline revalidation passes the unchanged accepted page 3 source and unchanged raw provider response under v14. All nine source pages prepare successfully; conservative request sizing is below the fresh 20,000-byte bound. This is regression/revalidation evidence, not a completed real run.

Fresh case `_02` source-only server started once from new `serve_acceptance_04.py` (SHA256 `e1c6837b4c3ac5fe11f701ec6f899009f2a0012987f64c436b9512076da08630`), PID14164, port18887, workspace `arabic-acceptance-recovery-20260917`, asset `a3cd055c90e2`. Code inventory `18b9923d48340a70fadf4bf563df347e4b342ed943e7420749bf07dd7f0b9e0b` pins all230 source files and the unpublished fix; inventory equality and hashes are rechecked before each dispatch. Guard v2 `fb2e4f3e1c8dac48e1477f0f2d0b2112868019817c0e8dd2220786f776cb1228` passed38 disposable tests and binds181 finalized starting rows, ledger `6f96fa537a6cdd230770812adfdae8c67b4bde68818d0840134b57213aae6b45`.

The new normal file upload and genuine local OCR prepared once with9 Tesseract processes. All9 actual image hashes and baseline texts match the individually reviewed v2 evidence. Every source decision was entered and saved through the UI, exact text/coverage verified, and the whole-source accepted as review `75a670abcb31413eaec196dc419a7db1`, revision `b2a0139f8d2241a1ac7b29cc9ce05f8a`. Provider remains disarmed pending completion of the already-running Full wrapper. The old owned tab was closed after its evidence was retained; pre-existing blank tab remains untouched. No replay of `_01` helpers or checkpoint grafting occurs.

Full validation completed with exit0: browser233, Gmail unit2, Gmail intake5 (9 deselected), source review231 and formatting153 passed, plus compileall. Both Dart wrappers encountered the known AOT snapshot failure; direct-Dart docs and hygiene fallbacks passed. This is the required Full wrapper, not a complete-repository pytest run. Fresh manifest `df996d622c267829de97d58cd38fca42d014a2d018f7b30ed63b6b80b6e64494` then armed without dispatch or reservation. One normal UI Translate reviewed source click created nonce `77edc75612a2dd47ebc2685d07f70b4e`, job `tx-efa7983bc234`, run `20260917_132507_376136_source_AR_run`. Genuine bounded translation is in progress; full acceptance remains pending its terminal outcome, actual translation review, formatting revision/build/download and native output review.

The fresh job completed pages1–3, then stopped on page4 after its genuine normal correction also failed `block_language_or_token_defect`. Five actual responses settled at USD0.15160670. Combined acceptance cost is USD0.18970770; the original ledger now has186 finalized rows, USD3.01500445 spent, zero holds and USD6.98499555 remaining, SHA256 `98225f3ccf369026ac78e47cf1347f9b055139244a149fc3d7be64e24a741c07`. All181 pre-case rows and original campaign identity are preserved. Full output/formatting/native acceptance has not occurred. Exact offline page4 diagnosis and a broader remaining-source audit precede any recovery decision. Terminal public receipts and partial output are retained; the owned server was stopped normally after evidence capture.

The real manual-upload failure exposed a separate presentation issue: its recovery message incorrectly referred to a Gmail attachment. `translation_result_presentation.js` now uses context-neutral translation/rebuild wording for both failed and cancelled jobs. No route, payload or recovery behavior changes. This low-impact copy fix will be covered by existing browser validation in the next required Full pass, alongside any demonstrated validator fix.

Independent exact-contract diagnosis classifies page4 as genuine provider defects: the primary retained an ordinary printout word in Latin; the correction omitted a protected abbreviation and retained another ordinary label. The correction also changed an imperative to a past action, so passing literal checks alone would not prove translation fidelity. Strict validation remains unchanged. General Arabic instructions will clarify that all ordinary words, including borrowed/quoted labels, must be translated, while each supplied protected literal retains its exact spelling and occurrence. No case-specific allowlist or edited provider response is permitted.

The proactive all-nine source audit found a separate page5 attribution gap before another paid call: explicit marked witness names separated from their exact witness heading by single blank lines were not protected. The narrow source-only repair permits at most one explicit blank between a supported heading/valid marked entry and the next valid marked entry, while hard/table boundaries, two blanks, non-item prose and invalid names still terminate scope. Exact source offsets and strict occurrence checks remain authoritative. Version identity and bounded synthetic regressions will cover this change. A distinct `_03` scaffold is prepared, not executed; original scope remaining USD2.31029230, unchanged lifetime cap, and all earlier helpers remain consumed or explicitly unexecuted.

The v15 witness/prompt changes passed871 relevant synthetic tests. Five new spacing cases reproduced the old defect before implementation. Exact all-nine offline preparation adds only the two source-proven witness names on page5; no removed spans or changes to the other pages. Retained successful pages1–3 still pass with identical normalized target hashes; failed page4 responses remain rejected. Current Arabic instruction hash is `e2a92c0db8d4bc961114e19bf9da946ed03ecacaf4398f2db7bc507f2f28a127`; conservative primary/correction request bounds7062/7926 bytes remain below20000. The required Full wrapper is running again because production code changed.

Fresh `_03` source-only launcher `serve_acceptance_05.py` SHA256 `6e5eb7f2c6053c2f5e521c7381a3fa5b41810e34fa594014be53633c3c7107b8` started PID24964 on18888, workspace `arabic-acceptance-final-20260917`, asset `59464fa348f8`. Full230-file code inventory SHA256 `06a4fd0d6449660ad01d1ec618bd0a2090f550270c084d3a8d5f1f8f0b93c779`, guard `38bce55f757e17bd4c672ba8c26597bc708a3276cb72a8dddfd42a67c7fe6621`, original186-row ledger checkpoint unchanged. Normal UI upload and one genuine local source prepare created review `f80fbc4a4d8f4d9baa6196d60951254c`. Provider remains disarmed. A fresh raster hash comparison found a tiny page1 antialias-pixel difference; this is preserved and checked against actual pixels/source identity, not mislabeled byte-identical or a source-content change. The earlier owned browser tab was closed after stopping its server; the pre-existing blank tab remains untouched.

Fresh `_03` image review confirms pages2–9 byte/pixel identical to retained individually reviewed images. Page1 differs in exactly four header pixels and one OCR confidence value; word text/geometry and every baseline text/block remain identical, with no legible content change. All nine source decisions were entered through actual UI controls, saved, and checked for exact v2 text, complete baseline ownership, whole-page regions and reviewed boundaries. Whole-source acceptance selected revision `21dbf13b3a624c77838df7943350b6ab`. No job nonce, arming manifest, reservation or provider call exists yet; remaining Full formatting tests must finish before arming.

Latest Full wrapper completed exit0:233 browser,2 Gmail unit,5 filtered Gmail intake (9 deselected),231 source review and153 formatting tests, plus compileall. Both known Dart AOT wrapper failures were recovered by successful direct-Dart docs/hygiene checks. After that terminal pass, manifest `db07d0de7e8f72bdea6391c22eba1b8e73d56ed30ecb0690ca9ba3db65575dcf` armed without a reservation/call. One normal UI Translate reviewed source click started job `tx-7f6ecea646a8`, nonce `cb4d969b41563b32f40b20931adf8229`, fresh run `20260917_135100_572760_source_AR_run`. Genuine translation is running. Actual bilingual review, source/target formatting mappings, reviewed build/download and native-render acceptance remain pending; prior cases are not reused as its output.

Fresh `_03` terminated after seven committed pages: page8 primary and correction failed `block_citation_association_defect`; page9 was never called. Eleven genuine responses finalized at USD0.26177970. Combined acceptance cost is USD0.45148740, leaving USD2.04851260 of the original USD2.50 scope. Original lifetime ledger has197 finalized rows, USD3.27678415 spent, zero holds and USD6.72321585 remaining; SHA256 `a9787654e01890108d0661fe607f535111bffc352b3168140cecb36a81631246`. All prior rows and identities are intact. Normal owned shutdown confirmed unchanged canonical/isolated settings, one source prepare, nine Tesseract processes and no denied actions; owned tab closed, pre-existing blank tab untouched.

Actual bilingual review of all seven committed pages found a material page4 command-to-completed-action shift. This prevents clean quality and formatting acceptance despite passing mechanical checks. Exact glossary/addendum review confirms no saved term caused that clause shift. Page8's two unchanged rejected responses preserve both article281/282 pairs, but the parser omits the second282 before an Arabic comma. Exact source count is two each; punctuation-only offline diagnostics prove a narrow Arabic comma/semicolon grammar gap. Retain actual bytes, strict counts and all failed evidence. Fix the parser with synthetic boundary regressions; clarify the general Arabic prompt to preserve tense, mood, commands/requests and conditional legal effect, without case-specific target repair. Full validation must pass again after this new code batch.

A distinct unarmed `_04` scaffold is prepared on port18889, workspace `arabic-acceptance-modality-20260917`, operation04. Guard v4 SHA256 `921d88cb5e751f67afefc6abdfc297301df85c70835dbe2649a5d8923af8f564` binds the current197-row checkpoint and original inclusive scope. It allows nine ordered primary calls with at most one immediate normal correction per page, no transport retries,10000 UTF-8 input upper bound including framing and6000 output tokens, USD0.1015 per-call ceiling and18-call worst case USD1.827. Forty disposable guard tests passed. Actual `_03` request maximum6556 bytes corroborates margin but does not prove unattempted page9 sizing; the guard checks each actual request before reservation. Native operation04 is prepared but unexecuted, uses the same original shared Word slot, and cannot proceed without an actual full-quality reviewed build/download. Final code inventory, launcher pin, source review and arming remain pending.

Citation policy v4 is frozen with the general Arabic modality prompt change. Eight new fictional regressions failed before the punctuation fix;975 targeted tests now pass. Offline exact-contract revalidation passes all seven unchanged committed `_03` targets and both unchanged rejected page8 responses, without punctuation replacement. Page4's independent material meaning error remains; these checks do not repair or accept that run. Fresh Full wrapper is running again.

Fresh `_04` disarmed launcher `serve_acceptance_06.py` SHA256 `af8776f1e2cc0bfdd5fdd4eaa65fab6551b9d775af5bf0a80e324cf46664f73c` started serverPID3200 on18889 with all230 source files bound by inventory SHA256 `2ce4886316d766c3987bedadc79d1b77cdc15f7d49254d59111809f6f76eea27`. Browserasset `59464fa348f8` is unchanged because this batch changes Python protocol/prompt, not static UI assets. One initial read-only status request omitted the required workspace and was correctly rejected; this is retained as one rejected HTTP request, not a product/provider failure. Normal fresh manual upload, explicit per-run settings and one genuine local source prepare created review `82eb44c3c5c7464e9d3f3e402538a5ea`. Source image/baseline applicability review is pending before UI source decisions. No arming, provider call, reservation or native operation occurred.

All nine fresh `_04` app images and complete OCR baseline/word evidence match the individually reviewed `_01` originals. Source decisions were entered through the actual UI, verified exact against v2 and complete baseline ownership, then accepted as revision `000a79fa3cf34e90b5196db7f5053485`. Whole-source acceptance is actual browser evidence; no translation or formatting claim follows yet. Final Arabic instruction SHA256 `7ce017a15d6e6d3d6c080f863b5907639580f8f399f89925072dbf16c451a1c4` adds259 UTF-8 bytes; independent all-nine conservative primary/correction request bounds7320/8184 leave1816 bytes below the10000 guard. The earlier7926 was an offline bound, distinct from actual `_03` maximum6556. Required Full formatting tests are still running; provider remains disarmed with no nonce/job/reservation.

Execution-order adjustment before dispatch: the975 targeted parser/Arabic/protocol/adapter tests and exact unchanged-response checks have passed; fresh Full has also completed its browser/source groups and is now running the long formatting group. Allow the independently bounded genuine translation to run concurrently with that remaining regression group. The earlier wait-for-Full-before-arming order was this plan's scheduling precaution, not a user/provider scope gate. Source acceptance, frozen code, current ledger, exact manifest and all request/cost guards remain required before dispatch; final acceptance still requires terminal Full success and actual complete bilingual/formatting/native review. This reduces idle waiting without changing operation scope, cap or publication boundaries.

Fresh exact `_04` manifest SHA256 `d44a1595e7c10ef9ab5d4f67d64edb9626f61298b207facbc27f6be8bdeee931` armed without call/reservation, then one normal UI Translate reviewed source click created job `tx-e4423d04b1e8`, nonce `f84f70e0e8d9d8028103124494a2fcd8`, run `20260917_142623_276341_source_AR_run`. Genuine translation and remaining Full formatting tests are now concurrent under the amended order. All terminal outcomes, complete bilingual review and actual artifact/render evidence remain pending.

`_04` terminated immediately,6ms after job creation, before running/progress, with fixed public reason `ordinary_source_review_job_failed`. No provider request, reservation, run_state or target exists. Ledger is byte-identical to its197-row checkpoint and all balances remain unchanged. Actual terminal receipts are preserved; owned server stopped normally and tab closed. This is a zero-dispatch startup failure, not a translation result.

The sanitized job receipt does not retain the underlying exception, so exact cause is not yet proven. Static inspection identifies a reachable race between the background accepted-source loader and the UI-triggered source-image read: both acquire the nonblocking run slot, but only the latter uses the review-entry lock. Actual browser resource timing confirms Translate and page9 image requests started together at13:33:11.983UTC, and the image response completed after the6ms failed job. Independent deterministic reproduction is in progress before selecting any code fix. Fresh `_05` scaffolding will preserve the zero-call `_04` failure and add content-free private startup exception observation; no consumed launcher is replayed. Remaining Full formatting validation continues for the frozen pre-race-fix snapshot.

The deterministic private fictional regression reproduced the reachable startup race using actual image/service.read and background load_context code with the genuine disposable run slot: RunWorkspaceBusy produced the same fixed public error, queued/zero progress and zero Workflow constructions. Test expected completion and failed1case in2.20s; exact original `_04` exception remains unretained. Implement a narrow retained-loader closure acquiring the existing same-review entry.lock before loading the exact accepted context. This serializes UI reads against the loader without waiting on arbitrary external run owners, bypassing verification, changing nonces, or exposing private exception text. Add the deterministic repository regression and preserve external-lock negative coverage. Stop of `_04` was confirmed before this source edit; a new Full run will cover the revised snapshot alongside the fresh `_05` source/translation work.

The prior D Full wrapper completed exit0:233 browser,2 Gmail unit,5 filtered Gmail intake (9 deselected),231 source and153 formatting, plus compileall and successful direct-Dart docs/hygiene fallbacks after known AOT255 wrapper failures. Its earlier groups predate the race edits; do not call it full validation of the later snapshot. The new backend serialization has135 affected tests passing, including genuine disposable same-review contention and external-owner rejection. The UI no longer renders a fresh image during a busy review action;11 browser-state tests passed, including a pre-fix-failing deferred-request/nonce recovery regression. Both fixes are frozen, and new Full validation is running for them.

Fresh E (`_05`) launcher `serve_acceptance_07.py` SHA256 `3cc8dbf74b8da28620ec72f24d7849a36657278336ee21e4c1700df280885715` started owned serverPID25528 on18890, workspace `arabic-acceptance-startup-20260917`, asset `9a964e17fc6e`. Code inventory SHA256 `28cdbaadb2f2ccc6da70e63effc3515624b2931fd6d3abb916619d2772f3fdd7` pins230 source files; guard v5 `8f5fdd717985dcc1996e0281050e5a3103ee7410bc63385aa9b2dc89eccfbebd` retains the same197-row checkpoint and all original caps. Private content-free job-frame startup exception observation adds no execution/control behavior;40 guard plus11 tracing disposable tests passed. One normal upload/source prepare used9 local Tesseract processes. All images/baseline fields exactly match reviewed A/D; actual UI saved all9 source decisions and accepted review `e3d671c91b464a9c807fcef32a8e14aa`, revision `09491ad6a8a148728bad8f80a0458ac3`.

E manifest `3637e994a71979a0c934af229c0fb33c12d9f905f1f8323b75ebe109b6891e19` armed with no call/reservation. One actual UI Translate click created nonce `feca4f86350cace3420bf594f5c49210`, job `tx-f64406b60cec`, run `20260917_144611_123929_source_AR_run`. The job is running with no recorded startup exception. Actual browser timing shows the last page9 image request at13:48:14UTC and Translate at13:49:22UTC, with no new image request at dispatch. Full validation, terminal provider outcome, complete bilingual quality, formatting/download and native evidence remain pending. Provider/default/ledger boundaries are unchanged.

E terminal outcome: the genuine ordinary workflow completed all nine pages, with12 settled primary/correction calls costing USD0.29164730. The original USD10 ledger has209 finalized rows, USD3.56843145 spent, zero holds and USD6.43156855 remaining; SHA256 `8ba7c23bd6d0c9bcb3a689a0615a6538e5f5d963bc391c7de57ebc29d00cc7a3`. Inclusive A–E acceptance spend is USD0.74313470, leaving USD1.75686530 of the original USD2.50 scope. No startup exception, provider transport retry or target-byte edit occurred. Complete bilingual review produced147 exact source/target groups. The page4 imperative is preserved in the real output. Page1's procedure label remains a terminology-precision qualification: the original adverse review is preserved, with a separate independent review and root disposition; this is not exact procedural-nomenclature certification or unqualified legal-editor acceptance. Nine-page QA index SHA256 `ed13b63ad8208a104b54b0bf22da7ba34cea4a1cdfed608bd18f3d42b1421168` records the complete scope and qualifications.

E's required Full wrapper completed exit0 with627 selected tests (233 browser,2 Gmail unit,5 filtered Gmail intake,234 source,153 formatting),9 deselected, compilation and successful direct-Dart docs/hygiene fallbacks after the known AOT255 failures. Independent final validation/default/accounting receipt SHA256 `6e4cade4a640dc19cd75e8fb8376410024233bc5869ca3713a92859ea9d94787` verified all230 frozen source pins, actual response joins and unchanged canonical/isolated settings. This validates the frozen E snapshot before the following newly discovered UI fixes, not later edits.

Actual ordinary formatting acceptance followed complete source-image and bilingual review: all147 immutable source/target selections were entered using visible DOM controls, with explicit page/table/paragraph/footer decisions, source groups [1,2]/[3]/[4,9], strict text boundaries, capped source spacing and the run's saved page breaks. Review `1c26851e2f0a4d30a6bdb2b2c3059c28` submitted revision `d3df8a8a4bae43d2b5efed2ab21803db`; exact-revision check returned200 ready. Two mouse build clicks were intercepted by the still-open originating overflow menu; no nonce or build was dispatched by those attempts. Ordinary keyboard activation then reached the backend, which returned422 `browser_formatting_review_run_busy` while a fresh own-image read was active. No operations directory or intent existed. The actual saved-review read and enabled exact-build recovery used the same nonce `17392801567b9c5ba8ecebf5293e4031` and returned200 built, with no paid call. Retain the failed response and normal recovery as evidence, not a clean first-attempt success. Proposals address closing the originating overflow menu and suppressing a fresh image render while formatting actions are busy; both have private synthetic red/green regressions and are authorized for narrow implementation after artifact capture.

The actual normal-browser download at2026-09-17T14:21:05.493Z returned artifact `a398e1653dd94ec8b617cd2251fe71fa`, filename `reviewed-output_docx.docx`, HTTP200 and DOCX MIME. Its60342 bytes exactly match the run-owned reviewed DOCX SHA256 `f353627959afe208e2d1ffbb46714224d060695750df6d2c9107028e594c29b2`. The root observation receipt SHA256 `4f9b76a6b2d5cc6fa158914396139e431bd9a8e6780a3c20adee4a5766ffac4d` distinguishes the real browser event from later byte-identical private retention. Genuine post-build ordinary-job response SHA256 `808aa77c17f3440ede07636dc47e7d38b35939752b3d2248b179d33e0c563b54` joins the completed nine-page job and reviewed artifact IDs. Independent actual-package audit SHA256 `464c1f9068ab648fcfd62ecd10d5d46d4be3bf778527ba4510ea794fb2d79580` passes with zero findings,147 fragments,40 table fragments,460 runs and5 editable tables, complete text/hash/ownership coverage, Arial at least11pt and explicit RTL. Native rendering and complete visual layout acceptance remain pending.

Native predispatch file inspection found that the exact Windows PowerShell binary has two system hard links, conflicting with the prepared helper's universal one-link rule. The original helper is preserved unexecuted. Fresh derivative `export_reviewed_arabic_once_v2.py` SHA256 `6ffc317924c67699989e16d63db2e0ff836b158cc2a5aa8e3990524bcd8bd6f0` permits exactly that binary path, pinned digest/size and observed link count; all artifact/settings/ledger/code files retain one-link requirements. Nine disposable pin tests passed. This is a private predispatch guard correction, not a native export, product change or permission to replay consumed helpers. Final actual-artifact manifest and single supported export remain pending.

The reviewed E native manifest SHA256 `a56fa4c9e2f098b66c78d33c38b787216c59575a41276984971ef4db40517c0e` pinned135 actual files, including all62 revision members. Root independently rehashed every pin and verified completed-job/artifact/revision joins and no running Word process, then invoked v2 exactly once. It exited1 after3.15 seconds at `application_import` with `native_routing_changed`: zero native-export calls, zero PowerShell dispatches and zero boundary denials. `export_01` is consumed and preserved, with its exact manifest/helper/beforeimages/failure. The original shared Word journal remains SHA256 `19e65192eb6d85f0992ab9305ee86ffb7a4da4edac18783441a0976a745314c1`. Pure resolver/file-identity diagnosis proves `C:\WINDOWS` versus `C:\Windows` caused a strict string mismatch despite identical resolved path, physical file and expected hash; the command executable check had the same latent mismatch. Prepare a distinct operation02 helper/manifest/output with executable-path identity normalization only, leaving all other argv and pins exact. This corrects a demonstrated private predispatch guard error; it does not replay operation01 or increase the one-native-export allowance, which remains unused.

The two later formatting UI fixes are frozen in `formatting_review_ui.js` SHA256 `f9c6ef1267ddbc9824ab308c347ea8bc0737989188152c9ce85581618665c00e`, with test file SHA256 `373f78af74c366ead5ca8f3868c0c4c3ff5044c84569548c860858b20fd84352`. Combined source/formatting browser-state tests passed24 cases in31.85 seconds. Actual isolated Chromium153 geometry checks at1038x706,1440x1000 and800x900 passed normal pointer opening/build activation, using real UI/CSS with explicitly synthetic service responses and no E-session mutation. Geometry receipt SHA256 `cd501f5d4a174de58730440843c14efea7729b0842a4634a99941f3ac64709ae` is distinct from actual E translation/build evidence. A separate pre-existing checkbox-label overflow was observed only in that fictional fixture and retained for bounded UI polish; it did not obstruct the tested buttons and is not claimed as an actual E defect. Fresh post-formatting Full validation is running in session32643, log `application_ar_browser_acceptance_20260917_05/validate_dev_full_post_formatting_ui.log`; six Word modules and all captured E artifact bytes remain unchanged.

Operation02 used fresh helper SHA256 `b2c68d56d6732615dc04cd9e2c76fd6784064049fffde76c71cf8795c8d0555a` and manifest SHA256 `63ffc448593bf270ab128cbc6cd1f78c535aa9a3e0a3b239f1eb47f457b09938`;17 disposable routing/pin/command tests passed. Root rehashed all147 pins, verified identical selected artifacts/limits and no running Word process, then invoked this distinct helper exactly once. It returned failure at terminal verification, with one supported exporter function call but zero PowerShell dispatches, zero boundary denials, no staging/PDF and the unchanged original FR04 journal. Preserve consumed `export_02` and its exact old-journal observation; that historical successful journal is not evidence of a new native success. The helper's summary did not retain the returned app failure code. Static inspection identifies its own attempt to read `export.lock` while holding Windows byte0 as a likely predispatch OSError. Reproduce only with disposable lock files and a fictional application export path before selecting a new guard revision. No actual Word process has been launched in E, so the single native-process/render allowance remains unused; consumed helpers remain forbidden to replay.

E's owned server stopped normally at2026-09-17T14:32:57.779894Z after actual artifact/job/download evidence capture; PID25528 exited and only its browser tab was closed. The pre-existing blank tab remains. Shutdown confirms unchanged canonical/isolated settings, one source prepare,9 Tesseract processes and zero outer-boundary/network/provider rejection counts. The saved retrospective browser resource timing list is empty and supplies no extra timing evidence; rely on the actual request/status/duration records and separately labeled deterministic contention reproduction. No live Gmail, publication or global-default promotion occurred.

The final post-formatting Full wrapper completed exit0 in session32643:233 browser tests in193.71s,2 Gmail unit in0.53s,5 filtered intake in0.98s (9 deselected),234 source in78.85s and155 formatting in767.28s, totaling629 selected passes. Compilation passed; both known Dart AOT255 wrapper failures recovered through successful direct-Dart docs/hygiene checks. No pytest warning summary is present. This is the final product/test snapshot, distinct from the earlier627-test E runtime snapshot. A bounded code/test review found no actionable product findings; authorship overlap is disclosed in the private review, so it is not described as wholly independent authorship. Final scoped documentation checks remain required after closeout edits.

Disposable Windows-lock reproduction confirmed that reading the helper's own byte-locked file can raise PermissionError before process creation. This proves a reachable private-guard defect, while operation02's unretained original returned failure code remains qualified. Independent inspection found an additional latent guard incompatibility: trusted subprocess setup opens the Windows null device read/write before process creation. Fresh v4 helper `export_reviewed_arabic_once_v4_operation03.py` SHA256 `8f24c166ecda8750db19186141337cadd514a997ad2db3d54059f95c100978e0` keeps lock identity checks during ownership and exact bytes before/after, permits only the exact trusted subprocess null-device setup, separates attempted/boundary/confirmed process counters and retains the content-free app result. Twenty-six focused tests passed. The whole real helper/main/application setup was also exercised with fictional pins and intercepted process creation: zero actual native processes/network/original-slot access. No consumed helper was replayed.

Root independently verified all164 pins in fresh operation03 manifest SHA256 `a0fc89309c54096f4025ecf6a1ebc2c24d3d2784b1a67448a24912890c82fe6a`, unchanged actual DOCX/ledger/Word modules, the two prior zero-process failures and absence of Word. One invocation completed exit0 in24.906s, with one supported export, one confirmed PowerShell process, zero provider/probe/canary calls and zero boundary denials. Word cleanup is confirmed. Native result SHA256 `c5884546c9a28373d5f3483be2d483aca0e9a17db215ead4fb0c57a7d6556fbb` binds the209806-byte PDF SHA256 `22ddf036428f92a3c3569f3a654216cd1a820399df4adc9c4fbb61d466f0e61f`. The original shared journal advanced to `4cf1e07e3d3e3b7171f0a6e85e2594a7f62f13b7e85276cc2dd2feae35fb9e35`; exact previous journal/script/lock/failure beforeimages remain retained. Never restore the previous journal. Operation03 is consumed success and no further native operation is required.

Actual PDF review found nine physical pages. Each page was rendered independently from a fresh PDF document at144dpi and inspected individually by root; an independent reviewer also inspected pages1/4/8/9. All pages retain legible joined Arabic, reviewed content, expected headers/contact footers/local folios and separation of table/body areas, without observed clipping or overlap. Page4's imperative, page8's paired citations/amounts/three-month duration and page9's conditional/future instructions remain visible. Root visual receipt SHA256 `63a0a30f6095b8588b7d30d1b47f9bcadeb2183fd996354850a7c0fe961aae63`; independent critical-page receipt `8497929ebccc8f0897b783084f545c5899804ecae0a1cb01eb1de186853cac6e`. The text-only profile does not certify crest, drawn-rule, partial-inline-emphasis or exact geometry parity; mixed ordinal/bracket punctuation and bilingual terminology/style qualifications remain.

Strict actual PDF extraction report SHA256 `0d5921d78b2142dc036f78db852af8bebde386e854e501e9b488c32c720d5076` remains nonexact: no missing characters, but five surplus U+0651 shadda on pages3/4/6. Separate font trace SHA256 `0ba2354c17f1785e1c7d2759adf54e01a7efb44e790a0b97aed682d21ae7b647` binds font CID02F6/glyph758, whose embedded cmap is kasra but ToUnicode expands to shadda+kasra. Exactly five occurrences explain every surplus; all nine page counters reconcile diagnostically without changing PDF/DOCX/strict report or stripping arbitrary diacritics. Zero-width traces are combining marks or multicodepoint-glyph continuations, with no unexplained glyph. This is an explicit PDF copy/search-text limitation. Character counters do not establish sequence, legal meaning or visual acceptance; those rely on the separate actual reviews.

Final read-only integrity receipt SHA256 `3467224c2f268020b15a95c746127efbfd1ce3f172060c126ebba7e555b1c59a` reverified164 protected pins, all original Word beforeimages, the new success journal, unchanged reviewed DOCX/browser download and saved defaults. The original ledger remains byte-identical to its209-row terminal checkpoint: USD3.56843145 spent, zero holds and USD6.43156855 remaining under the unchanged USD10 cap. Inclusive A–E acceptance cost is USD0.74313470. No owned server/Word process remains. Canonical main is clean at the original base; the isolated branch retains all unpublished changes. Task-owned browser-tool scratch was safely moved to E `retained_browser_tool_evidence`, preserving29 files; historical receipts describe the original paths, while retained copies remain private.

## Closeout and bounded evidence entry points

The requested full Arabic ordinary-workflow gap is closed with the recorded qualifications. Source review, genuine complete translation,147 exact bilingual mappings, actual normal formatting acceptance/recovery/download, complete DOCX package checks, one actual native export and all-nine-page visual review are complete. Final code/test validation passed629 selected Full tests; no complete-repository pytest claim is made. Later formatting UI changes have separate synthetic-service and actual browser-geometry evidence and are not retroactively attributed to E. Publication, public hosting, live Gmail and global-default promotion remain separate work. The original activation/efficiency roadmap remains deferred.

Private evidence root: `C:/Users/FA507/.codex/legalpdf_translate_private_benchmarks/application_ar_browser_acceptance_20260917_05`.

- `FINAL_ACCEPTANCE_20260917.json`, SHA256 `c598da72eb32d1bbb3a31457b28b8a04c873dc7cad4b663d4b6cd01433d1b362`: pinned actual receipts, budget, qualifications and final16-file product/test inventory.
- `FINAL_ACCEPTANCE_20260917.md`, SHA256 `584cf48d924495cd2e3df4b9325c33fb2bd878c0675a232262f9ebf21c2688a5`: concise human-readable private closeout.
- `browser_artifact_review/reviewed_AR_browser_download.docx`: actual retained browser download, SHA256 `f353627959afe208e2d1ffbb46714224d060695750df6d2c9107028e594c29b2`.
- `native_review_preparation/export_03/reviewed_AR.pdf`: actual native nine-page review artifact, SHA256 `22ddf036428f92a3c3569f3a654216cd1a820399df4adc9c4fbb61d466f0e61f`.
- `native_output_review/`: strict extraction, separate font reconciliation and all-page/independent visual receipts. `final_integrity_03.json` binds unchanged defaults/accounting and cleanup.
- `docs_before_final_closeout/`: exact five-file entry-doc/plan beforeimages; earlier A/D/E snapshots and all failed/consumed operations remain preserved.

Residual qualification: page1 procedure-name precision and the recorded glossary/office/style/Print-medium limits remain explicit, with original adverse review preserved; this is assistant acceptance of one real document, not unqualified legal certification. A checkbox-label overflow observed only in a fictional geometry fixture remains deferred UI polish and is not an E failure. No acceptance helper should be replayed. Publication review can start from this exact isolated change set only under a separate current authorization.

Final scoped documentation validation completed exit0:105 agent-doc validator cases and7 workspace-hygiene validator cases passed; direct-Dart current docs and hygiene validators passed, and git diff --check was clean. Receipt final_documentation_validation_01.json SHA256 e0e9d5c3256edbd4e6e60ea6b7bb116565218671036cacbe7e9b5ca9a0d4927b preserves terminal outputs. Independent closeout review found no material misclaim or unsafe continuation instruction and verified33 relative documentation links. Its minor stale C/E accounting label was corrected. The plan is closed in completed/; no publication action occurred.
