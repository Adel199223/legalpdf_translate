# Ordinary browser source and formatting review integration

## Objective and scope

Complete the user's existing acceptance goal by making the reviewed source and editable layout workflow usable from the local browser application. Continue in `legalpdf_translate_structured_activation`, branch `feat/structured-translation-activation`, base `4780a2e16c32adf7af7479656f0b5fafdff83224`. The separate `legalpdf_translate_reviewed_regions` worktree supplies individually reviewed scopes. Preserve original handoff files, private documents, saved settings, accounting and historical operations. Publication, live Gmail and destructive cleanup remain outside this implementation pass.

## Local candidate closeout, 2026-09-16

### Completed authorized candidate scope

- **Implementation and validation:** reviewed-source acquisition/context/publication, owned browser source and formatting review, explicit derivative/table/gutter controls and recovery are integrated in R. The actual public browser smoke completed source acceptance, translation, formatting review, DOCX download and exact same-operation recovery using fictional OCR/SDK (`9d2109be`). It preserves saved page-break False; it is synthetic UI evidence, not real-document translation quality.
- **Complete regression:** repository run03 passed **6,477 tests**, zero failures/errors/skips, in 1,795.40 seconds (launcher 1,797.907787); result `0cceeb4c`, JUnit `ed5c1d79`. Separate standard `validate_dev.ps1 -Full` run02 passed in 1,262.541817 seconds, result `adfcafe8`. Both retained the same 481-file snapshot `bec79f2960c371adaec995e99403f2117de32b7244415cfa9991dc89e91769bb`. Standard Full and complete pytest are distinct proofs. The recorded direct-Dart fallbacks passed after the known wrapper failures; the standard synthetic boundary is not an independently audited zero-egress/native claim.
- **Historical French outcomes remain distinct:** FR01 stopped before SDK contact and retains its separate local USD0.413 hold. FR02 retained one actual response but failed the citation false positive; its USD0.0124665 expense and failed state remain immutable. The corrected citation policy passed 203 targeted tests, 74 new full-suite cases and the retained-candidate offline check (`1b5c481d`); that check created no paid commit. FR03 genuinely completed two pages and USD0.0230005 expense, but failed bilingual acceptance on `ofício de notificação` → `office de Notification`. Independent reconciliation `d3eaa6f0` confirms all 174 prior rows; its stale descriptive 173 count did not alter the strict actual guard. None of these operations is replayed, repaired or relabelled.
- **Actual FR04 ordinary run:** root `6e2124` / terminal `193bc0` completed in 62.598 seconds with two SDK calls and zero audit denials. Result `abfc67d5` binds two genuine commits and the original ordinary DOCX `bb6bd0cd`. The normal per-run project glossary supplied the single reviewed exact/PT/tier1 phrase `ofício de notificação` → `courrier de Notification`; actual preparation proved no page 1 prompt change and only that phrase plus numbering in page 2. Source, production defaults and saved preferences are unchanged. This is explicitly term-assisted evidence, not unassisted model quality.
- **Qualified bilingual acceptance:** review `24bf4035` accepts FR04 for reviewed formatting with no material findings and 34 independently cut unchanged target fragments (23+11; maps `50fb6ec9` / `61a472d2`). It does not claim unqualified acceptance. Preserve the disclosed interpretation of malformed source appointment wording, gender precision lost under configured `Le juge`, and retained `d.s.` readability. No reviewer edited source or paid targets.
- **Current lifetime accounting:** ledger `f187c2cf` has 178 finalized rows, USD2.825296750000 spent, zero lifetime holds and USD7.174703250000 available under the original USD10 cap. FR04 cost is USD0.0174034; its ordinary/lifetime views constrain the same calls and are not additive expenses. All 176 prior rows are preserved. The separate FR01 local hold remains historical evidence, not an additional settled expense. No further provider call follows automatically.
- **Normal run-owned formatting complete:** FF02 passed once in 11.469 seconds (`d4fe8e17`), with all 34 paid fragments unchanged, editable metadata cells at 25/53/22 percent widths and zero audit/provider/native calls. DOCX `d27b5360`, source map `2d11a531` and revision `b06d2516` bind the explicit derivative. Saved page-break False, original DOCX, paid text and protected inputs remain unchanged; the explicit derivative uses True. Ordinary summary bytes remained exact, and the checkpoint has only normal final-path/timestamp/sticky-notice changes. The consumed FF01 held-lock failure and its diagnostic remain intact; the private correction checks bytes before/after ownership and identity while held, without weakening application locks.
- **Qualified native/text/visual acceptance:** Native FN completed once in 12.515 seconds (`c88b010e`), with one owned hidden process/export, confirmed cleanup and unchanged lifetime ledger. PDF `3d8337e2` was rastered into two complete 144-dpi pages (`b811d922`). Root full-page review `ca0542f8` and independent review `2f497a62` found no material visual omission, clipping or overlap. FQ02 report `6d47c275` remains **`extraction_checks_failed`**: its unique-cell-band predicate rejects one trailing U+0020 space (index 289, x 547.18–549.82 pt) beyond the third-cell edge at 547.1 pt. The independent retained-report supplement `aaeee1e1` confirms the sole whitespace outlier, all other 60 band characters inside the cells, and exact metadata nonspace counts 14/29/8. Final qualified synthesis `b1b82a8e69a9af0f7a7d3564147b656e0758312ccb7271c342d32b1366baaf83` is retained at private `application_ordinary_fr_render_qa_20260916_02/synthesis.json`. Both page/document character multisets and all 34 fragment texts match under the checker's declared NFKC/bidi/whitespace fold, with no case folding, punctuation deletion or mark removal; every nonspace character is owned once, both pages are A4 with Times New Roman 10.56-pt PDF text backed by 21-half-point DOCX fonts, and no characters lie outside a page. Checker audit/provider/native/render counts are zero. The strict failure is preserved, with no checker rerun or artifact alteration; physical content/layout acceptance is qualified, not an unconditional automated pass.
- **Declared limits:** The final accepted profile is qualified and term-assisted: source appointment-wording interpretation, configured `Le juge` gender precision and retained `d.s.` are disclosed. Crest, source drawn rules and partial-bold emphasis are not reproduced; exact geometry/typography parity and human legal certification are not claimed.
- **Lifecycle and remaining boundary:** this ordinary integration scope is complete locally for the reviewed PR/release candidate. The 23 child scopes and three current acceptance/browser/docs parents are archived; the original global-activation plan remains explicitly deferred. The candidate stays unpublished on the integration worktree. Root final direct-Dart docs validation (`167bec`, 1.6579019 seconds), hygiene (`ae4f50`, 1.1462242 seconds) and CRLF-aware whitespace (`7356af`, 0.2438054 seconds) passed against freeze `abc955bc`, with no relevant code change. Root completed local scope review and working-tree triage; the unpublished reviewed candidate is ready to present. No commit, push, PR creation, merge, canonical restart, global protocol/model/effort promotion or Gmail action is authorized. The separate >=20% model-efficiency goal remains unproven; the single reader profile is not that evidence.

Everything below is chronological evidence and is superseded where this current state differs.


Current regression checkpoint after continuation: complete repository run01 finished **6,377 passed, 18 failed, zero skipped/errors** in 1,687.76s; source/test snapshot unchanged. Sixteen failures were obsolete class-replacing fixtures, and two exposed a fresh-run probe creating a permanent lock before writeability was known. Scoped harvest `ca2c9205` preserves beforeimages, corrects class fixtures and moves only truly absent-run probing before creation, with locked rival-evidence rechecking. Targeted run passed263 existing/new tests; six newly authored rival tests initially read the Windows lock while held. The sole fixture indentation correction preserves every byte/inode assertion, and subsequent preflight/probe verification passed24 tests3.59s (`666e7f`). Production Workflow remains `bf13f854`; writer EOL-only normalization is `60f156a0`, with unchanged AST and a clean99-addition/1-deletion semantic diff. Independent reviews cover both corrections. Fresh complete repository run02 is active against this corrected source; no current full pass is claimed.

Standard `validate_dev.ps1 -Full` subsequently passed in 1,167.25 seconds against the same unchanged source/test snapshot (`application_standard_full_validation_20260916_01/run_v1/result.json`). This includes 231 source-review and 153 formatting-review tests, baseline/Gmail unit groups, compilation, docs and hygiene. The known Dart AOT wrapper failure had successful direct-Dart fallbacks. Complete repository run02 remains active; this standard Full result does not replace that separate suite. New docs and policy changes still require their own current validation after integration.

The retained AR03 partial operation now has a successful fresh offline rebuild (`080f26`, 5.20 seconds), with exact paid pages1/2 and unchanged original 2-done/1-failed/6-pending history. The first failed import operation remains preserved. Its isolated diagnostic identified an inherited editable canonical import path; the corrected helper retained only the intended worktree and runtime paths, without expanding filesystem authority. Normal Workflow correctly declined strict page-matched formatting for this incomplete selection and saved page-break preference, then built the normal partial DOCX. Independent synthesis `43e344cb` verifies exact paid text, unchanged original artifacts, and zero provider/OCR/native/start calls. This is a partial fallback check, not a full ordinary formatted-document pass.

The user explicitly authorized autonomous Assistant Docs Sync and useful reorganization on 2026-09-16, without further approval. Current entry pages, navigation, guides and the validator's explicit sync policy are being updated in the isolated worktree, with prior bytes preserved and dated history separated. No reusable-template/UCBS maintenance is requested.

The remaining full real ordinary-workflow case is a fresh two-page French run, planned under `application_ordinary_real_fr_20260916_01/PLAN.md`. It uses the unchanged accepted AI source evidence and actual ordinary Workflow identity, with two Terra High primary requests only, a USD0.826 run ceiling, and matching reservations in the original USD10 lifetime ledger. The private composition and caller are under review; no preparation, reservation or paid dispatch has occurred. Subsequent strict AI formatting must use independently reviewed new target ranges and an explicit derivative configuration; saved page-break False remains unchanged. Original lifetime balance is USD2.772426350000 spent, zero held, USD7.227573650000 available. No model/protocol promotion or publication is implied.

### Terminal validation update

Complete repository run02 passed **6,403 tests, zero failures/errors/skips** in 1,781.15 seconds (launcher 1,783.62 seconds), with an unchanged 480-file source/test snapshot. JUnit `9e9f7f14` and stdout `d0752496` are independently verified by `final_regression_verification.json` (`cd146784`). All 18 earlier failed nodes are present and passing. Standard Full also passed on the same snapshot. This supersedes every running/pending full-suite statement above and below; the tests need not be repeated without a new relevant change.

The new French accounting composition passed **47 disposable lifecycle tests in 22.62 seconds**, with zero audit denials or source drift (`accounting_tests_02`, independent review `732537e5`). The first run reached 47 passing cases but its optional JUnit hostname lookup was blocked by the registry guard; that incomplete reporting attempt remains preserved. Only optional JUnit output was removed; the offline guard stayed intact. Actual French request preparation then completed once (`a50a1d`, 5.51 seconds), producing two prospective ordinary requests with zero provider/native/reservation activity and zero audit denials. The exact runtime caller and physical lifetime-budget binding remain under review; no paid dispatch has occurred.

Earlier chronological checkpoints follow and are superseded where they conflict with this checkpoint.

Fresh responsive verification completed: actual asset a3cd055c90e2 served exactCSSf18f847b, single474.4px column at494px and preserved220px/1088.4px desktop columns at1366px. No horizontal overflow or browser errors observed; viewport reset, zero synthetic OCR/SDK calls, ownedPID39632 stopped. Receipt07e6b55e preserves actual measurements and static-only Gmail-focus limitation. Full repository pytest is still running with two observed failures awaiting final diagnostics; no full-suite pass is claimed.

Profile follow-up completed: the same real synthetic integration node passed1 test31.50s,32.386s wall (d2211b), versus94.449s wall before the source-reader correction. Both profiles record6317 source reads and9685 BufferedReader reads; source-reader cumulative time fell65.659s to12.705s and BufferedReader self time62.358s to10.520s. Comparison953365b1 retains the single-observation/main-thread limits; this is not20%complete-workflow savings. Full repository pytest is now running under the canonical interpreter at private application_full_regression_20260916_01/run_v1, all tests without filters; R source/tests/validation script stay frozen. The completed combined smoke server PID33968 was verified by its exact helper command and stopped; its sole IAB tab was reset to about:blank.

The combined actual browser fixture now passes source acceptance, translation, explicit two-paragraph formatting review, document/revision acceptance, DOCX build/download and exact reload recovery. One synthetic SDK response and one local OCR response serve the same completed job; the recovered build retains its exact nonce/revision/artifact. Saved page-break False and strip True are preserved. Private receipt9d2109be and downloaded DOCX3e3b1907 record this synthetic result. No real ordinary document or provider was exercised.

Formatting runtime finished60pass/1fail1188.14s. The busy-lock failure was diagnosed and fixed by propagating only RunWorkspaceBusy through the source loader and trusted-job lookup. Profiling also found the retained source reader requesting64MB for tiny objects; it now reads observed size plus one sentinel with full unchanged identity/hash checks and exact-length validation. The combined seven-file harvest16fd1a09 passed89 targeted tests260.19s, including15 deterministic bounded-reader cases and both concurrency cases. A same actual integration-node profile is pending. Source UI handoff tests10passed18.33s. Responsive specificity is corrected in two CSS selectors, with fresh-server browser verification pending because the existing server retains immutable startup assets. Full validation adds seven source and six formatting suites; the complete repository regression is still separate and pending.

Current R package freeze was released after the successful EN native export. Ordinary-use/release readiness remains pending real-document ordinary integration evidence, current full regression and docs/hygiene validation. The following paragraphs retain earlier checkpoints.

Backend29, source UI8 and formatting manager/API/UI14 are integrated with exact beforeimages and independent reviews. Source API44 and source UI/bridge28 tests passed. Formatting options35 passed644.94s. The formatting manager/API/UI61 suite is still running and has reported a failure; final details are pending. Earlier isolated141 socketpair denial,142 missing fixture and143 watchdog timeout remain failed attempts. Browser/API tests use the standard synthetic path, preserving the isolated runner's socket prohibition.

Actual source-only browser smoke used the normal file chooser and PDF.js pixels, explicit review, genuine Workflow with synthetic OCR/SDK, DOCX download and exact reload recovery with one SDK response. Receipt682b8b07 retains its submitted page-break and incomplete summary fixture limitations. The owned server is stopped. Its false upload-handoff warning and accessible labels were corrected in four reviewed files (harvest62408478, independent371e2232); all10 source UI tests now pass18.33s. The first test invocation's absent output parent caused10 setup errors and is retained separately. A fresh combined source/formatting smoke is being authored with explicit page_breaks=False and strip_bidi_controls=True.

Ordinary-use and release readiness remain pending actual formatting UI acceptance, current full regression and docs/hygiene validation. R's136-module package is temporarily held stable for the independent English build, which uses saved translations. No defaults, key, original budget, Gmail or publication state changed.

## Citation and reporting follow-up after actual FR02

The retained paid response exposed a false positive in the explicit article-head counter: Portuguese can omit the repeated article noun while French repeats it. Fix only conservative recognized citation elision, retaining exact numeric coverage, multiplicities, article suffixes and rejection of changed article/reference associations. Unsupported prose, dates, deadlines and paragraph lists must not become article heads. Do not replay the failed operation or edit its response, run state, accounting or paid identity.

- Isolated authoring: W, `codex/reviewed-regions`, approved base `4780a2e16c32adf7af7479656f0b5fafdff83224`; harvest exact current R beforeimages and independently reviewed changes.
- Files: `src/legalpdf_translate/new_translation_blocks.py` and a focused synthetic citation test file. Bump the citation policy identity so retained historical requests are not presented as current-policy calls.
- Tests: show the old false positive with fictional positive cases; retain negative cases for missing/changed heads, equal-number article/reference swaps, paragraph lists and prose boundaries. Run affected structured suites and privately revalidate the retained actual response offline. Then run current required broader validation before claiming final candidate acceptance.
- Reporting clarification: root changed only `run_report.py` wording, keeping legacy payloads/counters intact. Attempts can stop before sending; authoritative provider accounting reports actual dispatches/cost. The existing 14 report tests passed (`10a29c`), and independent review accepted exact source `24feb3c6`. Original report beforeimage remains private.
- Further real execution is a distinct reviewed scope only after this fix is validated. No automatic correction, replay, source/target edits, budget reset, default promotion or publication follows from the failure.

## Historical backend preparation

The prospective inventory at private `application_ordinary_integration_20260916_01/prospective_harvest.json` (SHA256 `022355b6ddbf0c9b27c8e7c934dafd1c04facff48eae2400768bd60cd4556f58`) selects exactly27 production, test and plan files and preserves9 existing integration beforeimages. It explicitly selects the final source-submission freshness correction and pristine-claim correction over their earlier authoring versions. It does not select older writer/validator dependencies or unfinished UI files.

Source review acquires a genuine retained local baseline, collects typed explicit decisions and publishes an immutable accepted revision. Source identity and generation are rechecked during publication and before Workflow dispatch. Browser ownership binds mode, workspace, upload and exact saved configuration. A stable reviewed-start nonce recovers a lost response without dispatching a second translation. Unknown outcomes remain visible and require an explicit recovery action.

## Work sequence

1. Finish the actual corrected Arabic formatter/native operation before changing its pinned application code. Integrate the29 selected files only if their source and destination hashes still match the prospective inventory.
2. Run targeted synthetic source, bridge, API, formatting and existing workflow tests with the canonical `.venv311` interpreter and offline test boundary. Fix real failures without weakening credential/provider isolation, publication checks or safe rendering.
3. Integrate the separately reviewed source browser controls. Keep ordinary Translate available. The optional review panel displays full source pages, baseline evidence, explicit retain/replace/omit/transcribe decisions, reading order, boundaries and findings. It requires explicit page and whole-source acceptance and never inserts dynamic text as HTML. No operator enters paths, hashes or raw JSON.
4. Complete ordinary formatting acquisition and its browser path, including independent source/target ranges, editable tables, explicit source-supported gutters and an explicit page-matched derivative choice. Preserve saved `page_breaks=False`; do not silently promote settings, model or protocol. Register the resulting DOCX as a distinct reviewed artifact.
5. Exercise the actual isolated shadow browser flow with synthetic documents and provider responses. Verify stale settings/source handling, lost responses, safe Unicode rendering, exact revision selection, artifact download and invalidation after target edits. Do not operate live Gmail or the canonical live UI.
6. Run the required full regression and documentation/hygiene checks after the integrated changes. Record real-document acceptance separately from synthetic runtime tests. Prepare a concrete release/PR review state only after these remaining steps pass.

## Current status

Backend inventory and beforeimages prepared; no files harvested yet. Bounded independent backend reviews are clear, with runtime validation pending. Source UI authoring is in progress in the separate worktree. Ordinary formatting service exists but still needs its public browser path and explicit derivative/gutter options. No ordinary-use or release-readiness claim is made.

The explicit derivative/gutter service extension is now authored and independently reviewed. Final options review581d51d6 records and closes a persisted boolean/integer comparison gap with canonical JSON checks and six regressions;35 options cases are authored in total. Existing service tests remain unchanged. Private final_backend_harvest.json SHA88bb592a99ff39b9112d539051af19e43c7a91eee15f5acbae8c75a2361e164f supersedes the prospective inventory with exactly29 selected files, including final service5300c75b and new options tests9c2e5333. All source/destination hashes still matched at publication; no R code was changed. Actual F07 Arabic build passed; its separate N02 export remains the final code-freeze prerequisite for this harvest.


### Backend integration and first runtime validation

N02 actual export and ownership cleanup completed before any package mutation. Exact29-file harvest passed every W source/R beforeimage check; result64e1e00d24af29b9c1b31e056960a57129c1ca7952104df42360ae0c13ac1429. Runner beforeimage5f0432a2 is retained; the only runner changes add eight backend/API test files to a dedicated ordinary_source suite and all. Isolated141 is running. Source UI first static review found an unassociated restored-nonce trust gap and order-sensitive persisted decision comparisons; authoring corrections and regression cases are pending. UI has not been harvested.
