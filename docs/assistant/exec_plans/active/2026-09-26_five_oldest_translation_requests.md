# Five oldest unanswered translation requests

## Goal and non-goals

Find the five oldest genuinely unfinished Gmail translation requests received on or after 1 July 2026. Operate the canonical app sequentially as the user would, review each complete workflow and generated documents, and stop at verified unsent reply drafts. Never send email. Preserve existing work, saved defaults, historical evidence and the original USD10 ceiling.

## Scope

Included: bounded read-only candidate/reply/draft and saved-output reconciliation; fresh canonical Gmail intake through supported app UI; normal translation, review, download, case saving and batch finalization; independent per-request observations and scoped documentation. Demonstrated local defects received isolated fixes and no-provider tests. On 2026-09-26 the user explicitly approved publication/canonical application of the reviewed fixes and continuation of all remaining selected translations without routine reconsent. That authority supersedes earlier approval-pending/no-publication text in this plan's historical progress. Excluded: pre-July mailbox search, replay of consumed operations, cap increases, historical hold clearing, default promotion, public hosting and email sending.

## Worktree provenance

- Isolated authoring checkout: `C:/Users/FA507/.codex/worktrees/word-startup-readiness/legalpdf_translate` (existing clean reusable checkout).
- Branch: `codex/five-oldest-translations-20260926`.
- Base/integration branch: canonical `main`.
- Implementation baseline SHA: `2a6eb5962820b3c0f015dc6e296516ff2dac8049`, applied PR307; not a permanent current HEAD.
- Authorized publication/application is in progress. Exact PR/check/merge/application state belongs in `C:/Users/FA507/.codex/legalpdf_translate_private_benchmarks/five_oldest_requests_20260926_01/publication_01/publication_receipt.json` and the actual repository state; no future SHA is assumed.
- Actual live app must serve the verified applied clean canonical main source from `C:/Users/FA507/.codex/legalpdf_translate`, live ports 8877/8765. Record server PID, source identity and served asset version before intake. Preserve predecessor source identity separately. The isolated branch is not a live app replacement.

## Interfaces and contracts

No product contract changes planned. Use the existing `TranslationJobManager(accounting_factory, accounting_policy)` seam for a fresh caller-owned bounded ledger; ordinary form submission does not enforce a cost cap. Any private launch support must preserve genuine extension/UI behavior and be independently reviewed before launch. Never execute historical helpers.

## File-by-file steps

1. This plan: maintain decisions and per-request completion state.
2. Fresh private evidence under `legalpdf_translate_private_benchmarks/five_oldest_requests_20260926_01/`: minimal candidate aliases and private joins, selected-message/source identity, immutable beforeimages/hashes, fresh accounting, per-request outputs and review/draft receipts. Do not dump live mail or settings into terminal output.
3. Fresh private operation support, only if needed: bind canonical source, existing saved credentials/defaults and shared durable accounting; enforce one active job and operation consumption; no sends or automatic retries. Validate guards without provider/native/Gmail side effects.
4. Any product correction: identify concrete evidence first, scope a separate change in this worktree, run targeted tests and required validation. Do not alter canonical main in place.
5. Touched-scope handoff/session/validation docs: useful scoped sync with exact beforeimages; preserve historical qualifications.

## Tests and acceptance

- Candidate cutoff is 1 July 2026 in Europe/Lisbon. Inspect complete threads, sent replies, drafts and saved jobs, not unread state alone. No duplicate paid work.
- Reconcile authoritative budget receipts before allocating a fresh additive allowance. Initial USD2.71986735 headroom retained the entire old completion allowance; terminal retirement reconciliation below establishes USD5.69008760 actual remaining lifetime headroom, of which this task bounds USD5.69. Keep all 226 historical rows and FRlong USD0.119 hold/block/five-page partial unchanged.
- No-provider tests of new private accounting/binding guards before dispatch. Explicit per-run settings, one worker and no saved-default write.
- For each request: actual supported exact-message intake, correct attachments/languages/page range; all selected attachments translated; content/numbers/layout manually reviewed against source; ordinary download verified; save/confirm rows joined without duplicates; fee output reviewed; draft correct recipient/thread/attachments and DRAFT present/SENT absent.
- Record elapsed time, costs, app issues and disposition after each full workflow before beginning the next. On uncertainty, inspect same-operation evidence; do not blindly retry.

## Rollout and fallback

Publication and canonical application of the frozen reviewed fixes are explicitly authorized. Complete and verify that lifecycle before pinning fresh successor support to the actual applied source and continuing the remaining selected requests. Keep owning runtime alive through downloads and draft verification. Stop paid work on accounting uncertainty, cap exhaustion or material document issue. Preserve partial files and reservations. Leave unrelated Word/recovery windows untouched; stop only operation-owned processes through normal controls.

## Risks and mitigations

Older work may exist outside the request thread: correlate app records and sent attachments. Word readiness may occur at bootstrap and some GET responses: journal ownership and native outcomes. A fresh folder does not serialize app jobs: verify no active jobs across owned workspaces. Translation and draft creation may fail after durable local saving: reconcile state before another click.

## Initial assumptions and state (historical)

The user's current explicit five-request instruction authorizes this operational scope and supersedes historical no-new-operation scope, without increasing the original ceiling. July floor added explicitly. No new provider/native/Gmail write has occurred at plan creation. July candidate search returned 27 distinct threads; several already have sent replies and unrelated attachments. Final five selection is still being reconciled.

## Reconciliation progress

- Selected pending request dates: 20, 23 and 29 July; 8 and 14 September. Case searches found no matching sent/draft messages. Two earlier July candidates were excluded after matching translation attachments in separately sent August conversations. Other earlier messages concerned paid prior work or interpreter appointments.
- Bounded local duplicate check: all 65 current job-log rows plus 19 readable source-fingerprinted checkpoints reached from 54 row-owned output routes; no case/source match for the selected five. This is not an exhaustive disk scan.
- User clarified that translator-directed cover pages/instructions are omitted. Explicit document/email languages take precedence; country-to-language mapping is user-specified when the country is stated. Do not infer nationality from an ambiguous name. Selected page ranges are 1, 2-7, 2-17, 2-8 and 1-5 respectively.
- Request 4 has an explicit Spanish instruction in the order. The user explicitly confirmed that this is an error and requested Arabic. Preserve that clarification and the original source; do not edit its historical wording. App currently exposes only EN/FR/AR.
- Authoritative terminal receipts establish retirement of previous consumed phases. Frozen old ledgers total USD4.19091240 known plus the preserved USD0.119 hold. A fresh USD5.69 allowance gives maximum lifetime exposure USD9.99991240. This uses terminal actual charges and preserves old unused allowance history; it does not enlarge the USD10 ceiling or reopen old operations.
- Configured Gmail helper returned one connected account, matching connector and visible browser account. No extra permission test draft was created. Ports 8877/8765 were free before setup.
- Fresh private runtime support is being reviewed/tested without providers. No server, budget ledger, translation job or draft has yet been created for this scope.
- Current computer control exposes browser tabs but not the Edge extension toolbar/native desktop. Use the app's existing `Advanced message details > Load this Gmail message` UI for a real Gmail fetch. This is ordinary app UI operation, not direct API substitution or a simulated bridge. Record manual-message UI intake honestly; this task does not claim renewed extension-button acceptance.

## Live progress and demonstrated correction scope

- Runtime `five_requests_live_01` started from clean canonical main with reviewed immutable support/config; 30 no-provider author tests passed and the independent accounting review passed. The prior setup-only statement above is historical. Runtime owns ports 8877/8765 and remains active through draft verification.
- Request 1 translated one selected page successfully for USD0.08543325, with finalized usage and no new hold. One Arabic clause incorrectly suggested attendance instead of submitting proof to the case file. After the user closed Word, a single OOXML text-node correction was applied with the original retained; native Word rendering and independent review passed. Ordinary browser download matched the corrected file.
- Normal UI save and Gmail confirmation used one new row (92), preserving all 65 existing rows. App finalization created one unsent draft in the original conversation; recipient/thread and both attachment bytes were verified read-only.
- Independent fee review then found a reproducible stale-count defect: the corrected DOCX has 227 words, while the completion seed, saved row and fee request retained 228. Root corrected existing row92 through normal UI to 227 words / EUR20.43 expected / EUR20.34 profit. Original fee artifacts and draft evidence are preserved. A separately named corrected fee document and replacement of only that attachment in the existing draft are in progress. Do not run finalization twice or claim the historical session count was updated.
- Before any next dispatch, complete the request1 review and unsent-draft receipt. For subsequent edited outputs, recount the durable DOCX and reconcile visible metrics before the first case save/Gmail confirmation.
- Demonstrated local fix scope: refresh completion word count and dependent calculated amounts after an actual reviewed DOCX change; ensure the browser save form receives the fresh values before saving or Gmail confirmation. Preserve explicit user-entered amounts, existing records, saved defaults and contracts. No Gmail draft recreation or retroactive session mutation belongs in this fix. Minimal affected files and regression tests will be recorded after diagnosis, before implementation. Code remains isolated and publication is not authorized by this task.

## Edited DOCX count correction implementation

- `src/legalpdf_translate/translation_service.py`: count the durable completed DOCX locally, refresh only count-derived seed metrics, and reconcile unchanged calculated amounts when saving a job-bound record. Historical record editing remains independent of artifact availability.
- `src/legalpdf_translate/shadow_web/app.py`: refresh the existing completed-job response and bind Save to the owned job's durable artifact, preserving existing routes and payload shapes.
- `src/legalpdf_translate/gmail_browser_service.py`: count the actual staged attachment before confirming it, preserving row reuse and captured session history.
- `src/legalpdf_translate/shadow_web/static/translation.js` and `gmail.js`: refresh the visible count and calculated amounts after review and before Save/Confirm, preserving case fields, explicit financial edits and the current row ID. Use the existing job status route.
- `tests/test_translation_completion_metrics.py` (new), `tests/test_shadow_web_api.py`, `tests/test_gmail_browser_service.py`, and `tests/test_translation_browser_state.py`: valid edited DOCX regressions, stale submission protection, direct confirm and saved-row reuse, invalid/missing artifact failure, manual amount preservation, and browser form refresh without case/row reset. No provider/native/Gmail calls.
- Validate focused tests, standard `scripts/validate_dev.ps1`, and `scripts/validate_dev.ps1 -Full` within this isolated checkout. Record results here; canonical main, the active live server, private immutable runtime support, historical rows/drafts and publication remain untouched.

### Isolated correction validation

- Implemented authoritative recount at job-bound Save and exact staged-DOCX recount at Gmail Confirm. The existing completed-job GET refreshes metrics when readable; a missing/corrupt artifact preserves job details and original diagnostics with a visible count warning. Actual Save/Confirm still requires a successful fresh read. Historical no-job row editing remains manual.
- Browser refresh preserves case/date fields, current row ID and differing manual amounts; calculated amounts follow the new count/current rate/cost. An asynchronous refresh cannot apply after switching jobs/rows. No route, submitted payload shape, Gmail draft operation or saved-default change was introduced.
- Focused original regression selection passed 16 cases. Final completion/Gmail/browser/Arabic-review suites passed 57 cases in 2.18 seconds; five affected API/browser cases passed in 21.95 seconds. Tests use fictional local DOCX files and service/DOM stubs, including changed-original-after-staging, direct Confirm, row reuse, forged client paths, missing/corrupt artifacts and manual amount preservation. Counts overlap and are not additive unique coverage.
- Independent read-only source review found no blocker. Its initial observation that unreadable DOCX blocked job-details GET was corrected and regression-tested before final Full.
- Initial standard validation passed 243 tests in 154.57 seconds and compilation, then failed docs validation with AD046 because SESSION_RESUME still named a deleted completed branch. The expected Dart AOT exit255 and successful direct-validator launch are retained as an initial failed wrapper result. Root preserved exact docs beforeimages and corrected current routing; final Full is running against the finished product source and corrected docs. No integration/publication or claim of live-app readiness follows from isolated tests.

## Fresh successor preparation for unstarted requests 3–5

- Root authorized preparation only of `runtime_support_v2/` in the same private evidence root; immutable `runtime_support/` and operation `five_requests_live_01` remain unchanged. No launch, retirement, provider/native/Gmail call, paid retry or cap increase is authorized by this preparation step.
- `runtime_support_v2/bounded_ui.py`: copy reviewed v1 source and adapt only scope, predecessor retirement/pins, and allowance. Allow aliases request-03/04/05 only, new operation `five_requests_live_02`, fresh `outputs/successor-02/request-NN` directories created only by serve. Bind selection/settings/account/choices to the immutable predecessor config; require original reviews 1/2, final ledger, normal runtime result and independent terminal receipt before arming. Recheck their pins before every dispatch. Require predecessor stopped with no active jobs/owned processes and original historical pins unchanged.
- `runtime_support_v2/policy.json` and `policy_provenance.json`: preserve existing pricing/input400000/image8000 bounds, set provider-enforced output32000. Conservative reservation is USD1.148. Observed predecessor known spend USD0.81343395 leaves USD4.87656605 from the existing USD5.69 allocation; verify the exact final ledger at retirement. Combined lifetime maximum remains USD9.99991240, including the preserved USD0.119 hold. A truncated response must stop and preserve billed evidence, without automatic paid retry.
- `runtime_support_v2/prepare_config.py`, `config.unarmed.json`, and `README.md`: prepare reviewable execute_authorized:false support, with explicit missing final predecessor pins until root supplies terminal evidence. Same canonical main SHA/model/high effort/default preservation; no prior job replay. Arming requires a separate complete immutable config after root verifies request2's reviewed unsent draft and owned retirement.
- `runtime_support_v2/test_bounded_ui.py` plus focused successor tests: no-provider legacy bindings/accounting regression coverage; old-alias/source/output/settings rejection; predecessor active/incomplete/changed-pin failures; final-cap arithmetic; outgoing real accounted SDK seam enforces32000 and rejects overbound inputs before provider dispatch. Run independent source/test review before root can arm. The product count-fix Full validation continues separately in the isolated checkout.

## Demonstrated source-order and decimal-token corrections (before further paid work)

- Requests1–2 are complete with verified unsent drafts. Request2's reviewed output corrections exposed two reproducible local causes: a generic `p.` footer anchor misclassified a body statutory-citation line and moved it after the actual footer; standalone-number regex backtracking split decimal amounts before `/mês` or sentence punctuation, allowing separate numeric spans whose comma rendered RTL.
- `src/legalpdf_translate/pdf_text_order.py`: narrow footer recognition so legal citation/body lines remain in geometric reading order while genuine page markers/contact footers remain classified correctly. Use synthetic geometry and the bounded private source reproduction; no private document content enters repository tests.
- `src/legalpdf_translate/arabic_pre_tokenize.py`: protect the full decimal amount at punctuation/unit boundaries without consuming surrounding words or weakening identifier exclusions. Add synthetic regressions for comma/dot decimals, slash units, terminal punctuation and existing identity/reference forms. Avoid broad prompt/model changes.
- Relevant existing text-order/tokenizer tests and a minimal DOCX rendering assertion if needed: prove complete decimal protection and citation/body ordering, preserve existing protections, then run the appropriate focused checks and final combined Full once these changes are stable. The preceding count-fix Full is preserved as evidence for its earlier scope, not relabeled as validating new source changes.
- These product edits remain in the isolated checkout. Do not start request3 with the known faulty preprocessing. Finish and review the concrete combined fix before requesting the separately required publication/application approval. The fresh successor remains unarmed until actual approved/applied source identity and all predecessor pins are verified. Existing drafts, old source/checkpoints, v1 helper/config/ledger, saved defaults and original cap remain unchanged.
- Final isolated product Full completed with exit 0: 245 browser/API, 2 Gmail-state, 5 targeted intake, 239 source-review and both formatting shards passed, with compilation. Docs and workspace hygiene passed using the documented direct-Dart fallback after the wrapper AOT issue. Formatting receipts are under `tmp/validation/formatting-900b192cc2824d89a67c2054d5387e94/`; the earlier interrupted Full is not a completed result. The final 18 browser-state tests also passed. Integration/publication remains separate.
- Successor preparation exposed a real canonical legacy-client limitation: accounting injects an output cap, but legacy response extraction does not reject nonempty incomplete output. The private launcher will bind only its own process's `translation_service.OpenAIResponsesClient` constructor to a subclass supplying the supported `max_output_tokens=32000` argument, activating existing checked response handling for initial and correction calls. Assert that binding at admission/dispatch, reject reviewed-source contexts that bypass it, restore the binding on exit, and test clone behavior, both purposes, incomplete response/message handling and no subsequent dispatch. No canonical source or protocol change is included.
- Successor author preparation is now complete and remains UNARMED: 60 no-provider tests passed in 1.71 seconds (`runtime_support_v2/tests_04.xml`), including the actual two-page workflow with a fake SDK for image-first translation, correction and explicit incomplete response/message cases. No partial page text or later-page dispatch was produced; authoritative actual usage was finalized. Root's actual terminal receipt is pinned, all predecessor pins are present, and `config.unarmed.json` has `execute_authorized:false`. No new operation/output directory exists. Private launcher SHA `bc3e6c73657a592cb5bd501120b950767ca4bf64d64745c7540c62e1ea5fd10a`; unarmed config SHA `cc29bc45fff29f15828621dba946818f7a3f1061b49b7ca8ca83d1dc58da41c7`. Independent review is pending.
- Root has separately identified demonstrated source-order/amount-token defects and is preparing bounded product fixes before request3. The completed word-count Full remains valid historical evidence for its frozen source (664 selected test executions, not all repository tests); combined product changes need a new Full. The successor old-head draft must stay unarmed until those fixes are tested, publication is explicitly approved, actual canonical main is updated, and successor source identity is explicitly revised/re-reviewed without changing predecessor history or allowance. This preparation grants no publication or launch authority.

## Combined correction review and validation complete

- The two source corrections are implemented and frozen with the count correction. Currency-context spans preserve complete comma/dot decimals before units/punctuation and take precedence over greedy case-reference matching only for explicit currency amounts; generic identifier boundaries remain unchanged. Bare `p.` now identifies footer furniture only as a complete page-counter line. Focused source/tokenizer/normalization tests passed 55 cases in 0.70 seconds. A bounded read-only private-source reproduction confirmed citation order and all three complete monetary values without changing the source. Exact four-file hashes and earlier repaired test failures are preserved in private `source_fix_validation_01/author_freeze_01.json` (SHA `ae1caeee4fe9fa85fcd81b2fb952d1eda3d581ac8b90fe00221e57844fae1243`). Independent final source and scoped-docs review found no blocker.
- Final **combined** `scripts/validate_dev.ps1 -Full` passed with exit 0 in 812.809 seconds on 2026-09-26. Selected coverage: 245 browser/API, 2 Gmail-state, 5 intake (9 deselected), 239 source-review and 173 formatting tests, totaling 664 test executions; this is not complete repository pytest. Compilation passed. Formatting workers passed 78/95 tests in 513.48/490.58 seconds. Docs and workspace hygiene passed via the documented direct-Dart fallback after the wrapper AOT exit255.
- Durable combined receipt: private `five_oldest_requests_20260926_01/validation_combined_01/{intent.json,wrapper.log,result.json}`. Result SHA `058fe801245aa6d4804b53d7d8a6c77c1c79d7c3d95b7cfd1e4c88a2df11d6a1`; wrapper SHA `26b7f0a0e390acd11b402b724a1098222d74b85707a188c9c05ab9c63dffbd81`. The receipt confirms all frozen source, tests and docs hashes were unchanged throughout the run. Raw formatting evidence is under this checkout's `tmp/validation/formatting-13c1eb54d18549589aa55b43975e404c/`, including result, collection and both workers' stdout/JUnit/execution receipts. Earlier count-only and interrupted results remain separate.
- This completion paragraph is a post-run docs-only append. Its exact prior plan is retained at private `validation_combined_01/plan.before_result_append.md`; final docs/hygiene/whitespace checks and a separate docs-status receipt follow root's status-only updates. No product edits or broad test repeat accompany this append.
- V2 independent review completed with no blocker for the current **unarmed old-head draft**; 60 no-provider tests passed in 1.88 seconds (`runtime_support_v2/review_tests_01.xml`). `independent_review_receipt_01.json` SHA `757e0ae14b04811619ebeaf8cf460f49ca5d3d3549947818a3a5d14659538f3f` pins the reviewed files and all nine predecessor artifacts. No successor operation/output exists. After separately approved publication and actual canonical application, prepare fresh successor artifacts that preserve immutable predecessor HEAD `2a6eb5962820b3c0f015dc6e296516ff2dac8049` separately from the new successor HEAD, then test/re-review those actual-head bindings before arming.
- The five-request parent plan remains active: two reviewed unsent drafts are complete, three requests remain unstarted. The combined product fix is locally tested and reviewed with frozen product hashes. The user subsequently approved publication/application and continuation, as recorded below; exact lifecycle status belongs in the publication receipt and actual repository state. No email send, paid replay or allowance increase is authorized.

## Publication and remaining-request continuation authorized

- On 2026-09-26 the user explicitly approved publishing/applying the concrete reviewed combined fix and continuing all remaining selected translations without routine reconsent. This supersedes the earlier approval-pending/no-publication wording, while retaining all source, accounting, manual-review and no-send safeguards. Requests3–5 remain the only unstarted work; this parent plan stays active.
- Publication/application is in progress under root ownership. Read private `publication_01/publication_receipt.json` and actual repository state for exact commits, CI, merge and applied HEAD; `2a6eb5962820b3c0f015dc6e296516ff2dac8049` remains the implementation/predecessor baseline. Do not infer a future revision or replay completed lifecycle steps.
- Product source/test hashes remain those frozen for the passing combined Full. Only current status docs are updated for this authorization; exact six-file beforeimages are retained under private `publication_01/beforeimages/`. One final direct docs/hygiene/whitespace pass checks these status edits; product tests are not repeated.
- After verified canonical application, preserve the old-head unarmed artifacts and prepare fresh reviewed successor support with immutable predecessor HEAD separated from the actual successor HEAD. Continue requests3–5 sequentially under the remaining USD4.87656605 allowance, preserving the original USD10 ceiling, old USD0.119 hold/block/partial, existing drafts and every previous ledger. No sends or duplicate paid work.
