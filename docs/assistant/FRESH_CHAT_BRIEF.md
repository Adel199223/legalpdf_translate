# Fresh-chat brief — prepared 2026-09-10

Public-copy note: `Álvaro Exemplo` is an explicitly fictional replacement for a personal-name example. Exact original text is retained in private beforeimage evidence; historical execution outcomes and legal artifacts are unchanged.


## Current continuation — offline diagnostics complete; AR4 cause unresolved

Fresh `NEXT_STAGE_2` completed ONLY the offline guard-diagnostic prerequisite.
Read private `structured_activation_guard_diagnostic_stage2_2026_09_11`:
`assessment.md`, `findings.json`, `independent_review.json`, `validation.json`,
`closeout.json` and `next_stage3_scope.json`. Final fixture snapshot: build_03.
The original consumed AR4 packet and all frozen caller bytes remain unchanged.

The recorder now retains the first allowlisted code/phase/event without exception
text, paths, credentials or document content, with exclusive no-overwrite/no-retry
persistence. All20 stdlib tests passed. Five probes passed their intended checks:
module-origin snapshot, local TLS construction, Boundary.deny, direct need and
network refusal. One deliberate socket-constructor probe was blocked BEFORE
creation; no provider/socket connection occurred. No actual key inspection or
resolution, original ledger constructor/write, source/request rebuild or re-review,
Word/Gmail/native operation, product fix or permission broadening occurred.

All112 code and65 config members were already bound. The tested module/TLS
hypotheses did not reproduce the original rejection. Import timing/full-case
preflight were not identical; specific original cause remains UNCONFIRMED.
The new recorder is test-only, not installed into the frozen paid caller.
Build_01 was unexecuted; build_02 had a new test-launcher preflight defect fixed
in build_03. That unrelated defect is not attributed to the original AR4 failure.

Original USD10 lifetime ledger:149 finalized, USD2.36852785 settled, zero held,
USD7.63147215 remaining. Preserve every consumed launcher/operation/case intent,
canonical13 dirty docs, runtime112/candidate60, original Word journal and saved
legal/formatting preferences. Keep existing key, GPT-5.2/high and ordinary
legacy_text_v1. Codex does AI testing review; user reviews ordinary future work;
no human certification or new external English-reference requirement.

Next `NEXT_STAGE_3` is conditional evidence review ONLY: assess a precise,
separately scoped offline preflight design and stop before new execution while
cause/authority remain unresolved. No paid retry, recovered authority, frozen
file edit, guard exemption, activation, Stage4 or publication is authorized.
Real AR/FR/EN fidelity/coverage, exact-DOCX every-page layout, measured all-call
cost/user acceptance and distinct >=20% efficiency remain open.

## Historical continuation — authorized AR4 stopped; offline diagnostic next

Fresh `NEXT_STAGE_3` plus exact `APPROVE_PAID_AR4` digest 473b8f9b...
authorized one AR 1/5/6/9 launch, not a retry. The exact five templates were
materialized and independently checked; unchanged launcher ran once with
-NoProfile. Read the private v3 packet's
`paid_execution_ar4_20260911_01/assessment.md`, `launch_result.json`,
`independent_accounting_review.json`, `closeout.json` and `next_stage2_scope.json`.
The frozen prior proposal/closeout are historical, not current replay authority.

The child stopped in 9.4597205s with exit 2, BootstrapDenied/pilot_exception.
Exception closeout keeps completed_pages=null; no page/response artifact exists.
The initialized dispatch journal has zero events. The original USD10 ledger
is byte-identical: 149 finalized, USD2.36852785 settled, zero held,
USD7.63147215 remaining.
No new reservation, settlement or dispatch event was recorded; external billing
was not queried. Existing key was resolved only by the approved executor inside
durable intent; no replacement or separate auth test. No automatic retry occurred.

Launcher/operation/case-phase intents remain consumed. Never repeat the argv,
reset markers or remint directories to replay this approval. The specific
guard-denial code/phase was not retained, so the cause remains unconfirmed.
Next is ONLY `NEXT_STAGE_2`: a new private offline sanitized-diagnostics fixture,
fake key, disposable budget and hard-disabled transport; no real main, original
key/ledger constructor, source/request rebuild or re-review. Preserve frozen
code; diagnose before proposing a minimal fix. Any paid recovery requires a
separate reviewed recovery design and fresh digest-specific user approval.

Canonical 13 dirty docs, runtime 112/candidate 60, source/preferences, old private
artifacts and Word journal remain unchanged. Only six feature handoff/reference
docs are synchronized. Keep GPT-5.2/high, ordinary legacy_text_v1, formatting and
legal preferences, original lifetime budget and all consumed intents. Codex does
AI test review; user reviews future ordinary work; no human-certification claim
or new external English-reference requirement. All-language legal fidelity,
exact-DOCX every-page layout, measured all-call cost/user acceptance and >=20%
efficiency remain open. No Word/Gmail, activation, Stage4 or publication authority.

## Historical continuation — all13 offline requests frozen; paid pilot then unapproved

Fresh `NEXT_STAGE_3` completed the single authorized NON-DISPATCH preparation
in private `structured_activation_request_preparation_v3_2026_09_11`.
Read its `assessment.md`, `private_attempt_result.json`, `validation.json`,
`closeout.json` and `proposals/paid_ar4_scope.json` first. Older packets are
historical, not authority to repeat completed operations.

All13 current-candidate requests are saved: AR9/FR2/EN2. The real preparation
completed once in 101.364s with zero provider/credential/auth/ledger/OCR/render/
Word/Gmail operations and zero guard denials. Every final request hash and its
GPT-5.2/high/default-tier/store=False/24000-output controls were verified.
Accepted source decisions remain unchanged; effective preferences equal the
previous preserved preferences. The earlier partial AR9 remains untouched and
is not relabelled as this new preparation identity.

Before intent: ten copied execution/helper/test files remained byte-identical;
only two private bookkeeping scripts were rebound. All111 fictional tests,
cold actual-SDK/mock4, fictional launcher13, deadline/reuse and guard checks
passed. Independent review and a3061-file executable/dependency/interpreter/CA
freeze preceded intent. Runtime112/candidate60 were not edited in this stage.
Prior Stage2 full3934 and Full evidence remain valid; do not rerun completed
paid/Word/Gmail operations or alter frozen executables to bypass a failure.

The original USD10 lifetime ledger remains149 finalized rows:
2.36852785 settled / zero held / 7.63147215 remaining. The proposed first paid
operation is ONLY AR pages1/5/6/9, one primary request each, zero retries or
corrections, maximum USD4.144. This is a conservative reservation ceiling,
not a measured bill. All13 capacity USD13.468 is not guaranteed affordable.
Current Stage3 supplied NO paid authority; the exact separate proposal requires
fresh `NEXT_STAGE_3` AND digest-specific `APPROVE_PAID_AR4 <operation SHA256>`.
Do not materialize approval-ready templates or touch the original ledger/key
before both approvals. Keep the existing key; no replacement or test is needed.

Canonical main and its13 dirty handoff docs remain protected. Model/defaults,
ordinary `legacy_text_v1`, formatting/translation preferences and every consumed
intent are unchanged. Codex performs AI test review; the user reviews future
ordinary output, without human-certification claims. English keeps the agreed
source-versus-output review; no external corrected reference is required.
Real all-language legal fidelity, exact-DOCX every-page layout, measured
all-call cost/user acceptance and >=20% efficiency remain open. Ordinary
activation, Stage4, publication and later paid/native phases are not authorized.

## Historical continuation — header-precedence fix implemented offline

Fresh `NEXT_STAGE_2` implemented the approved two-file glossary prerequisite.
Read private `structured_activation_header_precedence_stage2_2026_09_11/assessment.md`,
`implementation_review.md`, `validation.json`, `closeout.json` and
`next_stage3_scope.json` first. Older continuation packets remain historical.

The six-line change makes generated headers yield only to eligible configured
entries for the exact same source phrase. Configured wording/case/tier/mode,
strict configured conflicts and invalid-entry rejection remain intact.
Citation aliases, exact source matching, non-PT behavior, limits and Arabic
literal/fingerprint checks are unchanged. The prior deliberate header-conflict
test expectation was explicitly revised. There are 28 added cases plus that
changed case: parent RED reproduced 11 failures, then all 212 glossary and
488 focused regressions passed. Independent stage-only review found no issues.
Full pytest passed 3,934 tests in 568.80s on a bounded rerun; the initial process
exited without a pytest summary and is retained with its hang-diagnostic record.
Two nearby tests passed in isolation; no tests were skipped or checks weakened.
Serial Full passed: 233 browser/state, 2 review and 5 intake tests, compilation,
docs and hygiene. Known Dart AOT wrapper failures had successful direct-Dart
fallbacks. See validation.json for exact timings, limitations and final checks.

Only two product/test files and five feature handoff docs change this Stage2.
Cumulative candidate60/runtime112 membership is unchanged. Canonical main and
its13 dirty handoff docs, the original ledger/Word journal, accepted sources,
old private caller/scripts/manifests and consumed partial AR9 remain protected.
No real preparation, source adaptation/re-review, provider/auth/token-count,
credential resolution, original-ledger constructor/write, OCR/render/Word/Gmail/
live operation or publication ran. Existing key, GPT-5.2/high, ordinary
`legacy_text_v1`, translation/formatting preferences and original USD10 cap
remain unchanged: 2.36852785 settled / zero held / 7.63147215 remaining.

Stop at fresh **`NEXT_STAGE_3`** only after the completed closeout. Its proposed
scope is offline: NEW private root, copy/rebind and fictionally revalidate the
reviewed engine, independent executable freeze BEFORE one new non-dispatch
AR9/FR2/EN2 preparation attempt. The historical manifest still pins old product
bytes; never modify it, replay its intent or relabel old AR requests. Keep exact
accepted sources and every failure. Any unexpected defect stops without retry.
No paid/native authority follows from the stage token; only successful complete
preparation can lead to a separate exact AR1/5/6/9 paid proposal.

Codex AI testing review and future user review remain assigned without human
certification. English retains the agreed source-versus-output review route;
no external corrected English reference is required or claimed. Actual
all-language legal fidelity,
exact-DOCX every-page layout, complete all-call cost/user acceptance and the
distinct >=20% efficiency target remain open; ordinary activation/Stage4 are closed.

## Historical continuation — one-shot preparation stopped on header precedence

Fresh `NEXT_STAGE_3` consumed the single NON-DISPATCH preparation attempt.
Read private `structured_activation_request_preparation_v2_2026_09_11/assessment.md`,
`private_attempt_result.json`, `private_glossary_diagnosis.json`,
`validation.json`, `closeout.json` and `next_stage2_scope.json` first.
All older packets below are historical; preserve every consumed intent.

The caller/preparer/launcher were fictionally tested and frozen BEFORE private
intent: 111 private tests passed, the final 20-test timing subset passed, actual
SDK cold mock integration passed four exact requests, and the final fictional
launcher prepared all13. The one real-source attempt then saved AR9 exact
requests, but FR1 failed before glossary completion/request capture/journal;
EN was not reached. No complete 13-request bundle exists. Never retry this
intent or change its frozen executable bytes; retain partial AR as historical
evidence, not as requests approved under a later candidate identity.

Pure read-only diagnosis and a fictional example identify a configured-versus-
generated header conflict for `audiência de julgamento`: generated FR/EN target
capitalization differs from configured wording; AR agrees. This is a product
prerequisite, not an API-key/auth/model or already-fixed citation-alias problem.
Stop at fresh **`NEXT_STAGE_2`** for the proposed narrow offline precedence fix:
eligible exact-source configured entries override generated header defaults;
genuine configured conflicts/invalid entries still fail. The existing test that
expects header/config conflict failure must change explicitly under this scope.
No real preparation, paid operation or activation is authorized in Stage2.

Product candidate60/runtime112 and the 3,061-file executable freeze are unchanged.
Canonical main/13 dirty handoff docs, accepted source decisions, original ledger
and consumed Word journal remain protected. No provider/auth/token-count/OCR,
render/Word/Gmail/live, real credential or original-ledger constructor/write ran.
Existing key, GPT-5.2/high, ordinary `legacy_text_v1`, translation and formatting
preferences remain unchanged. USD10 lifetime cap: 2.36852785 settled, zero held,
7.63147215 remaining. No paid approval is requested with incomplete preparation.

Codex AI test review and future user review remain assigned, without human
certification. All-language legal fidelity, exact-DOCX all-page layout, measured
all-call cost/user acceptance and the distinct >=20% efficiency target remain
open. Stage4/ordinary activation are unavailable. Prior 3,906 product tests/Full
results are historical passing evidence for unchanged product bytes; fresh
private and final docs/protection results are separated in `validation.json`.

## Historical continuation — citation-alias prerequisite implemented offline

Fresh `NEXT_STAGE_2` implemented only the derived ordinal-alias separator fix
and fictional regressions. Read private
`structured_activation_citation_alias_stage2_2026_09_11/assessment.md`,
`implementation_review.md`, `validation.json` and `next_stage3_scope.json`
first; `closeout.json` pins the completed validation and final freeze.
All older continuation packets below are historical; preserve their consumed
intents and do not replay preparation/adaptation/source-review operations.

The five-line runtime change retains exact source spelling and numeric labels,
strict entry/literal validation, configured preferences and the shared legacy
recognizer. 124 new fictional cases were added. Focused validation passed 430
tests; full pytest passed **3,906 in 529.90s**. Serial Full validation passed:
233 browser, 2 review and 5 intake tests, compilation, docs and hygiene.
The known Dart AOT launcher failures had successful direct-Dart fallbacks.
Independent code review found no actionable issue. Initial failed fixture
expectations are preserved and corrected without changing product strictness.

The new candidate has 60 code/test/tool files and 112 runtime Python files.
Canonical main/13 dirty handoff docs, original ledger/Word journal, accepted
sources, existing key, model/defaults and formatting preferences remain unchanged.
No private request preparation, provider/auth/OCR/render/Word/Gmail/live operation,
ledger mutation, publication or ordinary activation ran in this Stage2.

Stop at fresh **`NEXT_STAGE_3`** for the bounded offline-only scope: author,
fictionally test and freeze the actual private caller/launcher FIRST, then one
new non-dispatch preparation attempt for 13 exact requests using accepted source
bytes. Recheck public pricing and freeze exact config/request/source identities.
No real credential/provider/original-ledger/native branch may run. A product
defect or post-intent executable change requires a fresh scope; never auto-retry.
Only afterward may an exact AR1/5/6/9 paid proposal be presented separately.
No paid caller is implemented/frozen now; no complete private requests exist.

Original USD10 cap remains: 2.36852785 settled, zero held, 7.63147215 remaining.
Historical USD4.144 AR4 capacity proposal stays conditional on fresh applicable
pricing/bounds; all13 affordability is not guaranteed. Codex AI test review and
future user review remain assigned. All-language legal fidelity, exact-DOCX
all-page layout, all-call cost/user acceptance and the separate >=20% efficiency
target remain open; Stage4 is unavailable.

## Historical continuation — request preparation blocked by derived citation alias

The latest `NEXT_STAGE_3` performed public pricing verification and guarded
offline request preparation only. Read private
`structured_activation_request_preparation_2026_09_11/assessment.md`,
`closeout.json`, `fictional_reproduction.json` and `next_stage2_scope.json` first.
Do not replay earlier adaptations or the new failed private preparation intent.

Fictional full-case preparation captured 13 requests; the private attempt stopped
on AR1 before capture/provider contact. Pure predicate diagnosis and a harmless
`nº 2` reproduction identified a generated-alias boundary defect: the recognizer
includes the following separator in `nº `, then strict glossary validation rejects
it. The rejected term is code-generated, not an invalid user preference.
No product fix, source edit, glossary change or real preparation retry occurred.

Stop at fresh **`NEXT_STAGE_2`** for the narrow offline alias-span repair and
regression tests. Keep strict entry/literal validation and exact source/preferences.
A later fresh Stage3 may prepare requests in a new private attempt after the fix.
The actual one-shot paid caller also needs its own reviewed executable freeze;
the preparation wrapper is not a paid executor. No paid approval is requested yet.

Fresh public pricing supports a conservative 400000 input / unchanged 24000
inclusive output reservation: USD1.036 per call, USD4.144 for AR1/5/6/9.
All13 capacity reservations total USD13.468 and are not guaranteed affordable.
Later phases remain conditional. These are proposed bounds, not actual spend
or frozen private request hashes; byte-count-plus64 is not billing proof.

Original USD7.63147215 remainder, existing key, model/defaults/preferences,
58 candidate/112 package files, canonical13 dirty docs and consumed Word journal
remain unchanged. No provider/auth/OCR/render/Word/Gmail/live/source-rebuild call.
Codex AI testing review and future user review remain assigned. All-language
fidelity, exact-DOCX layout, all-call cost/user acceptance and separate >=20%
efficiency remain open; ordinary activation/Stage4 remain unavailable.

## Historical continuation — offline sources accepted for AI translation testing

Fresh `NEXT_STAGE_3` completed the two frozen adaptations once each: AR 9 pages
(1.829s), shared FR/EN 2 pages (1.812s), 1037 bound checks before/after each.
Read private `structured_activation_reviewed_source_integration_stage3_2026_09_10/closeout.json`,
`inspection.json`, `source_decisions.json`, `assessment.md` and
`next_operation_proposals.json` before older packets.

Exact text/newlines, all action/baseline ownership, source/readiness/output hashes
and starts AR 1/3/4, FR/EN 1 passed saved-output inspection without another
adapter/build call. Codex separately accepted transcription/document boundaries
for these AI-reviewed tests using the unchanged completed reviews (11 pages,
44 resolved observations). Two proposed source-review envelopes are private
evidence, not config/paid authority. Adapter outputs still say nonproduction/
not_evaluated; geometry/layout and translation acceptance remain unclaimed.
One reviewed block per page does not prove paragraph/table/furniture layout.
Cross-page lists/sentences and literal awkward source syntax are review watchpoints.

No adapter defect was observed. Stop at fresh **`NEXT_STAGE_3`** for the exact
request/price-preparation-only proposal (hash in closeout): read-only official
pricing verification and guarded non-dispatching request/config/bounds preparation.
No paid/provider/auth-test, OCR/render, Word/Gmail, source rebuild, ledger mutation,
ordinary activation, model/default change or publication. Then ask precise paid
operation approval; Word requires later exact new DOCX hashes and separate scope.
The old adaptation intents are consumed and must never be replayed.

All 58 candidate/112 package files, canonical 13 dirty docs, existing key/defaults/
preferences, original USD7.63147215 remainder and Word journal are preserved.
Only five feature handoff docs changed. No paid/OCR/Word/Gmail operation occurred.
Parent AI review is not independent or certified human review; existing agents
remain quota-limited. User reviews future ordinary outputs. All-language quality,
exact-DOCX Word layout, all-call cost/user acceptance and separate >=20% efficiency
remain open. Stage 4 is unavailable.

## Historical continuation — acceptance-only reviewed-source integration validated

Fresh `NEXT_STAGE_2` completed the approved offline integration. Read private
`structured_activation_reviewed_source_integration_stage2_2026_09_10/closeout.json`,
`implementation_review.md`, `validation.json` and the active ExecPlan first.
The existing API key is retained unchanged; no API call or credential operation ran.

The explicit acceptance lane now verifies externally approved candidate/evidence
bytes, requires a separately authored source decision, routes reviewed text before
extraction/OCR, and retains request/accounting/resume guards. New reviewed IDs
map to original actions/blocks. Exact text/newlines are preserved; geometry,
paragraph joins, furniture and layout acceptance are not invented. Ordinary
jobs still use unchanged GPT-5.2/high and `legacy_text_v1`.

Validation: 57 new product tests; 253 focused tests, final 66-test subset and
full pytest **3,782 passed in 527.70s**. Serial Full validation passed, including
233 browser, 2 review and 5 intake tests, compilation and successful direct-Dart
fallbacks after the known wrapper AOT failure. Private continuation tests passed
24 assertions in 17 cold runner processes plus one guard process (16 actual
access denials). These counts overlap; fictional tests are not legal/layout proof.

Stop at fresh **`NEXT_STAGE_3`** for the new frozen `continuation_scope.json`
(exact hash in closeout): one cache-only adaptation per saved AR/shared FR/EN
candidate, full-case saved-output inspection and separate source-decision
preparation only if justified by unchanged completed reviews. No real adaptation
or acceptance record was produced in Stage 2. No candidate rebuild, re-review,
OCR/render, provider, Word/Gmail, live app, publication or ordinary activation.
No automatic retry; preserve every consumed historical intent. The next runner
retains `production_eligible=false` and never authors an acceptance decision.

Cumulative freeze: 58 candidate files / 112 runtime Python files. Only seven
runtime files, two tests and these five feature handoff files changed this turn;
all canonical 13 dirty docs, prior evidence, original USD7.63147215 remainder,
consumed Word journal and translation/formatting preferences are preserved.
Cached A4-assumed page frames are not measured/certified geometry. Codex is the
AI test reviewer; the user reviews future ordinary outputs. Parent-local review
is not independent/human certification; existing agents remain quota-limited.
All-language legal quality, exact-DOCX Word layout, all-call cost acceptance and
the separate >=20% efficiency target remain open. Stage 4 is unavailable.

## Historical continuation: both offline reviewed-source builds complete

Fresh `NEXT_STAGE_3` completed one AR and one shared FR/EN build from the
unchanged reviews, with no retry. Read private
`structured_activation_source_review_build_v2_2026_09_10/closeout.json`,
`inspection.json`, `assessment.md` and the active plan before older packets.

AR: 9 pages/13 variants/112 baseline blocks/35 resolved observations.
FR/EN: 2 pages/4 variants/16 blocks/9 resolved observations. All 11 records
are complete; starts remain AR 1/3/4 and shared FR/EN 1. Saved-output hashes,
manifest/review equality, exact action text/order and baseline ownership passed.
Each build made one helper call and checked 895 bound files before/after.
All 810 protected files were unchanged immediately afterward.

Candidates are still nonproduction, with source/layout acceptance not evaluated.
They are reviewed Portuguese source text, not translated outputs or Word-layout
proof. Whole-page corrections do not supply corrected word/paragraph/table
geometry. Preserve both consumed successful intents and the earlier failed intent;
never replay a build, reprepare, re-review or repeat OCR automatically.

Next prerequisite: the runtime still selects native/OCR text. Stop at fresh
**`NEXT_STAGE_2`** for an acceptance-only reviewed-source adapter/routing plus
fictional integration tests. Exact `next_stage2_scope.json` SHA256:
`e67bf3de711b2d2fff59befff32ec7ba8e2bff36d36cfdcbc290983e084a8cd4`.
The active plan lists allowed files and validation. No real source adaptation or
accepted-source generation during that Stage 2; no ordinary activation. After
its own passing freeze, fresh Stage 3 must have a new exact cache-only scope.
Paid translation and Word require separate operation-specific authorization.

All 56 candidate/111 runtime files and canonical 13 dirty handoff docs remain
unchanged. Only the five feature handoff files were synchronized. Preferences,
GPT-5.2/high, legacy_text_v1, original USD7.63147215 remainder and consumed Word
journal are preserved. No paid/OCR/render/Word/Gmail/live/publication operation.
Parent-local inspection is not independent/human certification; current agents
remain quota-limited. Full AR/FR/EN quality, rendered Word layout and measured
all-call cost acceptance remain open; Stage 4 unavailable.

## Historical continuation: private runner fixed; real builds gated

Fresh `NEXT_STAGE_2` implemented only the separately approved private runner
correction. No product/helper/test-source bytes or real review records changed.
Read `structured_activation_source_review_runner_fix_2026_09_10/closeout.json`
for the frozen executor, current protections and exact `continuation_scope.json`
hash. The original failed runner/AR intent and all 11 completed reviews remain
untouched; do not run the old runner or repeat preparation/review/OCR.

The runner preloads the missing stdlib dependency before strict guards and tests
helper loading in preflight. It rechecks runner/scope/evidence bytes before result
commit, requires complete review records, uses exclusive durable intents and a
separate output root, and has no prepare command or automatic retry.
Final synthetic validation passed 25 assertions across 21 cold Python processes,
including complete fictional 9-page/2-page cases, 17 variants, UTF-8/CRLF input,
invalid bindings/unresolved reviews/replay and 16 actual access-API denials.
These are not real source/translation/layout acceptance. Review is parent-local,
not independent; existing agents remain usage-limited.

Stop at **fresh `NEXT_STAGE_3`** for the exact frozen scope: one replacement
Arabic build and one first FR/EN build using the unchanged reviewed inputs.
Output root: `structured_activation_source_review_build_v2_2026_09_10`.
No prepare, re-review, new OCR/render, provider/Word/Gmail, production integration,
activation or publication. Each invocation has a 600-second deadline; no retry.
A valid candidate remains nonproduction with source/layout acceptance not evaluated.

All 56 candidate/111 runtime-package files, canonical 13 dirty docs, defaults/
preferences, original USD7.63147215 remainder and consumed Word journal remain
unchanged. Overall AR/FR/EN quality, Word layout and all-call cost acceptance
remain open; Stage 4 unavailable. Exact validation/preflight/protection results
are retained in the new private closeout. Older sections are historical.

## Historical continuation: full source review saved; build stopped before validation

The fresh `NEXT_STAGE_3` is consumed. One preparation completed; Codex inspected
all 11 full-page saved images and 17 retained baseline/pilot texts, then authored
11 image-bound review records with 44 resolved observations. Raw evidence,
before-text/IDs and source boundaries are preserved; no candidate has yet been
validated. The first Arabic build wrote its intent but stopped before
`build_candidate`: the private executor's strict read guard blocked the helper's
cold stdlib `__future__` import. FR/EN was not started; no automatic retry.
Read private `structured_activation_source_review_stage3_2026_09_10/closeout.json`,
`failure.json`, `ai_review_summary.md` and the active plan first.

Read-only diagnosis reproduced the failure and confirmed that preloading the
dependency permits helper import/canonical JSON while 11 access denials still
hold. Zero candidate builds/writes in those probes; no executor fix implemented.
Prior guard/preflight tests missed cold helper loading. Saved page reviews are
AI assessments, not verified candidates, accepted sources or layout/translation
certification. Independent agents remain usage-limited.

Stop at **fresh `NEXT_STAGE_2`** for the private runner correction and actual
guarded cold-start synthetic end-to-end tests only. Exact proposal:
`next_stage2_scope.json`, SHA256
`b49234dd722f3256c23d99464a9c9abce65c6366e7059c263df1a690d47ad685`.
No product/helper/test-source edits or real candidate builds in that Stage 2.
Reuse the completed real review bytes; do not prepare/review/OCR again. After
tests, freeze a separate additive Stage 3 scope for one replacement Arabic build
and one first FR/EN build, requiring another fresh `NEXT_STAGE_3`; do not replay
the failed runner/intent or separately use its unconsumed FR/EN dispatch.

The 56-file candidate/111-file package, canonical 13 dirty handoff docs, original
USD7.63147215 remainder, consumed Word journal, defaults and preferences remain
unchanged. No new OCR/browser/render/provider/Word/Gmail or publication ran.
Real AR/FR/EN translation, Word layout and measured cost acceptance remain open;
ordinary activation and Stage 4 are unavailable.

## Historical continuation: offline source-review helper implemented

Fresh `NEXT_STAGE_2` authorized the two-file offline helper/synthetic-test scope.
It is implemented in the existing isolated activation worktree, without any
production `src` edits or real-source corrections. All 70 new tests and the
151-test focused gate passed; full pytest passed **3,725 tests in 474.76s**.
Serial Full validation passed, including 233 browser tests, compilation,
2 review tests, 5 intake tests and the successful known direct-Dart fallbacks.
Read `structured_activation_source_review_stage2_2026_09_10/closeout.json`
for the final frozen candidate, protections and next-stage scope hash.

`tooling/source_review_candidate.py` consumes supplied, hash-bound bytes only.
It records image-bound text corrections/ordering separately from raw OCR,
requires exact block ownership and complete case inventory, and retains
unresolved findings. A structurally complete review record is still
`production_eligible=false`, with source/layout acceptance not evaluated.
It cannot fabricate OCR boxes/confidence, emit production source reviews,
translate or change defaults. Review honesty and every-glyph fidelity remain
AI/operator responsibilities, not properties proved by schema checks.

Stage 2 is complete. Stop at a **fresh `NEXT_STAGE_3`** for the exact
private `stage3_scope.json`/executor bound by that closeout: prepare/review/build
offline candidates for all 11 saved pages and 17 raw variants (11 baseline plus
six pilot). Reuse saved images; zero new OCR, browser/render, provider, Word/Gmail
or download. One draft preparation and at most one build per case, no automatic
retry. No production integration or source/layout acceptance follows.
The earlier OCR pilot and failed helper remain consumed/preserved. Original
canonical 13 dirty docs, lifetime USD7.63147215 remainder, Word journal,
preferences and model/protocol defaults are unchanged. Independent reviewers
remain usage-limited; this implementation review is parent-local, not independent.

## Historical continuation: six-pass pilot complete; no safe whole-page replacement

The latest fresh `NEXT_STAGE_3` is consumed. The corrected v2 local pilot ran
all six approved PSM3/11 passes once on saved AR1/AR4/shared FR1 inputs in 9.328
seconds. No retries. All three full-page images and all six new plus three
baseline texts were compared by Codex. Useful field recovery coexists with
omitted/truncated substantive words, identifiers, citations and invented text:
**zero candidates accepted as complete page replacements**. Raw source/winners
remain unchanged. Read private
`structured_activation_source_repair_pilot_v2_2026_09_10/ai_comparison.md`,
`closeout.json` and the active plan's corrected-pilot packet first.

Before execution, nine negative guard checks, two command-shape checks and all
six actual-Popen no-launch checks passed; 780 file checks passed before/after.
The corrected explicit-executable guard held for all six real launches. All
canonical 13 dirty docs, frozen 54-file/111-package runtime, defaults, original
USD7.63147215 remainder and consumed Word journal remain intact. No new render,
browser, provider, Word/Gmail, download, production change or publication ran.

Stop at a **fresh `NEXT_STAGE_2`** for the bounded offline reviewed-source
candidate helper and synthetic tests, not full-case OCR or a default flip.
Exact proposal: v2 packet `next_stage2_scope.json`, SHA256
`4035670533d79ca722c52b1a207cfee1ebe85924c61e2463a51c4eb43e5f2c19`.
It preserves raw OCR, records image-bound corrections with distinct provenance
and unresolved findings, and cannot manufacture OCR boxes/confidence or accepted
source/layout status. No runtime/provider integration or private real-source
correction in that Stage 2. Freeze a subsequent cache-only exercise for a fresh
Stage 3 gate. All 11 pages still need full source acceptance; AR/FR/EN translation,
Word layout and measured all-call cost acceptance remain open. Ordinary use,
model/default promotion and Stage 4 are unavailable.

## Historical continuation: local OCR pilot stopped before launch

The latest fresh `NEXT_STAGE_3` authorized the six-pass local pilot, but its first
launch stopped in the private Python audit guard before Windows CreateProcess.
The guard mishandled an omitted `executable` field (`None`); **zero OCR passes
ran**, no candidate text was produced and no comparative quality verdict exists.
The no-automatic-retry rule was observed. Read private
`structured_activation_source_repair_pilot_2026_09_10/closeout.json` and the active
plan's prelaunch incident section before the historical results below.

The correction (explicit pinned executable plus strict command validation) passed
a dry run through the actual Windows Popen path with CreateProcess replaced by
a rejecting test stub: one positive and two negative checks, zero launches.
This is a private harness issue, not a product/OCR defect or repaired source.
The original executor, intent and failure are preserved. Runtime/defaults, all
13 canonical dirty docs, original USD7.63147215 remainder and Word journal remain
unchanged. No browser/PDF render, paid, Word/Gmail or publication operation ran.

Stop at a **fresh `NEXT_STAGE_3`** for the exact new private
`continuation_scope.json` (SHA256
`c567ae9049a0caba90ab9c99fb5e11bffb30c408b2ead47c112da27e8ab4eaab`).
It keeps the same three inputs and six unexecuted PSM3/11 comparisons, pinned
tool/languages, 90 seconds/pass, 600 seconds overall and no retry, in a separate
v2 output root. Freeze a corrected executor and current feature-doc snapshot
after its no-launch dispatch test; do not replay the consumed first executor.
No full-case OCR, source acceptance, paid/native operations or activation follows
automatically. Stage 3 overall and Stage 4 remain unavailable.

## Historical cache-only Stage 3 complete; source repair pending

The fresh `NEXT_STAGE_3` is consumed. The approved cache-only revalidation ran
once on all 11 retained pages in 1.734 seconds, with zero OCR/browser/PDF-render/
provider/Word/Gmail operations. All 2,682 words, 128 blocks, word ownership,
geometry and cached pixels are unchanged. Nine non-first pages now require
boundary review, including the missed AR document starts at pages 3 and 4;
this does not mean there are nine new documents. All 11 pages still require
source/layout review. Missing and historical diagnostic reviews were rejected
for both complete cases (four checks); no accepted source review was generated.
Read the active plan's cache-only closeout and private
`structured_activation_source_readiness_stage3_2026_09_10/closeout.json` first.

The frozen 54-file candidate/111-file package and 13 canonical dirty handoff
documents are unchanged. Defaults and original lifetime USD7.63147215 remainder
remain intact. No source repair, paid translation, bilingual acceptance, Word
layout/cost acceptance, ordinary activation or publication is claimed. Stage 3
overall has not passed and Stage 4 is unavailable; work remains uncommitted.

Next proposal: a **fresh `NEXT_STAGE_3`** authorizes only the frozen private
`next_local_ocr_pilot_scope.json`: AR pages 1/4 and the shared FR/EN page 1,
two local profiles (PSM 3/11) once per unchanged saved input, six passes maximum,
90 seconds/pass and 600 seconds execution overall, no automatic retry. Reuse
the existing PSM6 baseline, pinned tool/languages and rasters. No new render,
download, crop/scale, provider, Word/Gmail, runtime/default change or automatic
winner promotion. Codex compares all three pilot pages before proposing further
work; this is not permission for full-case OCR, paid acceptance or activation.

## Historical source-readiness corrective Stage 2

The latest exact `NEXT_STAGE_2` authorized the offline source-readiness/boundary
slice. Implementation is complete in the isolated feature worktree: 428 focused
tests, 3,655 full tests and serial Full validation passed, with the documented
successful Dart fallbacks. The candidate freezes 54 changed files and the 111-file
Python package. Read the active plan's source-readiness Stage 2 packet and private
`structured_activation_source_readiness_stage2_2026_09_10` evidence first.
OCR ranking is now explicitly distinct from source fidelity. Paid acceptance
requires a complete hash-bound source review in its approved config manifest;
unresolved later pages block the first pilot request. Missing-title scan
boundaries require explicit source-bound review; geometry/layout uncertainty
is retained. No model/default/budget/credential change or real OCR/provider/
Word/Gmail operation occurred in this slice. Stop at fresh `NEXT_STAGE_3` for the
frozen cached-source-only revalidation (11 pages, zero new recognition/paid/native
operations); do not activate ordinary use. Real source defects remain unresolved.
Original canonical docs, defaults and lifetime USD7.63147215 remainder are intact.
Work is uncommitted/unpublished; local review is not independent review.
All earlier stage tokens and validation totals below are historical.

## Historical source acquisition and preceding corrective stages

Latest next step: fresh approved source capture completed on both PDFs, 11 pages,
using one owned browser session and 11 actual local OCR passes. Read the active
plan's current source-capture closeout and private
`structured_activation_source_capture_2026_09_10/source_readiness.json` and
`closeout.json`. All pages have real provenance but remain layout-review-required;
AI source review found column mixing, critical small-text/identifier corruption
and missed internal document boundaries. No embedded text fallback exists in
these image-only PDFs. A narrow offline source-readiness/boundary corrective
proposal was subsequently authorized by the latest `NEXT_STAGE_2` above.
Do not repeat the completed browser capture, OCR or Tesseract setup. Preserve the
initial prelaunch guard failure and additive OCR-only continuation. Defaults,
global PATH, original USD7.63147215 remainder and all 13 canonical dirty docs are
unchanged. No paid translation, Word, Gmail or publication occurred.

The assessment below has now produced the
[active ordinary-use ExecPlan](exec_plans/active/2026-09-10_structured_translation_ordinary_use.md).
The user supplied exact `NEXT_STAGE_2`; offline implementation and validation
are complete in `C:/Users/FA507/.codex/legalpdf_translate_structured_activation`
on `feat/structured-translation-activation`. Read that plan's current packet
before following historical next-step instructions below. Canonical handoff docs
were snapshotted and copied, not rewritten. PR #295 remains completed work.
The default remains legacy, models/preferences and original lifetime budget are
unchanged, and no paid/Word/Gmail/publication authority was renewed. The final
37-file candidate passed 3,508 full-suite tests and serial full validation, with
the documented successful direct-Dart fallbacks. Work is uncommitted/unpublished;
the saved canonical handoff is still byte-identical. The user then supplied
`NEXT_STAGE_3`. Preparation found ordinary pricing/bounds wiring, tier-aware
reservation, saved-price compatibility and multi-phase acceptance-continuation
gaps. Historical evidence is not
current-model/current-browser or human acceptance; English reference/reviewer is
still missing. The user supplied the new `NEXT_STAGE_2`; the corrective scope is
implemented in the same isolated feature tree. A new 46-file candidate passed
385 targeted tests, 3,624 full tests, independent review and serial full validation,
with the documented successful Dart fallbacks. See the plan's corrective packet
and private `corrective_stage2_closeout.json`; the original candidate is history.
The fresh `NEXT_STAGE_3` was supplied and consumed for acceptance preparation.
Read the plan's current Stage 3 packet and private `acceptance_draft_v2.json`:
historical replay lacks current browser/OCR proof, and a fully reserved 26-call
campaign exceeds the original remainder before input. Source acquisition has now
completed as described above, but source-quality defects block an executable
affordable phased request manifest; no paid authority exists.
The user explicitly appointed Codex to review AR/FR/EN for these tests; the user
will review ordinary-use outputs. This is AI review, not human sign-off, and no
review has passed merely by assigning the role. English uses source-versus-output
review; there is still no external corrected English reference. The earlier human-
review appointment requirement below is superseded for testing only. Stage 3 has
not passed; `NEXT_STAGE_4` is unavailable. No runtime bytes changed in preparation.
No paid/native amendment or dispatch was made. Keep the original
USD10 ledger (USD7.631472150000 remaining), consumed Word journal and all preferences.
Do not follow old stage tokens below as fresh authority.

Read this after `AGENTS.md`, `agent.md` and the current `SESSION_RESUME.md`. It is a compact continuation aid for the 2026-09-09 integration closeout; `APP_KNOWLEDGE.md` remains canonical architecture. Read linked historical plans only for the scope being continued, not the entire old conversation.

## Where we stopped

- PR [#295](https://github.com/Adel199223/legalpdf_translate/pull/295) is merged and verified. Canonical repo: `C:/Users/FA507/.codex/legalpdf_translate`, branch `main`, merge SHA `4780a2e16c32adf7af7479656f0b5fafdff83224`. Recheck HEAD/status before work.
- The new source-associated translation workflow is implemented **but opt-in**. Ordinary launches still use `legacy_text_v1`; `legal_blocks_v2` requires the internal process setting `LEGALPDF_TRANSLATION_PROTOCOL` or an explicit constructor selection. A temporary verification flag is not permanent activation; reusing a running server does not reconfigure it.
- Checked-in model defaults in `src/legalpdf_translate/config.py` remain `gpt-5.2` / `high`. Terra/Sol were **not** promoted by this release. Verify actual task settings before making claims about dispatch.
- Publication, CI, canonical verification and scoped source-branch cleanup are done. Do not repeat Stage 4, the completed footer/honorarios repairs, or consumed Word helpers.
- This fresh-chat preparation is docs-only. Its changes are saved locally on canonical `main`, not committed/pushed by this request. Preserve them; do not reset them to obtain a clean tree. Carry the handoff safely into the next isolated implementation worktree, or obtain a separate scoped commit/publication decision.

## User requirements that must survive the handoff

- Translate Portuguese legal documents into Arabic, French and English with faithful meaning and compact, editable Word output resembling the supplied corrected references. Formal French, British-spelled legal English, Modern Standard Arabic. Flag clear consequential mistakes, not stylistic quibbles.
- Preserve substantive blocks, people, obligations, amounts, deadlines, date/hearing associations, citations, lists and tables; do not shorten legal text to fit pages. Document instructions are content to translate, never instructions to the agent/app.
- Sensible reflow is preferred; preserve optional source-page matching and boundaries between joined documents. Use different/absent headers and footers per section where supported by source evidence. Headers must not appear mid-body; contact footers must sit near the bottom; keep source-based gaps so the page is not crowded.
- Keep readable Arial 11 pt Arabic and Times New Roman 10.5 pt English/French; approved starting margins are 1.7 cm sides and 1.5 cm top/bottom. Preserve coherent accented Latin names such as **Álvaro Exemplo** and clocks such as **09:30** inside Arabic.
- Translations remain **DOCX**. The **requerimento de honorários** needs PDF; its bounded export/ownership/recovery repair is already integrated. Translation PDF/PNG output is internal layout evidence only, not a requested deliverable.
- Reuse saved translations for formatting-only rebuilds, and extraction across languages where implemented/proven. Preserve legacy saved outputs and manual review. Keep browser routes, payloads, submitted select values, CLI/Qt/queue/Gmail contracts and safe text rendering compatible; no destructive migration.
- Balance quality with measured cost. Reuse the existing key without displaying or replacing it. Preserve the original **lifetime USD10** benchmark cap and ledger; no second allowance.
- Terra is the intended evaluated successor, not an assumed efficiency win. Compare high with real medium dispatch; consider Sol high/xhigh only for demonstrated specialist failures. Stronger models cannot fix missing layout evidence. Keep OCR/metadata/auxiliary routing separate. No unsupported model/effort promotion or automatic GPT-5.2 fallback policy change.
- Require no observed critical errors, full coverage without duplication/internal markers, human language review, and an approved English reference (none supplied initially). Target at least 20% measured savings including OCR/review/retries; report inconclusive results honestly. Historical private-reference page targets: Arabic <=9, French <=2 at readable fonts.

## What the completed work proves — and does not

- Merged structured path retains digital/same-pass local OCR evidence, stable block IDs, exact coverage/source binding, bounded selected-page context, commit-last bundles, safe resume/recovery/cancellation, confirmed continuations and local rebuilds. Evidence-gated simple scans can use the approved section furniture/gaps; uncertain/complex/API-only/plain-text OCR remains layout-review-required.
- All structured pages retain `fidelity_review_required` / `not_evaluated`. Numeric/block agreement is not bilingual legal acceptance. Structured summaries still mark total cost `not_evaluated_all_calls`; audit complete OCR/auth/cache/failure/review accounting and reservation enforcement before paid acceptance.
- Accepted earlier private Arabic/French formatting rebuilds were five/one pages; these do not validate the newly integrated pipeline on full real reference cases. The latest accepted EN/FR/AR integration fixtures are synthetic, each one page with 20 mapped blocks and one join. Fresh real-document/model/pagination evidence remains a separate acceptance task.
- The one approved corrected Arabic native check took 6.183 seconds; full-page review and physical glyph coordinates confirmed `09:30`, names, header gap and bottom footer. User said **“looks right.”** English/French reused package-identical native evidence. Native authority is consumed.
- Final local full pytest: **3,391 passed**, 428.49s; focused hardening: **318 passed**. Serial `validate_dev.ps1 -Full` passed with the documented direct-Dart fallback. These are publication-build results, not fresh tests of later edits.
- Final exact-head [CI run 34411719376](https://github.com/Adel199223/legalpdf_translate/actions/runs/34411719376): both jobs passed; Windows **3,391 passed**, one upstream Starlette/AnyIO deprecation warning, 635.44s. Initial CI failed two docs-fixture cases due to lowercase `branch:`; corrected in `2ef286c36a2e29191d54d582dcc00bc07f7d8ca6`. Preserve exact `- Branch: \`main\`` in the resume anchor.
- Canonical post-merge: **20 offline checks passed** in 4.11s; imports/default/opt-in/explicit policy isolation passed; all 46 runtime/test files matched accepted digest `dcc4981ce7efd5a03ca50a64b3b18988346c1b85a8e2a2cd7b1a0db3d1fb689e`.
- Guarded canonical shadow browser smoke verified build identity, New Job/Recent Jobs, language controls and UI/API wiring only. Real provider/OCR/auth/Gmail/native readiness was **not evaluated**. Its owned processes/tab were closed; no new paid/native operation occurred.

## Recommended next task — assess, do not simply flip a default

Prepare **Stage 1 assessment and an ExecPlan for controlled durable activation and real-document acceptance of the already-merged structured workflow**. No active master roadmap needs resuming.

1. Recheck canonical state, saved checkpoint/protocol behavior and actual caller/model selection. Inspect only relevant retained research; do not merge it wholesale.
2. Identify prerequisites for an ordinary-launch policy that is reversible, evidence-gated and consistent across browser/CLI/queue/Qt, while preserving old run identity and conservative fallbacks.
3. Assess startup/provider-state isolation, all-call accounting and the original ledger before planning real API/schema/OCR tests. Decide whether a narrower safety/accounting fix must precede activation. Offline fake-provider tests and UI wiring do not establish those capabilities.
4. Specify bounded real-case legal/layout/cost acceptance, reusable evidence, missing English human reference and explicit promotion criteria. Keep protocol activation separate from Terra/Sol evaluation; do not silently broaden to both.
5. Present the recommended smallest scope, risks, tests and staged prompt packets. Stop at **`NEXT_STAGE_2`** before implementation. Future paid/native/Gmail/host work needs fresh, operation-specific scope; old tokens and blanket computer-access replies are not a reusable allowance. Any new publication requires separate explicit approval.

## Read selectively / evidence locations

- Current architecture: `APP_KNOWLEDGE.md`; current anchors: `SESSION_RESUME.md`, `HANDOFF.md`; commands: `VALIDATION.md`; staged rules: `workflows/STAGED_EXECUTION_WORKFLOW.md`.
- Completed implementation/history: `exec_plans/completed/2026-09-09_structured_new_translations.md`. Prior footer integration: `exec_plans/completed/2026-09-09_footer_spacing_integration.md`. Do not replay historical pending instructions.
- Preserved research: `C:/Users/FA507/.codex/legalpdf_translate_quality`, branch `feat/translation-quality-balance`, checkpoint `66a7374a2b874f59994215177af4ff48d1d5f100`; relevant plan `docs/assistant/exec_plans/active/2026-09-06_translation_quality_cost_structure.md`. It is not canonical production truth.
- Private latest evidence root: `C:/Users/FA507/.codex/legalpdf_translate_private_benchmarks/structured_new_runs_corrective_stage3_2026_09_09`. Start with **`stage4_publication.json`**, status `complete_merged_verified_and_scoped_cleanup_done`, for exact tests, merge, cleanup, artifact digests and smoke boundaries. Inspect selected fields, not private app-data dumps.
- That root's approved Arabic DOCX: `clock_rebuild_run/AR/20260909_201821_168775_source_AR_20260909_201955.docx`; preview: `clock_native_run/AR/page-1.png`. Preserve originals and historical failed outputs. Read the bound review records before considering any fresh check.
- Original budget: `C:/Users/FA507/.codex/legalpdf_translate_private_benchmarks/translation_quality_2026_09_06/budget-ledger.json`. Last verified: **149 finalized entries, $2.36852785 spent, $0 held, $7.63147215 remaining**; SHA256 `eca21ec2b9bbc8fb3830442a411b6256e3c629c6f256a36bd85ccd92f9e7fc8f`. Reconcile before any future reservation; do not reset it or rerun completed trials blindly.
- Original Word safety journal: `C:/Users/FA507/.codex/legalpdf_translate_honorarios_pdf/tmp/stage2_word_probe_fixed/LegalPDFTranslate/word_pdf_export_v2/active.json`; legitimately advanced SHA256 `94cdbb415aa8439951958680528c461296e3a0f12174ce96a493ad9e7a97dd8f`. Never restore older bytes, delete it, bypass it with another profile or replay consumed helpers.
- Private reference pairs in `C:/Users/FA507/Downloads`: `187 (2).pdf` -> `187 (1).docx` (Portuguese/Arabic, nine/nine reference pages); `marcação de julgamento (1).pdf` -> `marcacao_de_julgamento_FR (1).docx` (Portuguese/French, two/two). Keep private documents/screenshots/evidence outside Git.

## Host and worktree safety

- Canonical daily UI is browser/live on 8877; bridge 8765. Do not operate Gmail/extension without explicit live scope. Check actual process/port identity; this docs pass did not launch the app.
- **Shadow + GET-only + disabled live bridge is not complete isolation.** Bootstrap/capabilities can call Word readiness/export and account/OAuth providers; constructors can resolve live paths before enable flags. Follow `workflows/HARNESS_ISOLATION_AND_DIAGNOSTICS_WORKFLOW.md`. Do not run ordinary bootstrap as a “harmless read” under offline-only authority.
- The retained private guarded smoke injected honest `not_evaluated` capability boundaries and denied native/process/registry/outbound/live-path access before app construction. Its success is only UI wiring. Do not replay its consumed/fresh-root helpers.
- `_new_runs` is retained **clean, detached at the merge SHA**; local and remote `codex/structured-new-translations` were deleted. Its `.venv311` is a junction to the canonical environment. Never recursively delete it or its private logs casually.
- Preserve unrelated `_formatting`, `_honorarios_pdf`, `_quality`, `_structure` worktrees and user files. Do not switch canonical branches under a live server. Use an isolated worktree for the next implementation.
- Tests use `C:/Users/FA507/.codex/legalpdf_translate/.venv311/Scripts/python.exe`; assert worktree import origin when reusing that environment. Docs use direct Dart fallback at `C:/dev/tools/flutter/bin/cache/dart-sdk/bin/dart.exe` if the wrapper reports the known AOT failure. Never hide a failed validator/test behind historical green counts.
