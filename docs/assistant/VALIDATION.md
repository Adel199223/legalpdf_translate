# Validation guide

Use the smallest meaningful check first. Do not claim a test passed until its process and terminal result are known. [HANDOFF.md](HANDOFF.md) and the [test performance plan](exec_plans/completed/2026-09-26_test_performance.md) identify current work. The [completed Gmail/formatting plan](exec_plans/completed/2026-09-24_gmail_formatting_readiness.md), [completed multilingual plan](exec_plans/completed/2026-09-17_multilingual_gmail_acceptance.md), its [September24 recovery](exec_plans/completed/2026-09-24_acceptance_interruption_recovery.md), the [completed Arabic acceptance plan](exec_plans/completed/2026-09-17_arabic_normal_browser_acceptance.md) and [ordinary integration plan](exec_plans/completed/2026-09-16_ordinary_browser_integration.md) retain earlier qualified results. This page defines the validation tiers.

## Current validation checkpoint

The active [five-request workflow](exec_plans/active/2026-09-26_five_oldest_translation_requests.md) is separate from the completed test-performance benchmark below. Requests1-2 have genuine app-UI intake, reviewed translations and fee PDFs, exact ordinary downloads, and independently verified unsent draft attachment bytes. Their paid calls and native exports are historical operations, not fixtures to replay. Three requests remain unstarted.

The reviewed product patch corrects reviewed-DOCX counting, citation/footer classification and complete decimal currency protection. Test refreshed counts and calculated amounts after an edit, preservation of explicit amounts, unreadable-artifact failure before saving, exact staged Gmail bytes and row reuse. Synthetic source regressions must keep legal prose in body order, retain genuine page counters, and protect a complete decimal amount before a unit or sentence punctuation. Use fictional documents; never commit private legal text. Earlier count-only Full results do not validate later citation/decimal changes; the active plan records the final combined run and independent review.

Final combined Full passed on 2026-09-26: exit0 in812.809seconds, 664 selected executions (245 browser/API,2 Gmail state,5 selected intake,239 source review,173 formatting), compilation and docs/hygiene. Nine intake deselections are expected. The known dartdev AOT255 issue recovered through the successful direct-Dart fallback. All frozen scope hashes stayed unchanged throughout the run. Separate focused source regressions passed55 cases; overlapping tiers are not additive unique coverage. `five_oldest_requests_20260926_01/validation_combined_01/result.json` and `wrapper.log` retain terminal evidence. The user has now approved publication, canonical application and continued work on the three remaining selected requests. Publication is in progress; exact CI/merge/application state belongs in `C:/Users/FA507/.codex/legalpdf_translate_private_benchmarks/five_oldest_requests_20260926_01/publication_01/publication_receipt.json` and the actual repository. The implementation baseline is `2a6eb5962820b3c0f015dc6e296516ff2dac8049`; product hashes remain frozen, and this authorization status update does not repeat product tests or establish a future HEAD. The first two drafts used the earlier canonical build and received separately reviewed corrections; later actual-use evidence remains separate.

## Historical test-performance checkpoint

The test-performance changes below were authored and locally validated on `codex/test-performance-20260926`, based on `b35be1c3ad401ba70ca3dc4464f09bf64782958b`. Publication/application were approved on 2026-09-26; the bounded publication receipt routed from HANDOFF owns exact-head CI and installed revision. Completed local measurements on 2026-09-26 are:

| Scope | Verified result | Elapsed time |
|---|---|---|
| Complete baseline, before runner changes | 6,717 passed | 1,835.010 seconds wall time (30 minutes 35.01 seconds) |
| Quick feedback | 87 passed; exactly two DOM-to-workflow cases excluded from Quick only | 10.72 seconds pytest; 16.202 seconds whole wrapper |
| Selected Full | 659 passed; broader selected integration scope retained | 838.559 seconds whole wrapper (13 minutes 58.56 seconds) |
| Full formatting component | 173 cases across two isolated workers | Worker wall times 538.359 and 516.531 seconds, running concurrently |
| Complete four-worker comparison | 6,754 passed: exact ordered 6,717 baseline cases plus 37 runner regressions | 537.941 seconds wall time (8 minutes 57.94 seconds) |

These rows have overlapping tests: do not add their counts or compare selected Full with complete baseline as an equal-coverage speedup. The complete comparison preserves every original ordered node ID and adds 37 runner cases; its observed wall time is **3.411x faster / 70.685% shorter**. This is one local measurement, not a hosted-CI guarantee. The baseline overlapped short documented Quick/tool checks; the parallel comparison did not overlap another test suite. Both complete runs used the same explicit offscreen Qt platform and Windows system fonts. Formatting worker times are neither additive elapsed time nor the Full wrapper total. The comparison audit verified all five frozen comparison pins, 484 existing source/test pins and 68 protected file pins unchanged. Twenty later-added artifact-selector tests are outside that measured snapshot and passed separately in 0.76 seconds; final collection has 6,774 cases. Private `test_performance_20260926_01/comparison_audit_01.json`, `baseline_summary.json`, `baseline_complete_result.json`, `quick_02_result.json` and `full_01_result.json` retain the evidence; the completed plan and publication receipt own subsequent outcomes.

Earlier PR #303 merged at canonical `ca395d601efe4e00aad3458526b7d961d3cf5f0c`, matching reviewed `dad5ea1`. Full_04 passed **655 selected tests**, nine expected deselections, compilation and hygiene, with 486 matching source pins. Direct docs validation passed separately; the initial Dart AOT failure and successful fallback remain preserved. All four required exact-head CI jobs passed. Private `gmail_formatting_readiness_20260924_01/pr303_publication_01/` and `validation/full_04/independent_terminal_review.json` provide exact evidence. Full is not the complete repository pytest collection; overlapping older suites are not additive unique coverage.

The [completed Word startup deadline plan](exec_plans/completed/2026-09-25_word_startup_readiness_deadline.md) passed **91 targeted no-provider tests**, with no skips/warnings and six unchanged source/test pins (`word_startup_fix_01/green_04/`). Seven new regression cases first produced six failures and one pass on the old source; every failed runner/isolation attempt remains preserved. Fresh Full on source `58a2df5` passed **655 selected tests** with nine expected Gmail deselections, compile, docs and hygiene; both Dart AOT exit-255 events and successful direct fallbacks remain recorded. All 486 prepared/before/after/current/committed source pins matched through docs-only head `11beb6c`. `validation/word_startup_full_01/independent_terminal_review.json` verifies the result. All four PR/push CI jobs passed on that exact head; each Windows run passed **6,713 tests and 34 core tests**, with one retained Starlette dependency-deprecation warning. Counts across overlapping tiers are not additive unique coverage. PR #304 merged as `1d6fdb3` with full Git-tree equality and clean canonical application.

One fresh canonical default-readiness aggregate then passed in **27.673 seconds**, using one 45-second-default launch/preflight (12.185 seconds) and one separate 45-second-default synthetic canary (15.392 seconds), without override or retry. The complete operation took 40.156 seconds including observations/reporting. Both stages confirmed cleanup; source/tools and five protected file pins were unchanged, with stable Word-free observations and free browser/bridge ports afterward. `word_startup_acceptance_01/independent_actual_review_01.json` verifies the actual result; `word_startup_publication_01/final_application_receipt.json` indexes all completed gates. This does not diagnose the earlier startup delay, export a legal document, prove Gmail finalization or refresh another process's cache. Separate stage deadlines plus cleanup/recovery overhead are not a guaranteed aggregate wall-clock bound.

EN03 completed genuine three-page source/translation/formatting review, package validation of 54 fragments/four editable tables and one native export with qualified review of every A4 page. The feature matrix separates these output results from 27 focused recovery/browser-state tests. EN03's browser downloads remain blocked/unconfirmed, so no downloaded-copy identity is claimed. Do not repeat completed acceptance.

Gmail03 ordinary image-only/OCR-off output passed qualified root/peer review and normal Save/Confirm. Row 90 joins the actual run/output; all 63 original job rows and schema are unchanged. The normal metadata step added only `court_emails_by_city`; saved defaults/all other fields remain equal, but original settings-byte preservation is false. The owned live03 runtime stopped with zero jobs and expected exit 1 for that sole baseline difference. Its post-stop free-port/Word 0 snapshot predates shadow06, which later stopped with its own confirmed cleanup receipt; each observation describes its recorded time. Exact evidence routes are in [HANDOFF.md](HANDOFF.md).

AR01's1/3-page failure and unknown offending response remain preserved. AR02 upload succeeded after the user enabled the browser extension's file-URL setting. Its one-page Arabic job completed through normal reviewed-source UI, with one structured-block correction and two fully costed calls totaling USD0.02136925. The phase now has16 finalized reservations/USD0.23975525, no unfinished entries/new hold/block and zero remaining request slots. This is actual provider accounting, not an estimate or permission to reuse the exhausted phase.

AR02's saved page/document decisions passed16 independent checks and the exact reviewed DOCX package passed20. All15 fragments cover every source and actual target character once; the3x2 editable table retains three pipes,40/60 column widths and a27.9px gutter. Full-spacing policy writes the signature gap as2188twips/109.4pt. Header/footer/folio ownership and whole literal/bidi scopes match the accepted decisions. Ordinary DOCX SHA256 `3e16b716e762785f5b4e665d9da42ec9623cca8916c4bcd1e65d6aca0db6e882` remains unchanged; reviewed DOCX SHA256 `cfd9771ec4e86f77665b75ceb407223df6e9ccc995b6fa1d3b564616b91a9b61` is39162bytes. Its build records zero provider dispatches. Evidence: private `cases/formatting_ar_02/formatting_saved_audit_01/` and `terminal_evidence_01/`.

One actual AR02 native export completed in6.593seconds with confirmed cleanup; all22 independent factual checks passed. The actual PDF SHA256 `778abbbc6ccfa0a0e7c66ece52e11ffd40993f9cdc743e557b3a7cd0f0245f54` has one A4 physical page, inspected completely by root and peer with no clipping/overlap, broken glyphs or leaked markup. All original/source/sidecar/tool/protected pins remained unchanged. Additive reviews beneath `native_review/operations/ar_reviewed_native_01/` support qualified closure of the three remaining matrix rows; original receipt flags remain unchanged. Retain limitations: room identifier A was localized to Arabic alif; original gray table borders/blue separator strokes are not replicated; strict raw Arabic PDF extraction matches only6/15 whole fragments despite readable rendered glyphs. Exact DOCX text/structure and visual readability are separate from PDF extraction fidelity. No speculative text normalization or product fix was applied.

The ordinary AR02 download route returned exact valid DOCX bytes with correct attachment/MIME headers, but Edge reported the new `output_docx` record as “Couldn't download - Blocked”. A reviewed-file click separately timed out. A later actual browser screenshot exposed the local-origin multiple-download permission prompt. After explicit user approval, root clicked Allow; the queued transfer then reported a network issue after the runtime had already stopped. No downloaded-copy identity or definitive attribution of every historical block is established. Future checks must inspect browser prompts before closing the owning runtime. Local artifact/package acceptance stays separate from browser download acceptance.

Gmail04 refused a private attachment-token equality guard before creating a job or reservation. Source/message/thread/filename and fresh-session evidence isolated the local helper defect; normal shutdown preserved its zero-charge result. A distinct successor passed16 no-provider tests and15 independent binding checks, changed only that predicate, and consumed the same existing USD3/max4 phase under its unchanged Gmail04 identity.

Gmail05 then completed one genuine extension-to-translation flow on canonical `a8f7950`/asset `54fa93db4c30`. The one finalized call has3,753 input/1,658 output tokens and USD0.02977975 actual, with18 independent terminal-accounting checks passing and no unknown usage/new hold. One normal browser download is byte-identical to the38,490-byte app DOCX (SHA256 `3d5b474474cab4f775a0c9151604febee9be91e760ce5e1e019d1d675f4842bf`). Root/peer reviewed its complete one-page Word rendering against the source image; all29 paragraphs match saved text. The retained `source_block_layout_unavailable` warning reflects zero extracted source blocks; automated numeric/citation counts against empty extracted text are not proof of actual missing content. Linear reflow, source-emblem omission and legal-language review remain qualifications.

Normal Save added row91 once; normal Gmail Confirm reused that row. Finalization produced fee DOCX/PDF and a single actual draft failure: Google403 `insufficientPermissions`. No draft or send succeeded. Independent finalization checks pass28/28: exactly65 rows, row91 unchanged, all64 prior rows/schema/settings/pins preserved and staged translation byte-identical. The actual fee PDF has one page; its export took8.056seconds, separate from the successful launch/canary readiness checks. Operation-owned native processes were absent; the unrelated recovery Word document remained untouched. Root/peer subsequently reviewed the sole fee A4 page: all nonempty DOCX paragraphs appear in the PDF, case fields and150-word count match, and there is no clipping, overlap or missing body. The template has no monetary total; profile/payment/legal correctness is not certified. The valid root visual receipt is `root_fee_visual_review_02.json`; receipt01 is a preserved null-only PowerShell failure, not review evidence. The runtime then returned exit0/zero jobs through owned cleanup; its independent terminal audit passed 28/28, with owned processes absent and ports8877/8765/8893 free. All64 prior rows/schema/row91/settings,232 source pins and21 historical pins matched; the unrelated recovery Word process remained untouched. The temporary draft staging copy was normally removed at shutdown after exact verification, while durable translation/download/fee/source files remained intact.

Pre-fix Full finished at2026-09-26T11:13:55Z with exit0 and655 selected passes/nine expected deselections on unchanged product source. Compile/docs/hygiene passed through the known direct-Dart fallback after the preserved AOT wrapper failure. This is the standard selected Full tier, not a new complete-repository pytest count. It predates the newly demonstrated post-save EN defect: bootstrap restoration calls the Arabic-only review state after a successful save and receives422. The local guard now skips Arabic-only state restoration for known ineligible jobs while preserving completed AR, no-current-job restoration and real errors. All 20 targeted no-provider tests passed in 0.91 seconds and independent semantic review is clear; the fresh Full at `validation/english_save_review_full_01/` finished on2026-09-26T11:50:24Z with all659 selected tests passing (240+2+5+239+173), nine deselections and successful compile. Its wrapper exited1 at docs-only AD046 marker failures after the known AOT255 event; the direct fallback did not pass at that point. Exact SESSION_RESUME markers were then restored, with a second correction separating the branch from the authoring path; both failed attempts remain preserved. Direct docs/hygiene subsequently passed under `docs_sync_ar02_gmail_closeout_01/session_markers_repair_01/result_02.json`, completing those checks without repeating passed tests. Never relabel the wrapper as exit0. Source commit `24f7ecbd80984223f0a55c012ab369b40ccfb720` remains frozen; publication/application status is recorded in the receipt below. Frozen SHA256 values are `44c4f19603a0104a2e84904314fe37df75f0184e16af56e34a78c757f496ccb4` for `translation.js` and `2645da876660ec828329f296070aa50f6bc68b472b454787041134f6863f0a07` for its browser-state test. The successful actual Gmail/native evidence remains bound to `a8f7950`/pre-fix PR304 source; no paid/native replay follows. `bounded_acceptance_closeout_01.json` indexes 11 completed actual receipts and their qualifications; the later verified merge/application identity belongs in `gmail_closeout_publication_01/publication_receipt.json` after publication. Evidence is in `docs_sync_ar02_gmail_closeout_01/validation_01/`; actual Gmail05 accounting/download/manual/finalization receipts remain under `cases/gmail_live_05/`.

The original FRlong uncertainty stays shelved without changing its hold/block or partial outcome. Current user authority permits bounded readiness work, not replay of consumed operations or removal of unknown usage. Exact validation text replaced here is preserved in `gmail_formatting_readiness_20260924_01/docs_sync_ar02_gmail_closeout_01/beforeimages/docs/assistant/VALIDATION.md` and `gmail_formatting_readiness_20260924_01/docs_sync_ar02_gmail_closeout_01/beforeimages_gmail05_sync_01/docs/assistant/VALIDATION.md`; earlier beforeimages and all failure/fallback receipts remain intact.

## Interpreter and isolation

Project tests use `C:/Users/FA507/.codex/legalpdf_translate/.venv311/Scripts/python.exe`, available as `.\.venv311\Scripts\python.exe` in a configured worktree. Do not use bare/global Python for pytest. Run from the intended worktree and record its path, branch and HEAD.

For Windows Qt tests deliberately using the `offscreen` platform, set child-only `QT_QPA_FONTDIR` to the existing Windows font directory before importing Qt, and verify that `QFontDatabase.families()` is nonempty. The September24 private runner initially had zero font families and invalid missing-glyph metrics; the unchanged sidebar test and a production-styling probe passed after supplying system fonts. Keep font readiness, launcher failures and product regression results distinct in the evidence. This setting belongs to the isolated test process and does not change saved application defaults.

Use fresh temporary settings/output roots and existing synthetic fixtures. Never point tests at live Gmail, credentials, the user's app data or private outputs by default. Browser `shadow` mode alone does not disable provider, auth or native operations. A named isolated acceptance runner can have a stricter socket boundary than the ordinary browser/API test harness; do not weaken that boundary to make a different test fit it.

## 1. Targeted tests

```powershell
.\.venv311\Scripts\python.exe -m pytest -q tests/test_source_review_browser_state.py tests/test_formatting_review_browser_state.py
.\.venv311\Scripts\python.exe -m pytest -q tests/test_shadow_web_source_review_api.py tests/test_shadow_web_formatting_review_api.py
```

Choose affected modules and real regression cases rather than repeating broad suites without a new change or unresolved concern. Source/accounting changes need guard, resume and failure-path coverage. Formatting changes need content/mapping/package checks, including Arabic where relevant. A synthetic SDK/Word test is not a real-provider/native acceptance result.

For an uncertain provider failure, join the original reservation, dispatch and any received usage before interpreting cost. Missing usage is not zero; aggregate dashboard totals need request attribution and reporting-finality evidence before they can settle a specific failed call. Diagnose only retained exception evidence. Preserve safe cause-class diagnostics through a fictional failure regression; never replay the paid request merely to diagnose the original attempt, and never log raw error messages, credentials or legal-document content to improve observability.

## 2. Complete repository pytest

```powershell
.\.venv311\Scripts\python.exe -m pytest -q -ra tests
.\.venv311\Scripts\python.exe tooling/test_shards.py run --workers 4 --timings tooling/test_timings.json --output tmp/validation/complete-01 -- tests
```

Use the complete collection before merge and after integrated code/test/workflow changes when required. Retain the full terminal count, failures, skips, duration and JUnit when available. For a long run, use a bounded owned launcher, capture logs and freeze its named source/test snapshot. Do not duplicate collection or start another full run merely because the active run is quiet.

The shard runner collects the requested scope, assigns every collected test exactly once by whole file, then checks each worker's collection and actual execution against that plan. It balances files using measured timings; new files are included automatically. Qt UI files remain together to avoid local desktop focus conflicts. Workers have separate app-data, temporary and report roots, and preserve the normal test guards. Use `--workers 1` for sequential diagnosis or `2` on a constrained machine. Output directories must be new: choose another suffix instead of overwriting evidence. Planning estimates are not measured elapsed time. Per-worker logs, durations, JUnit and the complete assignment remain in the named output directory.

## 3. Standard validation wrapper

```powershell
powershell -ExecutionPolicy Bypass -File scripts/validate_dev.ps1 -Quick
powershell -ExecutionPolicy Bypass -File scripts/validate_dev.ps1
powershell -ExecutionPolicy Bypass -File scripts/validate_dev.ps1 -Full
powershell -ExecutionPolicy Bypass -File scripts/validate_dev.ps1 -Full -Workers 1
```

`-Quick` runs core CLI/workflow/report checks and translation/source/formatting browser-state checks for early feedback. The two measured DOM-to-workflow integration cases, `test_actual_dom_operator_actions_to_public_service_context_and_workflow` and `test_actual_dom_to_owned_formatting_api_build_and_download_preserves_original_run`, are excluded only from Quick and remain in Full and complete coverage. It does not replace the standard browser/API tier or required complete coverage. `-Quick` and `-Full` cannot be combined.

The default wrapper retains focused browser/API tests, compileall, relevant docs validation and workspace hygiene. `-Full` adds the same Gmail filter and source-review/formatting-review files as before; its six formatting files run in two isolated processes by default, with `-Workers 1` retaining sequential execution. **Full is not the complete repository pytest collection.** The wrapper reports slow tests without changing assertions or granting live Gmail/native authority.

CI runs the complete suite on every PR, main push and manual dispatch. Four isolated Windows jobs execute exhaustive partitions; the stable required `test (3.11)` check requires all partitions and Windows/Linux contract checks, then verifies matching collections, exactly-once execution and JUnit receipts. Failed, cancelled, skipped or missing jobs cannot make the aggregate pass. Feature-branch pushes no longer duplicate an open PR's full run; use manual dispatch before a PR exists. Superseded PR runs are cancelled, while main runs are retained. In the local CI rerun design, artifacts are named `pytest-shard-N-attempt-${{ github.run_attempt }}` and downloaded only from the current workflow run. `tooling/ci_test_evidence.py` selects the highest numeric attempt for each of four shards, then delegates complete-collection/execution/JUnit checks to the strict verifier. Older artifacts remain retained; a failed or incomplete latest attempt cannot fall back to an earlier success. Timing/artifact details belong in the [CI workflow](workflows/CI_REPO_WORKFLOW.md). This design has not yet established a hosted-CI result.

## 4. Documentation and policy checks

After docs/manifest/policy-validator changes, run the direct Dart tools on this machine (or the same current runtime resolved by the wrapper):

```powershell
& 'C:\dev\tools\flutter\bin\cache\dart-sdk\bin\dart.exe' test/tooling/validate_agent_docs_test.dart
& 'C:\dev\tools\flutter\bin\cache\dart-sdk\bin\dart.exe' tooling/validate_agent_docs.dart
& 'C:\dev\tools\flutter\bin\cache\dart-sdk\bin\dart.exe' tooling/validate_workspace_hygiene.dart
```

The known `Unable to find AOT snapshot for dartdev` wrapper error is distinct from the direct-Dart fallback. Record the wrapper failure and an actual successful fallback; do not infer success if the fallback did not finish. Standing Docs Sync authorization does not waive docs/manifest consistency checks.

## 5. Browser and rendered-document evidence

For browser checks, record the actual build/asset version and owned isolated workspace, use fictional inputs where possible, and test the real UI path. Source review, exact operation recovery and artifact download should use the public APIs rather than injected accepted commits. A new CSS file on disk does not change an old server's immutable static snapshot; verify the served asset.

For a perceptual DOCX change, retain exact source/output identities and inspect the actual relevant pages. Programmatic package/text checks and visual/native acceptance answer different questions. Translation DOCX creation does not require automatic Word export. Any necessary native or paid operation must follow its current explicit scope, retained ownership and one-shot recovery rules; failed/consumed helpers are not reusable.

When a visual review appears to lose repeated headers or footers, inspect the relevant image individually and corroborate the actual file content before removing source text or diagnosing a renderer defect. Preserve and explicitly supersede incorrect review aids. For Arabic literal failures, reproduce with the exact accepted source, saved glossary contract and unchanged provider response; a probe without the actual contract is not proof of a product defect. Offline validation of retained paid bytes remains distinct from a completed ordinary run.

Successful source preparation proves valid supported input, not complete entity attribution. After a demonstrated attribution gap, inspect the remaining document’s source-proven names and reference fields before another paid run. Distinguish a missing source span from a genuine provider omission or untranslated ordinary word; keep strict validation for the latter. Literal compliance alone does not establish legal fidelity. Bilingual review must check tense, mood, conditional gates and legal effect, including whether an instruction has become a claim that the action already happened. Saved glossary/addendum influence must be checked under the exact run contract before changing prompts or preferences.

For citation failures, compare actual source and target citation occurrences with parser results, including valid target-language punctuation. A closed coordinated citation followed by Arabic comma U+060C must not be mistaken for missing coverage merely because the parser recognizes only ASCII closing punctuation. Keep true missing, extra or reassociated citations rejected. Pure probes or retained-response revalidation do not create an accepted run, authorize another dispatch or repair a separate linguistic defect.

## Readiness and historical results

Historical product publication (2026-09-24): PR #300 exact head `dd208272733e1835bb336ef1a97ddf1968f0de0d` passed the required PR/push checks, with 6,666 tests in each successful full run and merged at `1722cf112f65b40c37ba4bb864c59be987973d13`. The subsequent historical closeout branch changed documentation and four CI action references only; its final follow-up status is recorded separately. Canonical source-only HTTP/static receipt `canonical_http_verification_02/run_01/result.json` (SHA256 `64d26bf1a3a97306baa437c4d43465ede9aa68dedb056334968a1e91b7c40106`) verifies `1722cf112f65b40c37ba4bb864c59be987973d13` on September24, including all 93 served assets against disk, 185 literal module imports, expected build/asset identity and unchanged source/protected files, and owned cleanup. No provider, OCR, Gmail/OAuth or native Word dispatch occurred in that check. This is HTTP/runtime/static evidence, not browser JavaScript or new document acceptance. The final documentation/CI follow-up preserves product source. For final follow-up PR/CI, current main and cleanup disposition, consult private `C:/Users/FA507/.codex/legalpdf_translate_private_benchmarks/application_multilingual_publication_20260924_01/final_publication_receipt.json` and `FRESH_CHAT_HANDOFF.md`; these records must be read rather than inferring an unrecorded future merge or clean state. The [publication closeout](exec_plans/completed/2026-09-24_multilingual_acceptance_publication.md) separates actual product validation from its final closure route. PR attempt1 retained three 20-second subprocess timeouts; one unchanged-code rerun of the failed job passed all 6,666 tests. The same three tests passed locally in 3.55 seconds. The two passing full runs each reported one dependency warning. Different runner images and the slower failed run suggest environment timing but do not establish a root cause. Original failures remain preserved. HTTP operation01 stopped before serving requests on a verifier allowlist refusal; only the fresh operation02 established the HTTP result.

Completed bounded multilingual test matrix (2026-09-24): the paid cases used a frozen source inventory that passed Full636 plus214 additional workflow checks; guard/server preparation passed192 fictional checks. Native04's50 fictional checks preceded a genuine pre-export refusal after an Office update. Fresh narrowly pinned native05 passed58 fictional checks and independent review, then actually exported ARshort03 once with confirmed cleanup. Its2 physical pages passed root/independent qualified visual review; raw PDF extraction order remains qualified. FRlong01 completed full real source review and5 translated pages before page6 APIConnectionError. The exact partial DOCX containing the five completed source-page translations and incomplete accounting, including an unreleased USD 0.119 uncertain hold, are genuine evidence; no complete FRlong/formatting/native pass is claimed. All four independently reviewed UI fixes were validated in the isolated acceptance worktree and published through PR #300. The final affected suites passed 39 tests; the combined Full wrapper passed 639 selected tests plus compilation and docs/hygiene checks, using the documented direct-Dart fallback. Full is not the complete repository pytest collection. Post-edit closeout docs validation is recorded separately. Desktop and narrow normal-click partial downloads passed using fictional loopback bytes; the actual French download used its observed artifact URL before the UI fix. No paid translation was replayed. The separate live Gmail check is complete with qualifications on canonical main: after user-operated read-only Google consent, Advanced message details loaded the retained exact message once, both real PDFs were previewed (1 and 8 pages), and one one-page English translation session was prepared without starting it. This is manual recovery after the original failed extension handoff, not one seamless extension-to-output pass. The first preparation returned 422 because the fresh output folder did not exist; creating it allowed one corrected successful preparation. Four actual Word readiness probe/canary pairs all reported cleanup. The fourth was an automatic consequence of the 422 response and exceeded the original three-assessment bound; dependent work stopped for inspection before the narrowly scoped recovery. No legal-document export, provider dispatch, draft or send occurred in the Gmail check. The owned browser/server are closed, both ports are free and Word process count is zero. Saved defaults and the lifetime ledger are unchanged; the canonical readiness journal advanced with exact beforeimages, while the separate native05 historical journal is unchanged. The authorized read-only helper account remains connected. Final Gmail evidence is `gmail_live_01/continuation_20260924_01/final_intake_receipt_01.json` (SHA256 `a615652036f4fa741c309574a99dc78d0dc89d3fea6d92c6e714b4cb7d9be0e5`). Synthetic tests, direct artifact downloads and actual visible-browser end-to-end evidence remain distinct.

PR #298 publication (2026-09-17): exact head `b3480e092530ab65d569842ba488a3d7c1e6f102` completed required Windows and Linux CI successfully. All four PR/push Windows and Linux jobs succeeded. Both complete pytest runs passed 6,607 tests with two dependency-deprecation warnings each (PR 2,314.94 seconds; push 2,201.84 seconds); each targeted core run passed 34 tests. Original failed runs remain preserved. Canonical `main` was updated to merge `751185fca959ab074c328050adf0184f5db76d2c`. Fresh owned isolated verification result was recorded at 2026-09-17T19:44:52.8118593Z and checked runtime identity, asset version `3a37cb2d78df` and every one of 93 served static assets against disk, with recorded process ownership and cleanup. During this isolated verification, no provider, OCR, Word readiness/native, Gmail, OAuth or external network operation was performed; saved settings, lifetime ledger and Word journal hashes were unchanged. This is HTTP/runtime/static evidence, not browser JavaScript execution or another document acceptance. The HTTP verification applies to that implementation revision. Later documentation-only revisions do not change its evidence scope or establish a new HTTP-tested HEAD. The [publication closeout](exec_plans/completed/2026-09-17_arabic_acceptance_publication.md) pins CI and the retained HTTP receipt.

Arabic closeout (2026-09-17): E (`_05`) completed nine genuine ordinary pages with12 settled calls and qualified bilingual review of147 immutable mappings. Its frozen runtime snapshot passed627 selected Full tests. Actual submitted formatting, same-nonce build recovery and browser download bind a60342-byte DOCX; complete package checks passed147 fragments,460 runs and five editable tables. One real native Word export produced nine PDF pages, all individually visually reviewed, with confirmed cleanup. Strict extraction remains nonexact because of five extra shadda in the PDF font Unicode map; a separate font trace reconciles that copy/search-text qualification, while the original report remains unchanged. No missing characters were found; character counts alone do not prove reading order or visual quality. Later formatting UI fixes passed24 focused tests, Chromium geometry checks with fictional service responses, and the final Full wrapper:629 selected passes,9 deselected, compilation and direct-Dart docs/hygiene fallbacks after the known AOT255 failures. This is not a complete-repository pytest run or evidence that E used the later UI fixes. Retain the initial build decline/recovery, original terminology concern with qualified disposition, all paid/zero-dispatch failures and the two consumed pre-native failures. Current spend and the unchanged original USD 10 budget are recorded in [HANDOFF.md](HANDOFF.md).

A passing targeted suite, Full wrapper, full pytest run, real browser check and real-document acceptance are separate evidence. Report unexecuted or unavailable checks plainly. Before publication, use [COMMIT_PUBLISH_WORKFLOW.md](workflows/COMMIT_PUBLISH_WORKFLOW.md), exact-head required CI and the user’s current scope.

Detailed historical validation, Google Photos/native cases and old PR-specific coverage are retained in the [dated public guide](history/2026-09-16_front_doors/VALIDATION.md), which discloses one fictional personal-name substitution. The exact original remains privately preserved. Its old budgets, pending instructions and stage tokens are historical, not active execution authority.

For reviewed-source startup failures, distinguish a zero-dispatch job from translation failure. Public fixed diagnostics may intentionally omit the originating exception. Preserve actual job/accounting and browser timing, then reproduce suspected concurrency using disposable fictional evidence; overlapping requests alone do not prove the hidden exception. Keep normal same-review read serialization separate from rejection of an unrelated operation holding the run workspace.

When an Arabic PDF extraction differs from the reviewed DOCX, preserve the strict report and inspect the actual font mapping and affected glyphs before classifying the difference. Font-to-Unicode expansion can affect copy/search text while visible glyphs remain correct. Retain exact bytes, independently inspect every affected page, and label a reconciled extraction limitation explicitly; never strip arbitrary Arabic letters or diacritics to manufacture an exact pass.

## September24 final closeout checks

After the scoped documentation update and archive of 11 completed plans, direct-Dart agent-docs and workspace-hygiene checks passed, and git diff --check passed. Exact output is private `resume_20260924_01/post_edit_validation_01.json`; final reporting edits have a separate validation receipt. Independent actual Gmail evidence review `gmail_live_01/continuation_20260924_01/independent_intake_evidence_review_01.json` (SHA256 `8c39d3c1b1b2b28a7d0b061366616a1e266bc1924f1f22d95b62922e50d017e6`) found no material unresolved intake issue within the stated qualifications. These checks do not establish a complete French output or publish the isolated fixes.
