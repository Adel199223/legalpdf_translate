# Current handoff

Updated 2026-09-24. Read `AGENTS.md` and `agent.md`, then this page. Current user instructions and explicit operation scopes take precedence over historical continuation text. Do not replay a consumed provider/native helper or renew an obsolete stage/credit gate merely because an old file says pending.

## Current direction: Gmail-to-output and advanced formatting

The user now authorizes autonomous completion of the actual Gmail attachment-to-finished-output workflow and advanced-formatting verification, including necessary computer/API and native work and fixes for demonstrated related problems. Continue the [active readiness plan](exec_plans/active/2026-09-24_gmail_formatting_readiness.md). Gmail execution belongs on canonical main; feature work belongs in `C:/Users/FA507/.codex/worktrees/gmail-formatting-readiness/legalpdf_translate`, branch `codex/gmail-formatting-readiness-20260924`, based on `5f49b7363963cb78691e95ef5d0e15b2b1977b82`. Private evidence is under `C:/Users/FA507/.codex/legalpdf_translate_private_benchmarks/gmail_formatting_readiness_20260924_01/`. Sending email, public hosting and saved-default promotion are excluded. An unsent draft is conditional on supported existing permissions; the connected helper has read-only Gmail authorization.

The preceding everyday-app update is complete. The actual private `C:/Users/FA507/.codex/legalpdf_translate_private_benchmarks/app_update_publication_20260924_01/final_publication_receipt.json` records `publication_and_canonical_application_complete`: PR #302 merged at `5f49b7363963cb78691e95ef5d0e15b2b1977b82`, canonical main was clean and matched origin/main, and all four expected CI jobs passed. Each of the two Windows runs passed 6688 full-suite tests plus 34 targeted tests; these are separate runs, not additive coverage. Fresh corrected HTTP operation02 verified the real canonical identity and 93 static assets with offline services, then stopped its owned server. This does not establish browser JavaScript, real-document or new Gmail/native/provider acceptance. The old authoring worktree and merged source branches were removed after preservation. The [publication closeout](exec_plans/completed/2026-09-24_everyday_app_update_publication.md) remains historical authority; do not replay it.

Current readiness work has not completed a new paid translation, native output review or seamless Gmail acceptance. The optional manager accounting seam passed 67 targeted tests and `full_01` passed 641 selected tests with unchanged source pins. Actual shadow operation `shadow_01` uploaded the three-page fictional packet, then source preparation failed because a fresh profile had no settings file. The isolated fix removes that unintended existing-file requirement from source registration and later formatting lookup without saving defaults; its new regression and Full results must be checked separately. The failed run and zero-reservation accounting are preserved, and its owned server stopped cleanly. A fresh operation must use a new source/output/workspace, not replay that claim. The existing portable OCR installation is verified and can be supplied through child-only environment values. Successful Word readiness checks remain separate from real output acceptance. The current app deliberately separates Gmail-prepared ordinary jobs from manual reviewed-source formatting, so acceptance still requires both genuine tracks.

On2026-09-24 the user explicitly set aside the small uncertain FRlong01 page6 charge and historical connection cause, asked to revisit only if the problem recurs, and directed work toward making the app ready for normal use. This supersedes earlier continuation text requiring original-charge resolution before all progress. It accepts uncertainty; it does not establish a charge, refund or environmental cause.

The [everyday-app preparation](exec_plans/completed/2026-09-24_everyday_app_update_preparation.md) and its private `app_readiness_forward_20260924_01/` evidence are historical preparation for the completed PR #302 publication. Their earlier awaiting-decision status is superseded.

The retained budget block belongs to the historical private acceptance campaign, not the ordinary app. Preserve its USD0.119 hold, all 226 ledger rows, five-page partial and original USD10 limit. The fresh shared ordinary USD3 phase is prepared with explicit standard tier, existing output capacities, one in-flight request and at most 16 reservations. Reuse its existing accounting across fresh owned servers; do not create or reset it. The earlier self-imposed USD1 allowance cannot cover the USD2.492 full-capacity ordinary reservation. Old known spend USD3.92137740 plus the old hold and full new USD3 phase totals USD7.04037740, within USD10. This is an additive external acceptance cap, not a browser-form setting or clearance of the old block. `shadow_01` closed with zero reservations; check the current ledger before later dispatch. Preserve saved defaults and record actual charges and uncertainty under the new plan.

## Preserved French reconciliation and tested patch

The [continued regression assurance plan](exec_plans/completed/2026-09-24_frlong_regression_assurance.md) completed bounded local work in the former isolated worktree before PR #302 publication. Six additional private offline transport probes and13 source-review browser tests passed. The missing independent-work guidance is repaired, preserving exact-operation recovery and all nonce guards. Full passed641 selected tests; the supplement passed6051, with five overlapping tests:6687 unique passes and one actual COM test deliberately excluded. The runs overlapped in time with separate temporary data roots and identical frozen source snapshots. Compilation/docs/hygiene passed, including the documented direct-Dart fallbacks. Assurance evidence is under `C:/Users/FA507/.codex/legalpdf_translate_private_benchmarks/frlong_regression_assurance_20260924_01/`; `final_assurance_receipt.json` indexes final validation and preservation. These tests do not resolve the historical billing/cause questions; publication is recorded separately above.

Safety preflight found a separate damaged local dependency: the canonical `.venv311` was missing the installed `backports.tarfile`1.2.0 `__init__.py`, preventing `keyring` import. The one absent file was restored exclusively from the exact cached wheel, matching its installed RECORD hash. Existing package files, dependency versions, Python, keys and settings were preserved. Null-backend import and the unchanged safety preflight then passed. The reason that file disappeared is unknown; this does not explain FRlong01, whose first five provider calls completed. The current direct connection also returned HTTP401 to one unauthenticated `/v1/models` GET; this shows present direct HTTP reachability only. Both failed preflights and the repair receipts are retained in the new assurance directory.

The [FRlong01 reconciliation](exec_plans/completed/2026-09-24_frlong_readonly_reconciliation.md) was authored on `feat/frlong-reconciliation-20260924` in the now-removed `C:/Users/FA507/.codex/legalpdf_translate_frlong_reconciliation`, based on `f5002be85e33601a8c5f4e13c5fbd2d7dfd209d7`, and its tested fix was published through PR #302. Initial read-only checks matched all six protected publication hashes and found no relevant server/Word process. The independently rechecked226-row ledger and all historical prefixes/campaigns remain unchanged: USD3.92137740 known, USD0.119 held, USD5.95962260 available within the original USD10 limit before the new additive phase. The five-page partial DOCX still matches its41355-byte receipt.

Page6 uniquely joins the reservation and ordinary dispatch at UTC08:57:29–08:57:46 on September24, but no authoritative usage, charge/refund, response ID or underlying connection cause was retained. The hold and `ordinary_multilingual_browser_unknown_cost` block stay intact. The bounded Windows event metadata queries returned no events in four named logs for UTC08:55–09:00; that absence does not establish a cause or exclude a connectivity fault. Fresh private receipts are under `C:/Users/FA507/.codex/legalpdf_translate_private_benchmarks/frlong_reconciliation_20260924_01/`: start with `root_evidence_reconciliation.json` and `independent_accounting_review.json`. Original receipts/helpers remain unchanged and consumed. Public documentation is explanatory only; it cannot settle this individual charge.

After the user authorized computer/API investigation and signed in, the provider dashboard for September24/Default project/All API keys showed5 requests,4648 input tokens and4486 output tokens, exactly matching the five known French completions. Responses logs showed0 results. This is useful aggregate corroboration, not page6-specific or finalized noncharge evidence; the hold remains. `platform_usage_observation.json` records that bounded live read. The retained `RECONCILIATION_AND_RETRY_PROPOSAL.md` prepared a conditional fresh nine-page ordinary-browser proposal with at most13 new calls and USD1.547, within the existing campaign/lifetime caps. It originally required authoritative hold disposition; the latest user decision supersedes that historical prerequisite, while fresh supported budgeting and paid scope remain necessary. It is not an armed helper. Backend original-owner restore exists, but ordinary-browser restart and relocated-copy resume are not established by that capability.

The demonstrated local defect is missing structured transport diagnostics: the SDK cause survives in memory but the structured failure handler discarded it from run events. The isolated fix retains only fixed allowlisted transport class labels in the existing `api_call_failed` event when diagnostics are enabled. It does not recover the historical cause or change retries, accounting, API payloads, defaults or source/target text. See [VALIDATION.md](VALIDATION.md) for current no-provider checks. The earlier private retry proposal remains preserved historical preparation; its mandatory original-billing prerequisite is superseded by the user's current acceptance of uncertainty. A future paid operation still needs a fresh supported, conservatively budgeted execution scope; none has run.

## Completed requested testing

PR #300 merged at `1722cf112f65b40c37ba4bb864c59be987973d13` after green checks for `dd208272733e1835bb336ef1a97ddf1968f0de0d`. Canonical HTTP/static verification passed for that merge; detailed CI and runtime limits are in [VALIDATION.md](VALIDATION.md). This follow-up changes only documentation and four CI references. For its final PR/CI, current main and cleanup state, consult private `C:/Users/FA507/.codex/legalpdf_translate_private_benchmarks/application_multilingual_publication_20260924_01/final_publication_receipt.json` and `FRESH_CHAT_HANDOFF.md`. Read those actual results before assuming final merge or clean state. The [publication closeout](exec_plans/completed/2026-09-24_multilingual_acceptance_publication.md) preserves the work and closure route.

The requested broader real-document testing across EN/FR/AR and separate live Gmail check are complete as bounded execution, with the failures and qualifications below. The [multilingual/Gmail ExecPlan](exec_plans/completed/2026-09-17_multilingual_gmail_acceptance.md) and [September24 recovery](exec_plans/completed/2026-09-24_acceptance_interruption_recovery.md) retain the full record. The concrete fixes authored in `C:/Users/FA507/.codex/legalpdf_translate_multilingual_acceptance`, based on `7a86cd07a28496eecfbe3cc7e69398a611242184`, are published through PR #300. Final source-branch/worktree disposition is recorded in the private final publication receipt; the original authoring path is historical provenance, not a required current checkout. The live Gmail check used that unchanged canonical main. Historical completed acceptance helpers remain consumed.

Actual results: ENshort01 and FRshort02 completed normal-browser source review, genuine translation, explicit formatting/build/download and native Word export; every physical page was individually reviewed, with wording/source-ambiguity qualifications retained. FRshort01 and ARshort02 remain consumed paid failures with no output. ARshort01 remains a consumed zero-OCR/zero-provider setup refusal. The narrow French prompt, Arabic citation, cost-reporting and formatting-recovery fixes were tested in the isolated acceptance worktree and are now published through PR #300. Required combined Full completed2026-09-18 with636 selected tests plus compilation/docs/hygiene;214 additional workflow tests passed. Those automated checks do not establish later document quality.

ARshort03 completed both pages in3 actual calls costing USD0.08979985. Complete bilingual review is qualified; the malformed source appointment/agency criterion is not certified equivalent. The actual39655-byte ordinary browser-downloaded DOCX passes exact package/content checks. Its optional formatting draft was interrupted before acceptance/build; preserve it without inventing acceptance. After an installed-Office update caused a pre-export preparation refusal, fresh native continuation05 performed one supported export with confirmed cleanup. The91089-byte PDF has2 physical pages; root and independent reviewer individually inspected both and found no material rendering blocker. Raw extraction order remains qualified, while all target characters are present. FRlong01 then completed actual source review for all9 pages and started once with accepted revision `8fdac47b020b4610a6e49e748cac3bed`. Five pages completed; page6 failed with APIConnectionError and no received response/usage. Pages6–9 have no committed translation. The partial DOCX containing the five completed source-page translations is preserved and exactly matches the completed targets; its artifact route downloaded successfully through an observed URL, while the visible partial-download control was hidden. The isolated fix now exposes a clearly incomplete partial-download card and makes Open run navigate to the selected run; its post-fix browser proof uses fictional data. This is a failed complete-document test, not French end-to-end acceptance. The owned case server has stopped; no helper, paid case or native operation may be replayed.

The inclusive campaign ceiling remains USD4/30 attempts inside the original USD10 lifetime cap. Final FRlong accounting confirms226 lifetime rows:225 finalized and1 uncertain, USD3.92137740 known spend, USD0.119 retained hold and USD5.95962260 available after that hold. Campaign17 attempts have USD0.35294595 known spend plus the same hold. FRlong used6 SDK attempts:5 measured completions totaling USD0.07093800 and1 uncertain page6 request. No transport retry or refund occurred. Original209/213/215/217/220 prefixes, nine historical campaigns, both saved settings and all230 frozen source files were verified before ending the case. The blocked uncertain request remains intact. Actual known/partial/unknown accounting now has real-document evidence; the aggregate total correctly stays unknown. The underlying connection cause was not retained beyond APIConnectionError and must not be invented.

The separate live Gmail check is complete with qualifications on canonical main: after user-operated read-only Google consent, Advanced message details loaded the retained exact message once, both real PDFs were previewed (1 and 8 pages), and one one-page English translation session was prepared without starting it. This is manual recovery after the original failed extension handoff, not one seamless extension-to-output pass. The first preparation returned 422 because the fresh output folder did not exist; creating it allowed one corrected successful preparation. Four actual Word readiness probe/canary pairs all reported cleanup. The fourth was an automatic consequence of the 422 response and exceeded the original three-assessment bound; dependent work stopped for inspection before the narrowly scoped recovery. No legal-document export, provider dispatch, draft or send occurred in the Gmail check. The owned browser/server are closed, both ports are free and Word process count is zero. Saved defaults and the lifetime ledger are unchanged; the canonical readiness journal advanced with exact beforeimages, while the separate native05 historical journal is unchanged. The authorized read-only helper account remains connected. All four independently reviewed UI fixes are published through PR #300; their retained local and fictional-browser checks remain distinct from real-document acceptance. The final affected suites passed 39 tests; the combined Full wrapper passed 639 selected tests plus compilation and docs/hygiene checks, using the documented direct-Dart fallback. Full is not the complete repository pytest collection. Post-edit closeout docs validation is recorded separately. Desktop and narrow normal-click partial downloads passed using fictional loopback bytes; the actual French download used its observed artifact URL before the UI fix. No paid translation was replayed. Bounded private evidence: `gmail_live_01/continuation_20260924_01/final_intake_receipt_01.json`, SHA256 `a615652036f4fa741c309574a99dc78d0dc89d3fea6d92c6e714b4cb7d9be0e5`; `cases/fr_long_01/actual_partial_cost_reconciliation_01.json`, SHA256 `1867d0b514ec0662cdc00c085ca55effc9026297725e0b6e462018e091c86bee`; independent accounting review `0fd39e6f72403b6cbd9acfa9ce2a7296bdef2a707303454f60b0cc74d0b2314f`. Gmail proof distinguishes retained response arrays from stale observer counters, the failed first HTTP-body save from the matching cached/prepared bytes, and nonblank preview/navigation evidence from legal visual acceptance or exact bitmap equality. The preserved preparation failure and native-bound overrun are not erased by the successful recovery.

## Historical September17 acceptance and publication

Historical September17 status: the authorized Arabic reliability publication is complete. PR #298 merged at `751185fca959ab074c328050adf0184f5db76d2c` on 2026-09-17. The [publication closeout](exec_plans/completed/2026-09-17_arabic_acceptance_publication.md) records exact-head CI, canonical update, owned isolated HTTP verification and cleanup. The accepted real-document results below remain qualified. No new paid/native acceptance, live Gmail, public hosting or global-default promotion was included.

The authorized [full Arabic normal-browser acceptance](exec_plans/completed/2026-09-17_arabic_normal_browser_acceptance.md) has completed with the qualifications below. PRs #296 and #297 are earlier published history. PR #298 publishes the accepted fixes authored on `feat/arabic-browser-acceptance-20260917` in `C:/Users/FA507/.codex/legalpdf_translate_arabic_acceptance`. Canonical `main` was verified at implementation merge `751185fca959ab074c328050adf0184f5db76d2c`; its owned HTTP verification result was recorded at 2026-09-17T19:44:52.8118593Z with asset version `3a37cb2d78df`. The HTTP verification applies to that implementation revision. Later documentation-only revisions do not change its evidence scope or establish a new HTTP-tested HEAD. Bounded signature/witness, Arabic prompt/citation, source-startup locking and recovery-copy fixes supported the genuine nine-page E run. Later formatting UI fixes close the originating overflow menu and suppress image reads during busy actions; they passed24 focused tests, separate Chromium geometry checks and the final Full wrapper with629 selected tests. They were added after E artifact capture. One actual native Word export produced nine pages, all individually visually reviewed. Keep exact tested inventories and historical failures separate; public hosting, live Gmail and global-default promotion remain separate work.

Historical cases A (`_01`) and B (`_02`) are consumed failures, preserved with their partial outputs. A reviewed all nine source pages and committed two Arabic pages before a strict signature-literal failure on page 3. Three calls settled at USD0.038101. A narrow one-blank signature-spacing fix validated the unchanged failed response offline with its exact glossary; its 807 focused tests and required Full wrapper passed. B job `tx-efa7983bc234` committed pages 1–3 and stopped on page 4 after genuine primary/correction compliance failures. Five calls settled at USD0.15160670. The historical A/B checkpoint was USD0.18970770 combined acceptance spend, 186 finalized lifetime rows, zero holds and USD6.98499555 remaining; the current E accounting below supersedes that balance.

Historical case C (`_03`), job `tx-7f6ecea646a8`, is terminal `compliance_failure` on page 8 after seven committed pages; page 9 had no call. The nine-page source revision was explicitly accepted and the pre-C snapshot passed 871 focused tests and the required Full wrapper with 624 selected tests plus compilation/docs/hygiene. Those results did not establish translation quality: independent bilingual review found a material page 4 imperative translated as a completed action. The exact effective glossary/addendum did not force that wording. Separate exact-contract diagnosis found that page 8’s citation parser excluded Arabic comma U+060C as the closing punctuation after a coordinated article; both unchanged responses retained the required citation counts. All responses and failures remain preserved. The seven committed pages have 121 reviewed source/target groups; these are partial mapping evidence, not formatting acceptance or full-document quality.

C settled at USD0.26177970; historical A/B/C combined spend was USD0.45148740. D (`_04`) accepted all source pages but failed before any provider call or reservation and spent USD0. Actual timing plus a deterministic fictional regression support its loader/image-read race diagnosis; the original exception was not retained. Both stopped cases remain preserved. Historical E (`_05`) job `tx-f64406b60cec` completed nine pages with12 actual calls costing USD0.29164730. Inclusive A–E acceptance spend is USD0.74313470. At the September17 closeout, the original USD10 lifetime ledger had209 finalized rows, USD3.56843145 spent, zero holds and USD6.43156855 remaining. Saved canonical and isolated defaults were unchanged at that checkpoint.

E has complete qualified bilingual review and147 exact unchanged source/target mappings. The page4 imperative is preserved in the actual target. Page1's procedure label remains a precision qualification, with the original adverse review and separate independent/root disposition preserved; exact Portuguese procedural nomenclature is not certified. Actual ordinary formatting revision `d3df8a8a4bae43d2b5efed2ab21803db` built through same-nonce recovery after a pre-intent own-image run-busy decline. The real browser download matches the60342-byte reviewed DOCX SHA256 `f353627959afe208e2d1ffbb46714224d060695750df6d2c9107028e594c29b2`. Complete package checks passed147 fragments,460 runs and five editable tables. Native operation03 produced the209806-byte nine-page PDF SHA256 `22ddf036428f92a3c3569f3a654216cd1a820399df4adc9c4fbb61d466f0e61f`, with confirmed Word cleanup. All nine pages passed qualified visual review. Strict PDF extraction remains nonexact: five extra shadda arise from Word's font-to-Unicode mapping; separate font evidence reconciles that extraction/search-text limitation without changing artifacts. No characters are missing by the per-page comparison; this does not by itself establish reading order. Exact graphic/geometry parity is not certified. Use the acceptance plan for exact receipts and qualifications. Original and intermediate doc beforeimages remain preserved, including E `docs_before_final_closeout`.

The reviewed-source/formatting implementation was published through PR #296 at merge `068e4312269106e26677185ae33bc1b556e3c073`, after required CI passed on exact head `79fecff413414ebac927b5a9cacdf78b3077bc22`. Canonical app verification completed on `2026-09-16T22:23:49.151967+00:00` with scope and limits recorded in the [publication closeout](exec_plans/completed/2026-09-16_reviewed_candidate_publication.md). The real-workflow acceptance remains qualified. There is no active master roadmap. Global protocol/model/effort defaults remain unchanged and the older activation plan stays deferred.

| Role | Path / branch |
| --- | --- |
| Canonical daily-use C | `C:/Users/FA507/.codex/legalpdf_translate` / `main` |
| Arabic acceptance authoring at capture | `C:/Users/FA507/.codex/legalpdf_translate_arabic_acceptance` / `feat/arabic-browser-acceptance-20260917` |
| Retained integration R | `C:/Users/FA507/.codex/legalpdf_translate_structured_activation`; inspect current Git branch before work |
| Isolated authoring W | `C:/Users/FA507/.codex/legalpdf_translate_reviewed_regions` / `codex/reviewed-regions` |
| Private evidence P | `C:/Users/FA507/.codex/legalpdf_translate_private_benchmarks` |

The historical PR #296 app verification used `068e4312269106e26677185ae33bc1b556e3c073`; PR #297 then advanced documentation to `2e31edccad2252c20d55332fe5167c7ff439460f`. Historical PR #298 implementation verification is recorded above. Recheck Git and owned-process state before work. Original canonical documentation was preserved before the update: all 13 original files retained byte-for-byte in private `application_publication_20260916_01/canonical_preservation_01` and path-scoped stash `4786048b40a32c11f195e09379c19312f71371ee`; preservation receipt SHA256 `2773310538f2f6fe546d5a9ddf7468b28ea3cca14cc11ae88041ea3342b47697`. Do not automatically apply or drop the stash. Integration-worktree disposition: retained for scoped documentation closeout from the merged main revision; the fully merged feature branch was removed locally and remotely. Recheck actual Git state before new work; retained private/ignored evidence is not disposable. Do not repeat completed publication or acceptance operations.

## Historical evidence: keep the categories separate

Current campaign results are recorded in the [multilingual plan](exec_plans/completed/2026-09-17_multilingual_gmail_acceptance.md) and [September24 recovery](exec_plans/completed/2026-09-24_acceptance_interruption_recovery.md). The table below retains earlier publication and acceptance scopes; it does not supersede the current partial/failed French result or the qualified manual Gmail recovery result.

| Evidence | What it supports |
| --- | --- |
| PR #298 publication validation | Exact CI head `b3480e092530ab65d569842ba488a3d7c1e6f102`: All four PR/push Windows and Linux jobs succeeded. Both complete pytest runs passed 6,607 tests with two dependency-deprecation warnings each (PR 2,314.94 seconds; push 2,201.84 seconds); each targeted core run passed 34 tests. Original failed runs remain preserved. Canonical merge `751185fca959ab074c328050adf0184f5db76d2c` passed owned HTTP/runtime/static checks for 93 served assets, asset version `3a37cb2d78df`; no browser-JavaScript or new real-document acceptance claim follows. |
| Actual Arabic case E (`_05`) | Completed nine-page real translation, qualified bilingual review of147 immutable mappings, actual formatting acceptance, same-nonce build recovery and matching browser download; complete package/RTL/font/table checks passed. One actual native Word export and individual nine-page visual review completed. Procedure terminology, text-only layout and five extraction-only diacritics remain explicit qualifications. |
| Actual Arabic case C (`_03`) | Terminal page 8 failure, seven committed pages and page 9 unattempted. A material page 4 modality error blocks clean quality/formatting acceptance; page 8 has a separate diagnosed citation-punctuation parser defect. The 121 mapped groups and two uncommitted page 8 linguistic reviews remain partial evidence. |
| Reviewed private FR/EN and full Arabic derivatives | Completed declared text-only profiles with their recorded limits; not automatically ordinary-user-path acceptance. |
| Synthetic source-to-formatting browser run | Actual public UI upload/review/Workflow/build/download and exact recovery using fictional OCR/SDK; no real translation quality claim. |
| Actual Arabic ordinary partial rebuild (RP02) | Saved 2-of-9-page ordinary rebuild and expected unsupported formatting decline, with no provider/native call; not complete ordinary translation. |
| Actual ordinary French FR04 | Genuine completed two-page, two-call ordinary run and normal reviewed formatting/native export; 34 unchanged target fragments and editable 3-cell metadata. Qualified term-assisted bilingual/full-page visual acceptance. The strict extraction check remains failed on one trailing-space cell-bound predicate; separate text/ownership and visual evidence support the qualified disposition. Failed operations remain preserved. |
| Historical PR #296 regression | Corrected publication head `79fecff4` passed fresh CI: 6,477 tests, 2 warnings. Local Full passed 624 cases; six offline SDK transport tests and docs/hygiene checks also passed. The earlier pre-correction 6,477-test/481-file receipt remains separate. Exact evidence and scopes are in the [publication closeout](exec_plans/completed/2026-09-16_reviewed_candidate_publication.md). |

For **historical September17 build results**, start with the [publication closeout](exec_plans/completed/2026-09-17_arabic_acceptance_publication.md); the [Arabic acceptance closeout](exec_plans/completed/2026-09-17_arabic_normal_browser_acceptance.md) describes its historical document evidence. Earlier qualified results are retained in:

1. [Ordinary browser integration](exec_plans/completed/2026-09-16_ordinary_browser_integration.md).
2. [Arabic rendered corrections](exec_plans/completed/2026-09-16_arabic_render_corrections.md).

Those closeout plans own exact artifact identities, outcomes and qualifications. Read older acceptance plans only for a specific referenced operation. The small paired profiling result is not proof of the overall workflow-efficiency target.

## Working rules that remain current

- The user authorized useful scoped Docs Sync without another question. Maintain these entry points as facts change; preserve dated evidence before replacing current guidance.
- The user's Astra Ultra preference is for the assistant/Codex task. Terra model/effort evaluation is an app-provider decision. Do not change either implicitly or promise unavailable model/tool capabilities.
- Use tools actually available in the current session. Identify the app/build/owned tab or process from evidence before acting; never infer live state from a stale screenshot or old server asset version.
- Saved OCR, retention, page-break and bidi settings remain authoritative. Reviewed derivatives require explicit profile/revision selection; unknown operations do not authorize retries or automatic latest-revision selection.
- Preserve complete legal content, editable DOCX, source uncertainty, existing findings and accounting. Original documents, the lifetime ledger, Word journal and consumed helper evidence remain intact.
- PR #302's Git/PR/CI/merge lifecycle and canonical static verification are complete. The current readiness plan separately authorizes necessary bounded live Gmail, paid and native acceptance. Use the isolated follow-up branch for fixes; sending, public hosting, global default promotion, destructive data operations and history rewrites remain excluded.
- Keep runtime fixtures, logs and private legal evidence outside Git. Do not reset dirty worktrees or delete old evidence merely to obtain clean Source Control.

### Historical FR04 qualification

FR04 uses the normal run-specific glossary for `ofício de notificação` → `courrier de Notification`; it is explicitly term-assisted. Review `24bf4035` accepts the unchanged text for formatting with no material findings, while disclosing source-clause interpretation, configured `Le juge` gender precision and retained `d.s.`. Normal run-owned formatting and native export completed; both full pages passed qualified visual review. Final synthesis `b1b82a8e` binds the qualified disposition and its separate evidence. The strict extraction report remains failed solely on one trailing space beyond its metadata-cell bound; complete text/nonspace ownership remains exact. Crest, drawn-rule, partial-bold and exact-geometry parity are not claimed. The historical pre-Arabic lifetime checkpoint was 178 finalized rows, USD2.825296750000 spent and zero lifetime holds; use the completed acceptance plan and original ledger for current accounting.

## Next concrete action

Continue the [Gmail-to-output and advanced-formatting readiness plan](exec_plans/active/2026-09-24_gmail_formatting_readiness.md) from its actual latest progress: finish and validate supported shared-budget wiring before paid starts, establish current canonical runtime/extension ownership, then execute the bounded real Gmail and manual-formatting tracks and review every finished output page. Do not promote fixture preparation, mocked tests or prior HTTP/static proof into actual acceptance. Preserve the old hold, block, ledger, partial and all consumed operations. Historical ENshort01, FRshort02 and ordinary ARshort03 remain qualified passes; AR03 optional formatting remains unaccepted, FRlong01 remains incomplete, and earlier Gmail evidence covers intake/preview/preparation only. Original page6 diagnosis remains shelved unless the problem recurs. Exact pre-sync versions of these current guidance files are preserved under [the September24 readiness history](history/2026-09-24_gmail_formatting_readiness/HANDOFF.md).

Historical September17 closeout: the [publication lifecycle](exec_plans/completed/2026-09-17_arabic_acceptance_publication.md) is complete. No execution remains in that earlier Arabic E acceptance: its owned server/tab are closed and Word cleanup is confirmed. Its native operations01/02 are consumed pre-process failures and operation03 is consumed success. Do not replay those helpers or restore their earlier Word journal. A/B/C paid failures, D's zero-dispatch failure and E's original build decline remain preserved. Its fictional checkbox-label finding was deferred at that closeout; the later actual AR03 overflow and isolated fix belong to the current campaign. Use [VALIDATION.md](VALIDATION.md) and the completed acceptance plan for historical evidence. The [original activation plan](exec_plans/active/2026-09-10_structured_translation_ordinary_use.md), global defaults and the separately unproven >=20% model-efficiency goal remain unchanged.

The previous long handoff is preserved [byte-for-byte in dated history](history/2026-09-16_front_doors/HANDOFF.md). [NEW_CHAT_HANDOFF_2026-09-15.md](NEW_CHAT_HANDOFF_2026-09-15.md) and [FRESH_CHAT_BRIEF.md](FRESH_CHAT_BRIEF.md) are historical pointers, not current stage gates.
