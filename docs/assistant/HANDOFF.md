# Current handoff

Updated 2026-09-26. Read `AGENTS.md`, `agent.md`, this page and only the applicable current plan. Current user authority takes precedence over historical continuation text. Consumed provider/native helpers must not be replayed.

## Current build and work

The implementation baseline is applied PR #307 at `2a6eb5962820b3c0f015dc6e296516ff2dac8049`. On 2026-09-26 the user explicitly approved publishing/applying the reviewed fixes and continuing all remaining selected translations without routine reconsent. Publication is in progress; exact PR/check/merge/application state belongs in `C:/Users/FA507/.codex/legalpdf_translate_private_benchmarks/five_oldest_requests_20260926_01/publication_01/publication_receipt.json` and the actual repository state. Do not infer current HEAD or completed publication from the implementation baseline. Current work remains [five oldest unanswered translation requests](exec_plans/active/2026-09-26_five_oldest_translation_requests.md), operated sequentially through the genuine canonical app UI to unsent drafts. Gmail reads, these translations, normal native document review and draft creation are authorized; never send email or increase the original ceiling. Only requests from July2026 onward qualify. Exclude interpretation-only appointments and standalone notices to the translator. Request4's target is Arabic by explicit user correction of the source's Spanish instruction.

The isolated authoring checkout is `C:/Users/FA507/.codex/worktrees/word-startup-readiness/legalpdf_translate`, branch `codex/five-oldest-translations-20260926`. The demonstrated reviewed-word-count, citation-order and currency-decimal fixes passed focused regressions, independent review and final combined Full:664 selected test executions, exit0,812.809seconds, compilation and docs/hygiene (successful direct-Dart fallback after the known AOT255 issue). Product hashes remain frozen while the authorized publication/application lifecycle proceeds. Canonical runtime `five_requests_live_01` has retired through its owned stop control with zero active jobs, absent owner processes and free ports8877/8765. Native Word process count was zero at terminal verification. It is consumed and must not restart. Its served source was the implementation baseline/asset`a095ddd3a9f8`.

Requests1–2 have reviewed unsent drafts and verified ordinary downloads. Request1 is row92/227words with a corrected fee attachment; its original artifacts and stale historical batch snapshot remain preserved. Request2 is row93/1343words/USD0.72800070, with citation, identifier and three RTL decimal-display corrections; its first Save/Confirm and fee use the corrected count. All65 earlier rows/schema remain unchanged. Requests3–5 are unstarted. Review every output, recount any edited DOCX before first Save/Confirm, and verify draft recipient/thread/attachment bytes before creating the next request's reviewed receipt. Existing drafts are corrected in place; successful finalization must not be repeated.

The original USD10 limit and FRlong USD0.119 hold/block/partial remain unchanged. The first five-request phase finalized eight calls/USD0.81343395, with no new hold/block; together with older knownUSD4.19091240 and the preserved hold, lifetime exposure isUSD5.12334635. At mostUSD4.87656605 remains for a fresh successor, preserving maximumUSD9.99991240. Current evidence is `C:/Users/FA507/.codex/legalpdf_translate_private_benchmarks/five_oldest_requests_20260926_01/`; begin with the active plan, `operations/five_requests_live_01/terminal_root_verification_01.json` and applicable `reviews/request-NN/`. Exact beforeimages are in `docs_sync_live_01/beforeimages/`.

Fresh successor support for only unstarted requests3–5 passed60 no-provider tests and independent review in `runtime_support_v2/`, with the remaining allowance and a32000output-token hard ceiling. It is UNARMED, pinned to the earlier canonical head and has not launched. Before use, preserve those reviewed files, adapt fresh support to the actually approved/applied new source, separate immutable predecessor HEAD from successor HEAD, and re-review that binding. Do not infer readiness from file presence, change immutable old support or reset a ledger.

Request2's output review reproduced two local preprocessing defects: a generic `p.` footer anchor moved a body statutory citation after the real footer, and number-regex backtracking split decimal amounts at unit/punctuation boundaries. The minimal fixes passed55 focused synthetic regressions and bounded actual-source checks; the final combined Full above covers the frozen combined patch. Its terminal evidence is `validation_combined_01/result.json` and `wrapper.log` under the private root. Do not start request3 using the known faulty preprocessing. Complete the already-authorized publication/application lifecycle, then pin and review the actual applied source for the fresh successor before continuing requests3–5. The new authorization supersedes the earlier approval-pending text; it does not waive source, accounting or review checks. Earlier count-only Full and failed intermediate tests remain historical evidence. Remaining case/contact field distinctions are prepared privately in `reviews/remaining_requests_metadata_preparation_01.json`. Exact authorization-status docs beforeimages are under `publication_01/beforeimages/`.

## Historical test-performance publication

The completed scoped implementation is [test feedback performance](exec_plans/completed/2026-09-26_test_performance.md): measure bottlenecks and shorten feedback without reducing coverage, fixture isolation or external/native guards. The isolated checkout is `C:/Users/FA507/.codex/worktrees/word-startup-readiness/legalpdf_translate`, branch `codex/test-performance-20260926`, based on clean canonical `main` at `b35be1c3ad401ba70ca3dc4464f09bf64782958b`. Local implementation and validation are complete: Quick passed in 16.202 seconds; selected Full passed all 659 cases in 13m59s; complete four-worker validation passed 6,754 cases in 8m58s versus the unchanged 6,717-case baseline in 30m35s. Exact original coverage is preserved, with 37 new runner cases. Twenty later CI artifact-selector cases passed separately. This single local comparison measured 70.68% less wall time; hosted CI timing is unverified. The user explicitly approved publishing and applying this test-only change on 2026-09-26. Exact PR/check/merge/application status belongs in the private `test_performance_20260926_01/publication_01/publication_receipt.json`; inspect that receipt and actual checkout before resuming, rather than treating the authoring baseline as a permanent current head.

PR #306 publication and canonical application are complete at `b35be1c3ad401ba70ca3dc4464f09bf64782958b`; the publication receipt records clean canonical main, matching reviewed tree, four successful CI jobs and 6,717 full-suite plus 34 core passes in each Windows run. Those repeated runs are not additive unique coverage. The EN save-panel guard is applied; no runtime or Gmail acceptance was replayed for that fix.

Canonical daily use belongs in `C:/Users/FA507/.codex/legalpdf_translate`, branch `main`. Actual Gmail05 acceptance used clean docs-only PR #305 checkpoint `a8f795098f15685b106d4c354be6f7d531635b2d`, with PR #304 product source: the shared 45-second Word startup allowance passed 91 targeted tests, Full with 655 selected passes/nine expected deselections and all four exact-head CI jobs. Each Windows CI run passed 6,713 tests plus 34 core tests. One actual default-readiness pair passed with confirmed cleanup in 27.673 seconds. The [completed startup plan](exec_plans/completed/2026-09-25_word_startup_readiness_deadline.md) retains the evidence; no readiness replay is needed for documentation changes.

The [Gmail/formatting parent plan](exec_plans/completed/2026-09-24_gmail_formatting_readiness.md) and [Gmail closeout plan](exec_plans/completed/2026-09-26_gmail_finalization_and_arabic_closeout.md) retain completed scoped implementation and qualified acceptance. Their former authoring branch `codex/gmail-finalization-20260926` is historical provenance. Source commit `24f7ecbd80984223f0a55c012ab369b40ccfb720` passed 20 targeted no-provider tests, independent semantic review, all 659 selected Full tests/nine deselections and compilation. The Full wrapper exited 1 at docs-only AD046 markers; exact-marker repair and final direct docs/hygiene/whitespace checks passed separately. Preserve that failure and repair rather than relabeling Full as exit 0. Actual Gmail/native acceptance remains bound to `a8f7950`/PR #304 product source.

## Recorded acceptance and account permission follow-up

- **English formatting: qualified complete.** EN03 translated three pages, mapped 54 fragments in four editable tables and passed package checks plus visual review of all three native A4 pages. The private formatting matrix records columns, gutters, header/footer/folio ownership, document groups, alignments, separators, spacing and page-matched output. Its historical downloads remain blocked/unconfirmed; do not repeat its completed acceptance.
- **Arabic formatting: qualified complete.** AR02 completed one page in two fully costed calls for USD0.02136925. All 15 fragments passed the saved-decision and DOCX audits; the derivative has an editable 3x2 table, 40/60 widths, three pipes and a 109.4pt full signature gap. One native export completed in 6.593 seconds with owned cleanup; root/peer reviewed its sole A4 page. Retain room-identifier localization, borderless-table fidelity and non-codepoint-exact Arabic PDF extraction qualifications. AR01's partial failure remains preserved.
- **Gmail05 translation, download and saved batch item: qualified complete.** One genuine extension intake on canonical main used the same verified one-page source as Gmail03. Explicit EN/high/adaptive, Image Always, OCR Off and one worker produced one successful finalized call: 3,753 input / 1,658 output tokens, USD0.02977975. The normal browser download is byte-identical to the 38,490-byte app DOCX. Root/peer reviewed its one Word page against the actual source image; content is readable and complete within the recorded translation qualifications. Keep `source_block_layout_unavailable`: the scan yielded no extracted layout blocks, and the output is a linear reflow rather than source-layout replication. Normal Save added row **91**; Gmail confirmation reused row 91 without a duplicate.
- **Gmail05 finalization: qualified complete with its historical draft permission failure.** The normal finalization generated fee DOCX/PDF; root/peer reviewed the sole A4 page with matching case fields and 150-word count, without clipping, overlap or missing body text. The template prints word count, not a monetary total; profile/payment/legal correctness still needs professional review. The single unsent-draft attempt returned Google 403 `insufficientPermissions` (`draft_failed`); no draft or send succeeded in that operation. Independent finalization checks passed 28/28, preserving exactly 65 rows, all 64 prior rows/schema/settings/pins and the exact staged translation. Do not retry translation or draft creation to change this result.

The separate user-approved Gmail permission follow-up is complete. The same configured account was reconnected requesting `gmail.readonly` plus `gmail.compose`, without requesting modify/settings scopes. One fresh harmless recipient-free, attachment-free draft was created and read back successfully with `DRAFT` present and `SENT` absent; reading that new message also succeeded. Actual operations prove the exercised permissions; stored scope metadata alone does not. The test draft remains unsent. All 68 protected files, saved settings, budgets and historical outputs matched; no provider/native operation or Gmail05 replay occurred. This supersedes the current account-permission limitation, while preserving Gmail05's original 403. It does not turn that failed finalization into a successful real-message draft or authorize sending.

Gmail04's private pre-dispatch guard rejected a changed attachment token before any job or reservation. Bounded evidence proved unchanged source/message/thread/filename and a fresh genuine session; this was a private helper identity defect. Gmail04 stopped normally with zero jobs/charges/new rows. A separately created Gmail05 helper corrected only that predicate, passed 16 no-provider tests and 15 independent binding checks, and reused the same existing phase. All Gmail04 evidence remains intact; no product/Gmail contract changed.

## Runtime and preservation

Gmail05 served canonical ports 8877/8765, asset `54fa93db4c30`, and returned normally through its owned stop file with exit 0, zero active jobs, one new row 91 and unchanged launch settings. The independent terminal audit passed 28/28: owned runtime/native/Gmail-helper/focus processes were absent, ports 8877/8765/8893 were free, all 64 prior rows/schema/row 91/settings and 232 source plus 21 historical pins matched. The temporary draft staging copy was normally removed at shutdown after exact verification; durable translation/download/fee/source files remain intact. Earlier live03, shadow06, shadow07 and Gmail04 operations are stopped and consumed. Opening the downloaded translation also exposed an unrelated AutoRecovered Word document. The reviewed download was closed normally; the recovery document remains untouched. Native cleanup must prove absence of operation-owned processes, not close every Word window.

The original phase is exhausted at **16 finalized reservations / USD0.23975525 / zero request slots**, with no unfinished entry or new hold. The distinct Gmail completion phase is **USD3 / max4 sequential reservations**, now **one finalized reservation / USD0.02977975 / no new hold or block**. Its immutable operation identity remains `gmail_live_04`; actual consumer is Gmail05. Including the original known spend, preserved hold, exhausted-phase actual and the full new cap gives USD7.28013265 exposure under the unchanged USD10 ceiling. Do not reset, enlarge or reuse consumed operations; no more translation is planned for this scope.

Preserve all 226 historical ledger rows, FRlong01's USD0.119 uncertain hold/block and exact five-page partial. Page6 billing and the inner APIConnectionError cause remain unknown and shelved unless the failure recurs. The old private campaign block is not a global ordinary-app lock. Saved defaults remain unchanged; the earlier normal Gmail confirmation's sole `court_emails_by_city` metadata addition remains preserved. The original settings-byte flag stays false; do not restore old settings or rewrite that historical result.

The earlier AR02 transfer was blocked; after explicit permission approval its queued transfer failed because the owner had already stopped. Gmail05's actual ordinary download succeeded while its runtime stayed alive, with no permission prompt. These are separate results, not a rewrite of earlier failures.

## Historical next-action text and bounded evidence

The following test-task continuation paragraph is preserved historical text. Its publication/application is complete; continue the current five-request plan above, not its consumed lifecycle.

Local implementation, complete coverage reconciliation and validation in the [completed test-performance plan](exec_plans/completed/2026-09-26_test_performance.md) are finished. Publication/application is authorized. Inspect `publication_01/publication_receipt.json` for terminal status; if it is absent, continue only the authorized push/PR/exact-head checks/merge/application, preserving any failed checks. If complete, no further task is implied. Do not repeat finished local benchmarks or infer a hosted CI result from them. Current private evidence: `C:/Users/FA507/.codex/legalpdf_translate_private_benchmarks/test_performance_20260926_01/`; exact pre-sync HANDOFF/SESSION bytes are under `beforeimages/docs/assistant/`. Start with `comparison_audit_01.json`, `full_01_audit.json`, `selector_validation_01/result.json` and `final_tooling_01/result.json`. The completed baseline/pipeline helpers must not be restarted; local timing qualifications are recorded in the plan. The test task includes no provider, native, Gmail, OAuth, send, public-hosting or saved-default work.

Prior acceptance evidence root: `C:/Users/FA507/.codex/legalpdf_translate_private_benchmarks/gmail_formatting_readiness_20260924_01/`. Its `gmail_closeout_publication_01/publication_receipt.json` establishes completed PR #306 publication/application. The separate `C:/Users/FA507/.codex/legalpdf_translate_private_benchmarks/gmail_draft_permission_20260926_01/completion_receipt.json` establishes the later account-permission closure. Neither receipt authorizes replay. For historical acceptance, start with `bounded_acceptance_closeout_01.json`, which indexes 11 actual receipts and retained qualifications, then only the relevant `cases/gmail_live_05/terminal_evidence_01/terminal_accounting_independent_01.json`, `normal_browser_download_01.json`, `manual_review_01/`, `finalization_evidence_01/finalization_independent_01.json`, `finalization_review_01/` and `terminal_cleanup_01/terminal_cleanup_independent_01.json` beneath that case; `gmail_completion_preparation_02/` owns the consumed successor binding. AR02's saved/package audits and `native_review/operations/ar_reviewed_native_01/` retain qualified formatting closure. Exact canonical and pre-Gmail05 draft beforeimages are in `docs_sync_ar02_gmail_closeout_01/beforeimages/` and `docs_sync_ar02_gmail_closeout_01/beforeimages_gmail05_sync_01/`; this final pass also preserves `docs_sync_ar02_gmail_closeout_01/beforeimages_guard_closeout_01/`. Do not load the chronological corpus by default. [SESSION_RESUME.md](SESSION_RESUME.md) owns routing; [VALIDATION.md](VALIDATION.md) separates evidence tiers. Historical [FRlong reconciliation](exec_plans/completed/2026-09-24_frlong_readonly_reconciliation.md), [PR302 publication](exec_plans/completed/2026-09-24_everyday_app_update_publication.md) and [multilingual acceptance](exec_plans/completed/2026-09-17_multilingual_gmail_acceptance.md) remain preserved evidence.
