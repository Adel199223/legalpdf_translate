# Current handoff

Updated 2026-09-25. Read `AGENTS.md` and `agent.md`, then this page and the relevant active plan. Current user authorization takes precedence over historical continuation text. Do not replay consumed provider/native helpers.

## Current build and work

Previous published base: PR #303 merged on canonical `main` at `ca395d601efe4e00aad3458526b7d961d3cf5f0c`, with the reviewed `dad5ea149c090bea0f1313fa00109a8a0ed6e5ce` tree. Its four readiness fixes cover caller-owned accounting, absent-settings ownership, safe explicit recovery from unsubmitted formatting saves and stale-warning clearing. Full_04 passed 655 selected tests with nine expected deselections; all 486 pinned files matched and all four exact-head CI jobs passed. Direct docs validation and the known Dart AOT failure/successful fallback remain separate evidence. Private `pr303_publication_01/publication_checkpoint.json` records integration. Publication is complete; overall workflow readiness is still active.

PR #304 is published on canonical `main` at `1d6fdb3e7bfdd0c60ef74f8b9f5f2d3a977c5a46`, with entire-tree equality to reviewed head `11beb6c5c296c4087e3455aa176e3de70f158722`. The [completed Word startup deadline plan](exec_plans/completed/2026-09-25_word_startup_readiness_deadline.md) records source commit `58a2df5`, 91 targeted no-provider passes, fresh Full with 655 selected passes/nine expected deselections and 486 matching source pins, and all four exact-head CI jobs passing. Each Windows CI run passed 6,713 tests with one dependency-deprecation warning. The shared 45-second startup default preserves the separate canary deadline and all ownership/cleanup gates. Canonical application and one actual default-readiness pair are complete, independently reviewed and indexed by `word_startup_publication_01/final_application_receipt.json`.

PR #304 remains the tested product checkpoint. Any later docs-only publication and updated canonical HEAD are recorded in `docs_sync_word_startup_closeout_01/publication_receipt.json` when produced; an absent or unsuccessful receipt does not establish that later publication. A docs-only merge does not require replaying native acceptance.

The [Gmail-to-output and advanced-formatting plan](exec_plans/active/2026-09-24_gmail_formatting_readiness.md) remains the parent task. The user authorized autonomous genuine workflow verification and demonstrated related fixes, including necessary computer/API/native work. Sending, new Gmail permissions, public hosting and saved-default promotion remain excluded.

## Actual output and remaining acceptance

- **English formatting complete with qualifications:** EN03 translated all three pages, accepted/built 54 fragments in four editable tables, and passed package checks plus native review of all three A4 pages. The private `formatting_coverage_01/MATRIX.md` distinguishes 1–4 columns, editable widths/cells, measured gutters, headers/footers/folios, document groups, alignments/emphasis, strict separators, capped spacing and the page-matched derivative. Recovery has 27 focused browser-state tests. Blocked/unconfirmed downloads do not establish an EN03 downloaded-copy identity. Do not repeat completed EN03 acceptance.
- **Genuine Gmail output saved:** Gmail03 used Image Always with OCR off for the actual one-page attachment. Root/peer reviewed the ordinary DOCX/native page with qualifications; normal UI Save/Confirm added row **90**, joined to run `20260924_234212` and its exact output. All 63 prior job rows and schema remain unchanged. Private `cases/gmail_live_03/after_confirm_01/root_saved_output_join_01.json` and `gmail_live_preparation_01/job_log_after_confirm_01/receipt.json` preserve these separate checks. Gmail01/Gmail02 deficient outputs were not Confirmed and remain preserved. No Gmail draft or send is established.
- **Arabic remains incomplete:** AR01 completed 1/3 pages before page 2 language/token compliance failed, using four fully costed calls. Failed response bytes and detailed validator evidence were not retained, so the exact offending token/cause and model error versus local false positive remain unknown. Its partial output is not formatting acceptance. New fictional AR02 is intended only for the remaining reviewed inline-pipe cells, full vertical spacing and RTL/mixed-literal rows.

## Runtime, native recovery and preservation

After Gmail03 Save, an automatic eight-second Word preflight left unknown ownership. One separately reviewed exact-process cleanup and one fresh supported 45-second preflight succeeded with confirmed cleanup. The latter took 5.268 seconds inside the helper and 10.309 seconds including the normal recovery gate, with matching helper/journal identity and retained five-second Word-free observations. `native_startup_recovery_01/actual_independent_review_01.json` verifies the evidence and protected pins. The earlier startup cause remains unknown; this fresh preflight is not a canary, document export, server-cache refresh or completed Gmail finalization.

After publication, one fresh canonical readiness aggregate used the actual default deadlines without overrides or retries. It completed in 27.673 seconds: launch/preflight 12.185 seconds and the separate synthetic-document canary 15.392 seconds; the full owned operation, including observations/reporting, took 40.156 seconds. Both stages confirmed cleanup, protected/source/tool pins matched, and the final observation found Word absent and ports 8877/8765/8893 free. `word_startup_acceptance_01/operations/word_startup_fixed_acceptance_01/result.json` and `word_startup_acceptance_01/independent_actual_review_01.json` retain this consumed result. This is native readiness evidence, not a legal-document export, Gmail finalization or another app process's cache refresh; the earlier failure cause remains unknown.

Live `gmail_live_03` stopped through its owned stop file with zero active jobs. Its wrapper exit 1 is explained by the authorized settings metadata change below, not an unexplained running job. At 23:15:23 UTC on September24, the named prior processes were absent, ports 8877/8765/8893 were free and Word count was zero. Shadow06 subsequently ran the unchanged `dad5ea1` source on port 8893 with no bridge, but its upload was refused before source review or provider dispatch. It then stopped through its owned stop file with zero jobs and wrapper exit 0. `shadow_06_preparation_01/root_terminal_cleanup_01.json` confirms the named processes, relevant ports and Word were clear afterward; the original settings-byte baseline remains false while its separately reviewed preservation check passes. Both runtimes and their launch helpers are consumed. Recheck actual state before any fresh runtime.

AR02 upload is currently blocked by the browser automation extension's local-file access restriction (`Not allowed`); the user-facing file-URL setting is pending. `cases/formatting_ar_02/upload_observation_01.json` preserves the observation. This is not evidence of an application PDF defect, and no AR02 provider dispatch occurred.

The shared USD 3/16-reservation phase has **14 finalized reservations / USD 0.21838600 known / no new hold or block / two slots and USD2.78161400 remaining** at this checkpoint. Transport retries consume the same global slots; a two-logical-call estimate is not a per-job reservation guarantee. Reconcile actual state before a fresh invocation.

Normal Gmail confirmation added only `court_emails_by_city` to canonical settings. All other JSON fields, including saved translation defaults, equal the original beforeimage. The original whole-settings-byte baseline remains truthfully false; preserve it and the separately reviewed narrow metadata amendment. Do not restore old settings or relabel the original runtime result as an unchanged-baseline pass.

Preserve the original USD 10 limit, all 226 historical ledger rows, FRlong01's USD 0.119 uncertain hold/block and exact five-page partial. Its page6 charge and inner connection cause remain unknown and shelved unless the failure recurs. The old private campaign block is not a global ordinary-app lock. Historical [FRlong reconciliation](exec_plans/completed/2026-09-24_frlong_readonly_reconciliation.md), [PR302 publication](exec_plans/completed/2026-09-24_everyday_app_update_publication.md) and [multilingual acceptance](exec_plans/completed/2026-09-17_multilingual_gmail_acceptance.md) retain their qualified scopes.

## Next concrete action

Word-startup implementation, publication, canonical application and native-readiness gates are complete; do not repeat them. Resume AR02 only after the local-file restriction is resolved and a fresh runtime, source acceptance and remaining ledger capacity are verified. Inspect actual Arabic targets before entering formatting decisions; then validate only the remaining matrix rows and record final owned cleanup. Gmail draft/finalization remains separate from the already saved translation; do not repeat Confirm or a consumed provider/native operation to reconstruct state.

Current private evidence root: `C:/Users/FA507/.codex/legalpdf_translate_private_benchmarks/gmail_formatting_readiness_20260924_01/`. Start with `gmail_live_preparation_02/terminal_stop_independent_review_01.json`, `native_startup_recovery_01/actual_independent_review_01.json`, the feature matrix and the applicable active plan. Exact beforeimages for this scoped closeout are in `docs_sync_word_startup_closeout_01/beforeimages/`; the earlier `docs_sync_word_startup_01/beforeimages/` and dated history remain intact. Do not load the full chronological corpus by default. [SESSION_RESUME.md](SESSION_RESUME.md) owns worktree routing and [VALIDATION.md](VALIDATION.md) separates evidence tiers.
